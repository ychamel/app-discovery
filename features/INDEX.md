# features/INDEX.md — Feature Registry

**Every feature ever started is listed here, once.** [CONTROL.md](../CONTROL.md) tracks
*where we are now* (the active feature); this file is the answer to *"have we built
anything about X, and where is it?"* — findability that a single active-feature dashboard
can't give you at volume.

Maintained by the **Coordinator** (adds a row when a feature folder is created) and the
**Retrospective Analyst** (fills in the outcome when a feature reaches `done`).

## Registry

Listed in dependency-build order (breakdown §5), grouped by phase — more useful for a
backlog than newest-first. Keep each outcome to one line; detail lives in the feature
folder.

**`backlog`** = folder + the 7 artifacts scaffolded by the Coordinator from
[../docs/mvp-component-breakdown.md](../docs/mvp-component-breakdown.md), but **not yet
entered Stage 1**. Activating one (set `Stage: 1-define` in [../CONTROL.md](../CONTROL.md))
is decision **D2** and belongs to the user.

| Slug | Phase | Stage | Started | Proves | Depends on | One-line outcome |
|------|-------|-------|---------|--------|------------|------------------|
| [identity-accounts](identity-accounts/) | 0 Foundation | closed-out | 2026-06-13 | enabler | — | _released local/dev (108 tests green); Stage 6 retrospective skipped per user — outcome review deferred/reopenable_ |
| [interest-taxonomy](interest-taxonomy/) | 0 Foundation | closed-out | 2026-06-17 | enabler | — | _released local/dev (11 clusters / 67 tags, 184 tests green; rollback rehearsed); Stage 6 retrospective skipped per user (DN-1 A) — live-metrics/real-catalog coverage + outcome report deferred/reopenable_ |
| [signal-capture](signal-capture/) | 0 Foundation | closed-out | 2026-06-18 | H3 | identity-accounts | _released local/dev ([RELEASE_NOTES.md](signal-capture/RELEASE_NOTES.md), apps/signals, 374 tests green; rollout→rollback rehearsed); established global D-7 event-schema contract; Stage 6 retrospective skipped per user (DN-7 A) — outcome review needs a live emitter (weekly-digest), deferred/reopenable_ |
| [submission-intake](submission-intake/) | 1 Catalog | closed-out | 2026-06-17 | H2 | identity-accounts, interest-taxonomy | _released local/dev (apps/catalog, 315 tests green; rollout→rollback rehearsed); established global D-6 catalogued-app contract; Stage 6 retrospective skipped per user (DN-3 A) — live-metrics/outcome report deferred/reopenable_ |
| [app-pages](app-pages/) | 1 Catalog | closed-out | 2026-06-20 | H1, H2 | submission-intake | _released local/dev ([RELEASE_NOTES.md](app-pages/RELEASE_NOTES.md); new app `apps/pages/`, pure D-6/D-7 consumer owning no model/migration; 3 thin views + `app_page` template; 417 tests green; rollout→rollback rehearsed — primary rollback = remove the `config/urls` include, zero data migration); first live emitter of D-7 `app_page` impressions (AP-3/4/5); Stage 6 retrospective skipped per user — build re-verified (417 tests / ruff / check / no drift); outcome review deferred/reopenable until a prod target/traffic exists_ |
| [editorial-curation-tools](editorial-curation-tools/) | 1 Catalog | backlog | 2026-06-24 | H1, H3 | interest-taxonomy, submission-intake | _**PAUSED 2026-06-26** (was 1-define; only stubs existed, no Stage-1 artifact produced — nothing lost). Deprioritized by the developer-wedge pivot ([global D-10](../DECISIONS.md)): editorial curation/matching is a network feature, held back until per-niche density turns the discovery network on. Reactivating it (with weekly-digest) is a future D2 call._ |
| [interest-profile](interest-profile/) | 2 User loop | closed-out | 2026-06-22 | H1 | identity-accounts, interest-taxonomy | _released local/dev ([RELEASE_NOTES.md](interest-profile/RELEASE_NOTES.md); new app `apps/interests/` owning one mutable table `interests_interest` — the user side of the Ring-0 match via the `declared_tag_ids` matcher contract (AC8); **no parent row** → empty=structural default AC6, **CASCADE** user FK AC9; §7 set-replace **preserve-on-edit** reconcile (AC4 × AC7/M5=0); **no `signals.capture` import** (IP-5); two-part activation switch, no flag; 616 tests green; rollout→rollback rehearsed up→down→up). Stage 6 retrospective skipped per user — build re-verified (616 tests / ruff / check / no drift); outcome review deferred/reopenable until a prod target/traffic + a matcher/digest consumer exist_ |
| [weekly-digest](weekly-digest/) | 2 User loop | backlog | 2026-06-13 | H1 | editorial-curation-tools, app-pages, signal-capture | _backlog_ |
| [ratings-reviews](ratings-reviews/) | 2 User loop | closed-out | 2026-06-21 | H1, H3 | app-pages, signal-capture | _released local/dev ([RELEASE_NOTES.md](ratings-reviews/RELEASE_NOTES.md); new app `apps/ratings/` owning one mutable table `ratings_rating` + one additive index on signals; established global **D-8** curated-rating gate ("curated = a `DIGEST` impression"); 486 tests green; rollout→rollback rehearsed up→down→up); Stage 6 retrospective skipped per user — build re-verified (486 tests / ruff / check / no drift); outcome review deferred/reopenable until a prod target/traffic + a `DIGEST` emitter exist_ |
| [open-search-browse](open-search-browse/) | 2 User loop | closed-out | 2026-06-23 | enabler / H3 | app-pages | _released local/dev ([RELEASE_NOTES.md](open-search-browse/RELEASE_NOTES.md); Phase-2 open discovery surface — browse / keyword-search / interest-filter the D-6 accepted-app catalogue → app-page, **money never buys position** (vision §4.1). Additive `catalog_app.accepted_at`+`search_vector` (+indexes, `django.contrib.postgres`), write-path maintenance, backfill migration, reverse-resolution `taxonomy.tag_ids_resolving_to` (AC3), the paginated DB-pushed `catalog.search_catalogue -> CatalogPage` (constant-query-count + ORDER-BY-neutrality assertions, AC9/AC5), model-less `apps/discovery/` + the `discover/` include (no `signals` import = AC6); 676 tests green; rollout→rollback rehearsed up→down→up). Reuses D-3/D-5/D-6/D-7/D-8 — no new global ADR. Stage 6 retrospective skipped per user — build re-verified (676 tests / ruff / check / no drift); outcome review (M1/M5/M6 structural, M2 thin until traffic) deferred/reopenable until a prod target/traffic exists_ |
| [app-subscriptions](app-subscriptions/) | 2 User loop | closed-out | 2026-06-21 | H1 (feeds H3) | app-pages, identity-accounts, signal-capture | _released local/dev ([RELEASE_NOTES.md](app-subscriptions/RELEASE_NOTES.md)); new app `apps/subscriptions/` owning one mutable table `subscriptions_subscription` (near-twin of `apps/ratings/`) with three deliberate contrasts: deletion **CASCADE**s (AS-5/AC9), the follow-write + its one `subscribe` emit are **one `transaction.atomic()`** (M5 1:1 by construction), and an empty-until-producer notice seam (AS-3=A); **no feature flag** (activation = the `config/urls` `subscriptions/` include + the one `app_page.html` Follow section); 552 tests green; rollout→rollback rehearsed up→down→up. Stage 6 retrospective skipped per user — build re-verified (552 tests / ruff / check / no drift); outcome review deferred/reopenable until a prod target/traffic + a `developer-updates` notice producer exist_ |
| [developer-dashboard](developer-dashboard/) | 3 Dev value | closed-out | 2026-06-24 | H2 | signal-capture, ratings-reviews | _released local/dev ([RELEASE_NOTES.md](developer-dashboard/RELEASE_NOTES.md); model-less `apps/dashboard/` consumer (one `config/urls` include + `INSTALLED_APPS` line = activation/rollback, owns no schema) + two additive **neutral** `signals.selectors` reads (`impression_breakdown[_for_apps]` AC3/AC4 + `impression_trend` AC10 + `TrendGranularity`; no model/migration/index) resolving OQ-DD-4; curated split reuses `ratings.gate.CURATED_SURFACES`; 8 windows + per-window granularity in `apps/dashboard/windows.py`; loud-core/soft-reviews failure split; read-only (role-gated GET, no `signals.capture` import, AST-enforced); **740 tests green**, [TEST_PLAN.md](developer-dashboard/TEST_PLAN.md) complete (AC1–AC10); rollback rehearsed (remove the include + INSTALLED_APPS line → routes 404, upstream green; zero data migration)). Reuses D-3/D-5/D-6/D-7/D-8 — no new ADR. Stage 6 retrospective skipped per user — build re-verified (740 tests / ruff / check / no drift); outcome review (M3/M4 thin until a `DIGEST` emitter; M5 leak target 0 structural) deferred/reopenable until a prod target/traffic exists_ |
| [embeddable-update-widget](embeddable-update-widget/) | 3 Dev value | closed-out | 2026-06-26 | H2 (wedge) | developer-updates, app-pages, submission-intake | _**released local/dev** ([RELEASE_NOTES.md](embeddable-update-widget/RELEASE_NOTES.md)) — the core bring-your-own-audience capture mechanism — an embeddable "what's new" widget a developer drops inside their own app, surfacing that app's update notices (the AS-3 `Notice` contract) + a path back to its app page, pulling the developer's existing users onto the platform. Widget interactions are a **non-curated** surface (no D-8 eligibility) but their **view source is tracked** for developer-dashboard attribution (AC9). Stage 1 **brief APPROVED** (DN-EUW-BRIEF): 6 stories / **AC1–AC9** / M1–M6 + EUW-1…6. **Stage 2 DESIGN APPROVED** (DN-EUW-DESIGN, 2026-06-26 — EUW-7…11 RATIFIED): embedding = a server-rendered **`<iframe>`** (one-line, zero-build, AC7); attribution = a **dedicated `apps/widget`-owned daily-rollup store** (`widget_reach_count`) that **never touches `signals`** ⇒ the AC6 firewall (M5=0) is **structural by absence** (apps/widget imports nothing from signals); the **closed** `apps/dashboard` gains a fail-soft **off-platform widget-reach slot** (AC9); per-IP GET rate limit + `Cache-Control` (AC8); **M3 per-account conversion deferred** (OQ-EUW-5). New `apps/widget/` (owns one table) + additive `apps/dashboard`/`apps/core`; no new global ADR (reuses D-4/D-6/D-7/D-8/D-9/D-10). **Stage 3 TASKS.md complete** ([TASKS.md](embeddable-update-widget/TASKS.md): **7 ordered tasks T-01…T-07, all S/M**, full AC1–AC9 coverage; risk front-loaded at T-02 = the AC6 structural-firewall proof + atomic-increment concurrency). **Stage 4 BUILD COMPLETE** (Senior Engineer, 2026-06-26): all of T-01…T-07 shipped — new `apps/widget/` (`widget_reach_count` + `attribution`/`selectors`/`content`/`views` + framable templates + the `widget/` activation include), additive `apps/core` (per-IP GET limiter + 3 tunables + 10 metrics) + fail-soft dashboard widget-reach slot; **AC1–AC9 covered** ([TEST_PLAN.md](embeddable-update-widget/TEST_PLAN.md)); **full suite green (893 tests)**, `ruff` clean, no migration drift; firewall proven structurally (AST + 0-corpus + has_impression False). **Stage 5 RELEASED to local/dev** (Release Engineer, 2026-06-26): [RELEASE_NOTES.md](embeddable-update-widget/RELEASE_NOTES.md) written; build independently re-verified (893 tests / `ruff` / `check` / no drift); rollback rehearsed (migration up→down→up + `git revert b7db60f` removes app+include+dashboard-edit atomically with `check` clean = the **DU-REL-1** property proven). Stage 6 retrospective deferred/reopenable per the standing pattern — outcome review (M1–M6) opens when a prod target/traffic exists; **M3 per-account conversion deferred** (OQ-EUW-5)._ |
| [widget-conversion-attribution](widget-conversion-attribution/) | 3 Dev value | 3-plan | 2026-06-26 | H2 (wedge) | embeddable-update-widget | _**active** — follow-up picking up the deferred **M3 / OQ-EUW-5** from [embeddable-update-widget](embeddable-update-widget/): per-account conversion attribution (which new account/follow came from which widget click-through), via a widget-source token carried through an anonymous click → app page → sign-up. Must preserve the AC6 firewall (M5=0, structural) + no-PII posture. **Brief APPROVED (DN-WCA-BRIEF; WCA-1 follow+account / WCA-2 last-touch ~30d / WCA-3 aggregate-only source-keyed).** **Stage 2 DESIGN APPROVED (DN-WCA-DESIGN, 2026-06-27 — WCA-DESIGN-1…8 RATIFIED):** a **first-party signed source-only cookie** `widget_src` set on the click-through 302 carries the source app-id (public id + flags, **no person field**) through the anonymous click → app page → conversion — first-party from birth because the click is a `target="_top"` top-level nav onto the platform origin (OQ-WCA-3 dissolved); conversions (new **follow** + new **account**) counted in a **separate, PII-free `widget_conversion_count`** rollup via **fail-soft hooks** at `subscriptions.views.follow` + `accounts.views.register`; dashboard widget slot gains a conversions funnel stage + M2 rate. **AC6 firewall stays structural** (`apps/widget` imports no `signals`; AST test extends to the new `source` module); **no PII processed → no consent banner** (revisit with counsel pre-EU-prod; one-call consent gate is a documented contingency). `subscriptions`/`accounts`/`dashboard` import `widget` (DAG); reuses D-3/D-4/D-6/D-7/D-8/D-9/D-10 — no new global ADR. **At Stage 3-plan** — Planner decomposes DESIGN.md into TASKS.md (risk-front-load the firewall proof + the source-marker codec)._ |
| [developer-updates](developer-updates/) | 3 Dev value | closed-out | 2026-06-24 | H2 | app-subscriptions, app-pages, signal-capture | _released local/dev ([RELEASE_NOTES.md](developer-updates/RELEASE_NOTES.md)). Net-new from signal-capture SC-7/SC-8 (OQ-4): developer→follower channel (post update/early-access notices to an app's **existing** followers; the producer for the **AS-3** notice seam). Shipped: a **new `apps/updates/`** owning `updates_notice` as the single **AS-3 producer**; **pull delivery** → **M5=0 structural** + no fan-out (OQ-DU-1); **transparency line** — imports no `signals`, AST-enforced (OQ-DU-2); durable table-derived **rate limit** (AC8); additive `subscriptions.subscriber_count` + `subscriptions_app_idx`; the AS-3 repoint as a single `PublishedNotice → Notice` adapter with a proven **no-import-cycle DAG**. **828 tests green** [740+88], `ruff`/`check`/`makemigrations --check` clean, both migrations reversible; [TEST_PLAN.md](developer-updates/TEST_PLAN.md) maps every AC1–AC8 + metric to a named test. **DU-DESIGN-1…6 BUILT**; reuses **D-3/D-6/D-7 + AS-3 — no new ADR**. First feature to repoint a **closed** app's seam — rollback rehearsed up→down→up; **DU-REL-1**: the honest revert is broader than DESIGN §12's "3 parts" (also the seam imports + the rewritten `subscriptions` tests), so the clean rollback is `git revert` of the build commit. Stage 6 retrospective skipped per user — outcome review (M1–M6) deferred/reopenable until a prod target/traffic exists_ |

> Stage values: see the routing table in [../CLAUDE.md](../CLAUDE.md) §2. `backlog` is a
> pre-pipeline holding state, not a canonical pipeline stage.
