# features/INDEX.md — Feature Registry

**Every feature ever started is listed here, once.** [CONTROL.md](../CONTROL.md) tracks
*where we are now* (the active feature); this file is the answer to *"have we built
anything about X, and where is it?"* — findability that a single active-feature dashboard
can't give you at volume.

Maintained by the **Coordinator** (adds a row when a feature folder is created) and the
**Retrospective Analyst** (fills in the outcome when a feature reaches `done`).

## Registry

Listed in dependency-build order (breakdown §5), grouped by phase — more useful for a
backlog than newest-first. Keep each outcome to one line; detail lives in the feature
folder.

**`backlog`** = folder + the 7 artifacts scaffolded by the Coordinator from
[../docs/mvp-component-breakdown.md](../docs/mvp-component-breakdown.md), but **not yet
entered Stage 1**. Activating one (set `Stage: 1-define` in [../CONTROL.md](../CONTROL.md))
is decision **D2** and belongs to the user.

| Slug | Phase | Stage | Started | Proves | Depends on | One-line outcome |
|------|-------|-------|---------|--------|------------|------------------|
| [identity-accounts](identity-accounts/) | 0 Foundation | closed-out | 2026-06-13 | enabler | — | _released local/dev (108 tests green); Stage 6 retrospective skipped per user — outcome review deferred/reopenable_ |
| [interest-taxonomy](interest-taxonomy/) | 0 Foundation | closed-out | 2026-06-17 | enabler | — | _released local/dev (11 clusters / 67 tags, 184 tests green; rollback rehearsed); Stage 6 retrospective skipped per user (DN-1 A) — live-metrics/real-catalog coverage + outcome report deferred/reopenable_ |
| [signal-capture](signal-capture/) | 0 Foundation | closed-out | 2026-06-18 | H3 | identity-accounts | _released local/dev ([RELEASE_NOTES.md](signal-capture/RELEASE_NOTES.md), apps/signals, 374 tests green; rollout→rollback rehearsed); established global D-7 event-schema contract; Stage 6 retrospective skipped per user (DN-7 A) — outcome review needs a live emitter (weekly-digest), deferred/reopenable_ |
| [submission-intake](submission-intake/) | 1 Catalog | closed-out | 2026-06-17 | H2 | identity-accounts, interest-taxonomy | _released local/dev (apps/catalog, 315 tests green; rollout→rollback rehearsed); established global D-6 catalogued-app contract; Stage 6 retrospective skipped per user (DN-3 A) — live-metrics/outcome report deferred/reopenable_ |
| [app-pages](app-pages/) | 1 Catalog | closed-out | 2026-06-20 | H1, H2 | submission-intake | _released local/dev ([RELEASE_NOTES.md](app-pages/RELEASE_NOTES.md); new app `apps/pages/`, pure D-6/D-7 consumer owning no model/migration; 3 thin views + `app_page` template; 417 tests green; rollout→rollback rehearsed — primary rollback = remove the `config/urls` include, zero data migration); first live emitter of D-7 `app_page` impressions (AP-3/4/5); Stage 6 retrospective skipped per user — build re-verified (417 tests / ruff / check / no drift); outcome review deferred/reopenable until a prod target/traffic exists_ |
| [editorial-curation-tools](editorial-curation-tools/) | 1 Catalog | backlog | 2026-06-24 | H1, H3 | interest-taxonomy, submission-intake | _**PAUSED 2026-06-26** (was 1-define; only stubs existed, no Stage-1 artifact produced — nothing lost). Deprioritized by the developer-wedge pivot ([global D-10](../DECISIONS.md)): editorial curation/matching is a network feature, held back until per-niche density turns the discovery network on. Reactivating it (with weekly-digest) is a future D2 call._ |
| [interest-profile](interest-profile/) | 2 User loop | closed-out | 2026-06-22 | H1 | identity-accounts, interest-taxonomy | _released local/dev ([RELEASE_NOTES.md](interest-profile/RELEASE_NOTES.md); new app `apps/interests/` owning one mutable table `interests_interest` — the user side of the Ring-0 match via the `declared_tag_ids` matcher contract (AC8); **no parent row** → empty=structural default AC6, **CASCADE** user FK AC9; §7 set-replace **preserve-on-edit** reconcile (AC4 × AC7/M5=0); **no `signals.capture` import** (IP-5); two-part activation switch, no flag; 616 tests green; rollout→rollback rehearsed up→down→up). Stage 6 retrospective skipped per user — build re-verified (616 tests / ruff / check / no drift); outcome review deferred/reopenable until a prod target/traffic + a matcher/digest consumer exist_ |
| [weekly-digest](weekly-digest/) | 2 User loop | backlog | 2026-06-13 | H1 | editorial-curation-tools, app-pages, signal-capture | _backlog_ |
| [ratings-reviews](ratings-reviews/) | 2 User loop | closed-out | 2026-06-21 | H1, H3 | app-pages, signal-capture | _released local/dev ([RELEASE_NOTES.md](ratings-reviews/RELEASE_NOTES.md); new app `apps/ratings/` owning one mutable table `ratings_rating` + one additive index on signals; established global **D-8** curated-rating gate ("curated = a `DIGEST` impression"); 486 tests green; rollout→rollback rehearsed up→down→up); Stage 6 retrospective skipped per user — build re-verified (486 tests / ruff / check / no drift); outcome review deferred/reopenable until a prod target/traffic + a `DIGEST` emitter exist_ |
| [open-search-browse](open-search-browse/) | 2 User loop | closed-out | 2026-06-23 | enabler / H3 | app-pages | _released local/dev ([RELEASE_NOTES.md](open-search-browse/RELEASE_NOTES.md); Phase-2 open discovery surface — browse / keyword-search / interest-filter the D-6 accepted-app catalogue → app-page, **money never buys position** (vision §4.1). Additive `catalog_app.accepted_at`+`search_vector` (+indexes, `django.contrib.postgres`), write-path maintenance, backfill migration, reverse-resolution `taxonomy.tag_ids_resolving_to` (AC3), the paginated DB-pushed `catalog.search_catalogue -> CatalogPage` (constant-query-count + ORDER-BY-neutrality assertions, AC9/AC5), model-less `apps/discovery/` + the `discover/` include (no `signals` import = AC6); 676 tests green; rollout→rollback rehearsed up→down→up). Reuses D-3/D-5/D-6/D-7/D-8 — no new global ADR. Stage 6 retrospective skipped per user — build re-verified (676 tests / ruff / check / no drift); outcome review (M1/M5/M6 structural, M2 thin until traffic) deferred/reopenable until a prod target/traffic exists_ |
| [app-subscriptions](app-subscriptions/) | 2 User loop | closed-out | 2026-06-21 | H1 (feeds H3) | app-pages, identity-accounts, signal-capture | _released local/dev ([RELEASE_NOTES.md](app-subscriptions/RELEASE_NOTES.md)); new app `apps/subscriptions/` owning one mutable table `subscriptions_subscription` (near-twin of `apps/ratings/`) with three deliberate contrasts: deletion **CASCADE**s (AS-5/AC9), the follow-write + its one `subscribe` emit are **one `transaction.atomic()`** (M5 1:1 by construction), and an empty-until-producer notice seam (AS-3=A); **no feature flag** (activation = the `config/urls` `subscriptions/` include + the one `app_page.html` Follow section); 552 tests green; rollout→rollback rehearsed up→down→up. Stage 6 retrospective skipped per user — build re-verified (552 tests / ruff / check / no drift); outcome review deferred/reopenable until a prod target/traffic + a `developer-updates` notice producer exist_ |
| [developer-dashboard](developer-dashboard/) | 3 Dev value | closed-out | 2026-06-24 | H2 | signal-capture, ratings-reviews | _released local/dev ([RELEASE_NOTES.md](developer-dashboard/RELEASE_NOTES.md); model-less `apps/dashboard/` consumer (one `config/urls` include + `INSTALLED_APPS` line = activation/rollback, owns no schema) + two additive **neutral** `signals.selectors` reads (`impression_breakdown[_for_apps]` AC3/AC4 + `impression_trend` AC10 + `TrendGranularity`; no model/migration/index) resolving OQ-DD-4; curated split reuses `ratings.gate.CURATED_SURFACES`; 8 windows + per-window granularity in `apps/dashboard/windows.py`; loud-core/soft-reviews failure split; read-only (role-gated GET, no `signals.capture` import, AST-enforced); **740 tests green**, [TEST_PLAN.md](developer-dashboard/TEST_PLAN.md) complete (AC1–AC10); rollback rehearsed (remove the include + INSTALLED_APPS line → routes 404, upstream green; zero data migration)). Reuses D-3/D-5/D-6/D-7/D-8 — no new ADR. Stage 6 retrospective skipped per user — build re-verified (740 tests / ruff / check / no drift); outcome review (M3/M4 thin until a `DIGEST` emitter; M5 leak target 0 structural) deferred/reopenable until a prod target/traffic exists_ |
| [embeddable-update-widget](embeddable-update-widget/) | 3 Dev value | closed-out | 2026-06-26 | H2 (wedge) | developer-updates, app-pages, submission-intake | _**released local/dev** ([RELEASE_NOTES.md](embeddable-update-widget/RELEASE_NOTES.md)) — the core bring-your-own-audience capture mechanism — an embeddable "what's new" widget a developer drops inside their own app, surfacing that app's update notices (the AS-3 `Notice` contract) + a path back to its app page, pulling the developer's existing users onto the platform. Widget interactions are a **non-curated** surface (no D-8 eligibility) but their **view source is tracked** for developer-dashboard attribution (AC9). Stage 1 **brief APPROVED** (DN-EUW-BRIEF): 6 stories / **AC1–AC9** / M1–M6 + EUW-1…6. **Stage 2 DESIGN APPROVED** (DN-EUW-DESIGN, 2026-06-26 — EUW-7…11 RATIFIED): embedding = a server-rendered **`<iframe>`** (one-line, zero-build, AC7); attribution = a **dedicated `apps/widget`-owned daily-rollup store** (`widget_reach_count`) that **never touches `signals`** ⇒ the AC6 firewall (M5=0) is **structural by absence** (apps/widget imports nothing from signals); the **closed** `apps/dashboard` gains a fail-soft **off-platform widget-reach slot** (AC9); per-IP GET rate limit + `Cache-Control` (AC8); **M3 per-account conversion deferred** (OQ-EUW-5). New `apps/widget/` (owns one table) + additive `apps/dashboard`/`apps/core`; no new global ADR (reuses D-4/D-6/D-7/D-8/D-9/D-10). **Stage 3 TASKS.md complete** ([TASKS.md](embeddable-update-widget/TASKS.md): **7 ordered tasks T-01…T-07, all S/M**, full AC1–AC9 coverage; risk front-loaded at T-02 = the AC6 structural-firewall proof + atomic-increment concurrency). **Stage 4 BUILD COMPLETE** (Senior Engineer, 2026-06-26): all of T-01…T-07 shipped — new `apps/widget/` (`widget_reach_count` + `attribution`/`selectors`/`content`/`views` + framable templates + the `widget/` activation include), additive `apps/core` (per-IP GET limiter + 3 tunables + 10 metrics) + fail-soft dashboard widget-reach slot; **AC1–AC9 covered** ([TEST_PLAN.md](embeddable-update-widget/TEST_PLAN.md)); **full suite green (893 tests)**, `ruff` clean, no migration drift; firewall proven structurally (AST + 0-corpus + has_impression False). **Stage 5 RELEASED to local/dev** (Release Engineer, 2026-06-26): [RELEASE_NOTES.md](embeddable-update-widget/RELEASE_NOTES.md) written; build independently re-verified (893 tests / `ruff` / `check` / no drift); rollback rehearsed (migration up→down→up + `git revert b7db60f` removes app+include+dashboard-edit atomically with `check` clean = the **DU-REL-1** property proven). Stage 6 retrospective deferred/reopenable per the standing pattern — outcome review (M1–M6) opens when a prod target/traffic exists; **M3 per-account conversion deferred** (OQ-EUW-5)._ |
| [widget-conversion-attribution](widget-conversion-attribution/) | 3 Dev value | closed-out | 2026-06-27 | H2 (wedge) | embeddable-update-widget | _**released local/dev** ([RELEASE_NOTES.md](widget-conversion-attribution/RELEASE_NOTES.md)) — follow-up picking up the deferred **M3 / OQ-EUW-5** from [embeddable-update-widget](embeddable-update-widget/): per-account conversion attribution (which new account/follow came from which widget click-through), via a widget-source token carried through an anonymous click → app page → sign-up. Preserves the AC6 firewall (M5=0, structural) + no-PII posture. **Brief APPROVED (DN-WCA-BRIEF; WCA-1 follow+account / WCA-2 last-touch ~30d / WCA-3 aggregate-only source-keyed).** **DESIGN APPROVED (DN-WCA-DESIGN, 2026-06-27 — WCA-DESIGN-1…8 RATIFIED):** a **first-party signed source-only cookie** `widget_src` set on the click-through 302 carries the source app-id (public id + flags, **no person field**) through the anonymous click → app page → conversion — first-party from birth (the click is a `target="_top"` top-level nav onto the platform origin); conversions (new **follow** + new **account**) counted in a **separate, PII-free `widget_conversion_count`** rollup via **fail-soft hooks** at `subscriptions.views.follow` + `accounts.views.register`; dashboard widget slot gains a conversions funnel stage + M2 rate. **AC6 firewall structural** (`apps/widget` imports no `signals`, AST-enforced over the new `source`/`rollup` modules); **no PII → no consent banner** (revisit pre-EU-prod). **Stage 4 BUILT (2026-06-27, T-01…T-08): new `apps/widget` `rollup`/`source` modules + `widget_conversion_count` table (migration `0002`) + conversion writer/selectors + the dashboard funnel slot + the two fail-soft hooks; full suite 962 green, ruff/check clean, no drift, migration up→down→up clean; [TEST_PLAN.md](widget-conversion-attribution/TEST_PLAN.md) maps every AC1–AC6.** **Stage 5 RELEASED to local/dev** (Release Engineer, 2026-06-27): [RELEASE_NOTES.md](widget-conversion-attribution/RELEASE_NOTES.md) written; build independently re-verified (**962 tests** / `ruff` / `check` / no drift); rollback rehearsed (migration `widget/0002` down→up clean + `git revert 5df8daa` removes the `source`/`rollup`/`kinds` modules + migration + the two hooks + the dashboard slot atomically with `check` clean = the **DU-REL-1** property proven, no dangling `subscriptions`/`accounts`/`dashboard → widget` import). Stage 6 retrospective deferred/reopenable per the standing pattern — outcome review (M1–M6) opens when a prod target/traffic exists; ePrivacy consent revisit pre-EU-prod._ |
| [platform-staging](platform-staging/) | 4 Validation | closed-out | 2026-06-28 | H1, H2, H3 (validates the wedge) | the whole wedge (all closed-out features) | _**released local/dev — build complete + verified; live deploy deliberately deferred** (the user decided 2026-06-28 to land [premium-frontend](premium-frontend/) first and deploy staging on the polished UI). Executing global [D-11](../DECISIONS.md): stand up a reachable **staging** env, deploy the code-complete developer wedge, and walk it end-to-end as **user / developer / admin on web + mobile**; polish + make the Django templates ([D-4](../DECISIONS.md)) responsive; exercise the deferred deploy work (hosting, domain, stubbed D-4 email provider, monitoring). Live recruitment (vision §5.4 step 3) follows once staging validates; SPA split deferred + evidence-gated (D-11). Brief **APPROVED** 2026-06-27 (DN-PS-BRIEF; PS-1 free-tier→$20–100/mo, prod-bound; PS-2 no email provider yet → Architect picks one + ships setup `.md`; PS-3 user walks it). **DESIGN APPROVED** 2026-06-27 (DN-PS-DESIGN — user confirmed **Render / Resend / Consolidate**; PS-DESIGN-1…8 **RATIFIED** as global **[D-12](../DECISIONS.md)**): deployment/serving stack = **Render** Blueprint (managed Postgres + disk + free Redis) + **gunicorn**/**WhiteNoise**/`dj-database-url`; media on a persistent disk (object store = growth path); email = **Resend** over Django SMTP (pure config) + a setup `.md`; shared cache from `REDIS_URL` (LocMem fallback) → the auth limiter holds across workers; observability = stdout logs + env-gated **Sentry** + a new **DB-only `/health/live`** liveness probe (split from the SMTP-touching `/health`); frontend = **one shared responsive base template + stylesheet** in `apps/core` (6 `base.html` consolidated, **widget stays isolated**); **no schema/migration**. **TASKS.md WRITTEN** 2026-06-27 (Planner / Tech Lead): **11 ordered S/M tasks T-01…T-11** (no `L`), risk front-loaded at **T-01** (the serving/static/DB-URL stack — the real C4 gap), the ~30-template consolidation sequenced **T-05→T-06** with a render-every-surface check + the widget firewall held, full AC1.1–AC6.2 coverage map, the two `.md` deliverables (`email-provider-setup.md` at T-04, `staging-walkthrough.md` + `TEST_PLAN.md` at T-11) named. **BUILD COMPLETE** 2026-06-28 (Senior Engineer): all **T-01…T-11** built; **975 tests green** (+13), `ruff`/`check` (both DEBUG states) clean, **no migration drift**. Serving stack (gunicorn/WhiteNoise/`dj-database-url`/`CSRF_TRUSTED_ORIGINS`), non-DEBUG media route, [`render.yaml`](../render.yaml) Blueprint, SMTP env wiring + [`email-provider-setup.md`](../docs/deploy/email-provider-setup.md), the shared responsive shell (`apps/core/templates/core/base.html` + `app.css`, 6 bases consolidated, **widget firewall held**), DB-only `/health/live`, `REDIS_URL` cache (LocMem fallback), env-gated Sentry, [`deploy-runbook.md`](../docs/deploy/deploy-runbook.md), [`TEST_PLAN.md`](platform-staging/TEST_PLAN.md) (every AC mapped) + [`staging-walkthrough.md`](../docs/deploy/staging-walkthrough.md). One walkthrough finding logged (PS-OQ-1: bare `/` 404 — carried into [premium-frontend](premium-frontend/) as its landing-surface concern). **Stage 5 RELEASED to local/dev** (Release Engineer, 2026-06-28): [RELEASE_NOTES.md](platform-staging/RELEASE_NOTES.md) written; build independently re-verified (975 tests / ruff / check both DEBUG / no drift / `render.yaml` secret-clean / collectstatic + gunicorn OK). **The live Render deploy + the AC1/AC5/AC6 walkthrough are deferred, not cancelled** — sequenced after `premium-frontend` so staging debuts on the polished UI; every deploy artifact is written, secret-clean, and runs unchanged when staging is reactivated (DN-PS-DEPLOY parked, reopenable). Stage 6 retrospective + M1–M7 live readings open when staging goes live._ |
| [premium-frontend](premium-frontend/) | 4 Validation | closed-out | 2026-06-28 | H1, H2 (premium feel for the dev's marketing landing page) | platform-staging (the shared shell it upgrades) | _**active** — the user's 2026-06-28 frontend-direction decision: resolve the [D-11](../DECISIONS.md) frontend evidence-gate **inside the [D-4](../DECISIONS.md) envelope** — a **premium server-rendered frontend** (polish, interactivity, premium feel), **NOT** an SPA. Driver: the developer's **app page is their marketing landing page** ([D-10](../DECISIONS.md)) so premium feel is load-bearing, and the page must stay SEO-friendly + fast-first-paint; the current UI gap is **design**, not architecture (one ~199-line `app.css`, no design system). Sequenced **before** the live staging deploy so staging debuts polished. Carries in **PS-OQ-1** (the bare `/` 404 → build a real landing surface here). **Brief APPROVED** 2026-06-28 (Product Analyst; DN-PF-BRIEF — all 3 recommended defaults: **Q1** wedge surfaces first (landing + app page + discover/browse); **Q2** light progressive enhancement only; **Q3** a real platform landing at `/` → PF-CARRY-1): US-1…6, AC-1…7, M1–M7, C1–C7; "premium feel" checkable via a **design system** + a PS-3 sign-off. **DESIGN APPROVED** 2026-06-28 (Software Architect; DN-PF-DESIGN → **Option A / no build step**; PF-DESIGN-1…7 **RATIFIED**; global ADR **[D-13](../DECISIONS.md)**): the premium look is delivered **build-free** — deepen the hand-authored one `core/app.css` into a full token-driven **design system** (palette+states+elevation, modular type + spacing scales, motion, a real component set, 3 breakpoints); **reject Tailwind's Node/CLI toolchain** (affirms+extends [D-12](../DECISIONS.md)'s build-free posture; eliminates risk R1). **Tailwind via the standalone CLI is kept as an outcome-gated reversible fallback** (revisit = the M7 sign-off / authoring velocity). Plus a restyled shared shell (additive `{% block body_class %}` + skip-link), a new **static `/` landing** (`core.views.landing`, no DB read), an app-page + discover **presentation-only** restyle (slots/order/SEO unchanged), and **vendored** HTMX + `hx-boost` (fully degradable). Firewall (AC-6/M5=0), no-JS, SEO, a11y, **no schema** preserved by construction. **TASKS.md WRITTEN** 2026-06-28 (Planner / Tech Lead): **8 ordered S/M tasks T-01…T-08** (no `L`), risk-aware per DESIGN §13 — CSS substrate split T-01 (tokens/base/layout) → T-02 (components/utilities/breakpoints), then T-03 shell, T-04 landing, T-05 app page (load-bearing), T-06 discover, T-07 vendored HTMX, T-08 TEST_PLAN + M7 sign-off package; six standing guardrails (G1 suite≥975/no-drift, G2 widget firewall M5=0 non-change, G3 one-source-of-truth, G4 no-JS source of truth, G5 no view/url/schema change on restyle, G6 keep class names); full AC-1…7 + PF-DESIGN-1…7 coverage map. **BUILD COMPLETE** 2026-06-28 (Senior Engineer): all **T-01…T-08** built + [TEST_PLAN.md](premium-frontend/TEST_PLAN.md) written. **Stage 5 RELEASED to local/dev** (Release Engineer, 2026-06-28): [RELEASE_NOTES.md](premium-frontend/RELEASE_NOTES.md) written; build **independently re-verified** — **980 tests green** (M4: 975+5), `makemigrations --check` no drift, `ruff` clean (5 trivial lint misses found+fixed at release — dead import + whitespace + import-sort, no behaviour change), `manage.py check` clean, `collectstatic` (DEBUG=false manifest) produces hashed+gz `app.css` + `htmx.min.js` (R1 mitigated — WhiteNoise serving model intact), widget firewall **held** (G2/M5=0, `apps/widget` structurally untouched), `/` → 200/0-queries/405-on-POST (AC-3/M6). **Rollback rehearsed** (hard gate): **no migration** → nothing irreversible; one action = `git revert 6a01436 b14b238 8be0a51`, `manage.py check` clean mid-revert (no dangling refs), only CONTROL.md (prose) conflicts. **Outstanding = the M7 premium visual sign-off** (human-judgment, the user's, per PS-3 — surfaced as **DN-PF-SIGNOFF**); authed-surface restyle + the HTMX discovery fragment deferred by design; the live deploy (DN-PS-DEPLOY) follows. **CLOSED-OUT 2026-06-28** — the user **signed off** the M7 premium visual review (DN-PF-SIGNOFF resolved): the landing / app page / discover surfaces read as a polished product, not a prototype (*good enough for now*). The user flagged that the palette is still bland and there is no stylized/animated navigation, and scoped that **deeper modernization to a future Feature-Track enhancement** (`ui-modernization`, full design) — explicitly **not** a re-style of this feature and **not** an `issues/` item. On sign-off the **live staging deploy ([DN-PS-DEPLOY](../CONTROL.md)) was reopened** (the sequencing condition — staging debuts on the polished UI — is now met). Stage 6 retrospective + M1–M7 live readings open when staging goes live._ | app-subscriptions, app-pages, signal-capture | _released local/dev ([RELEASE_NOTES.md](developer-updates/RELEASE_NOTES.md)). Net-new from signal-capture SC-7/SC-8 (OQ-4): developer→follower channel (post update/early-access notices to an app's **existing** followers; the producer for the **AS-3** notice seam). Shipped: a **new `apps/updates/`** owning `updates_notice` as the single **AS-3 producer**; **pull delivery** → **M5=0 structural** + no fan-out (OQ-DU-1); **transparency line** — imports no `signals`, AST-enforced (OQ-DU-2); durable table-derived **rate limit** (AC8); additive `subscriptions.subscriber_count` + `subscriptions_app_idx`; the AS-3 repoint as a single `PublishedNotice → Notice` adapter with a proven **no-import-cycle DAG**. **828 tests green** [740+88], `ruff`/`check`/`makemigrations --check` clean, both migrations reversible; [TEST_PLAN.md](developer-updates/TEST_PLAN.md) maps every AC1–AC8 + metric to a named test. **DU-DESIGN-1…6 BUILT**; reuses **D-3/D-6/D-7 + AS-3 — no new ADR**. First feature to repoint a **closed** app's seam — rollback rehearsed up→down→up; **DU-REL-1**: the honest revert is broader than DESIGN §12's "3 parts" (also the seam imports + the rewritten `subscriptions` tests), so the clean rollback is `git revert` of the build commit. Stage 6 retrospective skipped per user — outcome review (M1–M6) deferred/reopenable until a prod target/traffic exists_ |

### Patch Track

| Slug | Source issue | Stage | Started | One-line outcome |
|------|--------------|-------|---------|------------------|
| [patch-dashboard-window-label](patch-dashboard-window-label/) | [`BUG-003`](../issues/BUG-003.md) (Medium) | closed-out (released local/dev) | 2026-06-28 | **RESOLVED** (999 tests) — multiline `{{ … }}` tags weren't tokenized (Django `tag_re` lacks `re.DOTALL`). Joined both offenders onto one line: the selected window's `{{ w.label }}` (reported) **and** a sibling `{{ summary.curated_impressions }}` surfaced by a repo-wide sweep (sweep now returns zero). Template + 1 red-first test only; ruff/check clean, no migration drift; rollback rehearsed. Patch Track scope held. |
| [patch-developer-submissions-nav](patch-developer-submissions-nav/) | [`UX-003`](../issues/UX-003.md) (High) | closed-out (released local/dev) | 2026-06-28 | **RESOLVED** — developers now reach their submissions: a developer-gated "My submissions" header link (new `is_developer` tag delegating to the one role gate) + a dashboard Analytics⇄Submissions sub-nav & empty-state "View my submissions" CTA + a reciprocal "View analytics" link. **988 tests** (8 new, red-first), ruff/check clean, no migration drift; rollback rehearsed. Presentation-only — Patch Track scope held. |
| [patch-profile-form-actions](patch-profile-form-actions/) | [`BUG-002`](../issues/BUG-002.md) (High) | closed-out (released local/dev) | 2026-06-28 | **RESOLVED** — the profile **Edit display name** + **Delete account** forms no longer plain-POST to the JSON `/me` API (the 405 source). Each now posts to a dedicated server-rendered §9 handler (PRG + Django messages): `update_display_name` (reuses `DisplayNameForm`) + `delete_my_account` (reuses the `delete_account` service); dead `data-method` markup removed. `MeView` left byte-unchanged → `/me` contract intact. **997 tests** (9 new, red-first), ruff/check clean, no migration drift; rollback rehearsed. Patch Track scope held. |
| [patch-interest-picker-duplicates](patch-interest-picker-duplicates/) | [`BUG-001`](../issues/BUG-001.md) (Medium) | closed-out (released local/dev) | 2026-06-28 | **RESOLVED** — resolved checkbox and label click targets for tags that appear in multiple active clusters by prefixing HTML IDs with the cluster ID. Added client-side visual sync so duplicate checkboxes check/uncheck each other. **998 tests** (1 new, red-first), ruff/check clean, no migration drift; rollback rehearsed. Patch Track scope held. |

> Stage values: see the routing table in [../CLAUDE.md](../CLAUDE.md) §2. `backlog` is a
> pre-pipeline holding state, not a canonical pipeline stage.
