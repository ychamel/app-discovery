# DECISIONS.md — premium-frontend (feature-local)

_Choice + rationale + rejected alternatives. Repo-wide decisions go in the top-level
[DECISIONS.md](../../DECISIONS.md)._

**Inherited context:** this feature exists to execute the user's 2026-06-28 frontend-direction
decision (recorded in [CONTROL.md](../../CONTROL.md)): resolve the [D-11](../../DECISIONS.md)
frontend evidence-gate **inside the [D-4](../../DECISIONS.md) envelope** — a **premium
server-rendered frontend (HTMX + Tailwind + Django templates), not an SPA** — because the
developer's app page is their marketing landing page ([D-10](../../DECISIONS.md)) and must stay
SEO-friendly + fast-first-paint. Tailwind's build step is expected to be a **new global ADR at
Stage 2**, revising [D-12](../../DECISIONS.md)'s build-free posture.

| ID | Decision | Rationale | Status |
|----|----------|-----------|--------|
| PF-D1 | **Brief APPROVED; scope fixed via DN-PF-BRIEF.** (Q1) restyle **wedge surfaces first** — landing + app page + discover/browse; (Q2) **light progressive enhancement only** for HTMX; (Q3) `/` is a **real platform landing** (resolves PF-CARRY-1). | The app page is the developer's marketing landing page ([D-10](../../DECISIONS.md)), so the public funnel is where "premium" is load-bearing; a shared design system makes authed surfaces a cheap follow-on. Light PE protects SEO/first-paint on those very surfaces. A real front door beats a throwaway redirect. "Premium feel" made checkable via a design system + a PS-3-style sign-off. | **RATIFIED** 2026-06-28 (user) |
| PF-2 | Tailwind **build step** (revises [D-12](../../DECISIONS.md)'s build-free posture). | Named as brief constraint **C3**; the Product Analyst does not decide architecture. | **RESOLVED (proposed) at Stage 2 → no build step** (see PF-DESIGN-1 / proposed D-13). The Architect recommends **against** Tailwind's toolchain; the build-free posture is **preserved**, not revised. **RESOLVED** 2026-06-28 via DN-PF-DESIGN → global [D-13](../../DECISIONS.md). |
| PF-DESIGN-1 | **No build step — deepen the hand-authored `core/app.css` into a full design system; reject Tailwind's Node/CLI toolchain.** Recorded as proposed global ADR **D-13** (affirms + extends [D-12](../../DECISIONS.md)). | The gap is *design depth*, not tooling; this eliminates the brief's #1 risk (R1 — build step destabilising the parked staging deploy), keeps [D-4](../../DECISIONS.md)'s single-language repo + D-12's serving model intact, and best serves AC-5 (one component stylesheet = one source of truth). Diverges from the user-named "Tailwind" → **not self-ratified**. Option B (Tailwind **standalone CLI**, no Node) is fully specified in [DESIGN.md](DESIGN.md) §11 as the contingency. | **RATIFIED** 2026-06-28 (DN-PF-DESIGN → **Option A**, recorded as global [D-13](../../DECISIONS.md); user chose it "for now" with Option B kept as an **outcome-gated reversible fallback** — revisit trigger = the M7 premium sign-off / future authoring velocity) |
| PF-DESIGN-2 | One served, **sectioned, token-driven** `app.css` (no per-page CSS); all colour/size/space literals live only in `:root`. | No build ⇒ partitioning is by clearly-labelled section, not file; one file = one request = best first-paint + trivially WhiteNoise-hashable; a token-only `:root` makes a re-theme a token edit (§10 change-locality). | **RATIFIED** 2026-06-28 (DN-PF-DESIGN, Option A) |
| PF-DESIGN-3 | Restyle the shared shell [`core/base.html`](../../apps/core/templates/core/base.html); **additive** `{% block %}` contract (new optional `{% block body_class %}`, existing blocks verbatim). | C7 (build on the consolidated shell, no 7th base); additive-only so no child template can break; restyling shared chrome lifts every surface consistently (US-5). | **RATIFIED** 2026-06-28 (DN-PF-DESIGN, Option A) |
| PF-DESIGN-4 | New static `/` landing via `core.views.landing` + `core/landing.html`, routed `path("", …)` **before** the accounts include; **no DB read**. | Closes PF-CARRY-1 / AC-3 with a real front door (Q3), not a redirect; a static, DB-free page = fastest first paint + cannot fail; rollback = remove the one route line + view + template. | **RATIFIED** 2026-06-28 (DN-PF-DESIGN, Option A) |
| PF-DESIGN-5 | Restyle `pages/app_page.html` + `discovery/catalogue.html` — **presentation only**; same slots, order, states, and server-rendered content/SEO strings; no view/selector/URL change. | AC-1/AC-2/AC-4 (premium + no-JS + SEO + responsive) with zero behaviour regression; uniformity (AC-3) preserved structurally; closed-feature inclusion tags (Follow/Share/Reviews) untouched. | **RATIFIED** 2026-06-28 (DN-PF-DESIGN, Option A) |
| PF-DESIGN-6 | **Vendored** HTMX (`core/vendor/htmx.min.js`, no CDN, no build) loaded `defer`; used **only** for `hx-boost` light progressive enhancement; fully degradable to the no-JS path; the discovery **fragment** enhancement is a **deferred extension point** (not built). | Honours the user-named HTMX + Q2 "light progressive enhancement only"; `defer` + vendored = no first-paint / supply-chain / no-JS cost; touches no closed-feature view; removable in one line (design-for-deletion). | **RATIFIED** 2026-06-28 (DN-PF-DESIGN, Option A) |
| PF-DESIGN-7 | The widget firewall (AC-6 / M5=0), the no-JS path (AC-1), the SEO posture (AC-2), and the a11y floor (AC-7) are **preserved by construction**; **no schema/migration** (C5). | The widget extends/links nothing from core (structural by absence); CSS-only restyle can't remove server content; `:focus-visible`/skip-link/AA-contrast/reduced-motion *raise* the a11y floor; no model work. | **RATIFIED** 2026-06-28 (DN-PF-DESIGN, Option A) |
