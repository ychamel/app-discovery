# CONTROL.md — Project Control Board

**This is the durable channel between the user and the agent.** It replaces chat
memory. The agent reads it first every session and updates it last. The user checks
this one file to know *where we are* and *what is waiting on a decision*.

Rules:
- The agent **must not** act on a question listed under *Decisions Needed From You*
  until the user answers it here.
- Every work session ends by updating *Current State* and the *Activity Log*.
- Keep it short and current. Detail lives in the feature artifacts, not here.

---

## Current State

| Field              | Value                                                            |
|--------------------|------------------------------------------------------------------|
| **Active feature** | **`open-search-browse`** (Phase 2 User loop; dep `app-pages` ✓) |
| **Stage**          | `1-define`                                                       |
| **Persona**        | **Product Analyst** (incoming; see [CLAUDE.md](CLAUDE.md) §2) — drafting [FEATURE_BRIEF.md](features/open-search-browse/FEATURE_BRIEF.md). Public search/browse over the **D-6** accepted-app catalogue: the open discovery surface where **money never buys position** (vision §4.1). A read-side consumer of D-6 (and likely **D-5** tags for filtering); emits D-7 signal on result views per the existing surface segregation. Folder scaffolded (7 artifacts). |
| **Folder**         | [features/open-search-browse/](features/open-search-browse/)     |
| **Last updated**   | 2026-06-23 — **Coordinator** (user: skip post-release, pick the next feature) re-verified the `interest-profile` build (**616 tests green / `ruff` / `check` / no drift**), **skipped the Stage-6 retrospective** and **closed it out** (deferred/reopenable — no prod target/traffic + the matcher/digest consumer is unbuilt; mirrors the seven prior close-outs). Surveyed the backlog for all-deps-met features: `open-search-browse` (dep app-pages ✓), `developer-dashboard` (signal-capture ✓ + ratings-reviews ✓), `developer-updates` (app-subscriptions ✓ + app-pages ✓ + signal-capture ✓). **User picked `open-search-browse`** (D2) — the Phase-2 open discovery surface (search/browse the accepted catalogue, no money-buys-position). Folder already scaffolded (7 artifacts); set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/open-search-browse/FEATURE_BRIEF.md). |

> **Closed out (Stage 6 skipped, reopenable):** `identity-accounts` (2026-06-17, 108
> tests), `interest-taxonomy` (2026-06-17, 184 tests), `submission-intake`
> (2026-06-18, 315 tests), `signal-capture` (2026-06-18, 374 tests — global D-7 event
> schema), `app-pages` (2026-06-20, 417 tests — first live `app_page` impression
> emitter), `ratings-reviews` (2026-06-21, 486 tests — `apps/ratings/`, global D-8
> curated-rating gate), `app-subscriptions` (2026-06-21, 552 tests — `apps/subscriptions/`,
> atomic follow + `subscribe` emit, empty-until-producer notice seam AS-3), and
> `interest-profile` (2026-06-23, 616 tests — `apps/interests/`, the user side of the
> Ring-0 match via the `declared_tag_ids` matcher contract) — all released local/dev,
> rollback rehearsed; live-metrics + outcome reports deferred until a consumer/prod
> target exists.

> Canonical stage values: `0-coordinator` · `1-define` · `2-design` · `3-plan` ·
> `4-build` · `5-release` · `6-post-release` · `done`. See the routing table in
> [CLAUDE.md](CLAUDE.md) §2.

### Latest session status (CLAUDE.md §6.7 — overwritten each session)

```
Stage: 1-define | Feature: open-search-browse | Persona: Coordinator → Product Analyst (incoming)
Done: User asked to skip the interest-profile post-release and pick the next feature. Re-verified the interest-profile build (616 tests green, ruff
      clean, manage.py check clean, makemigrations --check no drift), then SKIPPED the Stage-6 retrospective and CLOSED interest-profile OUT
      (deferred/reopenable — no prod target/traffic + the matcher/digest consumer is unbuilt; mirrors the seven prior close-outs). Surveyed the
      backlog for all-deps-met features: open-search-browse (dep app-pages ✓), developer-dashboard (signal-capture ✓ + ratings-reviews ✓),
      developer-updates (app-subscriptions ✓ + app-pages ✓ + signal-capture ✓). User picked open-search-browse (D2). Activated it: folder already
      scaffolded (7 artifacts), set Stage 1-define, updated INDEX.md, handed to the Product Analyst.
Verified by: 616 tests green / ruff / manage.py check / makemigrations --check no drift (interest-profile build re-verified before close-out).
Blocked/Deferred: interest-profile outcome review deferred/reopenable (no prod target/traffic + unbuilt matcher consumer). editorial-curation-tools
      and weekly-digest remain blocked (they gate each other; plus the Phase-3 dev features not chosen this round).
Decisions needed: none.
Next: Product Analyst — read the open-search-browse request + vision doc (§4.1 no-money-buys-position, §7 open questions), draft
      features/open-search-browse/FEATURE_BRIEF.md, log decisions/open-questions, and raise any scoping decision via Decisions Needed From You.
```

---

## Decisions Needed From You

The agent is blocked on these. Answer inline (edit the **Answer** cell), then the agent
proceeds.

| ID | Decision needed | Context | Answer |
|----|-----------------|---------|--------|
| _none_ | — | — | — |

> Resolved decisions (DN-1 … **DN-16**) are summarized under *Decisions Made* below; full
> rationale lives in the decision logs. `interest-profile` is at `3-plan` — the **DESIGN is
> ratified (DN-16 approved)** and the Planner / Tech Lead is decomposing it into `TASKS.md`.

---

## Decisions Made (recently)

A short, human-readable digest. Full rationale lives in [DECISIONS.md](DECISIONS.md)
(global) or `features/<slug>/DECISIONS.md` (local).

Only the **active/just-closed** feature is kept in full here. Once a feature is closed
out, its lifecycle collapses to a single pointer line — the full record lives in that
feature's `DECISIONS.md` / `RELEASE_NOTES.md` and in [DECISIONS.md](DECISIONS.md).

- **`interest-profile` closed out + `open-search-browse` activated (2026-06-23)** — Coordinator (user: skip post-release, pick the next feature) **re-verified the `interest-profile` build** (**616 tests green / `ruff` / `check` / no drift**), then **skipped the Stage-6 retrospective** and **closed it out** (deferred/reopenable — no prod target/traffic + the matcher/digest consumer is unbuilt; mirrors the seven prior close-outs). Backlog features with all deps met: `open-search-browse` (dep `app-pages` ✓), `developer-dashboard` (`signal-capture` ✓ + `ratings-reviews` ✓), `developer-updates` (`app-subscriptions` ✓ + `app-pages` ✓ + `signal-capture` ✓). **User picked `open-search-browse`** (D2) — the Phase-2 **open discovery surface** (search/browse the **D-6** accepted-app catalogue; the surface where **money never buys position**, vision §4.1). Folder already scaffolded (7 artifacts); set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/open-search-browse/FEATURE_BRIEF.md).
- **`interest-profile` released local/dev (2026-06-22)** — Release Engineer shipped **`apps/interests/`** to local/dev per [DESIGN §16](features/interest-profile/DESIGN.md). Wrote [RELEASE_NOTES.md](features/interest-profile/RELEASE_NOTES.md): an **additive new app owning one mutable table** `interests_interest` (**no parent row** → empty=structural default AC6; **CASCADE** user FK → account-deletion removal with **no `accounts` edit**, AC9; **no score/weight/rank** → AC8 structural; **emits no D-7 event / does not import `signals.capture`** → IP-5). The feature is the **user side of the Ring-0 match** read through the single contract `declared_tag_ids(user) -> frozenset[UUID]` (resolved current `Tag.id`s, AC8) — the future `weekly-digest`/matcher's input; the **§7 set-replace preserve-on-edit** reconcile keeps a *no-successor* retired ref the active picker can't show (AC4 × AC7/M5=0). **No feature flag** — the **two-part activation switch** (`config/urls` `interests/` include + the one `{% interest_prompt %}` line in `accounts/profile.html`) is the rollback; gate-based promotion; metric→signal→alert map (the **one actionable alert** = an unexpected `set_interests` write failure, **not** validation rejects; M2/M3/M6 analyst-derived, expected thin until a digest consumer; M5 reference-integrity 0 by construction via `count_unresolvable`). **Rehearsed rollout→rollback on a throwaway PostgreSQL DB**: `migrate interests` applied `0001` → table + `interests_one_per_user_tag` unique + per-user index + CASCADE `user` FK present → `check` clean → `migrate interests zero` reversed cleanly → re-`migrate` re-applied (**reversible up→down→up**); no drift. Re-verified **616 tests / `ruff` / `check` / no drift**; three `interests:*` routes resolve. Reuses **D-3/D-4/D-5** — **no new global ADR**. Advanced to `6-post-release`; handed to the **Retrospective Analyst** (outcome review expected to defer — no prod target/traffic + matcher unbuilt, as the seven prior closed-outs).
- **`interest-profile` built (2026-06-22)** — Senior Engineer built [TASKS.md](features/interest-profile/TASKS.md) T-01…T-06: new Django app **`apps/interests/`** owning one mutable table `interests_interest` (one row per user×`Tag.id`, **no parent row** → empty=structural default AC6, **CASCADE** user FK AC9, no score/`updated_at`, `interests/0001` reversible). The single write path `set_interests`/`clear_interests` implements the **§7 set-replace preserve-on-edit** reconcile (the named `_preserved_unshowable_ids` keeps a *no-successor* retired ref the active picker can't show → AC4 × AC7/M5=0) with all-or-nothing `is_valid_tag` validation (AC2); the single read surface `declared_tag_ids` is the matcher contract (AC8); the app **never imports `signals.capture`** (IP-5, AST-asserted). Activation = the `config/urls` `interests/` include + the one fail-soft `{% interest_prompt %}` line in `accounts/profile.html` (no flag). **616 tests green (+64), `ruff`/`check`/no drift**; `interests/0001` rehearsed up→down→up. [DECISIONS.md](features/interest-profile/DECISIONS.md) IP-DESIGN-1…4 marked **built**; [CODEMAP.md](CODEMAP.md)/[TEST_PLAN.md](features/interest-profile/TEST_PLAN.md)/[INDEX.md](features/INDEX.md) updated. Reuses **D-3/D-4/D-5** — **no new global ADR**. Advanced to `5-release`; handed to the **Release Engineer**.
- **DN-16 → approved (2026-06-22)** — `interest-profile` **[DESIGN.md](features/interest-profile/DESIGN.md) approved**; all three bundled calls confirmed: **(1)** the architecture — new app `apps/interests/` owning **one mutable** `interests_interest` membership table (`(user, tag_id)`, `unique(user, tag_id)`), a simpler near-twin of `apps/subscriptions/`: **no parent profile row** → empty profile is the structural default (AC6, IP-DESIGN-1); **CASCADE** user FK → removal on account deletion with **no `accounts` edit** (IP-4/AC9); single read surface `declared_tag_ids(user) -> frozenset[UUID]` (resolved current `Tag.id`s, AC8 — the future matcher's contract); **no D-7 emit** (IP-5/IP-DESIGN-3 — the app does **not** import `signals.capture`); reuses **D-3/D-4/D-5**, **no new global ADR**; **(2)** the **§7 set-replace + preserve-on-edit** rule (IP-DESIGN-2) — a save set-replaces the declared set **but preserves any stored id `resolve_tag` maps to a non-active tag** (a *no-successor* retired tag the active-only picker can't show), the minimal rule satisfying AC4 ∧ AC7 so M5=0 holds across edits (the full `clear_interests`/AC9 deliberately bypasses preserve); **(3)** onboarding as a **fail-soft `{% interest_prompt %}` inclusion tag** on the existing `accounts/profile.html` landing (one content line, the 2nd half of the activation switch, IP-DESIGN-4), **not** an auth-flow edit. Software Architect ratified the design (DECISIONS.md Stage-2 section → RATIFIED), set `Stage: 3-plan` + Persona Planner/Tech Lead, emptied Decisions Needed, updated [INDEX.md](features/INDEX.md), and handed to the **Planner / Tech Lead** to decompose [DESIGN.md](features/interest-profile/DESIGN.md) into [TASKS.md](features/interest-profile/TASKS.md).
- **DN-15 → approved (2026-06-21)** — `interest-profile` **[FEATURE_BRIEF.md](features/interest-profile/FEATURE_BRIEF.md) approved**; all three bundled items confirmed: **(1)** the brief as written (the Phase-2 **explicit-interest-declaration substrate** — a signed-in user declares which `interest-taxonomy` tags they care about, the **user side of the Ring-0 match** the future `weekly-digest`/matcher consumes to prove **H1**; 6 stories / 9 G-W-T ACs / 6 metrics; explicit tag declaration only — implicit/behavioral refinement, the matcher/scoring, ring adjacency, interest intensity all **out of scope**; reuses **D-3/D-5**, **no new global decision**); **(2) stored unit (IP-1) = tags-only** — a profile stores `Tag.id`s, clusters are a picker **selection aid** (not separately-stored cluster-level interests; the matcher matches app tags to user tags for Ring-0, cluster adjacency is the matcher's job over the D-5 cluster anchor); **(3) onboarding (IP-2) = optional/non-gating** — new users are prompted but may skip, an **empty profile is a valid handled state** (AC6), revisitable when a digest consumer exists. IP-3 (no hard min/max), IP-4 (mutable state removed on account deletion, AC9), IP-5 (no D-7 emit) carry as working assumptions to Stage 2. Product Analyst folded the resolutions into the brief + [DECISIONS.md](features/interest-profile/DECISIONS.md) (IP-1/IP-2 → RESOLVED) + [OPEN_QUESTIONS.md](features/interest-profile/OPEN_QUESTIONS.md) (OQ-IP-1/OQ-IP-2 → RESOLVED), advanced to `2-design`, updated [INDEX.md](features/INDEX.md), and handed to the **Software Architect** to draft [DESIGN.md](features/interest-profile/DESIGN.md).
- **`app-subscriptions` closed out + `interest-profile` activated (2026-06-21)** — Coordinator (user: skip post-release, pick the next feature) **re-verified the `app-subscriptions` build** (**552 tests green / `ruff` / `check` / no drift**), then **skipped the Stage-6 retrospective** and **closed it out** (deferred/reopenable — no prod target/traffic + no `developer-updates` notice producer to exercise the AS-3 seam; mirrors the six prior close-outs). Backlog features with all deps met: `developer-updates` (newly unblocked by this release), `interest-profile`, `open-search-browse`, `developer-dashboard`. **User picked `interest-profile`** (D2) — the Phase-2 **personalization substrate** (a signed-in user declares which `interest-taxonomy` tags/clusters they care about, D-5 `resolve_tag` soft-retire aware), the **H1** input a future `weekly-digest`/ranking consumes. Folder already scaffolded (7 artifacts); set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/interest-profile/FEATURE_BRIEF.md).
- **DN-14 → approved (2026-06-21)** — `app-subscriptions` **[DESIGN.md](features/app-subscriptions/DESIGN.md) approved**; all three bundled calls confirmed: **(1)** the architecture — a new app `apps/subscriptions/` owning its own **mutable** `subscriptions_subscription` store (**CASCADE**-on-delete, the deliberate contrast with ratings' SET_NULL — a follow is live relationship state, not corpus), the follow-write + its one `subscribe` emit in **one `transaction.atomic()`** (M5 subscribe-events==follows **1:1 by construction**; capture failure rolls back the follow + counts `CAPTURE_ERROR`), the **AS-3=A** empty-until-producer notice seam (`notices_for_apps()→[]` + `Notice` DTO), and an **additive** D-6 read `catalog.get_catalogued_apps(ids)` (bulk, accepted-only, no N+1); **(2) OQ-3** resolved = **no D-7 `unfollow` kind** at MVP (unfollow is an absence D-7 derives by read-time; additively reversible if a churn consumer ever needs it); **(3) OQ-4** resolved = a fail-soft `{% app_follow app %}` inclusion tag in a new `<section aria-label="Follow">` app-page slot (mirrors the `ratings` AP-1 pattern). Reuses **D-3/D-6/D-7 as-is — no new global ADR** (the additive catalog read + CASCADE posture are feature-local). Software Architect ratified the design (DECISIONS.md AS-DESIGN-1…4 + OQ-3/OQ-4 binding), advanced to `3-plan`, updated [INDEX.md](features/INDEX.md), and handed to the **Planner / Tech Lead** to decompose [DESIGN.md](features/app-subscriptions/DESIGN.md) into [TASKS.md](features/app-subscriptions/TASKS.md).
- **DN-13 → approved (2026-06-21)** — `app-subscriptions` **[FEATURE_BRIEF.md](features/app-subscriptions/FEATURE_BRIEF.md) approved**; all three bundled items resolved: **(1)** the brief as written (follow/unfollow + a personal followed-apps return feed + emit one D-7 `subscribe` event per follow; reuses D-3/D-6/D-7, **no new global decision**); **(2) AS-3 = option A** — MVP **ships a forward-compatible, empty-until-producer update/early-access notice surface** in the feed now (authoring stays out → `developer-updates`, Phase 3), fixing AC8 as in scope; **(3) AS-5 confirmed** — account deletion **removes** follow state while already-emitted `subscribe` events follow the existing **SC-10** anonymize-not-purge rule (AC9). Product Analyst folded the resolutions into the brief + [DECISIONS.md](features/app-subscriptions/DECISIONS.md) (AS-3/AS-5 → resolved), advanced to `2-design`, updated [INDEX.md](features/INDEX.md), and handed to the **Software Architect** to draft [DESIGN.md](features/app-subscriptions/DESIGN.md) (folding OQ-3 unfollow-corpus + OQ-4 follow-control placement).
- **`ratings-reviews` closed out + `app-subscriptions` activated (2026-06-21)** — Coordinator (user: skip post-release, start a new feature) **re-verified the `ratings-reviews` build** (**486 tests green / `ruff` / `check` / no drift**) then **skipped the Stage-6 retrospective** and **closed it out** (deferred/reopenable — no prod target/traffic + no `DIGEST` emitter to exercise the gate split; mirrors the five prior close-outs). Backlog features with all deps met: `app-subscriptions`, `interest-profile`, `open-search-browse`, and the **newly-unblocked `developer-dashboard`** (deps `signal-capture` ✓ + `ratings-reviews` ✓). **User picked `app-subscriptions`** (D2) — the Phase-2 user-side engagement loop (follow apps + update/early-access notices) net-new from `signal-capture` SC-7/SC-8 (OQ-4); generates the on-platform signal the corpus measures; unblocks `developer-updates`. Folder already scaffolded; set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the **Product Analyst**.
- **DN-12 → approved (2026-06-21)** — `ratings-reviews` **[DESIGN.md](features/ratings-reviews/DESIGN.md) approved**, all three bundled items confirmed: **(1)** the architecture — new `apps/ratings/` app with its **own mutable store** `ratings_rating`, the curated-rating gate **recorded as a frozen-yet-re-derivable** `weight_eligible`/`eligibility_basis` (OQ-4/RR-4), no score/weight/rank column (AC6 structural), the AP-1 slot via a fail-soft inclusion tag (RR-5), one **additive reversible** index on `signals.Impression`; **(2)** the **new factual `signals.selectors.has_impression`** selector (pure `EXISTS`, D-7-compliant) confirmed as an additive D-7 read-surface extension; **(3)** the gate semantic **promoted to global [D-8](DECISIONS.md)** (PROPOSED → **APPROVED**) — "curated = an organic-curation/`DIGEST` impression; an open `APP_PAGE` view never counts" — now binding on `editorial-curation-tools`/`developer-dashboard`/the Quality Score consumer. Advanced to `2-design` → `3-plan`; handed to the **Planner / Tech Lead** to decompose DESIGN.md into [TASKS.md](features/ratings-reviews/TASKS.md).
- **DN-11 → approved (2026-06-20)** — `ratings-reviews` **[FEATURE_BRIEF.md](features/ratings-reviews/FEATURE_BRIEF.md) approved.** The curated-rating gate is **recorded, not computed** (RR-1 — capture rating + optional review + a per-rating curated-eligibility determination, raw, no scoring; the Quality Score is a downstream consumer), and **"curated to app X" = the author has a `Surface.DIGEST` [D-7](DECISIONS.md) impression of the app** (RR-2 option a; `APP_PAGE`/open views never count — vision §4.1). Anyone signed-in may rate; the gate governs *weight*, not *permission*; outside ratings are displayed-but-unweighted (RR-3). At MVP ~all ratings record *not-eligible* until a `DIGEST` emitter exists (R3 — correct). Reuses D-6/D-7/D-5/D-3 as-is — **no new global decision** (the Architect will weigh promoting "curated = DIGEST impression" to a global ADR, since `editorial-curation-tools`/`developer-dashboard` share it). Advanced to `2-design`; handed to the **Software Architect**.
- **`app-pages` closed out + `ratings-reviews` activated (2026-06-20)** — Coordinator re-verified the `app-pages` build on the user's request (**417 tests / `ruff` / `check` / no drift**; `apps/pages/` deliverables present and matching TASKS.md T-01…T-06; 3 routes resolve; TEST_PLAN covers AC1–AC9), then **skipped the Stage-6 retrospective** per user (outcome review deferred/reopenable — no prod target/traffic; mirrors `identity-accounts`/`interest-taxonomy`/`submission-intake`/`signal-capture`). `app-pages` newly unblocked **open-search-browse / ratings-reviews / app-subscriptions**; user picked **`ratings-reviews`** (fills the empty reviews slot `app-pages` left — AP-1; proves H1/H3; unblocks `developer-dashboard`). Set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the Product Analyst.
- **`app-pages` released local/dev (2026-06-20)** — Release Engineer shipped **`apps/pages/`** (new app owning no model/migration; 3 thin views + the uniform `app_page` template) to local/dev per [DESIGN §12](features/app-pages/DESIGN.md): additive, **no feature flag** ("off" = don't include the URLconf). [RELEASE_NOTES.md](features/app-pages/RELEASE_NOTES.md) written (gate-based promotion, metric→signal→alert map, known limits); **first live emitter of D-7 `app_page` impressions** (AP-3/4/5). **Rollout→rollback rehearsed** on a throwaway Postgres DB — `signals/0002` (the additive `Surface.APP_PAGE` choices-only alter) applies and reverses cleanly (reversible); primary rollback = remove the `config/urls` include (zero data migration, app owns no schema). **417 tests / `ruff` / `check` / no drift** re-verified. Advanced to `6-post-release`. Outcome review deferred (no prod target/traffic — mirrors the four prior closed-outs).
- **DN-10 → approved (2026-06-20)** — `app-pages` **[DESIGN.md](features/app-pages/DESIGN.md) approved** (AP-3 page-view = `app_page`-surface impression confirmed — reinterprets the brief's "impression generation out of scope" as *not running the curated-feed allocator*, distinct + `surface`-segregated; AP-4 authenticated-only capture / anonymous render confirmed). Planner/Tech Lead decomposed it into [TASKS.md](features/app-pages/TASKS.md) (6 tasks, no `L`); advanced to `4-build`, handed to the Senior Engineer.
- **DN-9 → approved (2026-06-20)** — `app-pages` **[FEATURE_BRIEF.md](features/app-pages/FEATURE_BRIEF.md) approved** (reviews = slot-only AP-1; press-kit = the page AP-2). Advanced to `2-design`; the Software Architect drafted [DESIGN.md](features/app-pages/DESIGN.md) (see DN-10).
- **DN-8 → A (2026-06-19)** — **`app-pages` activated** as the next feature (Phase 1 Catalog; dep `submission-intake` ✓), chosen over `interest-profile` (Phase 2). It is the public surface that renders accepted apps (a D-6 consumer) and the widest downstream unblock (`open-search-browse`, `ratings-reviews`, `app-subscriptions`, and via those `weekly-digest`). Folder already scaffolded from backlog; set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the Product Analyst.
- **DN-7 → A (2026-06-18)** — `signal-capture` **closed out**; Stage-6 retrospective **skipped** (outcome review deferred/reopenable — it needs a live emitter, `weekly-digest`, that doesn't exist yet; mirrors `identity-accounts` and DN-1/DN-3). Returned to Coordinator. All four Phase-0/early features now closed-out. Surveyed backlog → `app-pages` (Phase 1) and `interest-profile` (Phase 2) have all deps met. Raised **DN-8** to pick the next feature (D2).
- **`signal-capture` lifecycle (2026-06-18)** — brief approved (DN-5) → pivoted to on-platform engagement (SC-7/SC-8) → DESIGN + global **[D-7](DECISIONS.md)** event schema ratified (DN-6) → built `apps/signals/` (T-01…T-09, 59 new / **374 total** tests green) → released local/dev (rollback rehearsed). 4 append-only tables (`Impression` anchor + frozen `ImpressionTag` snapshot + uniform `EngagementEvent` `kind` discriminator + `PlatformVisit`); structural no-score + no-PII whitelist; `SET_NULL`-anonymize on events (**SC-10**, resolves the `submission-intake` deletion deferral); derived-at-read funnel (**SC-9**). Live-metrics + prod promotion deferred (no live emitter, R6). Full detail: [signal-capture/DECISIONS.md](features/signal-capture/DECISIONS.md), [RELEASE_NOTES.md](features/signal-capture/RELEASE_NOTES.md).
- **`submission-intake` (closed 2026-06-18)** — `apps/catalog/`, 4 tables, fixed 5-floor gate enum (no "other"), ACCEPTED-only catalogue substrate = global **[D-6](DECISIONS.md)**. Built T-01…T-14 (315 total tests), released local/dev, closed out (DN-3). Detail: [submission-intake/DECISIONS.md](features/submission-intake/DECISIONS.md).
- **`interest-taxonomy` (closed 2026-06-17)** — flat tags + named clusters (M2M), UUID identity, soft-retire + read-time `resolve_tag` = global **D-5**. Built T-01…T-10 (184 total tests), released local/dev, closed out (DN-1). Detail: [interest-taxonomy/DECISIONS.md](features/interest-taxonomy/DECISIONS.md).

> **Foundational decisions** (`identity-accounts`, the stack, niche, build order — D1–D3,
> A1–A3, R1, DL-1/2, PL-1, global D-1/D-2/D-4) are recorded in [DECISIONS.md](DECISIONS.md)
> and the per-feature `DECISIONS.md` files. Highlights: niche = **"vibecoded webapps"**
> (D-1); stack = **Python / Django + PostgreSQL** (D-4); `identity-accounts` released
> local/dev (R1, 108 tests).

---

## Activity Log

Newest first. One line per session. **Keep the most recent ~10 rows here**; older rows
live in [process/activity-archive.md](process/activity-archive.md). The per-feature
folders remain the full record either way.

| Date       | Stage           | Summary                                                                 |
|------------|-----------------|-------------------------------------------------------------------------|
| 2026-06-23 | `6-post-release`→`0-coordinator`→`1-define` | **Coordinator** (user: skip post-release, pick the next feature) — **re-verified the `interest-profile` build** (**616 tests / `ruff` / `check` / no drift**), then **skipped the Stage-6 retrospective** and **closed `interest-profile` out** (deferred/reopenable — no prod target/traffic + the matcher/digest consumer is unbuilt; mirrors the seven prior close-outs). Surveyed the backlog for all-deps-met features: `open-search-browse` (dep `app-pages` ✓), `developer-dashboard` (`signal-capture` ✓ + `ratings-reviews` ✓), `developer-updates` (`app-subscriptions` ✓ + `app-pages` ✓ + `signal-capture` ✓). **User picked `open-search-browse`** (D2) — the Phase-2 **open discovery surface** (search/browse the **D-6** accepted-app catalogue; the surface where **money never buys position**, vision §4.1). Folder already scaffolded (7 artifacts); set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/open-search-browse/FEATURE_BRIEF.md). |
| 2026-06-22 | `5-release`→`6-post-release` | **Release Engineer** — released **`apps/interests/`** to local/dev per [DESIGN §16](features/interest-profile/DESIGN.md). Wrote [RELEASE_NOTES.md](features/interest-profile/RELEASE_NOTES.md): an **additive new app owning one mutable table** `interests_interest` (**no parent row** → empty=structural default AC6; **CASCADE** user FK → account-deletion removal with **no `accounts` edit**, AC9; **no score/weight/rank** → AC8 structural; **emits no D-7 event / does not import `signals.capture`** → IP-5). Documented the single read contract `declared_tag_ids(user) -> frozenset[UUID]` (the matcher's Ring-0 input, AC8), the **§7 set-replace preserve-on-edit** reconcile (a *no-successor* retired ref survives a re-save → AC4 × AC7/M5=0), the **two-part activation switch** (`config/urls` `interests/` include + the one `{% interest_prompt %}` line in `accounts/profile.html`) as the rollback (**no feature flag**), gate-based promotion, and the metric→signal→alert map (the **one actionable alert** = an unexpected `set_interests` write failure, **not** validation rejects; M2/M3/M6 analyst-derived, expected thin until a digest consumer; M5 reference-integrity 0 by construction via `count_unresolvable`). **Rehearsed rollout→rollback on a throwaway PostgreSQL DB** (`interests_release_rehearsal`, dropped): `migrate interests` applied `0001_initial` → `interests_interest` + the `interests_one_per_user_tag` unique + the per-user `user_id` index + the CASCADE `user` FK to `accounts_account` all present → `check` clean → `migrate interests zero` reversed cleanly (table gone) → re-`migrate` re-applied (**reversible up→down→up**); `makemigrations --check` no drift. Re-verified **616 tests green (+64) / `ruff` / `check` / no drift**; the three `interests:picker`/`:save`/`:clear` routes resolve; both activation halves + the six observability constants + the two config tunables (`interest_suggested_minimum`/`interest_declaration_max`) present. Updated [INDEX.md](features/INDEX.md) (→ `6-post-release`). Advanced to `6-post-release`; handed to the **Retrospective Analyst** (outcome review expected to defer — no prod target/traffic + the matcher/digest consumer unbuilt, as the seven prior closed-outs). |
| 2026-06-22 | `4-build`→`5-release` | **Senior Engineer** — built [interest-profile/TASKS.md](features/interest-profile/TASKS.md) T-01…T-06: new Django app **`apps/interests/`** (a simpler near-twin of `apps/subscriptions/`, minus the D-7 emit + notices seam). **T-01** the `Interest` **CASCADE** membership store (one row per user×`Tag.id`, `unique(user,tag_id)`, **no parent row** → empty=structural default AC6, no score/`updated_at` AC8, `interests/0001` reversible up→down→up, AC9 deletion test) → **T-02** the single write path `set_interests`/`clear_interests` with the **§7 set-replace preserve-on-edit** reconcile (the named `_preserved_unshowable_ids` keeps a *no-successor* retired ref the active picker can't show → AC4 × AC7/M5=0; successor-normalization case) + all-or-nothing `is_valid_tag` validation (AC2, `SetResult`, `InterestValidationError`) + two config tunables + four write metrics + the **IP-5 AST import-absence** assertion (no `signals.capture`) → **T-03** the single read surface `declared_tag_ids` (frozenset matcher contract AC8)/`declared_tags`/`has_declared_interests` (resolved, deduped, AC6/AC7) + the M5 `count_unresolvable` invariant (reuses `TAXONOMY_REFERENCE_BREAK`) → **T-04** thin `picker`/`save`/`clear` views + `urls` + cluster-grouped active `picker.html` + the `config/urls` `interests/` include (activation half 1) + `INTEREST_PICKER_DEGRADED` → **T-05** the fail-soft `{% interest_prompt %}` tag + `_prompt_slot.html` + the one content line in `accounts/profile.html` (activation half 2, no auth-logic edit) + `INTEREST_PROMPT_DEGRADED` → **T-06** read-only `Interest` admin + [README](apps/interests/README.md) + [CODEMAP](CODEMAP.md) + DECISIONS (IP-DESIGN-1…4 **built**). Wrote [TEST_PLAN.md](features/interest-profile/TEST_PLAN.md) (per-AC → test + edge/security/failure/regression). **616 tests green (+64), `ruff`/`check`/no drift**; tested against the real taxonomy D-5 surface (no `is_valid_tag`/`resolve_tag` mocking). Reuses **D-3/D-4/D-5** — **no new global ADR**. Updated [INDEX.md](features/INDEX.md) (→ `5-release`). Advanced to `5-release`; handed to the **Release Engineer**. |
| 2026-06-22 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the ratified [interest-profile/DESIGN.md](features/interest-profile/DESIGN.md) into [TASKS.md](features/interest-profile/TASKS.md): **6 ordered tasks (all S/M, no `L`)**, risk-front-loaded on the one load-bearing seam the DESIGN §7/§14 names — the **set-replace preserve-on-edit reconcile** (`_preserved_unshowable_ids` keeps a *no-successor* retired ref the active-only picker can't show so **AC4 × AC7/M5=0** hold across edits) — landed at **T-02** and unit-tested in isolation against a real D-5 no-successor `retire_tag` state before any UI. Spine: **T-01** the `Interest` **CASCADE** membership store (one row per user×`Tag.id`, **no parent row** → empty=structural default AC6, structural no-score/no-`updated_at` AC8, `interests/0001`, AC9 deletion test) → **T-02** the single write path `set_interests`/`clear_interests` (the §7 reconcile + all-or-nothing `is_valid_tag` validation AC2 + `SetResult` + config tunables `interest_suggested_minimum`/`interest_declaration_max` + the four write metrics + the **IP-5 no-`signals.capture`-import** assertion) → **T-03** the single read surface `declared_tag_ids`/`declared_tags`/`has_declared_interests` (resolved+deduped, AC7/AC8 — the matcher contract) + the M5 `count_unresolvable` ops selector (reuses `TAXONOMY_REFERENCE_BREAK`) → **T-04** thin `picker`/`save`/`clear` views + `urls` + `config/urls` include (activation half 1) + the cluster-grouped active picker + empty/empty-vocab/fail-soft states → **T-05** the fail-soft `{% interest_prompt %}` onboarding nudge + the one content-only `accounts/profile.html` line (activation half 2, no auth-logic edit) → **T-06** read-only admin + README/CODEMAP/DECISIONS finalize. Full **design-element + AC1–AC9** coverage table + AC roll-up; file-collision note (sequential `observability.py`/`config.py` edits). **No re-design** — every task traces to a DESIGN §; cross-checked all referenced contracts against the codebase (taxonomy selectors, `retire_tag(replaced_by=None)`, `accounts.delete_account` CASCADE, `verify`→`profile`, the `_positive_int`/`validate_all` + observability patterns). Reuses **D-3/D-4/D-5** — **no new global ADR**. Updated [INDEX.md](features/INDEX.md) (`interest-profile` → `4-build`). Advanced to `4-build`; handed to the **Senior Engineer** (build T-01 first, produce `TEST_PLAN.md`). |
| 2026-06-22 | `2-design`→`3-plan` | **Software Architect** (hand-off, DN-16 approved) — user answered DN-16 **approve**, so **ratified [interest-profile/DESIGN.md](features/interest-profile/DESIGN.md)** and performed the Stage-2→3 hand-off **only** (no second persona this session). The approved design stands unchanged: new Django app **`apps/interests/`** owning **one mutable table** `interests_interest` (one row per user×`Tag.id`) — a **simpler near-twin of `apps/subscriptions/`** (own mutable store + single write/read path + fail-soft inclusion tag + activation include), **minus** the D-7 emit (IP-5/IP-DESIGN-3: no `signals.capture` import) and the notices seam. **No parent profile row** → empty profile is the structural default (AC6, IP-DESIGN-1); **CASCADE** user FK → removal on account deletion with **no `accounts` edit** (IP-4/AC9); the **§7 set-replace + preserve-on-edit** rule (IP-DESIGN-2) preserves a *no-successor* retired tag the active-only picker can't show so AC7/M5=0 hold across edits; single read surface `declared_tag_ids(user) -> frozenset[UUID]` (AC8); onboarding via the fail-soft `{% interest_prompt %}` inclusion tag on `accounts/profile.html` (IP-DESIGN-4). Marked the [DECISIONS.md](features/interest-profile/DECISIONS.md) Stage-2 section **RATIFIED**; set `Stage: 3-plan` + Persona **Planner/Tech Lead** in Current State; **emptied Decisions Needed** (DN-16 resolved); added the Decisions-Made digest; updated [INDEX.md](features/INDEX.md) (`interest-profile` → `3-plan`). Reuses **D-3/D-4/D-5** as-is — **no new global ADR**. Advanced to `3-plan`; handed to the **Planner / Tech Lead** to decompose [DESIGN.md](features/interest-profile/DESIGN.md) into [TASKS.md](features/interest-profile/TASKS.md). |
| 2026-06-22 | `2-design` (awaiting DN-16) | **Software Architect** — drafted [interest-profile/DESIGN.md](features/interest-profile/DESIGN.md) via the 14-step protocol. New Django app **`apps/interests/`** owning **one mutable table** `interests_interest` (one row per user×`Tag.id`) — a **simpler near-twin of `apps/subscriptions/`** (own mutable store + single write/read path + fail-soft inclusion tag + activation include) with two deliberate simplifications and one harder seam. **Simplifications:** **(a) no D-7 emit** — the app does **not** import `signals.capture` (the cleanest proof of IP-5; declaration is preference state, not behavior); **(b) no parent profile row** — the profile *is* the set of `(user, tag_id)` rows, so an **empty profile is the structural default** (AC6), and a **CASCADE** user FK removes it on account deletion with **no `accounts` edit** (IP-4/AC9). **The harder seam (§7):** the picker's save is a **set-replace** that **preserves a stored ref the active-only picker can't show** — a *no-successor* retired tag (`retire_tag` allows `replaced_by=None`, **verified** in `apps/taxonomy/services.py`) — so AC7/M5=0 hold across edits (the minimal rule satisfying AC4 ∧ AC7). Single read surface **`declared_tag_ids(user) -> frozenset[UUID]`** (resolved current `Tag.id`s, AC8 — the future matcher's contract). Onboarding = a **fail-soft `{% interest_prompt %}` inclusion tag** on the existing `accounts/profile.html` landing (one content line, no auth-flow edit). Logged **IP-DESIGN-1…4** ([DECISIONS.md](features/interest-profile/DECISIONS.md)), re-affirmed **OQ-IP-3/OQ-IP-4** out-of-scope ([OPEN_QUESTIONS.md](features/interest-profile/OPEN_QUESTIONS.md)). Full **AC1–AC9 → design-element** map (§15), per-component failure modes (§9), ≥3 rejected alternatives + sacrifices (§14). Reuses **D-3/D-4/D-5** as-is — **no new global ADR**. Updated [INDEX.md](features/INDEX.md). **Raised DN-16** (approve DESIGN incl. §7 preserve-on-edit + onboarding placement + the additive `declared_tag_ids` contract); **no Stage advance until approved.** |
| 2026-06-21 | `1-define`→`2-design` | **Product Analyst** (DN-15 approved) — drafted [interest-profile/FEATURE_BRIEF.md](features/interest-profile/FEATURE_BRIEF.md): the Phase-2 **explicit-interest-declaration substrate** — a signed-in user declares which `interest-taxonomy` tags they care about, the **user side of the Ring-0 match** (vision §2.2/§3.1/§6) that the future `weekly-digest`/matcher consumes to prove **H1**. **6 stories / 9 G-W-T ACs / 6 metrics** (M1 declaration rate = adoption headline; M6 match-readiness = the *why*; M5 reference-integrity must be 0). Explicit tag declaration only — **implicit/behavioral refinement, the matcher/scoring, ring adjacency, and interest intensity are all out of scope**. Reuses **D-3** (account/role) + **D-5** (`Tag.id` ref, `is_valid_tag` write / `resolve_tag` read, soft-retire aware) — **no new global decision**. Logged **IP-1…IP-5** ([DECISIONS.md](features/interest-profile/DECISIONS.md)) + **OQ-IP-1…OQ-IP-4** ([OPEN_QUESTIONS.md](features/interest-profile/OPEN_QUESTIONS.md)). Money-buys-position vision test → **PASS**. **User answered DN-15: approve as written + stored unit = tags-only (IP-1) + onboarding = optional/non-gating (IP-2).** Folded the resolutions into the brief (→ APPROVED) + DECISIONS.md (IP-1/IP-2 → RESOLVED) + OPEN_QUESTIONS.md (OQ-IP-1/OQ-IP-2 → RESOLVED); performed the **Stage-1→2 hand-off only** (no second persona). Set `Stage: 2-design` + Persona **Software Architect**, emptied Decisions Needed, updated [INDEX.md](features/INDEX.md). Advanced to `2-design`; handed to the **Software Architect** to draft [DESIGN.md](features/interest-profile/DESIGN.md). |
| 2026-06-21 | `6-post-release`→`0-coordinator`→`1-define` | **Coordinator** (user: skip post-release, pick the next feature) — **re-verified the `app-subscriptions` build** (**552 tests / `ruff` / `check` / no drift**), then **skipped the Stage-6 retrospective** and **closed `app-subscriptions` out** (deferred/reopenable — no prod target/traffic + no `developer-updates` notice producer to exercise the AS-3 seam; mirrors the six prior close-outs). Surveyed the backlog for all-deps-met features: `developer-updates` (newly unblocked by this release), `interest-profile`, `open-search-browse`, `developer-dashboard`. **User picked `interest-profile`** (D2) — the Phase-2 **personalization substrate** (a signed-in user declares which `interest-taxonomy` tags/clusters they care about, D-5 `resolve_tag` soft-retire aware); the **H1** input a future `weekly-digest`/ranking consumes. Folder already scaffolded (7 artifacts); set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/interest-profile/FEATURE_BRIEF.md). |
| 2026-06-21 | `5-release`→`6-post-release` | **Release Engineer** — released **`apps/subscriptions/`** to local/dev per [DESIGN §15](features/app-subscriptions/DESIGN.md). Wrote [RELEASE_NOTES.md](features/app-subscriptions/RELEASE_NOTES.md): an **additive new app owning one mutable table** `subscriptions_subscription` (**CASCADE** `user` FK — the AS-5/AC9 contrast with ratings' SET_NULL); the **atomic** follow-write + its one `subscribe` emit (M5 1:1 by construction); the empty-until-producer `notices.Notice` seam (AS-3=A); the additive no-N+1 `catalog.get_catalogued_apps` read; **no feature flag** ("off" = remove the `config/urls` `subscriptions/` include + the one `app_page.html` Follow section — the **two-part activation switch**); gate-based promotion (schema→surface→write path→capture 1:1→display→stable); metric→signal→alert map tied to brief M1–M6 (the **one actionable alert** = `capture_error{kind=subscribe}`; M3 the headline is analyst-derived, expected thin until adoption + `developer-updates`). **Rehearsed rollout→rollback on a throwaway PostgreSQL DB** (`subs_release_rehearsal`, dropped): `migrate subscriptions` applied `0001` → `subscriptions_subscription` + the `subscriptions_one_per_user_app` unique + `subscriptions_user_created_idx` + the CASCADE `user` FK to `accounts_account` all present → `check` clean → `migrate subscriptions zero` reversed cleanly (table gone) → re-`migrate` re-applied (**reversible up→down→up**); no drift. Re-verified **552 tests / `ruff` / `check` / no drift**; the three `subscriptions:*` routes resolve; both activation halves + the six observability constants + `followed_feed_page_size()` present. Updated [INDEX.md](features/INDEX.md) (→ `6-post-release`). Advanced to `6-post-release`; handed to the **Retrospective Analyst** (outcome review expected to defer — no prod target/traffic + no `developer-updates` notice producer, as the six prior closed-outs). |
| 2026-06-21 | `4-build`→`5-release` | **Senior Engineer** — built [app-subscriptions/TASKS.md](features/app-subscriptions/TASKS.md) T-01…T-08: the new Django app **`apps/subscriptions/`** (near-twin of `apps/ratings/`). **T-01** additive bulk `catalog.get_catalogued_apps(ids)` (accepted-only, no N+1, no migration) → **T-02** the **mutable** `Subscription` store (**CASCADE** `user` FK = the AS-5/AC9 contrast with ratings' SET_NULL; structural **no** score/`updated_at`/soft-delete; `unique(user,app_id)`; `subscriptions/0001`) → **T-03** the single write path `services.follow_app`/`unfollow_app` + `UnknownAppError` + the **ATOMIC** follow-row + `record_subscribe` emit in **one `transaction.atomic()`** (M5 1:1 by construction; forced-capture-failure rollback + `CAPTURE_ERROR{kind=subscribe}` verified; `unfollow` emits no corpus event, OQ-3) → **T-04** `selectors.is_following`/`followed_apps` (bulk D-6, query count independent of follow count) + `followed_feed_page_size` config → **T-05** `notices.Notice` (frozen DTO) + `notices_for_apps`→`[]` (empty-until-producer, AS-3=A) → **T-06** thin `follow`/`unfollow`/`feed` views + `feed.html` two-region template + the `config/urls` `subscriptions/` include (fail-soft feed/notice, no-IDOR, CSRF, `login_required`) → **T-07** the fail-soft `{% app_follow %}` inclusion tag + `_follow_slot.html` + the **one content-only** `app_page.html` Follow section after the header (OQ-4) → **T-08** read-only admin + [README](apps/subscriptions/README.md) + [CODEMAP](CODEMAP.md) + DECISIONS (built) + revisit flags. Wrote [TEST_PLAN.md](features/app-subscriptions/TEST_PLAN.md) (AC1–AC9 → tests). **552 tests green (+66), `ruff`/`check`/no drift**; `subscriptions/0001` rehearsed up→down→up. One sanctioned cross-feature regression update: `apps/pages` `test_template.py` slot-count **6→7** (the §5f Follow section; uniformity preserved). Reuses **D-3/D-6/D-7** — **no new global ADR**. Updated [INDEX.md](features/INDEX.md) (→ `5-release`). Advanced to `5-release`; handed to the **Release Engineer**. |
| 2026-06-21 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the approved [app-subscriptions/DESIGN.md](features/app-subscriptions/DESIGN.md) into [TASKS.md](features/app-subscriptions/TASKS.md): **8 ordered tasks (all S/M, no `L`)**, risk-front-loaded on the two coupling-heavy seams the DESIGN §1/§14 self-critique names — the **CASCADE** mutable store (**T-02**: own `subscriptions_subscription`, `user` FK CASCADE = the AS-5/AC9 contrast with ratings' SET_NULL, structural no-score/no-`updated_at`, unique(user,app_id), AC9 deletion test) and the **atomic** follow-write + `subscribe` emit (**T-03**: `record_subscribe` *inside* the `get_or_create` `transaction.atomic()` only when `created` → M5 1:1 by construction, verified by a forced-capture-failure rollback test) — with the closed-out-template edit isolated to **T-07** (content-only `app_page.html` Follow-slot insertion after `<header>`, OQ-4, one-section rollback). Spine: **T-01** additive bulk `catalog.get_catalogued_apps(ids)` (the no-N+1 feed primitive, no migration) → T-02 store → T-03 write path (+ `errors`, the three `SUBSCRIPTION_*` metrics) → **T-04** read path `is_following`/`followed_apps` (2-query bulk D-6) + `followed_feed_page_size` config → **T-05** the empty-until-producer notice seam (`Notice` DTO + `notices_for_apps`→`[]`, AS-3=A) → **T-06** thin `follow`/`unfollow`/`feed` views + `feed.html` two-region template + the `config/urls` include (fail-soft feed/notice, no-IDOR, CSRF) → T-07 the `{% app_follow %}` tag + the one slot edit → **T-08** read-only admin + README/CODEMAP/DECISIONS finalize. Full **design-element + AC1–AC9** coverage table + AC roll-up; file-collision note (sequential `observability.py`/`config.py` edits). **No re-design** — every task traces to a DESIGN §; cross-checked all referenced contracts against the codebase (`record_subscribe` signature, catalog `_resolve_tag_labels`/`_to_catalog_app`/`CatalogApp`, `accounts.delete_account`, `app_page.html` `<header>`/slots, `apps/core` config+observability patterns). Reuses **D-3/D-6/D-7** — **no new global ADR**. Updated [INDEX.md](features/INDEX.md) (`app-subscriptions` → `4-build`). Advanced to `4-build`; handed to the **Senior Engineer** (build T-01 first, produce `TEST_PLAN.md`). |

> Older rows (`app-subscriptions` define/design, the `ratings-reviews` close-out +
> `ratings-reviews` build/release, `app-pages` close-out/release/define/design,
> `signal-capture` lifecycle, `submission-intake` close-out & earlier, `interest-taxonomy`,
> `identity-accounts`) live in [process/activity-archive.md](process/activity-archive.md).
