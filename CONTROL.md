# CONTROL.md — Project Control Board

**This is the durable channel between the user and the agent.** It replaces chat
memory. The agent reads it first every session and updates it last. The user checks
this one file to know *where we are* and *what is waiting on a decision*.

Rules:
- The agent **must not** act on a question listed under *Decisions Needed From You*
  until the user answers it here.
- Every work session ends by updating *Current State* and the *Activity Log*.
- Keep it short and current. Detail lives in the feature artifacts, not here.

---

## Current State

| Field              | Value                                                            |
|--------------------|------------------------------------------------------------------|
| **Active feature** | **`developer-updates`** (Phase 3 Dev value; deps `app-subscriptions` ✓ + `app-pages` ✓ + `signal-capture` ✓ — all met) |
| **Stage**          | `1-define` — **Product Analyst** |
| **Persona**        | **Product Analyst** — draft [FEATURE_BRIEF.md](features/developer-updates/FEATURE_BRIEF.md) for **developer-updates**, the Phase-3 developer-side channel (net-new from `signal-capture` SC-7/SC-8 / OQ-4): let a developer **post updates / early-access / talk to their subscribers** — the platform as the dev's front page. Reads: the user request, the vision doc (§6 Dev-facing), and the upstream contracts it composes — **D-7** signals (`apps/signals`, the `subscribe` events from `app-subscriptions`), the **`subscriptions_subscription`** follow graph (`apps/subscriptions`, the audience), and the **D-6** owner-scoped catalogue (`catalog.list_owned_apps`/`get_owned_app`) + the dev-role gate (**D-3**). The `app-subscriptions` build deliberately left an **empty-until-producer notice seam (AS-3)** — developer-updates is the producer. Define scope (in/out), G-W-T acceptance criteria, success metrics; log open questions + proposed decisions; **raise the brief-approval decision and do not advance until the user approves.** |
| **Folder**         | [features/developer-updates/](features/developer-updates/)   |
| **Last updated**   | 2026-06-24 (Coordinator: user said skip post-release + pick a new feature → re-verified the developer-dashboard build [740 tests / ruff / check / no drift], **skipped the Stage-6 retrospective and closed developer-dashboard out** [deferred/reopenable], then surveyed the backlog and — user picked **developer-updates** [D2, the only all-deps-met option; the editorial-curation-tools↔weekly-digest pair stays mutually blocked] — set `Stage: 1-define`, handed to the **Product Analyst**; see latest session status below) |

> **Closed out (Stage 6 skipped, reopenable):** `identity-accounts` (2026-06-17, 108
> tests), `interest-taxonomy` (2026-06-17, 184 tests), `submission-intake`
> (2026-06-18, 315 tests), `signal-capture` (2026-06-18, 374 tests — global D-7 event
> schema), `app-pages` (2026-06-20, 417 tests — first live `app_page` impression
> emitter), `ratings-reviews` (2026-06-21, 486 tests — `apps/ratings/`, global D-8
> curated-rating gate), `app-subscriptions` (2026-06-21, 552 tests — `apps/subscriptions/`,
> atomic follow + `subscribe` emit, empty-until-producer notice seam AS-3),
> `interest-profile` (2026-06-23, 616 tests — `apps/interests/`, the user side of the
> Ring-0 match via the `declared_tag_ids` matcher contract), `open-search-browse`
> (2026-06-23, 676 tests — `apps/discovery/`, the paginated DB-pushed
> `catalog.search_catalogue` open surface where money never buys position), and
> `developer-dashboard` (2026-06-24, 740 tests — model-less `apps/dashboard/`, a
> read-only view of an app's reception [D-7 reach/engagement + D-8 curated ratings] for
> the developer who owns it; two additive neutral `signals.selectors` reads, no schema) —
> all released local/dev, rollback rehearsed; live-metrics + outcome reports deferred
> until a consumer/prod target exists.

> Canonical stage values: `0-coordinator` · `1-define` · `2-design` · `3-plan` ·
> `4-build` · `5-release` · `6-post-release` · `done`. See the routing table in
> [CLAUDE.md](CLAUDE.md) §2.

### Latest session status (CLAUDE.md §6.7 — overwritten each session)

```
Stage: 6-post-release → 0-coordinator → 1-define | Feature: developer-dashboard (closed out) → developer-updates (activated) | Persona: Coordinator
Done: User said "skip post release and pick a new feature." Re-verified the released developer-dashboard build is green (740 tests passed/0 failed/0 skipped; ruff check
      clean; manage.py check no issues; makemigrations --check --dry-run "No changes detected" — matches the released state). SKIPPED the Stage-6 retrospective and CLOSED
      developer-dashboard OUT (deferred/reopenable — no prod target/traffic; M3/M4 curated reach thin until a DIGEST emitter ships [honest, not a bug, per brief R1/AC4];
      M5 owner-scope leak target 0 structural; mirrors the nine prior close-outs). Surveyed the backlog for all-deps-met features: developer-updates (app-subscriptions ✓ +
      app-pages ✓ + signal-capture ✓) is the only one startable; editorial-curation-tools and weekly-digest stay mutually blocked (each lists the other as a dep). User
      picked developer-updates (D2) — the Phase-3 developer-side channel (net-new from signal-capture SC-7/SC-8 / OQ-4): post updates / early-access / talk to subscribers;
      it is the producer for the empty-until-producer notice seam app-subscriptions left (AS-3). Folder already scaffolded (7 artifacts). Set Stage: 1-define + Persona
      Product Analyst in Current State; updated INDEX.md (developer-dashboard -> closed-out, developer-updates -> 1-define).
Verified by: python manage.py test (740 passed, 0 failed/0 skipped); ruff check . (All checks passed); python manage.py check (no issues); makemigrations --check --dry-run
      (No changes detected). Confirmed features/developer-updates/ has all 7 scaffolded artifacts (FEATURE_BRIEF/DESIGN/TASKS/TEST_PLAN/RELEASE_NOTES/OPEN_QUESTIONS/DECISIONS).
Blocked/Deferred: developer-dashboard live-metrics window (M1–M6) + prod promotion deferred (no prod target/traffic) — reopenable. editorial-curation-tools ↔ weekly-digest
      remain mutually blocked (each is the other's dep); starting either needs that dependency edge cut first — a future D2.
Decisions needed: none. (Next decision point = the developer-updates brief-approval call, raised by the Product Analyst after the brief is drafted.)
Next: Product Analyst — draft features/developer-updates/FEATURE_BRIEF.md (Phase-3 developer-side channel: post updates / early-access / talk to subscribers), grounding
      every dependency in code (D-7 signals incl. the subscribe events, the subscriptions_subscription follow graph as the audience, D-6 owner-scoping, D-3 dev-role gate;
      the AS-3 notice seam it produces); log OQs + proposed decisions; raise the brief-approval decision and do not advance until approved.
```

---

## Decisions Needed From You

The agent is blocked on these. Answer inline (edit the **Answer** cell), then the agent
proceeds.

| ID | Decision needed | Recommendation | **Answer** |
|----|-----------------|----------------|------------|
| _(none)_ | — | — | — |

> **Nothing is waiting on you.** The developer-dashboard is **released to local/dev** (740 tests
> green, [RELEASE_NOTES.md](features/developer-dashboard/RELEASE_NOTES.md) written, rollback
> rehearsed). The Retrospective Analyst now reviews outcome vs the brief. The next decision point
> is the **close-out call** (skip the Stage 6 retrospective + close out, as the prior nine).
>
> Resolved decisions (DN-1 … **DN-19**, **DN-DD-DESIGN**) are summarized under *Decisions Made*
> below; full rationale lives in the decision logs. **DN-DD-DESIGN** (approve the
> developer-dashboard DESIGN) was answered **2026-06-24**: approved → DD-DESIGN-1…5 ratified,
> OQ-DD-4 resolved.

---

## Decisions Made (recently)

A short, human-readable digest. Full rationale lives in [DECISIONS.md](DECISIONS.md)
(global) or `features/<slug>/DECISIONS.md` (local).

Only the **active/just-closed** feature is kept in full here. Once a feature is closed
out, its lifecycle collapses to a single pointer line — the full record lives in that
feature's `DECISIONS.md` / `RELEASE_NOTES.md` and in [DECISIONS.md](DECISIONS.md).

- **`developer-dashboard` closed out + `developer-updates` activated (2026-06-24)** — Coordinator (user: skip post-release, pick the next feature): **re-verified the `developer-dashboard` build** (**740 tests green / `ruff` / `check` / no drift** — matches the released state), then **skipped the Stage-6 retrospective** and **closed `developer-dashboard` out** (deferred/reopenable — no prod target/traffic; M3/M4 curated reach thin until a `DIGEST` emitter ships [honest, not a bug, brief R1/AC4]; M5 owner-scope leak target 0 structural; mirrors the nine prior close-outs). Surveyed the backlog for all-deps-met features: **`developer-updates`** (`app-subscriptions` ✓ + `app-pages` ✓ + `signal-capture` ✓) is the only one startable; `editorial-curation-tools` and `weekly-digest` stay **mutually blocked** (each is the other's dep). **User picked `developer-updates`** (D2) — the Phase-3 **developer-side channel** (net-new from `signal-capture` **SC-7/SC-8** / OQ-4): post updates / early-access / talk to subscribers — the platform as the dev's front page; it is the **producer** for the empty-until-producer notice seam `app-subscriptions` left (**AS-3**). Folder already scaffolded (7 artifacts); set `Stage: 1-define` + Persona **Product Analyst**, updated [INDEX.md](features/INDEX.md) (both rows). Handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/developer-updates/FEATURE_BRIEF.md).
- **`developer-dashboard` BUILD → RELEASE (Stage 5, 2026-06-24)** — Release Engineer: **released** the BUILT, green developer-dashboard to **local/dev** per [DESIGN §12](features/developer-dashboard/DESIGN.md). Re-verified before ship (**740 tests**, `ruff`/`check`/`makemigrations --check` clean, no drift); confirmed the **activation switch** in place (the `config/urls` `dashboard/` include + the `"apps.dashboard"` `INSTALLED_APPS` line) and both routes resolve. Wrote [RELEASE_NOTES.md](features/developer-dashboard/RELEASE_NOTES.md) (what changed / who is affected / how to use / operator rollout + promotion table / the rehearsed rollback / monitoring metrics→signals→alert — the **one actionable alert = `DASHBOARD_RECEPTION_DEGRADED`**, reviews fail-soft, M3/M4 thin-by-design until a `DIGEST` emitter, M5 leak target 0 / known limitations). **Rehearsed the rollback** (no schema = code-only): switch present → routes resolve + 740 green → removed the include + `INSTALLED_APPS` line → both routes **404** + `check` clean + upstream `apps.signals`/`apps.ratings`/`apps.catalog` **310 green** (the additive signals reads are inert without the dashboard) → restored → routes resolve, `git diff` on `config/urls.py`+`config/settings.py` **empty** (exact restore). **No code/scope change** (release-only). Reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Set `Stage: 6-post-release` + Persona **Retrospective Analyst**, updated [INDEX.md](features/INDEX.md). Handed to the **Retrospective Analyst** (outcome review — expected to defer live-metrics + close out, as the nine prior).
- **`developer-dashboard` TASKS → BUILD (Stage 4, 2026-06-24)** — Senior Engineer: built [TASKS.md](features/developer-dashboard/TASKS.md) **T-01…T-06** against the **APPROVED** [DESIGN.md](features/developer-dashboard/DESIGN.md) — **740 tests green (+64 over 676)**, `ruff`/`check`/`makemigrations --check` clean, no drift. **T-01 (risk centerpiece, first):** the additive, **neutral** reads on the **closed** [apps/signals/selectors.py](apps/signals/selectors.py) — `TrendGranularity` + `ImpressionBreakdown`/`ImpressionBucket` + `impression_breakdown[_for_apps]` (per-`Surface`, zero-filled, bulk no-N+1) + `impression_trend` (per-`Surface` per-UTC-bucket, sparse) — **no model/migration/index**, ORM-tested with the §4.2 invariant (`breakdown.total == app_funnel.impressions`) **before any dashboard module**; full `apps.signals` suite green. Then the **model-less `apps/dashboard/`**: **T-02** `windows.py` (8 windows + granularity + `resolve_window` fail-safe + epoch sentinel) → **T-03** pure stdlib inline-SVG `charts.py` (total + curated polyline) → **T-04** `reception.py` (frozen view-model DTOs; curated-first via `ratings.gate.CURATED_SURFACES`; trend densify; bounded my-apps AC9; owner-scope→`None` AC8; loud-core/soft-reviews split; takes the §4.3 `ResolvedWindow`) → **T-05** the two role-gated GET views + 3 templates + 6 `DASHBOARD_*` counters + the AST no-`signals.capture` test + **the activation switch** (`config/urls` `dashboard/` include + `INSTALLED_APPS` line) → **T-06** [README](apps/dashboard/README.md)/[CODEMAP.md](CODEMAP.md)/DECISIONS (**DD-DESIGN-1…5 BUILT**). Wrote [TEST_PLAN.md](features/developer-dashboard/TEST_PLAN.md) (AC1–AC10 + the invariant + the loud/soft split + M1–M6 → named tests). One impl note (no contract change): `impression_trend` uses `TruncDay` (not `TruncDate`) so `bucket_start` stays a `datetime`. **No re-design; no new `config` entry; reuses D-3/D-5/D-6/D-7/D-8 — no new global ADR.** Set `Stage: 5-release` + Persona **Release Engineer**, updated [INDEX.md](features/INDEX.md). Handed to the **Release Engineer** (release + `RELEASE_NOTES.md` + rollback rehearsal).
- **`developer-dashboard` DESIGN → TASKS (Stage 3, 2026-06-24)** — Planner / Tech Lead: decomposed the **APPROVED** [DESIGN.md](features/developer-dashboard/DESIGN.md) into [TASKS.md](features/developer-dashboard/TASKS.md) — **6 ordered tasks, all S/M** (no `L`), risk-front-loaded. **T-01 is the risk centerpiece**: the additive, **neutral** reads on the **closed** `apps/signals` app (`TrendGranularity` + `impression_breakdown[_for_apps]` + `impression_trend`; **no model/migration/index**) — built and ORM-tested with the §4.2 invariant (`impression_breakdown.total == app_funnel.impressions`) **before any dashboard module**, full `apps.signals` regression in its DoD. Then **T-02** `windows.py` (8 windows + granularity + `resolve_window`) → **T-03** pure inline-SVG `charts.py` → **T-04** `reception.py` composition (curated-first via `CURATED_SURFACES`, trend densify, bounded my-apps no-N+1, owner-scope→None, loud-core/soft-reviews split) → **T-05** the two role-gated GET views + templates + 6 `DASHBOARD_*` counters + the `config/urls` include + `INSTALLED_APPS` line (**the activation switch**) + the AST no-`signals.capture`-import test (AC8) → **T-06** docs/CODEMAP/DECISIONS. Every contract cross-checked against the codebase; **no re-design**, **no new `config` entry** (windows live in code), reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Set `Stage: 4-build` + Persona **Senior Engineer**, updated [INDEX.md](features/INDEX.md). Handed to the **Senior Engineer** (build T-01 first, produce `TEST_PLAN.md`).
- **`developer-dashboard` DESIGN APPROVED (DN-DD-DESIGN, 2026-06-24)** — Software Architect: user answered **DN-DD-DESIGN** ("accept and proceed" = approve), so **ratified [DESIGN.md](features/developer-dashboard/DESIGN.md)** (status → APPROVED) and promoted all five **DD-DESIGN-1…5 → RATIFIED** (binding for Stage 3): a **model-less `apps/dashboard/`** consumer (mirrors `apps/pages/` + `apps/discovery/`; one `config/urls` include = activation/rollback, owns no schema); **two additive, neutral reads on `signals.selectors`** — `impression_breakdown[_for_apps]` (per-`Surface` counts, every `Surface` zero-filled → AC3/AC4) + `impression_trend(…, granularity)` (per-`Surface` per-time-bucket → AC10) + `TrendGranularity` (**no model/migration/index**, backed by `signals_imp_app_time_idx`) — **resolving OQ-DD-4**; the 8 windows + per-window bucket granularity as a code-fixed `apps/dashboard/windows.py` table; **failure split** = loud core read (`DASHBOARD_RECEPTION_DEGRADED`) / soft reviews; **read-only** (role-gated GET, no `signals.capture` import, AST-enforced). Reach split reuses `ratings.gate.CURATED_SURFACES` (single D-8 source). Reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Set `Stage: 3-plan` + Persona **Planner / Tech Lead**, emptied Decisions Needed, updated [INDEX.md](features/INDEX.md). Handed to the **Planner / Tech Lead** to decompose the DESIGN into [TASKS.md](features/developer-dashboard/TASKS.md).
- **`developer-dashboard` brief APPROVED + scope expanded (DN-19, 2026-06-24)** — Product Analyst: user answered **DN-19**, so marked [FEATURE_BRIEF.md](features/developer-dashboard/FEATURE_BRIEF.md) **APPROVED** and did the Stage-1→2 hand-off (one persona/session). **DN-19.a** was **expanded** past the recommended binary curated/open split into a **combined impressions total + per-source breakdown** over the `Surface` vocabulary (Steam-style "where the impressions come from"), with **curated `DIGEST` first/highlighted** and an **impressions-over-time trend carrying a distinguished curated line** (new **AC10**; user-selectable series + funnel-over-time deferred). **DN-19.b** **expanded** to an 8-window fixed config set (week / 2 weeks / month / 3 / 6 months / year / 3 years / all-time). **DN-19.c** taken as recommended (per-review weight-eligibility **hidden**). Promoted **DD-1/DD-2/DD-3 → RESOLVED**, closed **OQ-DD-1/2/3**, widened **OQ-DD-4** for Stage 2: `app_funnel` collapses surfaces and is not time-bucketed, so the breakdown + trend need a **new surface-aware + time-bucketed read in `signals.selectors`** (never a direct `signals_*` read — D-7 boundary; confirmed by reading the selectors + the `Surface` enum). Reuses **D-3/D-5/D-6/D-7/D-8 as-is — no new global ADR**. Set `Stage: 2-design`, emptied Decisions Needed, updated [INDEX.md](features/INDEX.md). Handed to the **Software Architect** to draft [DESIGN.md](features/developer-dashboard/DESIGN.md).
- **`open-search-browse` closed out + `developer-dashboard` activated (2026-06-23)** — Coordinator (user: skip post-release, pick the next feature) **re-verified the `open-search-browse` build** (**676 tests green / `ruff` / `check` / no drift**), then **skipped the Stage-6 retrospective** and **closed it out** (deferred/reopenable — no prod target/traffic; M2 click-through thin until real traffic; M1/M5/M6 structural-by-construction; mirrors the eight prior close-outs). Backlog features with all deps met: `developer-dashboard` (`signal-capture` ✓ + `ratings-reviews` ✓) and `developer-updates` (`app-subscriptions` ✓ + `app-pages` ✓ + `signal-capture` ✓); `editorial-curation-tools` and `weekly-digest` remain blocked (mutual dep). **User picked `developer-dashboard`** (D2) — the Phase-3 **core developer value** (breakdown §6 Dev-facing, **H2**): a read-only view of an app's *reception* for the developer who cares most — *reach* (impressions / curated-user counts), *engagement* (click-through, opens, returns), and incoming *reviews* — reading what `signal-capture` (D-7) and `ratings-reviews` (D-8) already collect. Folder already scaffolded (7 artifacts); set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/developer-dashboard/FEATURE_BRIEF.md).
- **`open-search-browse` (closed 2026-06-23, 676 tests)** — brief approved (DN-17) → DESIGN ratified (DN-18) → built `apps/discovery/` (T-01…T-07) → released local/dev → closed out. The Phase-2 open discovery surface (browse / keyword-search / interest-filter the **D-6** accepted catalogue → app-page; **money never buys position**, vision §4.1). Additive nullable `catalog_app.accepted_at`+`search_vector` (+indexes, `django.contrib.postgres`), write-path maintenance via the single-source `_search_vector_expr`, backfill migration, reverse-resolution `taxonomy.tag_ids_resolving_to` (AC3), the paginated DB-pushed `catalog.search_catalogue -> CatalogPage` (constant-query-count + ORDER-BY-neutrality assertions, AC9/AC5), model-less `apps/discovery/` + the `discover/` include (no `signals` import = AC6 structural). Activation = the one `discover/` include; rollback = remove it. Reuses **D-3/D-5/D-6/D-7/D-8** — no new ADR. Detail: [open-search-browse/DECISIONS.md](features/open-search-browse/DECISIONS.md), [RELEASE_NOTES.md](features/open-search-browse/RELEASE_NOTES.md).
- **`interest-profile` (closed 2026-06-23, 616 tests)** — brief approved (DN-15) → DESIGN ratified (DN-16) → built `apps/interests/` (T-01…T-06) → released local/dev → closed out. New app owning one mutable `interests_interest` table (no parent row → empty=structural default AC6; CASCADE deletion AC9; no score AC8; emits no D-7 / does not import `signals.capture`, IP-5). The user side of the Ring-0 match read through `declared_tag_ids(user) -> frozenset[UUID]` (AC8, the future matcher's input); §7 set-replace preserve-on-edit reconcile keeps a no-successor retired ref the picker can't show (M5=0 across edits). Reuses **D-3/D-4/D-5** — no new ADR. Detail: [interest-profile/DECISIONS.md](features/interest-profile/DECISIONS.md), [RELEASE_NOTES.md](features/interest-profile/RELEASE_NOTES.md).
- **`app-subscriptions` (closed 2026-06-21, 552 tests)** — brief approved (DN-13) → DESIGN ratified (DN-14) → built `apps/subscriptions/` → released local/dev → closed out. New app owning mutable `subscriptions_subscription` (CASCADE delete, the AS-5/AC9 contrast with ratings' SET_NULL); atomic follow + one `subscribe` D-7 emit (M5 1:1 by construction); empty-until-producer notice seam (AS-3=A). Reuses **D-3/D-6/D-7** — no new ADR. Detail: [app-subscriptions/DECISIONS.md](features/app-subscriptions/DECISIONS.md), [RELEASE_NOTES.md](features/app-subscriptions/RELEASE_NOTES.md).
- **`ratings-reviews` (closed 2026-06-21, 486 tests)** — brief approved (DN-11) → DESIGN ratified (DN-12) → built `apps/ratings/` → released local/dev → closed out. New app owning mutable `ratings_rating`; the curated-rating gate recorded (not computed), no score/average column (AC6 structural). DN-12 **promoted the gate to global [D-8](DECISIONS.md)**: "curated = an organic-curation/`DIGEST` impression; an open `APP_PAGE` view never counts" (binding on `editorial-curation-tools`/`developer-dashboard`/Quality Score). Detail: [ratings-reviews/DECISIONS.md](features/ratings-reviews/DECISIONS.md), [RELEASE_NOTES.md](features/ratings-reviews/RELEASE_NOTES.md).
- **`app-pages` (closed 2026-06-20, 417 tests)** — brief approved (DN-9) → DESIGN ratified (DN-10) → built `apps/pages/` (a pure D-6/D-7 consumer owning no model) → released local/dev → closed out. The public surface for accepted apps; **first live emitter of D-7 `app_page` impressions** (AP-3/4/5); added the additive `Surface.APP_PAGE`. No feature flag ("off" = drop the `config/urls` include). Detail: [app-pages/DECISIONS.md](features/app-pages/DECISIONS.md), [RELEASE_NOTES.md](features/app-pages/RELEASE_NOTES.md).
- **`signal-capture` (closed 2026-06-18, 374 tests)** — brief approved (DN-5) → DESIGN + global **[D-7](DECISIONS.md)** event schema ratified (DN-6) → built `apps/signals/` (T-01…T-09) → released local/dev → closed out (DN-7). 4 append-only tables (`Impression` anchor + frozen `ImpressionTag` snapshot + uniform `EngagementEvent` `kind` discriminator + `PlatformVisit`); structural no-score + no-PII whitelist; `SET_NULL`-anonymize on events (**SC-10**, resolves the `submission-intake` deletion deferral); derived-at-read funnel (**SC-9**). Detail: [signal-capture/DECISIONS.md](features/signal-capture/DECISIONS.md), [RELEASE_NOTES.md](features/signal-capture/RELEASE_NOTES.md).
- **`submission-intake` (closed 2026-06-18, 315 tests)** — `apps/catalog/`, 4 tables, fixed 5-floor gate enum (no "other"), ACCEPTED-only catalogue substrate = global **[D-6](DECISIONS.md)**. Built T-01…T-14, released local/dev, closed out (DN-3). Detail: [submission-intake/DECISIONS.md](features/submission-intake/DECISIONS.md).
- **`interest-taxonomy` (closed 2026-06-17, 184 tests)** — flat tags + named clusters (M2M), UUID identity, soft-retire + read-time `resolve_tag` = global **D-5**. Built T-01…T-10, released local/dev, closed out (DN-1). Detail: [interest-taxonomy/DECISIONS.md](features/interest-taxonomy/DECISIONS.md).

> **Foundational decisions** (`identity-accounts`, the stack, niche, build order — D1–D3,
> A1–A3, R1, DL-1/2, PL-1, global D-1/D-2/D-4) are recorded in [DECISIONS.md](DECISIONS.md)
> and the per-feature `DECISIONS.md` files. Highlights: niche = **"vibecoded webapps"**
> (D-1); stack = **Python / Django + PostgreSQL** (D-4); `identity-accounts` released
> local/dev (R1, 108 tests).

---

## Activity Log

Newest first. One line per session. **Keep the most recent ~10 rows here**; older rows
live in [process/activity-archive.md](process/activity-archive.md). The per-feature
folders remain the full record either way.

| Date       | Stage           | Summary                                                                 |
|------------|-----------------|-------------------------------------------------------------------------|
| 2026-06-24 | `6-post-release`→`0-coordinator`→`1-define` | **Coordinator** (user: skip post-release, pick the next feature) — **re-verified the `developer-dashboard` build** (**740 tests / `ruff` / `check` / no drift** — matches the released state), then **skipped the Stage-6 retrospective** and **closed `developer-dashboard` out** (deferred/reopenable — no prod target/traffic; M3/M4 curated reach thin until a `DIGEST` emitter ships [honest, not a bug, brief R1/AC4]; M5 owner-scope leak 0 structural; mirrors the nine prior close-outs). Surveyed the backlog for all-deps-met features: **`developer-updates`** (`app-subscriptions` ✓ + `app-pages` ✓ + `signal-capture` ✓) is the only one startable; `editorial-curation-tools` and `weekly-digest` stay **mutually blocked** (each is the other's dep). **User picked `developer-updates`** (D2) — the Phase-3 **developer-side channel** (net-new from `signal-capture` **SC-7/SC-8** / OQ-4): post updates / early-access / talk to subscribers — the platform as the dev's front page; the **producer** for the empty-until-producer notice seam `app-subscriptions` left (**AS-3**). Folder already scaffolded (7 artifacts); set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md) (both rows), handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/developer-updates/FEATURE_BRIEF.md). |
| 2026-06-24 | `5-release`→`6-post-release` | **Release Engineer** — **released** the BUILT, green **developer-dashboard** to **local/dev** per [DESIGN §12](features/developer-dashboard/DESIGN.md) and wrote [RELEASE_NOTES.md](features/developer-dashboard/RELEASE_NOTES.md). Re-verified before ship: **740 tests green**, `ruff check` clean, `manage.py check` no issues, `makemigrations --check --dry-run` *No changes detected* (no schema drift — the app owns no model). Confirmed the **activation switch** in place (the `config/urls` `dashboard/` include + the `"apps.dashboard"` `INSTALLED_APPS` line) and both routes resolve (`dashboard:my-apps` `/dashboard/`, `dashboard:app` `/dashboard/apps/<uuid>/`). RELEASE_NOTES.md covers what changed (the model-less `apps/dashboard/` consumer + the two additive **neutral** `signals.selectors` reads — `impression_breakdown[_for_apps]` + `impression_trend` + `TrendGranularity`; **no schema/migration/index**) / who is affected (developers with ≥1 accepted app; everyone else unaffected — additive only) / how to use / operator rollout + promotion table (local/dev **done**; internal+prod **deferred**, no prod target/traffic) / the rehearsed one-action rollback / monitoring **metrics→signals→alert** (the **one actionable alert = `DASHBOARD_RECEPTION_DEGRADED`** [core read raise → loud 500]; `DASHBOARD_REVIEWS_DEGRADED` fail-soft; **M3/M4 thin-by-design** until a `DIGEST` emitter ships — honest, not a bug, brief R1/AC4; **M5** owner-scope leak target **0**, structural) / known limitations. **Rehearsed the rollback** (no schema = code-only): switch present → both routes resolve + **740 green** → removed the `config/urls` include + the `INSTALLED_APPS` line → both routes **404** + `manage.py check` clean + upstream `apps.signals`/`apps.ratings`/`apps.catalog` (**310 tests**) stay **green** (the additive signals reads are inert/harmless without the dashboard) → restored the switch → routes resolve again, `git diff` on `config/urls.py`+`config/settings.py` **empty** (exact restore). **No code/scope change** (release-only; the build was already green and APPROVED). Reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Set `Stage: 6-post-release` + Persona **Retrospective Analyst**; updated [INDEX.md](features/INDEX.md) (→ `6-post-release`). Advanced to `6-post-release`; handed to the **Retrospective Analyst** (outcome review — expected to defer the live-metrics window + close out, mirroring the nine prior close-outs). |
| 2026-06-24 | `4-build`→`5-release` | **Senior Engineer** — built [developer-dashboard/TASKS.md](features/developer-dashboard/TASKS.md) **T-01…T-06** against the **APPROVED** [DESIGN.md](features/developer-dashboard/DESIGN.md): **740 tests green (+64 over the 676 baseline)**, `ruff`/`check`/`makemigrations --check` clean, no drift. **T-01 (risk centerpiece, first):** additive, **neutral** reads on the **closed** [apps/signals/selectors.py](apps/signals/selectors.py) — `TrendGranularity` + `ImpressionBreakdown`/`ImpressionBucket` + `impression_breakdown[_for_apps]` (per-`Surface`, every `Surface` zero-filled AC3/AC4, bulk no-N+1 AC9) + `impression_trend` (per-`Surface` per-UTC-bucket, sparse-ascending AC10); **no model/migration/index** (backed by the existing `signals_imp_app_time_idx`). ORM-tested with the §4.2 invariant (`impression_breakdown.total == app_funnel.impressions`, random fixtures + several windows) + `assertNumQueries` K=2-vs-20 **before any dashboard module**; full `apps.signals` suite green (79). Then the **model-less `apps/dashboard/`**: **T-02** `windows.py` (the 8 `ReportingWindow`s + per-window granularity + `resolve_window` fail-safe + `ALL_TIME_START` epoch; scaffolded, not yet activated) → **T-03** pure stdlib inline-SVG `charts.py` (total + distinguished curated polyline, `None` on empty/all-zero, single-bucket guard, zero app imports) → **T-04** `reception.py` (frozen view-model DTOs; `build_my_apps_summaries` bounded no-N+1 / `build_app_reception` owner-scope→`None`; **curated-first via `ratings.gate.CURATED_SURFACES`** — the single D-8 source reused; trend densify; loud-core/soft-reviews split; takes the §4.3 `ResolvedWindow`, internal per §5.4) → **T-05** the two GET views (`@require_http_methods(["GET"])`+`@login_required`+`@require_role(DEVELOPER)`), `urls.py`, 3 server-rendered templates (no JS, auto-escaped; the 5 Screen-B sections incl. honest-zero affordance + SVG+`<table>` trend), the 6 `DASHBOARD_*` counters, the AST no-`signals.capture` test (AC8 structural), and **THE ACTIVATION SWITCH** — the `config/urls` `dashboard/` include + the `"apps.dashboard"` `INSTALLED_APPS` line → **T-06** [README](apps/dashboard/README.md)/[CODEMAP.md](CODEMAP.md)/feature [DECISIONS.md](features/developer-dashboard/DECISIONS.md) (**DD-DESIGN-1…5 BUILT**). Wrote [TEST_PLAN.md](features/developer-dashboard/TEST_PLAN.md) (**COMPLETE** — AC1–AC10 + the invariant + the loud/soft failure split + M1–M6 → named tests across 6 modules). Failure split verified end-to-end: core read raise → loud **500** + `DASHBOARD_RECEPTION_DEGRADED` (the one alert); reviews raise → soft **200** + `DASHBOARD_REVIEWS_DEGRADED`; owner-scope → **404** indistinguishable; **POST → 405**; a GET writes **no** `Impression`/`EngagementEvent` row. One impl note (no contract change): `impression_trend` uses `TruncDay` (not `TruncDate`) so `bucket_start` is uniformly a `datetime`. **No re-design** (every task traces to a DESIGN §); **no new `apps/core/config` entry** (windows live in code); reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Set `Stage: 5-release` + Persona **Release Engineer**; updated [INDEX.md](features/INDEX.md) (→ `5-release`). Advanced to `5-release`; handed to the **Release Engineer** (release + `RELEASE_NOTES.md` + rollback rehearsal). |
| 2026-06-24 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the **APPROVED** [developer-dashboard/DESIGN.md](features/developer-dashboard/DESIGN.md) into [TASKS.md](features/developer-dashboard/TASKS.md): **6 ordered tasks, all S/M** (no `L`), risk-front-loaded on the one load-bearing seam — the additive, **neutral** reads on the **closed** `apps/signals` app (DESIGN §5.1, DD-DESIGN-2; the OQ-DD-4 resolution): `TrendGranularity` + `impression_breakdown[_for_apps]` (per-`Surface`, zero-filled → AC3/AC4) + `impression_trend` (per-`Surface` per-time-bucket → AC10), **no model/migration/index** (backed by `signals_imp_app_time_idx`), landed at **T-01** and ORM-tested with the §4.2 equality invariant (`impression_breakdown.total == app_funnel.impressions`) + a constant-query-count assertion **before any dashboard module exists**; full `apps.signals` regression in its DoD. Spine: **T-01** signals reads → **T-02** `apps/dashboard/windows.py` (the 8 windows + per-window granularity + `resolve_window` + all-time epoch; scaffolds the package, **not** yet in `INSTALLED_APPS`) → **T-03** pure stdlib-only inline-SVG `charts.py` (total + curated line + `<table>` fallback, no JS) → **T-04** `reception.py` composition (the view-model DTOs; **curated-first** via `ratings.gate.CURATED_SURFACES`; trend densify; `build_my_apps_summaries` bounded no-N+1 AC9; `build_app_reception` owner-scope→`None` AC8; **loud-core/soft-reviews** split) → **T-05** the two `require_role(developer)` GET views + 3 templates + the 6 `DASHBOARD_*` counters + the `config/urls` `dashboard/` include + the `INSTALLED_APPS` line (**the entire activation switch**) + the AST no-`signals.capture`-import test (AC8 structural) → **T-06** README/CODEMAP/DECISIONS (DD-DESIGN-1…5 built). Full design-element coverage table + AC1–AC10 + M1–M6 roll-up + file-collision note (sequential edits; the new `apps/dashboard/` files never collide). Cross-checked every referenced contract against the codebase (`signals.selectors`/`kinds.Surface`/the `(app_id, occurred_at)` index; `ratings.gate.CURATED_SURFACES`; `ratings.selectors.reviews_for_app`; `catalog.selectors.list_owned_apps`/`get_owned_app`; `accounts.permissions.require_role` + `roles.DEVELOPER`; the `apps/discovery/` consumer pattern incl. its AST import test; `config/urls`/`INSTALLED_APPS`; observability constants; `manage.py test` = directory discovery). **No re-design** — every task traces to a DESIGN §; **no new `apps/core/config` entry** (windows live in code, labels reuse existing config). Reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Set `Stage: 4-build` + Persona **Senior Engineer**; updated [INDEX.md](features/INDEX.md) (→ `4-build`). Advanced to `4-build`; handed to the **Senior Engineer** (build T-01 first, produce `TEST_PLAN.md`). |
| 2026-06-24 | `2-design`→`3-plan` | **Software Architect** (hand-off, DN-DD-DESIGN approved) — user answered **DN-DD-DESIGN** ("accept and proceed" = approve), so **ratified [developer-dashboard/DESIGN.md](features/developer-dashboard/DESIGN.md)** (status header → APPROVED) and performed the Stage-2→3 hand-off **only** (no second persona this session, per CLAUDE.md). Promoted all five **DD-DESIGN-1…5 → RATIFIED** (binding for Stage 3) in the feature [DECISIONS.md](features/developer-dashboard/DECISIONS.md), and **OQ-DD-4 RESOLVED-in-design → RESOLVED** in [OPEN_QUESTIONS.md](features/developer-dashboard/OPEN_QUESTIONS.md). Ratified spine: a **model-less `apps/dashboard/`** consumer (mirrors `apps/pages/`+`apps/discovery/`; one `config/urls` include = activation/rollback, owns no schema); **two additive, neutral `signals.selectors` reads** — `impression_breakdown[_for_apps]` (per-`Surface`, zero-filled → AC3/AC4) + `impression_trend(…, granularity)` (per-`Surface` per-time-bucket → AC10) + `TrendGranularity` (**no model/migration/index**); the 8 windows + per-window bucket granularity in `apps/dashboard/windows.py`; loud-core/soft-reviews failure split; read-only (role-gated GET, no `signals.capture` import). Reach split reuses `ratings.gate.CURATED_SURFACES`. Reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Set `Stage: 3-plan` + Persona **Planner / Tech Lead** in Current State; **emptied Decisions Needed**; added the Decisions-Made digest; updated [INDEX.md](features/INDEX.md) (→ `3-plan`). Advanced to `3-plan`; handed to the **Planner / Tech Lead** to decompose [DESIGN.md](features/developer-dashboard/DESIGN.md) into [TASKS.md](features/developer-dashboard/TASKS.md). |
| 2026-06-24 | `2-design` (awaiting DN-DD-DESIGN) | **Software Architect** — drafted [developer-dashboard/DESIGN.md](features/developer-dashboard/DESIGN.md) via the 14-step protocol; **RESOLVED OQ-DD-4**. Spine: a **NEW model-less `apps/dashboard/`** consumer (mirrors `apps/pages/` + `apps/discovery/`) owning **no model/migration/table/index** — two GET views (my-apps list + per-app reception), a reception-composition layer, a charts helper, the reporting-window vocabulary; activation **and** rollback = one `config/urls` include (zero data migration). **OQ-DD-4 resolution:** two **additive, neutral** reads on the closed [apps/signals/selectors.py](apps/signals/selectors.py) (the only D-7-permitted `signals_*` reader) — **`impression_breakdown[_for_apps]`** (per-`Surface` counts, every `Surface` zero-filled → AC3/AC4) + **`impression_trend(…, granularity)`** (per-`Surface` per-time-bucket → AC10) + a **`TrendGranularity`** enum; **no model/migration/index** (backed by the existing `signals_imp_app_time_idx`). Signals stays **neutral** (never judges "curated"); the dashboard composes the curated split from `ratings.gate.CURATED_SURFACES` (single D-8 source). The **8 windows + per-window bucket granularity** (DAY ≤1m / WEEK 3–6m / MONTH ≥1y+all-time — the M6/AC9 bound) live as a code-fixed table in `apps/dashboard/windows.py`; all-time lower-bounded by an epoch sentinel so the existing range-based selectors are reused unchanged. **Failure split:** core reception read **fails loud** (500 + `DASHBOARD_RECEPTION_DEGRADED`, the one alert — a fake-empty dashboard would lie about H2/R1); reviews slot **fails soft**; owner-scope ⇒ **404 indistinguishable**; `login_required` + `require_role(developer)`, **GET-only**, **no `signals.capture` import** (AST-enforced — viewing emits no D-7 impression, AC8 structural). Invariant `breakdown.total == app_funnel.impressions`. Full **AC1–AC10 + M1–M6** → design-element map (§9), per-component failure modes (§7), assumption ledger (§13, all 9 verified vs code), 5 rejected alternatives (§10). Logged **DD-DESIGN-1…5 (PROPOSED)** in [DECISIONS.md](features/developer-dashboard/DECISIONS.md); marked **OQ-DD-4 RESOLVED-in-design** in [OPEN_QUESTIONS.md](features/developer-dashboard/OPEN_QUESTIONS.md). Reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Updated Current State + [INDEX.md](features/INDEX.md) (still `2-design`). **Raised DN-DD-DESIGN** (approve DESIGN); **no Stage advance until approved.** |
| 2026-06-24 | `1-define`→`2-design` | **Product Analyst** (DN-19 answered) — user answered **DN-19**: marked [developer-dashboard/FEATURE_BRIEF.md](features/developer-dashboard/FEATURE_BRIEF.md) **APPROVED** and did the Stage-1→2 hand-off only. **DN-19.a expanded** the reach scope from a binary curated/open split into a **combined impressions total + per-source `Surface` breakdown** (Steam-style; curated **`DIGEST` first/highlighted**) **plus an impressions-over-time trend with a distinguished curated line** (new **AC10**; user-selectable series + funnel-over-time deferred). **DN-19.b expanded** the windows to a fixed 8-set (week / 2 weeks / month / 3 / 6 months / year / 3 years / all-time). **DN-19.c** = per-review weight-eligibility **hidden** (as recommended). Revised the brief (Reach domain term, S2, AC3 rewrite, AC4 generalized, AC7 window list, **AC10**, In/Out-of-scope, C7 → verified gap), promoted **DD-1/2/3 → RESOLVED**, closed **OQ-DD-1/2/3**, widened **OQ-DD-4** (surface-aware **+ time-bucketed** `signals.selectors` read — confirmed needed by reading [apps/signals/selectors.py](apps/signals/selectors.py) + [kinds.py](apps/signals/kinds.py); `app_funnel` collapses surfaces and isn't bucketed; `Surface`={DIGEST,APP_PAGE}, documented extensible). Reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Set `Stage: 2-design`, emptied Decisions Needed, updated [INDEX.md](features/INDEX.md). Handed to the **Software Architect** to draft [DESIGN.md](features/developer-dashboard/DESIGN.md) (resolve OQ-DD-4 first). |
| 2026-06-23 | `1-define` (awaiting DN-19) | **Product Analyst** — drafted [developer-dashboard/FEATURE_BRIEF.md](features/developer-dashboard/FEATURE_BRIEF.md): the Phase-3 **core developer value** (breakdown §4.3, vision §6 Dev-facing; **H2**) — a **read-only** view of an app's **reception** for the developer who owns it. **5 stories / 9 G-W-T ACs / 6 metrics**; all domain terms defined; explicit in/out-of-scope (out: Quality Score / ring / allocation [don't exist at MVP], retention *curves*, any write/re-boost, per-review eligibility, public API, portfolio/category benchmarking, non-accepted apps). **Grounded every dependency in code:** reach/engagement via `signals.selectors.app_funnel`/`funnel_for_apps` (the full D-7 funnel; returns DERIVED; proxy segregated; bounded-query = AC9), **curated reach = `Surface.DIGEST`** per the **D-8** gate vs open `APP_PAGE`, reviews via `ratings.selectors.reviews_for_app` (count+distribution+list, **no average**), **owner-scoping** via `catalog.list_owned_apps`/`get_owned_app` (D-6, AC1/AC2), dev-role gate (D-3). **Money-buys-position test → PASS** (pure read, no allocation lever; vision §5.6 tool-not-position, AC8). Honest-zero MVP framing (no `DIGEST` emitter yet → curated reach ~0; AC4 / M4 thin-by-design). Logged **OQ-DD-1…4** + **DD-1…3 PROPOSED**; reuses **D-3/D-5/D-6/D-7/D-8 as-is — NO new global ADR**. Updated [INDEX.md](features/INDEX.md). **Raised DN-19** (approve brief + 3 bundled scoping calls); **no Stage advance until approved.** |
| 2026-06-23 | `6-post-release`→`0-coordinator`→`1-define` | **Coordinator** (user: skip post-release, pick the next feature) — **re-verified the `open-search-browse` build** (**676 tests / `ruff` / `check` / no drift**), then **skipped the Stage-6 retrospective** and **closed `open-search-browse` out** (deferred/reopenable — no prod target/traffic; M2 click-through thin until real traffic; M1/M5/M6 structural-by-construction; mirrors the eight prior close-outs). Surveyed the backlog for all-deps-met features: `developer-dashboard` (`signal-capture` ✓ + `ratings-reviews` ✓) and `developer-updates` (`app-subscriptions` ✓ + `app-pages` ✓ + `signal-capture` ✓) are startable; `editorial-curation-tools` and `weekly-digest` remain blocked (mutual dep). **User picked `developer-dashboard`** (D2) — the Phase-3 **core developer value** (breakdown §6 Dev-facing, **H2**): a read-only view of an app's *reception* (reach/engagement from **D-7** signals + incoming **D-8** curated ratings) for the developer who cares most. Folder already scaffolded (7 artifacts); set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md) (both rows), handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/developer-dashboard/FEATURE_BRIEF.md). |
| 2026-06-23 | `5-release`→`6-post-release` | **Release Engineer** — released **`open-search-browse`** to local/dev per [DESIGN §11/§16](features/open-search-browse/DESIGN.md) and wrote [RELEASE_NOTES.md](features/open-search-browse/RELEASE_NOTES.md). Re-verified green (**676 tests**, `ruff`/`check`/`makemigrations --check` no drift) before shipping. **Rehearsed rollout→rollback on a throwaway PG DB** (`osb_release_rehearsal`, dropped after): `migrate` applied **`catalog/0002`** (additive nullable `accepted_at`[timestamptz] + `search_vector`[tsvector] + the `(status,-accepted_at)` composite index `catalog_app_status_acc_idx` + the GIN `catalog_app_search_gin`, pulling in `django.contrib.postgres`) then **`catalog/0003`** (backfill from latest accept `ReviewDecision` + the shared `_search_vector_expr`) → both nullable columns + both indexes confirmed present (`information_schema`/`pg_indexes`) → `check` clean → `migrate catalog 0001` unapplied **both** cleanly (columns+indexes confirmed gone) → re-`migrate catalog` re-applied them (**reversible up→down→up**) → `discovery:browse` resolves at `/discover/`. Confirmed the **activation switch** (the one `config/urls` `discover/` include), the **3 config tunables** (`discovery_page_size`/`_page_size_max`/`_query_max_length`, under `validate_all`), the **6 `DISCOVERY_*` counters**, and `django.contrib.postgres`+`apps.discovery` in `INSTALLED_APPS`. RELEASE_NOTES.md covers what changed / who is affected / operator rollout (`migrate catalog`→`check`→deploy) / gate-based promotion table / the rehearsed **rollback = remove the `discover/` include** (zero data migration; optional column drop = `migrate catalog 0001`) / the M1–M6 → signal/alert map (**one actionable alert = `DISCOVERY_LISTING_DEGRADED`**; M2 derived from app-pages' non-curated `APP_PAGE` impressions, no D-7 emit AC6; M5 structural ORDER-BY-neutrality; M1/M6 by construction) / known limitations. **Production promotion + live-metrics window deferred** (local/dev target, no prod traffic). Reuses **D-3/D-5/D-6/D-7/D-8 — no new global ADR**. Updated [INDEX.md](features/INDEX.md) (→ `6-post-release`). Advanced to `6-post-release`; handed to the **Retrospective Analyst** (outcome review expected to defer + close out, mirroring the eight prior close-outs). |
| 2026-06-23 | `4-build`→`5-release` | **Senior Engineer** — built [open-search-browse/TASKS.md](features/open-search-browse/TASKS.md) **T-01…T-07** against the ratified [DESIGN.md](features/open-search-browse/DESIGN.md): **676 tests green (+60 over the 616 baseline)**, `ruff`/`check`/no-drift, all migrations reversible. **T-01** additive nullable `catalog_app.accepted_at`+`search_vector` columns + composite `(status,-accepted_at)` index + `search_vector` GIN + `django.contrib.postgres` (`catalog/0002`) → **T-02** single-source `_search_vector_expr` maintained in the catalog write path (`accept_app` stamps/re-stamps `accepted_at`; `submit_app`/`edit_app` recompute the vector only on name/desc change) → **T-03** reversible backfill `catalog/0003` (latest accept `ReviewDecision` + the shared expr) → **T-04** `taxonomy.selectors.tag_ids_resolving_to` reverse merge resolution (AC3) → **T-05 (the risk centerpiece)** `catalog.selectors.search_catalogue -> CatalogPage` — paginated DB-pushed, accepted-only, FTS+tag filter (AND-compose), **neutral ORDER BY only** (`rank`/`accepted_at`/`id`), with the **constant-query-count-at-5-vs-50-apps** (AC9) + **ORDER-BY-neutrality** (AC5/M5) assertions, + page-size config → **T-06** model-less `apps/discovery/` (view [GET, no `login_required`, AC8] / `catalogue.html` 5 states / the `config/urls` `discover/` include = the activation switch) + failure split (core read **loud 500**+`DISCOVERY_LISTING_DEGRADED`; facet sidebar **soft**+`DISCOVERY_FACETS_DEGRADED`) + **AST import-absence test enforcing no `signals` import (AC6)** + query-length config + 6 `DISCOVERY_*` counters → **T-07** [README](apps/discovery/README.md)/[CODEMAP.md](CODEMAP.md)/[DECISIONS.md](features/open-search-browse/DECISIONS.md) (OSB-DESIGN-1…6 **BUILT**)/[TEST_PLAN.md](features/open-search-browse/TEST_PLAN.md) (AC1–AC9 + M1/M3/M5/M6 + edge/security/failure/regression). **One implementation note (no contract change):** the tag filter uses `id IN Subquery` instead of join+`.distinct()` to avoid the Postgres `DISTINCT`+`SearchRank` conflict — same contract. T-01…T-05 are **additive, contract-preserving** changes to the **closed** `submission-intake`/`interest-taxonomy` read surfaces (`CatalogApp` DTO + all existing selectors unchanged; `apps.catalog` 142 + `apps.taxonomy` suites green). Reuses **D-3/D-5/D-6/D-7/D-8 as-is — no new global ADR**. Updated [INDEX.md](features/INDEX.md) (→ `5-release`). Advanced to `5-release`; handed to the **Release Engineer** (release `apps/discovery/` + `catalog/0002`+`0003`, write `RELEASE_NOTES.md`, rehearse rollback). |
| 2026-06-23 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the ratified [open-search-browse/DESIGN.md](features/open-search-browse/DESIGN.md) into [TASKS.md](features/open-search-browse/TASKS.md): **7 ordered tasks, all S/M (no `L`)**, risk-front-loaded on the one load-bearing seam the DESIGN §1/§3/§9 names — the **paginated DB-pushed `catalog.search_catalogue -> CatalogPage`** (the existing D-6 read materializes the whole catalogue → unpaginatable; the new primitive pushes filter/order/pagination/page-scoped resolve into the DB) — landed at **T-05** and unit-tested at the ORM level (the **constant-query-count-across-catalogue-size** AC9 assertion + the **ORDER-BY-neutrality** AC5/M5 assertion) before any view. Spine: **T-01** additive nullable `catalog_app.accepted_at` + `search_vector` columns/indexes + `django.contrib.postgres` (schema only, inert) → **T-02** forward maintenance in the single catalog write path (one-source `_search_vector_expr`; `accept_app` stamps/re-stamps `accepted_at`; `submit_app`/`edit_app` maintain `search_vector`) → **T-03** one-off backfill data migration (from latest accept `ReviewDecision` + the shared expr; reversible) → **T-04** isolated reverse-resolution `taxonomy.tag_ids_resolving_to` (AC3) → **T-05** the primitive + page-size config → **T-06** model-less `apps/discovery/` (view/urls/`catalogue.html` + the `config/urls` include = the whole activation switch; **no `signals` import = AC6 structural**; query-length config + the six `DISCOVERY_*` counters) → **T-07** README/CODEMAP/DECISIONS + rollback note. Full design-element coverage table + AC1–AC9 roll-up + file-collision note (sequential `config.py`/`observability.py`/`settings.py` edits). **No re-design** — every task traces to a DESIGN §; cross-checked all referenced contracts against the codebase (catalog selectors/services/models, taxonomy `replaced_by`/`related_name="replaces"`, config/observability patterns, `apps/pages/` consumer template, `config/urls`/`INSTALLED_APPS`). T-01…T-05 are **additive, contract-preserving** changes to the **closed** `submission-intake`/`interest-taxonomy` read surfaces (regression in each DoD). Reuses **D-3/D-5/D-6/D-7/D-8 as-is — no new global ADR** (CODEMAP entries logged at build). Updated [INDEX.md](features/INDEX.md) (`open-search-browse` → `4-build`). Advanced to `4-build`; handed to the **Senior Engineer** (build T-01 first, produce `TEST_PLAN.md`). |
| 2026-06-23 | `2-design`→`3-plan` | **Software Architect** (hand-off, DN-18 approved) — user answered DN-18 **approve**, so **ratified [open-search-browse/DESIGN.md](features/open-search-browse/DESIGN.md)** (status header → APPROVED) and performed the Stage-2→3 hand-off **only** (no second persona this session, per CLAUDE.md one-persona-per-session). Promoted the feature [DECISIONS.md](features/open-search-browse/DECISIONS.md) Stage-2 section + all six **OSB-DESIGN-1…6 → RATIFIED** (binding for Stage 3): the paginated DB-pushed read **`catalog.selectors.search_catalogue -> CatalogPage`**; additive nullable **`catalog_app.accepted_at`** (browse-order key) + **`catalog_app.search_vector`** (Postgres FTS, adds `django.contrib.postgres`); the reverse-resolution taxonomy read **`tag_ids_resolving_to`** (AC3); **no D-7 emit at MVP** (OQ-OSB-3); model-less **`apps/discovery/`** (one `config/urls` include = activation/rollback). **OQ-OSB-4 = name+desc search only.** Reuses **D-3/D-5/D-6/D-7/D-8 as-is — no new global ADR** (catalog/taxonomy adds are additive read-surface extensions). Set `Stage: 3-plan` + Persona **Planner / Tech Lead** in Current State; **emptied Decisions Needed** (DN-18 resolved); added the Decisions-Made digest; updated [INDEX.md](features/INDEX.md) (`open-search-browse` → `3-plan`). Advanced to `3-plan`; handed to the **Planner / Tech Lead** to decompose [DESIGN.md](features/open-search-browse/DESIGN.md) into [TASKS.md](features/open-search-browse/TASKS.md). |
| 2026-06-23 | `2-design` (awaiting DN-18) | **Software Architect** — drafted [open-search-browse/DESIGN.md](features/open-search-browse/DESIGN.md) via the 14-step protocol. Decisive finding: the existing D-6 read `list_catalogued_apps()` **materializes the whole accepted catalogue + resolves all tags in Python** (O(catalogue)/call) → cannot back a paginated surface (AC9). Spine: **(1)** new paginated DB-pushed primitive **`catalog.selectors.search_catalogue(query, tag_ids, page, size) -> CatalogPage`** (COUNT + LIMIT page + page-scoped tag resolve = constant queries/page, no N+1; the unchanged `CatalogApp` DTO); **(2)** two **additive nullable** `catalog_app` columns — **`accepted_at`** (browse-order key, stamped in `accept_app`'s txn, composite-indexed `(status,-accepted_at)`, backfilled from latest accept `ReviewDecision`) + **`search_vector`** (Postgres FTS name(A)+desc(B), GIN, maintained only in `submit_app`/`edit_app`), needs `django.contrib.postgres` in `INSTALLED_APPS`; **(3)** new reverse-resolution taxonomy read **`tag_ids_resolving_to(active_id) -> frozenset[UUID]`** (correct tag filter across merges, AC3; catalog filters a handed-in id set → stays decoupled from merge semantics); **(4)** model-less consumer app **`apps/discovery/`** (mirrors `apps/pages/`) — `views.catalogue` GET **no `login_required`** (AC8) + `catalogue.html` + one `config/urls` include = activation **and** rollback. **OQ-OSB-3 → no D-7 emit at MVP** (AC6 by construction; M2 derived from app-pages' `APP_PAGE` impressions; future `Surface.SEARCH` = named seam, not built); **OQ-OSB-4 → name+desc search only**. Core results read **fails loud** (never a fake empty state); facet sidebar fails soft. Full **AC1–AC9 → design-element map** (§15), per-component failure modes (§9), assumption ledger (§13, all 7 verified vs code), 6 rejected alternatives (§14). Logged **OSB-DESIGN-1…6** (PROPOSED) in [DECISIONS.md](features/open-search-browse/DECISIONS.md); marked **OQ-OSB-3/OQ-OSB-4 RESOLVED-in-design** ([OPEN_QUESTIONS.md](features/open-search-browse/OPEN_QUESTIONS.md), DN-18 pending). Reuses **D-3/D-5/D-6/D-7/D-8 as-is — no new global ADR** (the catalog/taxonomy adds are additive read-surface extensions). **Raised DN-18** (approve DESIGN); **no Stage advance until approved.** |
| 2026-06-23 | `1-define`→`2-design` | **Product Analyst** (DN-17 approved) — user answered DN-17 **approve the brief as drafted**; performed the **Stage-1→2 hand-off only** (no second persona this session). Marked [open-search-browse/FEATURE_BRIEF.md](features/open-search-browse/FEATURE_BRIEF.md) **APPROVED** and resolved the two bundled scoping calls inline: **OQ-OSB-1** — tag/cluster filtering **is in the MVP slice** (S3/AC3); **OQ-OSB-2** — default neutral order = **browse newest-accepted-first / search keyword-relevance**, both non-purchasable (AC5). Promoted **OSB-1/2/3 → RESOLVED** + added local **OSB-4** (tag-filtering in MVP) in [DECISIONS.md](features/open-search-browse/DECISIONS.md); closed **OQ-OSB-1/OQ-OSB-2** in [OPEN_QUESTIONS.md](features/open-search-browse/OPEN_QUESTIONS.md), leaving **OQ-OSB-3** (emit a non-curated D-7 signal at MVP? + new `Surface`?) + **OQ-OSB-4** (search tag labels?) **OPEN for Stage 2**. Reuses **D-3/D-5/D-6/D-7/D-8 as-is — no new global ADR**. Set `Stage: 2-design` + Persona **Software Architect**, emptied Decisions Needed, updated [INDEX.md](features/INDEX.md). Advanced to `2-design`; handed to the **Software Architect** to draft [DESIGN.md](features/open-search-browse/DESIGN.md) (incl. resolving OQ-OSB-3/OQ-OSB-4). |
| 2026-06-23 | `1-define` (awaiting DN-17) | **Product Analyst** — drafted [open-search-browse/FEATURE_BRIEF.md](features/open-search-browse/FEATURE_BRIEF.md): the Phase-2 **open discovery surface** (vision §4.1 open-access half + §6). Anyone, **including signed-out visitors**, can **browse / keyword-search / interest-filter** the **D-6** accepted-app catalogue and reach the **app-page** (stable `App.id` URL), with **position-neutral, non-purchasable** result order (AC5 / M5 audit=0 — the money-can't-buy-position invariant) and **non-curated** signal segregation (AC6 / R1 — a self-driven search/browse view never confers **D-8** curated-rating eligibility). **6 stories / 9 G-W-T ACs (AC1–AC9) / 6 metrics**; all domain terms defined; explicit in/out-of-scope (out: digest/personalization, reception-weighted ordering, collections/follows, advanced/faceted search, `DIGEST` emit, non-accepted apps). Grounded every dependency in code — D-6 `catalog.list_catalogued_apps`/`get_catalogued_apps` (accepted-only, no N+1), D-5 `list_active_tags`/`resolve_tag`, `app-pages` stable URL, `signals` `Surface={DIGEST,APP_PAGE}`. **Money-buys-position test → PASS.** Logged **OSB-1…3** ([DECISIONS.md](features/open-search-browse/DECISIONS.md), PROPOSED) + **OQ-OSB-1…4** ([OPEN_QUESTIONS.md](features/open-search-browse/OPEN_QUESTIONS.md)); reuses **D-3/D-5/D-6/D-7/D-8 as-is — no new global ADR proposed**. Updated [INDEX.md](features/INDEX.md). **Raised DN-17** (approve brief + OQ-OSB-1 tag-filtering-in-MVP + OQ-OSB-2 default neutral order); **no Stage advance until approved.** |
| 2026-06-23 | `6-post-release`→`0-coordinator`→`1-define` | **Coordinator** (user: skip post-release, pick the next feature) — **re-verified the `interest-profile` build** (**616 tests / `ruff` / `check` / no drift**), then **skipped the Stage-6 retrospective** and **closed `interest-profile` out** (deferred/reopenable — no prod target/traffic + the matcher/digest consumer is unbuilt; mirrors the seven prior close-outs). Surveyed the backlog for all-deps-met features: `open-search-browse` (dep `app-pages` ✓), `developer-dashboard` (`signal-capture` ✓ + `ratings-reviews` ✓), `developer-updates` (`app-subscriptions` ✓ + `app-pages` ✓ + `signal-capture` ✓). **User picked `open-search-browse`** (D2) — the Phase-2 **open discovery surface** (search/browse the **D-6** accepted-app catalogue; the surface where **money never buys position**, vision §4.1). Folder already scaffolded (7 artifacts); set `Stage: 1-define`, updated [INDEX.md](features/INDEX.md), handed to the **Product Analyst** to draft [FEATURE_BRIEF.md](features/open-search-browse/FEATURE_BRIEF.md). |
| 2026-06-22 | `5-release`→`6-post-release` | **Release Engineer** — released **`apps/interests/`** to local/dev per [DESIGN §16](features/interest-profile/DESIGN.md). Wrote [RELEASE_NOTES.md](features/interest-profile/RELEASE_NOTES.md): an **additive new app owning one mutable table** `interests_interest` (**no parent row** → empty=structural default AC6; **CASCADE** user FK → account-deletion removal with **no `accounts` edit**, AC9; **no score/weight/rank** → AC8 structural; **emits no D-7 event / does not import `signals.capture`** → IP-5). Documented the single read contract `declared_tag_ids(user) -> frozenset[UUID]` (the matcher's Ring-0 input, AC8), the **§7 set-replace preserve-on-edit** reconcile (a *no-successor* retired ref survives a re-save → AC4 × AC7/M5=0), the **two-part activation switch** (`config/urls` `interests/` include + the one `{% interest_prompt %}` line in `accounts/profile.html`) as the rollback (**no feature flag**), gate-based promotion, and the metric→signal→alert map (the **one actionable alert** = an unexpected `set_interests` write failure, **not** validation rejects; M2/M3/M6 analyst-derived, expected thin until a digest consumer; M5 reference-integrity 0 by construction via `count_unresolvable`). **Rehearsed rollout→rollback on a throwaway PostgreSQL DB** (`interests_release_rehearsal`, dropped): `migrate interests` applied `0001_initial` → `interests_interest` + the `interests_one_per_user_tag` unique + the per-user `user_id` index + the CASCADE `user` FK to `accounts_account` all present → `check` clean → `migrate interests zero` reversed cleanly (table gone) → re-`migrate` re-applied (**reversible up→down→up**); `makemigrations --check` no drift. Re-verified **616 tests green (+64) / `ruff` / `check` / no drift**; the three `interests:picker`/`:save`/`:clear` routes resolve; both activation halves + the six observability constants + the two config tunables (`interest_suggested_minimum`/`interest_declaration_max`) present. Updated [INDEX.md](features/INDEX.md) (→ `6-post-release`). Advanced to `6-post-release`; handed to the **Retrospective Analyst** (outcome review expected to defer — no prod target/traffic + the matcher/digest consumer unbuilt, as the seven prior closed-outs). |

> Older rows (`interest-profile` build/plan/design/define, the `app-subscriptions`
> close-out + release/build/plan/define/design, the `ratings-reviews` close-out +
> build/release, `app-pages` close-out/release/define/design, `signal-capture`
> lifecycle, `submission-intake` & earlier, `interest-taxonomy`, `identity-accounts`)
> live in [process/activity-archive.md](process/activity-archive.md).
