# CONTROL.md — Project Control Board

**This is the durable channel between the user and the agent.** It replaces chat
memory. The agent reads it first every session and updates it last. The user checks
this one file to know *where we are* and *what is waiting on a decision*.

Rules:
- The agent **must not** act on a question listed under *Decisions Needed From You*
  until the user answers it here.
- Every work session ends by updating *Current State* and the *Activity Log*.
- Keep it short and current. Detail lives in the feature artifacts, not here.

---

## Current State

| Field              | Value                                                            |
|--------------------|------------------------------------------------------------------|
| **Active feature** | **`patch-developer-submissions-nav`** (Patch Track) — resolving [`UX-003`](issues/UX-003.md) (High): `developer`-role users have no reachable path to their pending/withdrawn/rejected submissions. Template/nav only; no schema/API/ADR. *Context:* the user is testing the app and logging issues; this is the first to be worked. `premium-frontend` stays closed-out; **DN-PS-DEPLOY** (live deploy) + `ui-modernization` remain held bets. |
| **Stage**          | `P-plan` — [PATCH.md](features/patch-developer-submissions-nav/PATCH.md) **written + ready for your review.** On approval → `P-build` (Maintenance Engineer). |
| **Persona**        | **Maintenance Planner** ([patch-1-planner.md](process/personas/patch-1-planner.md)) — done; **awaiting plan approval** (see *Decisions Needed → DN-UX003-PLAN*). |
| **Folder**         | [features/patch-developer-submissions-nav/](features/patch-developer-submissions-nav/) (5 artifacts seeded; PATCH.md filled). Issue triage registry: [issues/README.md](issues/README.md). |
| **Last updated**   | 2026-06-28 (Maintenance Planner — pinned UX-003 root cause to two template-layer gaps; wrote PATCH.md = design + 5 S-tasks, T-01 = regression test; no schema/API/ADR). |

> **Closed-out features** (Stage 6 skipped per the standing pattern — reopenable; all
> released local/dev, rollback rehearsed; live-metrics/outcome reports deferred until a
> prod target exists). Full record in each feature's `DECISIONS.md` / `RELEASE_NOTES.md`
> and in [features/INDEX.md](features/INDEX.md):
> `identity-accounts` (108) · `interest-taxonomy` (184, D-5) · `submission-intake` (315, D-6) ·
> `signal-capture` (374, D-7) · `app-pages` (417) · `ratings-reviews` (486, D-8) ·
> `app-subscriptions` (552, AS-3 seam) · `interest-profile` (616) · `open-search-browse` (676) ·
> `developer-dashboard` (740) · `developer-updates` (828, AS-3 producer, DU-REL-1) ·
> `embeddable-update-widget` (893, AC6 firewall structural, DU-REL-1 rollback) ·
> `widget-conversion-attribution` (962, first-party `widget_src` cookie, M5=0 structural) ·
> `platform-staging` (975, D-12 serving stack + shared shell; **live deploy deferred** until staging debuts on the premium UI — artifacts ready, DN-PS-DEPLOY reopenable) ·
> `premium-frontend` (980, D-13 no-build token design system + static `/` landing + vendored HTMX).

> Canonical stage values: `0-coordinator` · `1-define` · `2-design` · `3-plan` ·
> `4-build` · `5-release` · `6-post-release` · `done`. See the routing table in
> [CLAUDE.md](CLAUDE.md) §2.

### Latest session status (CLAUDE.md §6.7 — overwritten each session)

```
Stage: P-plan | Feature: patch-developer-submissions-nav | Persona: Maintenance Planner
Done: Investigated UX-003 against the live code and pinned its root cause with certainty to two presentation-layer gaps: (1) core/base.html:19-32 renders a fixed authenticated link set with no developer link to catalog:my-apps — and roles (Django Groups) have no template-side accessor today (account_has_role is view-only); (2) dashboard/my_apps.html:60-67 empty state dead-ends at "Submit" with no path to pending/rejected submissions. Wrote PATCH.md: design (one reusable is_developer simple_tag reusing the account_has_role gate + 3 template edits) + No-Schema Assertion + 5 ordered S-tasks (T-01 = the regression test, red-before-fix).
Verified by: n/a — planning only, no code touched. Grounded every claim in code (catalog:my-apps @require_role(developer) at views.py:305; reception builds accepted-only summaries; 3 existing templatetags packages as the convention; make_account test helper). Scope gate re-confirmed: no schema/API/ADR → stays Patch Track.
Blocked/Deferred: Awaiting user approval of the patch plan before P-build (DN-UX003-PLAN). Held bets unchanged: DN-PS-DEPLOY (live staging deploy), ui-modernization. Q-001 + UX-002 still parked (non-blocking).
Decisions needed: DN-UX003-PLAN (approve PATCH.md → P-build). DN-Q001-TAXONOMY, DN-UX002-SCOPE still open/non-blocking. DN-PS-DEPLOY open/held.
Next: On approval, advance to P-build (Maintenance Engineer) and build T-01…T-05.
```

---

## Decisions Needed From You

The agent is blocked on these. Answer inline (edit the **Answer** cell), then the agent
proceeds.

**DN-PS-DEPLOY — REOPENED 2026-06-28 (the active next bet).** With `premium-frontend` signed off +
closed out, the sequencing condition is met (staging now debuts on the polished UI), so the live
Render deploy of the developer wedge can proceed. The build is verified and every deploy artifact
([render.yaml](render.yaml), [docs/deploy/](docs/deploy/)) is written + secret-clean + runs
unchanged when staging is reactivated. When you're ready to deploy: run
[docs/deploy/deploy-runbook.md](docs/deploy/deploy-runbook.md) end-to-end (Render account + email
provider with a **verified sending domain** + live secrets), paste the live `https://…` URL back
here, and the agent runs the **AC1.1 / AC1.2 / AC1.3 / AC5.2** checks; the **AC6.1 / AC6.2**
walkthrough ([staging-walkthrough.md](docs/deploy/staging-walkthrough.md)) is yours (PS-3).

**DN-UX003-PLAN — NEW 2026-06-28 (gates `P-build`).** The Maintenance Planner has written
[PATCH.md](features/patch-developer-submissions-nav/PATCH.md) for [`UX-003`](issues/UX-003.md):
root cause = two presentation-layer gaps (no developer link in the header; the dashboard
empty state dead-ends). Fix = one reusable `is_developer` template tag (reuses the existing
`account_has_role` gate — no new auth logic) + 3 template edits, across **5 ordered S-tasks**
(T-01 = the red-first regression test). No schema / API / ADR — stays Patch Track. **Approve
to advance to `P-build`** (Maintenance Engineer builds it), or request changes. **Answer:**
approve

**DN-Q001-TAXONOMY — NEW 2026-06-28 (non-blocking).** [`Q-001`](issues/Q-001.md): should a tag (subcategory) be allowed under multiple clusters (current schema — *duplicate*), or restricted to one (*unique*)? **Option 1 (keep duplicate)** matches real-world classification + multi-pathway discovery, keeps the current schema, but needs the [`BUG-001`](issues/BUG-001.md) fix (unique per-cluster HTML IDs) and optionally a small JS/HTMX cross-cluster visual-sync. **Option 2 (enforce unique)** simplifies the UI and dissolves BUG-001 structurally, **but requires a data migration → Feature Track** (out of Patch scope). This gates how deep the BUG-001 patch goes. *Recommendation: Option 1 (keep duplicate) — the schema already supports it and BUG-001's core fix is a cheap template change; only do the JS sync if you want it.* **Answer:** option 1 for now, can implement a better way in the future

**DN-UX002-SCOPE — NEW 2026-06-28 (non-blocking).** [`UX-002`](issues/UX-002.md): the app-submission tag picker is an overwhelming flat checkbox list. Scope decides the pipeline. **(a) Minimal (Patch Track):** add a client-side search/filter box + light visual grouping over the existing list — no schema/API. **(b) Full (Feature Track):** custom dynamic tags (schema), smart keyword/AI tag recommendations (API), or a Steam-style categorized wizard. *Recommendation: start with (a) as a patch to relieve the friction now, and log (b) as a future Feature-Track bet if (a) isn't enough.* **Answer:** approve recommendation

*Recently resolved:* **DN-PF-SIGNOFF** 2026-06-28 (user signed off the M7 premium review — *polished product, not a prototype*; deeper palette/nav modernization scoped to the future `ui-modernization` bet, a user D2 call) · **DN-PF-DESIGN** 2026-06-28 → [D-13](DECISIONS.md) (Option A / no build step; Tailwind-via-CLI kept as a reversible fallback) · **DN-PF-BRIEF** 2026-06-28 (wedge surfaces first / light PE / real `/` landing) · **DN-PS-DESIGN** 2026-06-27 → [D-12](DECISIONS.md) (Render / Resend / Consolidate) · **DN-PS-BRIEF** 2026-06-27 (free-tier, prod-bound, user signs off the walkthrough) · **DN-STRAT-1** 2026-06-27 → [D-11](DECISIONS.md) (staging-validation-before-live).

---

## Decisions Made (recently)

A short, human-readable digest. Full rationale lives in [DECISIONS.md](DECISIONS.md)
(global) or `features/<slug>/DECISIONS.md` (local). Only the **active** feature is kept
in full; closed features collapse to a single pointer line.

- **`premium-frontend` SIGNED OFF + CLOSED-OUT → live deploy reopened (2026-06-28)** — the user gave the **M7 premium visual sign-off** (DN-PF-SIGNOFF): the landing / app page / discover surfaces read as a **polished product, not a prototype**. They noted the palette is still bland with no stylized/animated nav and scoped that **deeper modernization to a future Feature-Track enhancement** (`ui-modernization` — distinctive palette + stylized/animated nav + premium motion; needs a full Stage-1→2 design), explicitly **not** an `issues/` item and **not** a re-style of `premium-frontend`. Closed the feature out in [INDEX.md](features/INDEX.md) (released local/dev; build re-verified at release — 980 tests / no drift / ruff / check; rollback rehearsed, no migration) and **reopened [DN-PS-DEPLOY](#decisions-needed-from-you)** (sequencing condition met). `ui-modernization` queued as a held future bet (activation = a user D2 call). Held density-gated bets (network / `weekly-digest` / `editorial-curation-tools` / D-9) unchanged.
- **`premium-frontend` design + build (2026-06-28 → global [D-13](DECISIONS.md))** — the premium gap was **design depth, not tooling**, so the hand-authored [`core/app.css`](apps/core/static/core/app.css) was deepened into a full token-driven design system (palette/states/elevation, type + spacing scales, motion, a component set, 3 breakpoints) in the same one WhiteNoise-hashable file — **no Tailwind/Node/build step** (Option A; Tailwind-via-CLI kept as an outcome-gated reversible fallback). D-13 affirms + extends [D-12](DECISIONS.md)'s build-free posture. Also: restyled shared shell (additive `{% block body_class %}` + skip-link), a new **static `/` landing** ([`core.views.landing`](apps/core/views.py), no DB → closes PF-CARRY-1/AC-3), an app-page + discover presentation-only restyle (slots/order/SEO unchanged), and **vendored** HTMX + `hx-boost` (light PE, fully degradable). Widget firewall (M5=0), no-JS, SEO, a11y, no-schema preserved by construction.
- **`platform-staging` RELEASED local/dev + closed-out (2026-06-28 → global [D-12](DECISIONS.md))** — the full T-01…T-11 serving stack + shared shell: gunicorn + WhiteNoise + `dj-database-url`, non-`DEBUG` media route, the [render.yaml](render.yaml) Blueprint, Resend SMTP wiring + setup `.md`, the shared responsive shell consolidating the 6 `base.html` (**widget firewall held**), the DB-only `/health/live` split, the `REDIS_URL` cache, env-gated Sentry, the runbook + walkthrough. Build complete + verified local/dev (975 tests / ruff / check / no drift / secret-clean); **live deploy deliberately deferred** to land `premium-frontend` first so staging debuts polished. DN-PS-DEPLOY parked → since reopened.
- **Next bet RATIFIED → staging-validation-before-live (Strategist, 2026-06-27 → global [D-11](DECISIONS.md))** — with the developer wedge code-complete but **all 13 features local/dev only** (H1/H2/H3 + the §8 one-line test unvalidated) and every standing-fork option (D-9 / network / editorial) density-gated → premature, the next bet is **not another feature and not yet a public launch** — stand up staging, deploy the wedge, and walk it end-to-end as user/developer/admin on web + mobile. Frontend: keep server-rendered Django templates ([D-4](DECISIONS.md)), polish + make responsive; a dedicated-SPA split is deferred + evidence-gated. Living picture in [STRATEGY.md](STRATEGY.md).
- **Developer-wedge pivot — global D-10 + D-9 (2026-06-26)** — **D-10:** MVP cold-start re-sequenced to a single-player **developer hub** (app page + changelog + embeddable widget) that drags a developer's existing audience in; the discovery network/digest is **held until per-niche density**. **D-9:** monetization = paid, labeled, time-boxed **promotion placements** sold for reach, **firewalled from the ranking** (a `Surface` outside `CURATED_SURFACES`); tools + curation stay free. The wedge funnel (impression → click → conversion) shipped complete via `embeddable-update-widget` + `widget-conversion-attribution`. Full text in [DECISIONS.md](DECISIONS.md).

**Closed-feature pointers** (full lifecycle in each folder's `DECISIONS.md`/`RELEASE_NOTES.md`):

- **`widget-conversion-attribution`** (closed 2026-06-27, 962 tests) — first-party signed `widget_src` source-only cookie carries the source through the anonymous click → app page → conversion; conversions tallied in a separate PII-free `widget_conversion_count` rollup via fail-soft hooks; AC6 firewall structural (M5=0). Detail: [widget-conversion-attribution/DECISIONS.md](features/widget-conversion-attribution/DECISIONS.md).
- **`embeddable-update-widget`** (closed 2026-06-26, 893 tests) — `apps/widget/` read-only embeddable "what's new" widget + reach rollup; AC6 firewall structural by absence; DU-REL-1 rollback. Detail: [embeddable-update-widget/DECISIONS.md](features/embeddable-update-widget/DECISIONS.md).
- **`developer-updates`** (closed 2026-06-24, 828 tests) — new `apps/updates/` owning `updates_notice`, the single **AS-3 producer**: pull delivery (M5=0 structural, no fan-out), imports no `signals` (AST-enforced), durable table-derived rate limit; rollback = `git revert` of the build commit (**DU-REL-1**). Detail: [developer-updates/DECISIONS.md](features/developer-updates/DECISIONS.md).
- **`developer-dashboard`** (closed 2026-06-24, 740 tests) — model-less `apps/dashboard/`, a read-only view of an app's reception (D-7 reach/engagement + D-8 curated ratings) for its owner; two additive neutral `signals.selectors` reads, no schema. Detail: [developer-dashboard/DECISIONS.md](features/developer-dashboard/DECISIONS.md).
- **`open-search-browse`** (closed 2026-06-23, 676 tests) — `apps/discovery/`, the paginated DB-pushed `catalog.search_catalogue` open surface where money never buys position. Detail: [open-search-browse/DECISIONS.md](features/open-search-browse/DECISIONS.md).
- **`interest-profile`** (closed 2026-06-23, 616 tests) — `apps/interests/`, the user side of the Ring-0 match via the `declared_tag_ids` matcher contract. Detail: [interest-profile/DECISIONS.md](features/interest-profile/DECISIONS.md).
- **`app-subscriptions`** (closed 2026-06-21, 552 tests) — `apps/subscriptions/`, atomic follow + one `subscribe` emit, the empty-until-producer notice seam **AS-3**. Detail: [app-subscriptions/DECISIONS.md](features/app-subscriptions/DECISIONS.md).
- **`ratings-reviews`** (closed 2026-06-21, 486 tests) — `apps/ratings/`; established the global **[D-8](DECISIONS.md)** curated-rating gate ("curated = a `DIGEST` impression"). Detail: [ratings-reviews/DECISIONS.md](features/ratings-reviews/DECISIONS.md).
- **`app-pages`** (closed 2026-06-20, 417 tests) — `apps/pages/`, first live emitter of D-7 `app_page` impressions. Detail: [app-pages/DECISIONS.md](features/app-pages/DECISIONS.md).
- **`signal-capture`** (closed 2026-06-18, 374 tests) — `apps/signals/`; established the global **[D-7](DECISIONS.md)** event schema (append-only, structural no-score, no-PII). Detail: [signal-capture/DECISIONS.md](features/signal-capture/DECISIONS.md).
- **`submission-intake`** (closed 2026-06-18, 315 tests) — `apps/catalog/`, the ACCEPTED-only catalogue substrate = global **[D-6](DECISIONS.md)**. Detail: [submission-intake/DECISIONS.md](features/submission-intake/DECISIONS.md).
- **`interest-taxonomy`** (closed 2026-06-17, 184 tests) — flat tags + clusters, soft-retire + read-time `resolve_tag` = global **[D-5](DECISIONS.md)**. Detail: [interest-taxonomy/DECISIONS.md](features/interest-taxonomy/DECISIONS.md).
- **`identity-accounts`** (closed 2026-06-17, 108 tests) — the foundation (the stack **D-4**, roles **D-3**). Detail: [DECISIONS.md](DECISIONS.md).

> **Foundational decisions** (the stack, niche, build order — D1–D3, A1–A3, R1, global
> D-1/D-2/D-4) live in [DECISIONS.md](DECISIONS.md). Highlights: niche = **"vibecoded
> webapps"** (D-1); stack = **Python / Django + PostgreSQL** (D-4).

---

## Activity Log

Newest first. One line per session. **Keep the most recent ~10 rows here**; older rows
live in [process/activity-archive.md](process/activity-archive.md). The per-feature
folders remain the full record either way.

| Date       | Stage           | Summary                                                                 |
|------------|-----------------|-------------------------------------------------------------------------|
| 2026-06-28 | `P-plan` | **Maintenance Planner** — investigated **UX-003** against the live code and wrote [PATCH.md](features/patch-developer-submissions-nav/PATCH.md). **Root cause pinned to two presentation-layer gaps:** (1) [`core/base.html:19-32`](apps/core/templates/core/base.html#L19-L33) renders a fixed authenticated link set with no developer link to `catalog:my-apps`, and roles (Django Groups) have **no template-side accessor** today ([`account_has_role`](apps/accounts/permissions.py#L23) is view-only); (2) the [`dashboard/my_apps.html` empty state](apps/dashboard/templates/dashboard/my_apps.html#L60-L67) dead-ends at *Submit* with no path to pending/rejected submissions. **Design** = one reusable `is_developer` `simple_tag` (delegates to the existing gate — one source of truth) + a developer-gated header link + dashboard sub-nav/empty-state CTA + a reciprocal submissions→dashboard link. **No-Schema Assertion** holds (templates + one read-only tag; no migration/API/ADR → Patch Track confirmed). **5 ordered S-tasks, T-01 = red-first regression test.** **No code touched.** Surfaced **DN-UX003-PLAN** (approval gates `P-build`). |
| 2026-06-28 | `0-coordinator`→`P-plan` | **Coordinator** — user switched to **issue-resolution mode** (testing the app + logging issues). **Triaged all 5 open issues** ([issues/README.md](issues/README.md)): **UX-003** (High, no nav path to pending/withdrawn submissions) → routed to a **new Patch Track** folder [patch-developer-submissions-nav](features/patch-developer-submissions-nav/) (template/nav only — scope gate confirmed; `catalog:my-apps` route verified at [apps/catalog/urls.py:49](apps/catalog/urls.py#L49)), set **IN-PROGRESS**; **UX-001** (responsive CSS) + **BUG-001** (unique per-cluster HTML IDs) → **TRIAGED**, queued behind it; **UX-002** (tag-picker overhaul) + **Q-001** (duplicate-vs-unique taxonomy) → surfaced as **DN-UX002-SCOPE** / **DN-Q001-TAXONOMY** (non-blocking decisions). Scaffolded the 5 patch artifacts, added the **INDEX.md Patch Track** row, updated each routed issue's status/slug. Noted a stale ref: BUG-000 points to a `patch-anonymous-profile-link/` folder that was never created. Set **Active=patch-developer-submissions-nav, Stage=P-plan, Persona=Maintenance Planner**. **No code touched.** Handed to the **Maintenance Planner**. |
| 2026-06-28 | `0-coordinator` | **Coordinator** — **CONTROL.md cleanup** (it had grown past its own "keep it short" rule). Condensed the Current State cells, dropped the stale closed-feature "design in one line" note, collapsed the resolved-decision notes to one-liners, tightened the Decisions Made digest, and moved the older Activity Log rows (the platform-staging design/brief/scaffold rows + the full `widget-conversion-attribution` and `embeddable-update-widget` lifecycles) into [process/activity-archive.md](process/activity-archive.md). **No code touched; no stage/decision change** — DN-PS-DEPLOY remains the open next bet. |
| 2026-06-28 | `5-release`→closed-out → `0-coordinator` | **Release Engineer → Coordinator** — recorded the user's **M7 premium visual sign-off** (DN-PF-SIGNOFF RESOLVED): the `premium-frontend` landing / app page / discover surfaces read as a **polished product, not a prototype**. **Closed `premium-frontend` out** in [INDEX.md](features/INDEX.md) (released local/dev; build independently re-verified at release — **980 tests** / no drift / ruff / check; rollback rehearsed, no migration). The user flagged the **palette is still bland + no stylized/animated navigation** and scoped **deeper modernization to a future Feature-Track enhancement** (`ui-modernization`) — **not** an `issues/` item, **not** a re-style → **queued as a held future bet** (activation = a user **D2** call). **Reopened [DN-PS-DEPLOY](#decisions-needed-from-you)** — the sequencing condition (staging debuts on the polished UI) is now met. Held density-gated bets unchanged. **No code touched.** |
| 2026-06-28 | `5-release` (released local/dev) | **Release Engineer** — **independently re-verified the handed-off `premium-frontend` build** and **released it to local/dev**, writing [RELEASE_NOTES.md](features/premium-frontend/RELEASE_NOTES.md). **980 tests green** (M4: 975+5); `makemigrations --check` no drift (presentation-only, no schema); `manage.py check` clean; **`collectstatic` (DEBUG=false manifest) hashes+gzips `app.css` + `htmx.min.js`** → WhiteNoise serving model intact (**R1 mitigated**); **widget firewall held** (G2/M5=0); `/` → 200/0-queries/405-POST (AC-3/M6); vendored HTMX, no CDN. **Found + `ruff --fix`ed 5 trivial lint defects the handoff missed** (dead import + 3 whitespace + import-sort; no behaviour change). **Rollback rehearsed (hard gate):** no migration → nothing irreversible; `git revert --no-commit 6a01436 b14b238 8be0a51` ran with `manage.py check` clean mid-revert, only CONTROL.md (prose) conflicting; aborted clean, HEAD intact. **Outstanding = DN-PF-SIGNOFF** (the user's, human-judgment, PS-3). The user committed an in-scope `app.css` consistency pass (`8be0a51`) + issue `UX-003` (`c77fd6f`) concurrently — folded into the verified build. Advanced [INDEX.md](features/INDEX.md) → 5-release/released-local-dev. **Awaiting sign-off.** |
| 2026-06-28 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [premium-frontend/DESIGN.md](features/premium-frontend/DESIGN.md) into [TASKS.md](features/premium-frontend/TASKS.md): **8 ordered tasks T-01…T-08, all S/M**. The `app.css` design system split **T-01** (tokens + reset/base + layout primitives) **→ T-02** (components/utilities/3 breakpoints); then **T-03** restyle the shared [`core/base.html`](apps/core/templates/core/base.html) (additive `{% block body_class %}` + skip-link), **T-04** the new **static `/` landing** ([`core.views.landing`](apps/core/views.py), no DB → closes PF-CARRY-1/AC-3/M6), **T-05** restyle [`app_page.html`](apps/pages/templates/pages/app_page.html) (load-bearing wedge; same slots/order/SEO strings), **T-06** restyle [`catalogue.html`](apps/discovery/templates/discovery/catalogue.html), **T-07** **vendored** HTMX (`core/vendor/htmx.min.js`, no CDN, `defer`) + `hx-boost` (no new endpoint), **T-08** [TEST_PLAN.md](features/premium-frontend/TEST_PLAN.md) + the M2 first-paint baseline + the M7 sign-off package. Defined **six standing guardrails** (G1 suite ≥975 + no drift; **G2 widget firewall M5=0**; G3 one-source-of-truth in `:root`; G4 no-JS path is the source of truth; G5 no view/url/schema change on restyle; G6 keep class names). Advanced [INDEX.md](features/INDEX.md) → 4-build. **No code.** Handed to the **Senior Engineer**. |
| 2026-06-28 | `2-design`→`3-plan` | **Software Architect** (DN-PF-DESIGN resolved) — surveyed the codebase + authored [premium-frontend/DESIGN.md](features/premium-frontend/DESIGN.md) (**PF-DESIGN-1…7**), and got it **APPROVED**. **Pivotal PF-2 call → Option A, NO build step**: deepen the hand-authored [`core/app.css`](apps/core/static/core/app.css) into a full token-driven design system in the same one WhiteNoise-hashable file; **reject Tailwind's Node/CLI toolchain** → global ADR **[D-13](DECISIONS.md)** (affirms + extends [D-12](DECISIONS.md)'s build-free posture; eliminates brief risk **R1**). **Option B (Tailwind via the standalone CLI, fully specified §11) kept as an outcome-gated reversible fallback.** Plus a restyled shared shell, a new **static `/` landing** (no DB read → closes PF-CARRY-1/AC-3), an [`app_page.html`](apps/pages/templates/pages/app_page.html) + [`catalogue.html`](apps/discovery/templates/discovery/catalogue.html) presentation-only restyle, and **vendored** HTMX + `hx-boost` (light PE, fully degradable). Firewall (AC-6/M5=0), no-JS, SEO, a11y, no-schema preserved by construction. Advanced [INDEX.md](features/INDEX.md) → 3-plan. **No code.** Handed to the **Planner / Tech Lead**. |
| 2026-06-28 | `1-define`→`2-design` | **Product Analyst** (DN-PF-BRIEF approved) — authored [premium-frontend/FEATURE_BRIEF.md](features/premium-frontend/FEATURE_BRIEF.md) from the upstream 2026-06-28 frontend decision + vision ([D-10](DECISIONS.md): the app page is the developer's marketing landing page). Problem/goal; **6 user stories**; **AC-1…7** each Given/When/Then and tagged *agent-verifiable* vs *human-judgment*; **M1–M7**; in/out scope; **C1–C7** + A1–A3; top-5 risks. Made "premium feel" checkable by anchoring it to a concrete **design system** plus a PS-3-style user sign-off. Grounded in code: **bare `/` 404s** → **PF-CARRY-1** folded in as US-3/AC-3 (real landing); the **7 `base.html` already consolidated** to [`core/base.html`](apps/core/templates/core/base.html) (build on the shell, C7); **widget firewall preserved** (AC-6 / M5=0). **User answered with all 3 recommended defaults** (wedge surfaces first · light PE only · real platform landing). Advanced [INDEX.md](features/INDEX.md) → 2-design. **No code.** Handed to the **Software Architect**. |
| 2026-06-28 | `5-release`→closed-out → `1-define` | **Release Engineer → Coordinator** (user-directed consolidation) — wrote the [platform-staging/RELEASE_NOTES.md](features/platform-staging/RELEASE_NOTES.md) for the verified T-01…T-11 build, **honestly framed: build complete + verified local/dev (975 tests / ruff / check both DEBUG / no drift / secret-clean), live deploy DELIBERATELY DEFERRED** per the user's decision to land `premium-frontend` first so staging debuts polished. **Closed out platform-staging** in [INDEX.md](features/INDEX.md) (**DN-PS-DEPLOY → parked/reopenable**; artifacts ready, run unchanged when reactivated). Then as **Coordinator** scaffolded [features/premium-frontend/](features/premium-frontend/) (7 artifacts; **PS-OQ-1** carried in as **PF-CARRY-1**), added its INDEX row, set **Active = `premium-frontend`, `Stage: 1-define`, Persona Product Analyst**. Held bets unchanged. **No code touched.** Handed to the **Product Analyst**. |
| 2026-06-28 | `5-release` (frontend direction) | **User decision (Strategist-lens), no code** — resolved the [D-11](DECISIONS.md) frontend evidence-gate **within** the [D-4](DECISIONS.md) envelope: build a **premium server-rendered frontend = HTMX + Tailwind + Django templates** (polish, premium feel), **NOT** an SPA. Driver: the local UI is barebone (one **199-line** [`app.css`](apps/core/static/core/app.css), no design system) and the developer's **app page is their marketing landing page** — the gap is *design*, not architecture. **Sequencing (user) = deploy staging first**, then activate. Committed + **queued** as suggested slug **`premium-frontend`**. Tailwind's build step deliberately revises [D-12](DECISIONS.md)'s build-free posture → designed at the new feature's Stage 2 (later resolved to *no build step* in D-13). **No Stage change; `platform-staging` stays active.** |
| 2026-06-28 | `5-release` | **Release Engineer** — **independently re-verified the handed-off `platform-staging` build** against the live local PG and confirmed every agent-runnable deploy artifact. **975 tests green**; `ruff` clean; `manage.py check` clean under **both** DEBUG states; `makemigrations --check` = **no drift**. **[render.yaml](render.yaml)** parses with **zero secret literals** (every secret is `sync:false`/`generateValue`/`fromDatabase|fromService`). **AC2.2 holds** (`.env` gitignored + untracked). `collectstatic` (DEBUG=false manifest) → hashed+gz `core/app.css`; `gunicorn config.wsgi` loads under DEBUG=false. **Surfaced [DN-PS-DEPLOY](#decisions-needed-from-you):** the live Render deploy is outward-facing user action the agent can't perform; user runs [deploy-runbook.md](docs/deploy/deploy-runbook.md) and returns the live URL, then the agent runs AC1/AC5; AC6 walkthrough is the user's (PS-3). **Stays at Stage 5.** No code changed. |
| 2026-06-28 | `4-build`→`5-release` | **Senior Engineer** — built **all T-01…T-11** from the APPROVED [platform-staging/TASKS.md](features/platform-staging/TASKS.md) + [DESIGN.md](features/platform-staging/DESIGN.md) (global [D-12](DECISIONS.md)), T-01 risk first. Serving stack: gunicorn + WhiteNoise (`CompressedManifest` gated on `not DEBUG`) + `dj-database-url` + `STATIC_ROOT` + `CSRF_TRUSTED_ORIGINS`; non-`DEBUG` media via [`serve_media`](apps/core/views.py); the [`render.yaml`](render.yaml) Blueprint; `EMAIL_*` env wiring + [`email-provider-setup.md`](docs/deploy/email-provider-setup.md); the shared shell [`core/base.html`](apps/core/templates/core/base.html) + `app.css` with the **6 app bases consolidated** (messages render once; **widget byte-unchanged → AC3.3 firewall held**); the DB-only [`/health/live`](apps/core/views.py) split; the `REDIS_URL` cache (LocMem fallback); env-gated Sentry; the [`deploy-runbook.md`](docs/deploy/deploy-runbook.md) + AC2.2 no-secret pass; [`TEST_PLAN.md`](features/platform-staging/TEST_PLAN.md) + [`staging-walkthrough.md`](docs/deploy/staging-walkthrough.md). **975 tests green** (+13), `ruff`/`check` (both DEBUG) clean, **no migration drift**. PS-IMPL-1…5 logged; **PS-OQ-1** (bare `/` 404) logged not fixed (out of scope). Advanced [INDEX.md](features/INDEX.md) → 5-release. Handed to the **Release Engineer**. |
| 2026-06-27 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [platform-staging/DESIGN.md](features/platform-staging/DESIGN.md) (global [D-12](DECISIONS.md)) into [TASKS.md](features/platform-staging/TASKS.md): **11 ordered tasks T-01…T-11, all S/M**. **Risk front-loaded at T-01** (production WSGI + WhiteNoise static + `dj-database-url` — the real C4/§2 serving gap). The ~30-template consolidation sequenced **T-05** (shared `core/base.html` + `app.css`) **→ T-06** (re-point the 6 app `base.html`) with a render-every-surface check and the **AC3.3 widget firewall held** as an explicit non-change. Plus T-02 non-`DEBUG` media route, T-03 `render.yaml`, T-04 SMTP wiring, T-07 DB-only `/health/live` split, T-08 `REDIS_URL` cache, T-09 env-gated Sentry, T-10 runbook + AC2.2 no-secret pass, T-11 `TEST_PLAN.md` + walkthrough. Full **AC1.1–AC6.2 + PS-DESIGN-1…8** coverage map. Advanced [INDEX.md](features/INDEX.md) → 4-build. **No code.** Handed to the **Senior Engineer**. |

> Older rows (platform-staging design/brief/scaffold + the `widget-conversion-attribution`,
> `embeddable-update-widget`, `developer-updates`, `developer-dashboard`, `open-search-browse`,
> `interest-profile`, `app-subscriptions`, `ratings-reviews`, `app-pages`, `signal-capture`,
> `submission-intake`, `interest-taxonomy`, `identity-accounts` lifecycles) live in
> [process/activity-archive.md](process/activity-archive.md).
