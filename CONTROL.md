# CONTROL.md — Project Control Board

**This is the durable channel between the user and the agent.** It replaces chat
memory. The agent reads it first every session and updates it last. The user checks
this one file to know *where we are* and *what is waiting on a decision*.

Rules:
- The agent **must not** act on a question listed under *Decisions Needed From You*
  until the user answers it here.
- Every work session ends by updating *Current State* and the *Activity Log*.
- Keep it short and current. Detail lives in the feature artifacts, not here.

---

## Current State

| Field              | Value                                                            |
|--------------------|------------------------------------------------------------------|
| **Active feature** | **`premium-frontend`** — build a **premium server-rendered frontend** = **HTMX + Tailwind + Django templates** (polish, interactivity, premium feel), **NOT** an SPA — resolving the [D-11](DECISIONS.md) frontend evidence-gate **inside the [D-4](DECISIONS.md) envelope** (user decision 2026-06-28). Driver: the developer's **app page is their marketing landing page** ([D-10](DECISIONS.md)), so premium feel is load-bearing + the page must stay SEO-friendly/fast-first-paint; the gap is **design**, not architecture (one ~199-line [`app.css`](apps/core/static/core/app.css), no design system). Sequenced **before** the live staging deploy so staging debuts polished. Carries in **PS-OQ-1** (bare `/` 404 → build a real landing surface). **`platform-staging` closed-out** (released local/dev; build verified; live deploy deferred — artifacts ready). `editorial-curation-tools` + `weekly-digest` + D-9 monetization stay **backlog** (density-gated, D-10). |
| **Stage**          | `5-release` — **RELEASED to local/dev; build independently re-verified; rollback rehearsed.** [RELEASE_NOTES.md](features/premium-frontend/RELEASE_NOTES.md) written. **One gate outstanding: the M7 premium visual sign-off (`DN-PF-SIGNOFF`) — the user's to give (human-judgment, PS-3 pattern).** All agent-verifiable ACs pass. |
| **Persona**        | **Release Engineer** (Stage 5, [phase-5-release-engineer.md](process/personas/phase-5-release-engineer.md)) — released local/dev; awaiting the user's M7 sign-off before close-out. After sign-off the next bet is the parked live deploy ([DN-PS-DEPLOY](#decisions-needed-from-you)) on the now-polished UI. |
| **Folder**         | [features/premium-frontend/](features/premium-frontend/) (brief APPROVED; [DESIGN.md](features/premium-frontend/DESIGN.md) **APPROVED**; [TASKS.md](features/premium-frontend/TASKS.md) **COMPLETED** (T-01…T-08); [DECISIONS.md](features/premium-frontend/DECISIONS.md) PF-DESIGN-1…7 RATIFIED; [TEST_PLAN.md](features/premium-frontend/TEST_PLAN.md) **WRITTEN**; [RELEASE_NOTES.md](features/premium-frontend/RELEASE_NOTES.md) **WRITTEN — released local/dev**). Prior feature [platform-staging](features/platform-staging/) **closed-out** (live deploy deferred — [render.yaml](render.yaml) + [docs/deploy/](docs/deploy/) ready, DN-PS-DEPLOY parked/reopenable). |
| **Last updated**   | 2026-06-28 (Release Engineer — re-verified build [980 tests / ruff clean after fixing 5 trivial lint misses / no drift / collectstatic hashes app.css+htmx / widget firewall held], wrote RELEASE_NOTES.md, rehearsed rollback [no migration; `git revert` clean + check clean mid-revert], surfaced DN-PF-SIGNOFF) |

> **Design in one line:** a **first-party, signed, source-only cookie** `widget_src`
> set on the click-through 302 (the click is a `target="_top"` top-level nav onto the
> platform origin → first-party from birth) carries the source app-id (public id +
> flags, **never a person**) through the anonymous click → app page → conversion;
> conversions are tallied in a **separate, PII-free `widget_conversion_count`** rollup
> via **fail-soft hooks** at the follow + register views; the dashboard widget slot
> gains a conversions funnel stage + M2 rate. AC6 firewall stays structural; no consent
> banner under the no-PII posture (one-call consent gate is a documented contingency,
> revisit with counsel pre-EU-prod). Full rationale in the feature [DECISIONS.md](features/widget-conversion-attribution/DECISIONS.md).

> **Closed-out features** (Stage 6 skipped per the standing pattern — reopenable; all
> released local/dev, rollback rehearsed; live-metrics/outcome reports deferred until a
> prod target exists). Full record in each feature's `DECISIONS.md` / `RELEASE_NOTES.md`
> and in [features/INDEX.md](features/INDEX.md):
> `identity-accounts` (108) · `interest-taxonomy` (184, D-5) · `submission-intake` (315, D-6) ·
> `signal-capture` (374, D-7) · `app-pages` (417) · `ratings-reviews` (486, D-8) ·
> `app-subscriptions` (552, AS-3 seam) · `interest-profile` (616) · `open-search-browse` (676) ·
> `developer-dashboard` (740) · `developer-updates` (828, AS-3 producer, DU-REL-1) ·
> `embeddable-update-widget` (893, AC6 firewall structural, DU-REL-1 rollback) ·
> `widget-conversion-attribution` (962, first-party `widget_src` cookie, M5=0 structural) ·
> `platform-staging` (975, D-12 serving stack + shared shell; **live deploy deferred** until staging debuts on the premium UI — artifacts ready, DN-PS-DEPLOY reopenable).

> Canonical stage values: `0-coordinator` · `1-define` · `2-design` · `3-plan` ·
> `4-build` · `5-release` · `6-post-release` · `done`. See the routing table in
> [CLAUDE.md](CLAUDE.md) §2.

### Latest session status (CLAUDE.md §6.7 — overwritten each session)

```
Stage: 5-release | Feature: premium-frontend | Persona: Release Engineer
Done: Independently re-verified the premium-frontend build, wrote RELEASE_NOTES.md (released to local/dev), and rehearsed rollback.
Verified by: 980 tests pass; makemigrations --check no drift; ruff clean (fixed 5 trivial lint misses the handoff left — dead import + whitespace + import-sort, no behaviour change); manage.py check clean; collectstatic (DEBUG=false manifest) hashes+gzips app.css + htmx.min.js; widget firewall held (apps/widget untouched, G2/M5=0); / -> 200/0-queries/405-POST. Rollback rehearsed: no migration; git revert 6a01436 b14b238 8be0a51 with check clean mid-revert, only CONTROL.md (prose) conflicts.
Blocked/Deferred: HTMX discovery-fragment + authed-surface restyle deferred by design; live deploy (DN-PS-DEPLOY) follows after sign-off.
Decisions needed: DN-PF-SIGNOFF — the M7 premium visual sign-off (the user's, human-judgment, per PS-3) is the one gate before close-out.
Next: User performs the M7 sign-off (TEST_PLAN.md §4 checklist); on sign-off, close out premium-frontend and reopen DN-PS-DEPLOY (live staging deploy on the polished UI).
```

---

## Decisions Needed From You

The agent is blocked on these. Answer inline (edit the **Answer** cell), then the agent
proceeds.

**DN-PF-SIGNOFF — the M7 premium visual sign-off (the one gate before `premium-frontend` close-out).**
The build is **released to local/dev** and every *agent-verifiable* AC passes (suite green, no-JS SEO,
`/` landing, widget firewall, a11y floor, design-system consistency). What remains is the **human-judgment**
half of AC-1/AC-3/AC-4/AC-5 — does the **landing page, app page, and discover/browse read as "a polished
product, not a prototype"** across the three breakpoints (~360 / ~600 / ~900px) and the anon/auth nav
states? An agent cannot certify this (it is the PS-3 pattern). **To give it:** run the local server, walk
the checklist in [TEST_PLAN.md](features/premium-frontend/TEST_PLAN.md) §4, and reply **sign off** (or list
what needs another pass — that would route back to the Engineer with a `DESIGN.md` update, not a quiet
re-style). On sign-off the agent **closes `premium-frontend` out** and **reopens [DN-PS-DEPLOY](#decisions-needed-from-you)**
(the live staging deploy, now on the polished UI).

*(DN-PF-DESIGN **RESOLVED** 2026-06-28 — user chose **Option A / no build step** "for now": a hand-authored,
token-driven CSS design system, **no Tailwind/Node/build step**, recorded as global ADR **[D-13](DECISIONS.md)**.
**Option B (Tailwind via the standalone CLI) is kept as an outcome-gated reversible fallback** — revisit
trigger = the **M7 premium sign-off** / future authoring velocity; the token system maps onto a Tailwind
config so the swap is clean. PF-DESIGN-1…7 RATIFIED.)*

*(DN-PF-BRIEF **RESOLVED** 2026-06-28 — user took all three recommended defaults: **Q1** wedge surfaces
first (landing + app page + discover/browse), **Q2** light progressive enhancement only, **Q3** a real
platform landing at `/`. PF-2 / the Tailwind build-step is now resolved-as-proposed inside DN-PF-DESIGN.)*

**DN-PS-DEPLOY — PARKED (reopenable), not blocking.** The live Render deploy of `platform-staging`
is deferred by the user's 2026-06-28 sequencing decision (build `premium-frontend` first, then
deploy staging on the polished UI). The build is verified and every deploy artifact
([render.yaml](render.yaml), [docs/deploy/](docs/deploy/)) is written + secret-clean + runs
unchanged when staging is reactivated. When you're ready to deploy: run
[docs/deploy/deploy-runbook.md](docs/deploy/deploy-runbook.md) end-to-end (Render account + email
provider with a **verified sending domain** + live secrets), paste the live `https://…` URL back
here, and the agent runs the **AC1.1 / AC1.2 / AC1.3 / AC5.2** checks; the **AC6.1 / AC6.2**
walkthrough ([staging-walkthrough.md](docs/deploy/staging-walkthrough.md)) is yours (PS-3).

*(Recently resolved: **DN-PS-DEPLOY parked** 2026-06-28 (deploy sequenced after premium-frontend);
**DN-PS-DESIGN** 2026-06-27 → [D-12](DECISIONS.md) (Render / Resend / Consolidate); **DN-PS-BRIEF**
approved 2026-06-27 (brief + PS-1/2/3); **DN-STRAT-1** 2026-06-27 → [D-11](DECISIONS.md).)*

---

## Decisions Made (recently)

A short, human-readable digest. Full rationale lives in [DECISIONS.md](DECISIONS.md)
(global) or `features/<slug>/DECISIONS.md` (local). Only the **active** feature is kept
in full; closed features collapse to a single pointer line.

- **`premium-frontend` RELEASED to local/dev (Release Engineer, 2026-06-28, 980 tests)** — independently re-verified the handed-off build against the live local PG and wrote [RELEASE_NOTES.md](features/premium-frontend/RELEASE_NOTES.md). **980 tests green** (M4: 975+5 landing tests); `makemigrations --check` = **no drift** (no schema — presentation-only); `manage.py check` clean; `collectstatic` (DEBUG=false manifest) produces hashed+gz `app.<hash>.css` + `htmx.min.<hash>.js` → the WhiteNoise serving model is intact (**risk R1 mitigated** — the no-build design system serves cleanly); **widget firewall held** (G2/M5=0 — `apps/widget` structurally untouched, no `app.css`/`base.html` reference); `/` → 200 / 0 DB queries / 405-on-POST (AC-3/M6 — closes PF-CARRY-1/PS-OQ-1); vendored HTMX present, no CDN. **Found + fixed 5 trivial ruff lint defects the build handoff missed** (a dead `observability` import in `test_landing.py`, three trailing-whitespace lines, an unsorted import block in `core/views.py`) — pure hygiene, no behaviour change, restoring the standing ruff-clean gate (logged transparently in RELEASE_NOTES; the two files are uncommitted working-tree changes pending the release commit). **Rollback rehearsed (hard gate):** **no migration → nothing irreversible**; one action = `git revert 6a01436 b14b238 8be0a51` (the two feature code commits + the user's `8be0a51` CSS-consistency commit); `git revert --no-commit` ran this session with `manage.py check` **clean mid-revert** (no dangling refs — `name="home"` has no external `reverse()` dependents) and the **only** conflict in CONTROL.md (prose, reconciled by hand); aborted clean, HEAD intact. **Outstanding = the M7 premium visual sign-off (`DN-PF-SIGNOFF`)** — the human-judgment half of AC-1/AC-3/AC-4/AC-5, the user's to give (PS-3 pattern); an agent cannot certify "premium." Authed-surface restyle + the HTMX discovery fragment deferred by design (Q1 wedge-first / PF-DESIGN-6). Advanced [INDEX.md](features/INDEX.md) → 5-release / released-local-dev. **Note:** the user committed an in-scope `app.css` consistency pass (`8be0a51`, fixing a broken `var(--space-1.5)` + badge-contrast/token alignment) and a new issue `UX-003` (`c77fd6f`) concurrently this session — both folded into the verified build. **No persona hand-off yet** (awaiting sign-off, then close-out + reopen [DN-PS-DEPLOY](#decisions-needed-from-you)).
- **`premium-frontend` TASKS.md WRITTEN → Stage 4 (Planner / Tech Lead, 2026-06-28)** — decomposed the APPROVED [DESIGN.md](features/premium-frontend/DESIGN.md) into [TASKS.md](features/premium-frontend/TASKS.md): **8 ordered tasks T-01…T-08, all S/M** (no `L`), in the DESIGN §13 smallest-useful-first / risk-aware order. The `app.css` **design system was split in two** — **T-01** (tokens + reset/base + layout primitives) → **T-02** (components/utilities/3 breakpoints) — so each is a single additive session that keeps the suite green. Then **T-03** restyle the shared shell (additive `{% block body_class %}` + skip-link, blocks verbatim), **T-04** the new **static `/` landing** (`core.views.landing` + `config/urls` `path("",…)` before accounts + `core/landing.html`, no DB → closes PF-CARRY-1/AC-3/M6), **T-05** restyle `app_page.html` (load-bearing wedge; slots/order/SEO strings + no-JS controls preserved), **T-06** restyle `discovery/catalogue.html` (app-grid + 5 states + empty-state), **T-07** **vendored** HTMX (`core/vendor/htmx.min.js`, no CDN, `defer`) + `hx-boost`, fully degradable, no new endpoint, and **T-08** [TEST_PLAN.md](features/premium-frontend/TEST_PLAN.md) (every AC-1…7 mapped) + the M2 first-paint baseline + the M7 premium sign-off package. Defined **six standing guardrails** applied to every task (G1 suite ≥975 + no drift; **G2 widget firewall M5=0 as an explicit non-change**; G3 one-source-of-truth; G4 no-JS path is the source of truth; G5 no view/url/schema change on restyle; G6 keep class names, `.button`↔`.btn` aliased), plus the full AC-1…7 + PF-DESIGN-1…7 → task coverage maps and ordered verification gates. **No re-design** (no design gap to escalate). Advanced [INDEX.md](features/INDEX.md) → 4-build. **No code touched.** Handed to the **Senior Engineer** to build T-01 first + write [TEST_PLAN.md](features/premium-frontend/TEST_PLAN.md).
- **`premium-frontend` DESIGN APPROVED → Stage 3 (DN-PF-DESIGN, Software Architect, 2026-06-28 → global [D-13](DECISIONS.md))** — authored [DESIGN.md](features/premium-frontend/DESIGN.md) and got it APPROVED. **Pivotal PF-2 call resolved → Option A, NO build step** (user, "for now"): the premium gap is **design depth, not tooling**, so deepen the hand-authored [`core/app.css`](apps/core/static/core/app.css) into a full token-driven design system (palette+states+elevation, a modular type + spacing scale, motion, a real component set, 3 breakpoints) in the same one WhiteNoise-hashable file; **reject Tailwind's Node/CLI toolchain**. Recorded as global ADR **D-13** (**affirms + extends [D-12](DECISIONS.md)**'s build-free posture; keeps [D-4](DECISIONS.md) single-language + the just-stabilised deploy intact; eliminates brief risk R1). **Option B (Tailwind via the standalone CLI, fully specified DESIGN §11) is kept as an outcome-gated reversible fallback** — revisit trigger = the **M7 premium sign-off** / future authoring velocity; the `:root` token system maps onto a Tailwind config so the swap is clean. Rest of the design (PF-DESIGN-2…7): restyled shared shell (additive `{% block body_class %}` + skip-link), a new **static `/` landing** (`core.views.landing`, no DB read → closes PF-CARRY-1/AC-3), an app-page + discover **presentation-only** restyle (slots/order/SEO unchanged, closed-feature inclusion tags untouched), and **vendored** HTMX + `hx-boost` (light PE per Q2, fully degradable, discovery-fragment deferred). Firewall (AC-6/M5=0), no-JS, SEO, a11y, no-schema preserved by construction; every AC-1…7 mapped; per-component failure modes documented. Promoted PF-DESIGN-1…7 → RATIFIED, flipped DESIGN.md → APPROVED, resolved DN-PF-DESIGN, advanced [INDEX.md](features/INDEX.md) → 3-plan. **No code touched.** Handed to the **Planner / Tech Lead** to write [TASKS.md](features/premium-frontend/TASKS.md).
- **`platform-staging` RELEASED local/dev + closed-out → `premium-frontend` activated (Release Engineer → Coordinator, 2026-06-28)** — wrote the platform-staging [RELEASE_NOTES.md](features/platform-staging/RELEASE_NOTES.md) (the full T-01…T-11 serving stack + shared shell, honestly framed: **build complete + verified local/dev, live deploy deliberately deferred**) and **closed the feature out** in [INDEX.md](features/INDEX.md). Per the user's 2026-06-28 sequencing, the live Render deploy is **deferred, not cancelled** — every artifact ([render.yaml](render.yaml), [docs/deploy/](docs/deploy/)) is written, secret-clean, and runs unchanged when staging is reactivated; **DN-PS-DEPLOY → parked/reopenable**. Then as **Coordinator** scaffolded [features/premium-frontend/](features/premium-frontend/) (7 artifacts; the brief carries the upstream pointer; **PS-OQ-1** carried in as **PF-CARRY-1** — build a real landing surface there), added the INDEX row, and set it **active at `Stage: 1-define`, Persona Product Analyst**. Held bets unchanged (network / `weekly-digest` / `editorial-curation-tools` / D-9, density-gated). No code touched.
- **Frontend direction resolved → premium server-rendered templates; next feature committed + queued (user decision, 2026-06-28)** — the [D-11](DECISIONS.md) frontend evidence-gate resolves **inside** the [D-4](DECISIONS.md) envelope: build a **premium frontend with HTMX + Tailwind + Django templates** (polish, interactivity, premium feel), **not** a dedicated SPA — keeping the developer's app page SEO-friendly + fast-first-paint, which is load-bearing because **that page is the developer's marketing landing page** (the bring-your-own-audience thesis). Evidence: the current local UI is barebone (one **199-line** [`app.css`](apps/core/static/core/app.css), system fonts, no design system) — a *design* gap, not an architecture one, so an SPA would relocate the cost without delivering the premium feel. **Sequencing = "deploy staging first" (user):** finish `platform-staging` (deploy + walkthrough) on the current UI, *then* activate the new feature. Committed + **queued** as suggested slug **`premium-frontend`**; the **Coordinator** (§4) scaffolds it once `platform-staging` closes out. Tailwind's build step is a **deliberate revision of [D-12](DECISIONS.md)'s build-free stylesheet posture** — to be designed (and likely recorded as a new global ADR) at the new feature's Stage 2. **No Stage change; `platform-staging` stays active (Release Engineer).**
- **`platform-staging` BUILD COMPLETE → Stage 5 (Senior Engineer, 2026-06-28)** — built all **T-01…T-11** from the APPROVED [TASKS.md](features/platform-staging/TASKS.md)/[DESIGN.md](features/platform-staging/DESIGN.md) (global [D-12](DECISIONS.md)), risk front-loaded T-01 first. Serving stack (gunicorn + WhiteNoise + `dj-database-url` + `STATIC_ROOT` + `CSRF_TRUSTED_ORIGINS`), non-`DEBUG` media route ([`serve_media`](apps/core/views.py)), the [`render.yaml`](render.yaml) Blueprint, SMTP env wiring + [`email-provider-setup.md`](docs/deploy/email-provider-setup.md), the shared responsive shell [`core/base.html`](apps/core/templates/core/base.html) + `app.css` with the **6 app bases consolidated** (messages render once in the shell; **widget byte-unchanged → AC3.3 firewall held**), the DB-only [`/health/live`](apps/core/views.py) split, the `REDIS_URL` cache (LocMem fallback), env-gated Sentry, the [`deploy-runbook.md`](docs/deploy/deploy-runbook.md) + AC2.2 no-secret pass, and [`TEST_PLAN.md`](features/platform-staging/TEST_PLAN.md) (every AC1.1–AC6.2 mapped) + [`staging-walkthrough.md`](docs/deploy/staging-walkthrough.md). **975 tests green** (+13), `ruff`/`check` (both DEBUG states) clean, **no schema/migration drift**. Five faithful impl notes (PS-IMPL-1…5) logged in the feature [DECISIONS.md](features/platform-staging/DECISIONS.md) (manifest static gated on `not DEBUG`; `serve_media` reads `MEDIA_ROOT` at request time; settings now expose `EMAIL_*`; cache/Sentry as testable functions; the page-slot test excludes shell chrome). One finding logged not fixed: **PS-OQ-1** (bare `/` 404 — out of TASKS/DESIGN scope). Advanced [INDEX.md](features/INDEX.md) → 5-release. Handed to the **Release Engineer**.
- **`platform-staging` TASKS.md written → Stage 4 (Planner / Tech Lead, 2026-06-27)** — decomposed the APPROVED [DESIGN.md](features/platform-staging/DESIGN.md) (global [D-12](DECISIONS.md)) into [TASKS.md](features/platform-staging/TASKS.md): **11 ordered tasks T-01…T-11, all S/M** (no `L`). Risk front-loaded at **T-01** (production WSGI + WhiteNoise static + `dj-database-url` — the real C4 serving gap), per §14 increment-1. The ~30-template work sequenced **T-05** (shared `core/base.html` + `app.css`) **→ T-06** (consolidate the 6 app `base.html`) with a **render-every-surface check** and the **AC3.3 widget firewall held** as an explicit non-change. Also: T-02 non-`DEBUG` media route, T-03 `render.yaml`, T-04 SMTP wiring + `email-provider-setup.md`, T-07 the DB-only `/health/live` split, T-08 the `REDIS_URL` cache, T-09 env-gated Sentry, T-10 deploy runbook + AC2.2 no-secret pass, T-11 `TEST_PLAN.md` + `staging-walkthrough.md`. Full **AC1.1–AC6.2 + §3.1-component + PS-DESIGN-1…8** coverage map; each task has a concrete DoD/files/deps. One faithful decomposition note (not re-design): settings.py doesn't expose `EMAIL_HOST/…` today → T-04 wires them from env. Advanced [INDEX.md](features/INDEX.md) row → 4-build. Handed to the **Senior Engineer** (build T-01 first + write [TEST_PLAN.md](features/platform-staging/TEST_PLAN.md)).
- **`platform-staging` DESIGN APPROVED → Stage 3 (DN-PS-DESIGN, Software Architect, 2026-06-27 → global [D-12](DECISIONS.md))** — user confirmed the three gate calls inline: **(1) host = Render**, **(2) email = Resend**, **(3) frontend = Consolidate**; the standard items (PS-DESIGN-2/3/5/6/8 — gunicorn/WhiteNoise/`dj-database-url`, media on a disk, the Redis cache, the liveness split + Sentry, the committed deploy artifacts) proceeded on that approval. Promoted **PS-DESIGN-1…8 → RATIFIED**, set [DESIGN.md](features/platform-staging/DESIGN.md) → APPROVED, and recorded the eight as a single global ADR **[D-12](DECISIONS.md)** (the deployment/serving stack + the shared frontend shell — Render Blueprint + managed Postgres + disk + free Redis; gunicorn + WhiteNoise + `dj-database-url`; media on a disk with R2/`django-storages` as the growth path; Resend over Django SMTP = pure config + an `email-provider-setup.md`; a Redis-backed cache from `REDIS_URL` with LocMem fallback so the fail-open auth limiter holds across workers; stdout logs + env-gated Sentry + a new DB-only `/health/live` split from the SMTP-touching `/health`; one shared responsive base template + stylesheet consolidating the 6 `base.html`, widget isolated; 5 new deps; **no schema/migration**; every new behavior env-gated with a dev-safe default). Marked DN-PS-DESIGN RESOLVED in [OPEN_QUESTIONS.md](features/platform-staging/OPEN_QUESTIONS.md), recorded the ratification in the feature [DECISIONS.md](features/platform-staging/DECISIONS.md), advanced the [INDEX.md](features/INDEX.md) row → 3-plan. Handed to the **Planner / Tech Lead** to write [TASKS.md](features/platform-staging/TASKS.md).
- **`platform-staging` brief APPROVED → Stage 2 (DN-PS-BRIEF, Product Analyst, 2026-06-27)** — user approved [FEATURE_BRIEF.md](features/platform-staging/FEATURE_BRIEF.md) and answered the three scoping calls: **PS-1** = start on **free tier**, **$20–100/mo** budget when deploying, staging is **prod-bound** (promote→prod later → deploy for durability, not throwaway); **PS-2** = **no transactional-email provider exists yet** → the Architect selects one within budget and the deploy ships an **`.md` setup guide** (account + credential wiring), magic-link/M5 still gate on a real inbox; **PS-3** = the **user performs/signs off** the human-judgment walkthrough, the agent supplies a **suggested walkthrough `.md`** (Stage 4). Added the two `.md` deliverables to In scope, marked assumptions A1–A3 Resolved, resolved PS-1/2/3 in [OPEN_QUESTIONS.md](features/platform-staging/OPEN_QUESTIONS.md). Provider/domain stay a Stage-2 architecture choice ([D-11](DECISIONS.md)/[D-2](DECISIONS.md)), now bounded by PS-1. Handed to the **Software Architect**.
- **`platform-staging` scaffolded → Stage 1 (Coordinator, 2026-06-27)** — turned the ratified [D-11](DECISIONS.md) staging-validation bet into a feature folder: created [features/platform-staging/](features/platform-staging/) with the 7 standard artifacts seeded (brief carries the D-11/[STRATEGY.md](STRATEGY.md) upstream pointer + one-line intent; rest are stage-pending), added the [INDEX.md](features/INDEX.md) row (Phase 4 Validation), set `Stage: 1-define` + Persona **Product Analyst**. Held bets (network / `weekly-digest` / `editorial-curation-tools` / D-9) stay backlog, density-gated. Handed to the **Product Analyst** to author the brief.
- **Next bet RATIFIED → staging-validation-before-live (Strategist, DN-STRAT-1, 2026-06-27 → global [D-11](DECISIONS.md))** — with the developer wedge code-complete but **all 13 features local/dev only** (nothing has touched a real developer/user; H1/H2/H3 + the §8 one-line test unvalidated) and every standing-fork option (D-9 / network / editorial) **density-gated → premature**, the user ratified: the next bet is **not another feature and not yet a public launch** — **stand up staging, deploy the wedge, and walk it end-to-end as user/developer/admin on web + mobile** (also exercising the deferred deploy work: hosting, domain, the stubbed D-4 email provider, monitoring). Live recruitment (§5.4 step 3) follows once staging validates. **Frontend:** keep server-rendered Django templates ([D-4](DECISIONS.md)), polish + make responsive; a dedicated-SPA split is **deferred + evidence-gated** (only if the walkthrough proves a surface needs it, as an explicit D-4-revisit). Network/D-9/editorial stay density-gated, unchanged. Living picture in [STRATEGY.md](STRATEGY.md). Handed to the **Coordinator** to scope (suggested slug `platform-staging`).
- **`widget-conversion-attribution` RELEASED → closed (Release Engineer, 2026-06-27, 962 tests)** — released to local/dev and wrote [RELEASE_NOTES.md](features/widget-conversion-attribution/RELEASE_NOTES.md); build independently re-verified (962 green, `ruff`/`check`/no drift); migration `widget/0002` down→up clean. **Rollback rehearsed (hard gate):** `git revert 5df8daa` removes the new `source`/`rollup`/`kinds` modules + the migration + the two fail-soft hooks + the dashboard funnel slot atomically, `manage.py check` clean with **no dangling `subscriptions`/`accounts`/`dashboard → widget` import** (**DU-REL-1** proven). The developer-wedge funnel (impression → click → conversion) is now **complete**. Stage-6 retrospective + the live-metrics window (M1–M6) deferred until a prod target exists; ePrivacy consent revisit pre-EU-prod. Handed to the **Coordinator**.
- **`widget-conversion-attribution` DESIGN APPROVED → Stage 3 (DN-WCA-DESIGN, 2026-06-27)** — ratified [DESIGN.md](features/widget-conversion-attribution/DESIGN.md) and promoted **WCA-DESIGN-1…8 → RATIFIED**: a first-party signed source-only cookie `widget_src` (no person field) carries the source through the anonymous click → app page → conversion; conversions tallied in a **separate PII-free `widget_conversion_count`** rollup via **fail-soft hooks** at [`subscriptions.views.follow`](apps/subscriptions/views.py) + [`accounts.views.register`](apps/accounts/views.py) — never breaks a follow/registration/reach count (AC6), adds nothing to `record_subscribe` (AC5). **AC6 firewall stays structural** (`apps/widget` imports no `signals`; AST test extends to the new `source` module); **AC4 no-PII by construction**. ePrivacy resolved **as recommended — no consent banner** under the no-PII WCA-3 posture (one-call consent gate is a documented contingency, revisit with counsel pre-EU-prod). Marked **OQ-WCA-2/3/4 → RESOLVED**. Reuses D-3/D-4/D-6/D-7/D-8/D-9/D-10 — **no new global ADR**. Rollback = single `git revert` of the build commit (DU-REL-1). Handed to the **Planner / Tech Lead**.
- **`widget-conversion-attribution` brief APPROVED → Stage 2 (DN-WCA-BRIEF, 2026-06-26)** — approved the brief + three scoping calls: **WCA-1** conversion set = new **follow** + new **account** (distinct counts); **WCA-2** = **last-touch** within a bounded **~30-day** (config) window; **WCA-3** = **aggregate-only, source-keyed** (no per-person profile, no PII, no consent banner; aggregate-only-infeasible returns as a decision). Token-carry mechanism left **OPEN** as OQ-WCA-2/3/4 for the Architect. Reuses D-3/D-4/D-6/D-7/D-8 — **no new global ADR**.
- **Next feature = `widget-conversion-attribution` (Coordinator, 2026-06-26)** — with the **developer wedge ([D-10](DECISIONS.md)) complete**, user picked **conversion attribution (M3)** (the deferred OQ-EUW-5) over D-9 monetization / turning on the network. Scaffolded the 7-artifact folder; deferred (not dropped) D-9 monetization; kept `weekly-digest` + `editorial-curation-tools` backlog. Handed to the **Product Analyst**.
- **`embeddable-update-widget` RELEASED → closed (Release Engineer, 2026-06-26, 893 tests)** — released to local/dev; rollback rehearsed (single `git revert b7db60f`, **DU-REL-1** proven). Stage-6 retrospective + M3 conversion (OQ-EUW-5 → this feature) deferred. **The developer wedge (D-10) is now complete** (`app-pages` + `developer-updates` + the widget all shipped). Detail: [embeddable-update-widget/DECISIONS.md](features/embeddable-update-widget/DECISIONS.md).
- **Developer-wedge pivot — global D-10 + D-9 (Coordinator, 2026-06-26)** — **D-10:** MVP cold-start re-sequenced to a single-player **developer hub** (app page + changelog + embeddable widget) that drags a developer's existing audience in; the discovery network/digest is **held until per-niche density**. **D-9:** monetization = paid, labeled, time-boxed **promotion placements** sold for reach, **firewalled from the ranking** (a `Surface` outside `CURATED_SURFACES`); tools + curation stay free. **Paused `editorial-curation-tools` → backlog.** Full text in [DECISIONS.md](DECISIONS.md).

**Closed-feature pointers** (full lifecycle in each folder's `DECISIONS.md`/`RELEASE_NOTES.md`):

- **`developer-updates`** (closed 2026-06-24, 828 tests) — new `apps/updates/` owning `updates_notice`, the single **AS-3 producer**: pull delivery (M5=0 structural, no fan-out), imports no `signals` (AST-enforced), durable table-derived rate limit; first to repoint a *closed* app's seam → rollback = `git revert` of the build commit (**DU-REL-1**). Detail: [developer-updates/DECISIONS.md](features/developer-updates/DECISIONS.md).
- **`developer-dashboard`** (closed 2026-06-24, 740 tests) — model-less `apps/dashboard/`, a read-only view of an app's reception (D-7 reach/engagement + D-8 curated ratings) for its owner; two additive neutral `signals.selectors` reads, no schema. Detail: [developer-dashboard/DECISIONS.md](features/developer-dashboard/DECISIONS.md).
- **`open-search-browse`** (closed 2026-06-23, 676 tests) — `apps/discovery/`, the paginated DB-pushed `catalog.search_catalogue` open surface where money never buys position. Detail: [open-search-browse/DECISIONS.md](features/open-search-browse/DECISIONS.md).
- **`interest-profile`** (closed 2026-06-23, 616 tests) — `apps/interests/`, the user side of the Ring-0 match via the `declared_tag_ids` matcher contract. Detail: [interest-profile/DECISIONS.md](features/interest-profile/DECISIONS.md).
- **`app-subscriptions`** (closed 2026-06-21, 552 tests) — `apps/subscriptions/`, atomic follow + one `subscribe` emit, the empty-until-producer notice seam **AS-3**. Detail: [app-subscriptions/DECISIONS.md](features/app-subscriptions/DECISIONS.md).
- **`ratings-reviews`** (closed 2026-06-21, 486 tests) — `apps/ratings/`; established the global **[D-8](DECISIONS.md)** curated-rating gate ("curated = a `DIGEST` impression"). Detail: [ratings-reviews/DECISIONS.md](features/ratings-reviews/DECISIONS.md).
- **`app-pages`** (closed 2026-06-20, 417 tests) — `apps/pages/`, first live emitter of D-7 `app_page` impressions. Detail: [app-pages/DECISIONS.md](features/app-pages/DECISIONS.md).
- **`signal-capture`** (closed 2026-06-18, 374 tests) — `apps/signals/`; established the global **[D-7](DECISIONS.md)** event schema (append-only, structural no-score, no-PII). Detail: [signal-capture/DECISIONS.md](features/signal-capture/DECISIONS.md).
- **`submission-intake`** (closed 2026-06-18, 315 tests) — `apps/catalog/`, the ACCEPTED-only catalogue substrate = global **[D-6](DECISIONS.md)**. Detail: [submission-intake/DECISIONS.md](features/submission-intake/DECISIONS.md).
- **`interest-taxonomy`** (closed 2026-06-17, 184 tests) — flat tags + clusters, soft-retire + read-time `resolve_tag` = global **[D-5](DECISIONS.md)**. Detail: [interest-taxonomy/DECISIONS.md](features/interest-taxonomy/DECISIONS.md).
- **`identity-accounts`** (closed 2026-06-17, 108 tests) — the foundation (the stack **D-4**, roles **D-3**). Detail: [DECISIONS.md](DECISIONS.md).

> **Foundational decisions** (the stack, niche, build order — D1–D3, A1–A3, R1, global
> D-1/D-2/D-4) live in [DECISIONS.md](DECISIONS.md). Highlights: niche = **"vibecoded
> webapps"** (D-1); stack = **Python / Django + PostgreSQL** (D-4).

---

## Activity Log

Newest first. One line per session. **Keep the most recent ~10 rows here**; older rows
live in [process/activity-archive.md](process/activity-archive.md). The per-feature
folders remain the full record either way.

| Date       | Stage           | Summary                                                                 |
|------------|-----------------|-------------------------------------------------------------------------|
| 2026-06-28 | `5-release` (released local/dev) | **Release Engineer** — **independently re-verified the handed-off `premium-frontend` build** (never trust a handoff) and **released it to local/dev**, writing [RELEASE_NOTES.md](features/premium-frontend/RELEASE_NOTES.md). **980 tests green** (M4: 975+5); `makemigrations --check` no drift (presentation-only, no schema); `manage.py check` clean; **`collectstatic` (DEBUG=false manifest) hashes+gzips `app.css` + `htmx.min.js`** → WhiteNoise serving model intact (**R1 mitigated**); **widget firewall held** (G2/M5=0, `apps/widget` structurally untouched); `/` → 200/0-queries/405-POST (AC-3/M6); vendored HTMX, no CDN. **Found + `ruff --fix`ed 5 trivial lint defects the handoff missed** (dead import + 3 whitespace + import-sort; no behaviour change) — restoring the ruff-clean gate, logged honestly in RELEASE_NOTES (two files uncommitted, pending the release commit). **Rollback rehearsed (hard gate):** **no migration → nothing irreversible**; `git revert --no-commit 6a01436 b14b238 8be0a51` ran with `manage.py check` **clean mid-revert** (no dangling refs; `name="home"` has no external `reverse()` dependents), only CONTROL.md (prose) conflicting; aborted clean, HEAD intact. **Outstanding = DN-PF-SIGNOFF** (the M7 premium visual sign-off — the user's, human-judgment, PS-3); on sign-off → close out + reopen [DN-PS-DEPLOY](#decisions-needed-from-you). The user committed an in-scope `app.css` consistency pass (`8be0a51`) + issue `UX-003` (`c77fd6f`) concurrently — folded into the verified build. Advanced [INDEX.md](features/INDEX.md) → 5-release/released-local-dev. **Awaiting sign-off — no persona hand-off yet.** |
| 2026-06-28 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [features/premium-frontend/DESIGN.md](features/premium-frontend/DESIGN.md) into [TASKS.md](features/premium-frontend/TASKS.md): **8 ordered tasks T-01…T-08, all S/M** (no `L`), in the DESIGN §13 smallest-useful-first / risk-aware order. The `app.css` **design system split T-01** (tokens + reset/base + layout primitives) **→ T-02** (components/utilities/3 breakpoints) so each is one additive, suite-green session; then **T-03** restyle the shared [`core/base.html`](apps/core/templates/core/base.html) (additive `{% block body_class %}` + skip-link, blocks verbatim), **T-04** the new **static `/` landing** ([`core.views.landing`](apps/core/views.py) + [`config/urls.py`](config/urls.py) `path("",…)` before the accounts include + `core/landing.html`, **no DB read** → closes PF-CARRY-1/AC-3/M6), **T-05** restyle [`app_page.html`](apps/pages/templates/pages/app_page.html) (load-bearing wedge; same slots/order/SEO strings, no-JS controls preserved), **T-06** restyle [`catalogue.html`](apps/discovery/templates/discovery/catalogue.html) (`.app-grid` + 5 states + `.empty-state`), **T-07** **vendored** HTMX (`core/vendor/htmx.min.js`, no CDN, `defer`) + `hx-boost`, fully degradable, **no new endpoint** (discovery-search fragment stays a §12 deferred point), and **T-08** [TEST_PLAN.md](features/premium-frontend/TEST_PLAN.md) (every AC-1…7 mapped) + the M2 first-paint baseline + the M7 sign-off package. Defined **six standing guardrails** for every task (G1 suite ≥975 + no drift; **G2 widget firewall M5=0 = explicit non-change**; G3 one-source-of-truth literals only in `:root`; G4 no-JS path is the source of truth; G5 no view/selector/url/schema change on restyle; G6 keep class names, `.button`↔`.btn` aliased); wrote the full **AC-1…7 + PF-DESIGN-1…7 → task** coverage maps + ordered verification gates. Grounded every task in current code (read `app.css`/`core/base.html`/`core/views.py`/`config/urls.py`/the brief). **No re-design** (no design gap to escalate). Advanced [INDEX.md](features/INDEX.md) → 4-build. **Hand-off only (one persona/session); no code.** Handed to the **Senior Engineer** to build **T-01 first** + write [TEST_PLAN.md](features/premium-frontend/TEST_PLAN.md). |
| 2026-06-28 | `2-design`→`3-plan` | **Software Architect** (DN-PF-DESIGN resolved) — surveyed the codebase + authored [features/premium-frontend/DESIGN.md](features/premium-frontend/DESIGN.md) via the 14-step protocol (**PF-DESIGN-1…7**), and got it **APPROVED**. **Pivotal PF-2 call → Option A, NO build step** (user, "for now"): deepen the hand-authored [`core/app.css`](apps/core/static/core/app.css) into a full token-driven **design system** (palette+states+elevation, modular type + spacing scales, motion, a real component set, 3 breakpoints) in the **same one** WhiteNoise-hashable served file; **reject Tailwind's Node/CLI toolchain** → global ADR **[D-13](DECISIONS.md)** that **affirms + extends [D-12](DECISIONS.md)**'s build-free posture (eliminates brief risk **R1**, keeps [D-4](DECISIONS.md) single-language + the just-stabilised deploy intact). **Option B (Tailwind via the standalone CLI, fully specified §11) kept as an outcome-gated reversible fallback** — revisit trigger = the **M7 premium sign-off** / future authoring velocity; the `:root` token system maps onto a Tailwind config (clean swap). Plus a restyled shared shell (**additive** `{% block body_class %}`, existing blocks verbatim, skip-link), a new **static `/` landing** ([`core.views.landing`](apps/core/views.py) + `core/landing.html`, routed `path("",…)` before the accounts include, **no DB read** → closes PF-CARRY-1/AC-3), an [`app_page.html`](apps/pages/templates/pages/app_page.html) + [`catalogue.html`](apps/discovery/templates/discovery/catalogue.html) **presentation-only** restyle (same slots/order/states/SEO strings; closed-feature Follow/Share/Reviews tags untouched), and **vendored** HTMX (`core/vendor/htmx.min.js`, no CDN) loaded `defer` for `hx-boost` only (light PE per Q2, fully degradable; the discovery-search **fragment** deferred). Firewall (**AC-6/M5=0**), no-JS (AC-1), SEO (AC-2), a11y floor (AC-7), no-schema (C5) preserved **by construction**; every **AC-1…7** mapped to a concrete verification; per-component failure modes documented. Promoted PF-DESIGN-1…7 → RATIFIED + PF-2 RESOLVED in the feature [DECISIONS.md](features/premium-frontend/DECISIONS.md); recorded global **D-13**; flipped DESIGN.md → APPROVED; resolved DN-PF-DESIGN in [OPEN_QUESTIONS.md](features/premium-frontend/OPEN_QUESTIONS.md); advanced [INDEX.md](features/INDEX.md) → 3-plan. **Hand-off only; no code.** Handed to the **Planner / Tech Lead** to write [TASKS.md](features/premium-frontend/TASKS.md). |
| 2026-06-28 | `1-define`→`2-design` | **Product Analyst** (DN-PF-BRIEF approved) — authored [features/premium-frontend/FEATURE_BRIEF.md](features/premium-frontend/FEATURE_BRIEF.md) from the upstream 2026-06-28 frontend decision + vision ([D-10](DECISIONS.md): the app page is the developer's marketing landing page). Problem/goal; **6 user stories US-1…6**; **AC-1…7** each Given/When/Then and tagged *agent-verifiable* vs *human-judgment*; **M1–M7**; in/out scope; constraints **C1–C7** + assumptions A1–A3; top-5 risks; vision alignment. Made **"premium feel" checkable** by anchoring it to a concrete **design system** (tokens + type/spacing scales + styled components) **plus a PS-3-style user sign-off**. Grounded in code: **bare `/` 404s** ([config/urls.py](config/urls.py) mounts accounts at `""` with no `/` view) → **PF-CARRY-1** folded in as **US-3/AC-3** (real landing); the **7 `base.html` already consolidated** to [`core/base.html`](apps/core/templates/core/base.html) (build on the shell, C7); **widget firewall preserved** (AC-6 / M5=0, C4). Kept architecture **out** per persona scope. Raised gate **DN-PF-BRIEF**; **user answered with all 3 recommended defaults** (Q1 **wedge surfaces first** · Q2 **light progressive enhancement only** · Q3 **real platform landing**). Folded the answers in → brief **APPROVED**; recorded **PF-D1 RATIFIED** + **PF-2 DEFERRED** in the feature [DECISIONS.md](features/premium-frontend/DECISIONS.md); resolved PF-CARRY-1 + PF-1 Q1/Q2/Q3 in [OPEN_QUESTIONS.md](features/premium-frontend/OPEN_QUESTIONS.md); advanced [INDEX.md](features/INDEX.md) → 2-design. **Hand-off only; no code.** Handed to the **Software Architect** to author [DESIGN.md](features/premium-frontend/DESIGN.md) (incl. the PF-2 Tailwind build-step ADR). |
| 2026-06-28 | `5-release`→closed-out → `1-define` | **Release Engineer → Coordinator** (user-directed consolidation) — wrote the [platform-staging/RELEASE_NOTES.md](features/platform-staging/RELEASE_NOTES.md) for the verified T-01…T-11 build (gunicorn/WhiteNoise/`dj-database-url` serving stack, non-DEBUG media route, [render.yaml](render.yaml) Blueprint, Resend SMTP wiring + setup `.md`, the shared responsive shell consolidating the 6 `base.html` with the **widget firewall held**, the DB-only `/health/live` split, the `REDIS_URL` cache, env-gated Sentry, the runbook + AC2.2 pass, TEST_PLAN + walkthrough) — **honestly framed: build complete + verified local/dev (975 tests / ruff / check both DEBUG / no drift / secret-clean), live deploy DELIBERATELY DEFERRED** per the user's 2026-06-28 decision to land `premium-frontend` first so staging debuts polished. **Closed out platform-staging** in [INDEX.md](features/INDEX.md) (**DN-PS-DEPLOY → parked/reopenable**; artifacts ready, runs unchanged when reactivated). Then as **Coordinator** scaffolded [features/premium-frontend/](features/premium-frontend/) (7 artifacts; the brief carries the upstream pointer = the 2026-06-28 frontend decision + [D-11](DECISIONS.md)/[D-4](DECISIONS.md)/[D-10](DECISIONS.md); **PS-OQ-1** carried in as **PF-CARRY-1**), added its INDEX row, set **Active feature = `premium-frontend`, `Stage: 1-define`, Persona Product Analyst**. Held bets unchanged. **No code touched.** Handed to the **Product Analyst**. |
| 2026-06-28 | `5-release` (frontend direction) | **User decision (Strategist-lens), no code** — resolved the [D-11](DECISIONS.md) frontend evidence-gate **within** the [D-4](DECISIONS.md) envelope: build a **premium server-rendered frontend = HTMX + Tailwind + Django templates** (polish, interactivity, premium feel), **NOT** an SPA. Driver: the local UI is barebone (one **199-line** [`app.css`](apps/core/static/core/app.css), system fonts, no design system) and the developer's **app page is their marketing landing page**, so premium feel is load-bearing for bring-your-own-audience — the gap is *design*, not architecture (an SPA would relocate the cost + regress SEO/first-paint). **Sequencing (user) = deploy staging first:** finish `platform-staging` (the [DN-PS-DEPLOY](#decisions-needed-from-you) live deploy + walkthrough) on the current UI, then activate. Committed + **queued** as suggested slug **`premium-frontend`**; **Coordinator** (§4) scaffolds once `platform-staging` closes. Tailwind's build step deliberately revises [D-12](DECISIONS.md)'s build-free posture → designed at the new feature's Stage 2 (likely a new global ADR). **No Stage change; `platform-staging` stays active (Release Engineer).** |
| 2026-06-28 | `5-release` | **Release Engineer** — **independently re-verified the handed-off build** (Stage-5 mindset: never trust a handoff blindly) against the live local PG, and confirmed every agent-runnable deploy artifact. **975 tests green**; `ruff` clean; `manage.py check` clean under **both** DEBUG states (`--deploy`: only the expected W009 from a throwaway key); `makemigrations --check` = **no drift**. **[render.yaml](render.yaml)** parses (web `app-discovery` + keyvalue `app-discovery-cache` + db `app-discovery-db`) with **zero secret literals** — every secret is `sync:false`/`generateValue`/`fromDatabase|fromService`; the only literal `value:` entries are non-secret constants. **AC2.2 holds** (`.env` gitignored + untracked). `collectstatic` (DEBUG=false manifest) → 155 copied / 447 post-processed incl. hashed+gz `core/app.css`; `gunicorn config.wsgi` → `WSGIHandler` loads under DEBUG=false; cleaned the generated (gitignored) `staticfiles/` so the tree is unchanged. **Surfaced [DN-PS-DEPLOY](#decisions-needed-from-you):** the live Render deploy is outward-facing user action the agent can't perform (Render account + connected repo, email provider + **verified** domain, live secrets) — user runs [deploy-runbook.md](docs/deploy/deploy-runbook.md) and returns the live URL, then the agent runs AC1/AC5 against it; AC6.1/AC6.2 walkthrough is the user's (PS-3). **PS-OQ-1 deferred to the walkthrough** per the user — no code change. **Stays at Stage 5.** No code changed. |
| 2026-06-28 | `4-build`→`5-release` | **Senior Engineer** — built **all T-01…T-11** from the APPROVED [features/platform-staging/TASKS.md](features/platform-staging/TASKS.md) + [DESIGN.md](features/platform-staging/DESIGN.md) (global [D-12](DECISIONS.md)), T-01 risk first. **Serving stack:** gunicorn + WhiteNoise (`CompressedManifest` gated on `not DEBUG` — dev-safe default, PS-IMPL-1) + `dj-database-url` (`DATABASE_URL` parsed, discrete `DB_*` fallback unchanged) + `STATIC_ROOT` + `CSRF_TRUSTED_ORIGINS`. **T-02** non-`DEBUG` media via [`apps.core.views.serve_media`](apps/core/views.py) (reads `MEDIA_ROOT` at request time, PS-IMPL-2). **T-03** [`render.yaml`](render.yaml) (web+Postgres+disk+keyvalue; release=migrate+collectstatic; no secret literal). **T-04** wired `EMAIL_HOST/PORT/HOST_USER/HOST_PASSWORD/USE_TLS` from env (PS-IMPL-3) + [`email-provider-setup.md`](docs/deploy/email-provider-setup.md). **T-05/T-06** the shared shell [`core/base.html`](apps/core/templates/core/base.html) + `app.css`; 6 app bases → thin `{% extends %}`; messages render once in the shell (removed the duplicate from `updates/base` + `interests/picker`); **widget templates byte-unchanged (AC3.3 firewall held)**. **T-07** DB-only [`/health/live`](apps/core/views.py) (reuses `_database_ok`, never opens SMTP). **T-08** `_cache_settings(REDIS_URL)` → RedisCache w/ LocMem fallback. **T-09** `_init_sentry(SENTRY_DSN)` env-gated. **T-10** [`deploy-runbook.md`](docs/deploy/deploy-runbook.md) + AC2.2 `.env.example` completeness + no-committed-secret grep. **T-11** [`TEST_PLAN.md`](features/platform-staging/TEST_PLAN.md) (every AC1.1–AC6.2 mapped) + [`staging-walkthrough.md`](docs/deploy/staging-walkthrough.md) (3 roles × web+mobile + verdict templates). **975 tests green** (+13); `ruff` clean; `check` clean under **both** DEBUG states; **no migration drift**; gunicorn boots; collectstatic produces hashed admin assets + manifest. Updated [CODEMAP.md](CODEMAP.md) (shell + core views + settings helpers) + PS-IMPL-1…5 in the feature [DECISIONS.md](features/platform-staging/DECISIONS.md); logged **PS-OQ-1** (bare `/` 404, not fixed — out of scope). Advanced [INDEX.md](features/INDEX.md) → 5-release. Handed to the **Release Engineer**. |
| 2026-06-27 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [features/platform-staging/DESIGN.md](features/platform-staging/DESIGN.md) (global [D-12](DECISIONS.md)) into [TASKS.md](features/platform-staging/TASKS.md): **11 ordered tasks T-01…T-11, all S/M** (no `L`). **Risk front-loaded at T-01** (production WSGI + WhiteNoise static pipeline + `dj-database-url` bridge — the real C4/§2 serving gap), per §14 increment-1 "serving stack reachable" first. The **~30-template consolidation sequenced T-05→T-06** (shared `core/base.html` + `app.css`, then re-point the 6 app `base.html`) with an explicit **render-every-surface check** and the **AC3.3 widget firewall held** (explicit non-change). Plus T-02 non-`DEBUG` media route, T-03 `render.yaml` Blueprint, T-04 SMTP env wiring + `email-provider-setup.md`, T-07 DB-only `/health/live` split, T-08 `REDIS_URL` cache (LocMem fallback), T-09 env-gated Sentry, T-10 deploy runbook + AC2.2 no-secret pass, T-11 `TEST_PLAN.md` + `staging-walkthrough.md` (PS-3). Each task: concrete DoD + files-touched + dependencies; **full AC1.1–AC6.2 + §3.1-component + PS-DESIGN-1…8 coverage map** + a dependency graph. Grounded every task in current code (6 app `base.html` + 19 child templates, `check_health`/`_database_ok`, the email/static/cache gaps). One faithful decomposition note (**not** re-design): settings.py doesn't expose `EMAIL_HOST/…` today → folded into T-04. Advanced [INDEX.md](features/INDEX.md) row → 4-build. **Hand-off only (one persona/session); no code.** Handed to the **Senior Engineer** to build **T-01 first** + write [TEST_PLAN.md](features/platform-staging/TEST_PLAN.md). |
| 2026-06-27 | `2-design`→`3-plan` | **Software Architect** (DN-PS-DESIGN resolved) — user confirmed the three gate calls inline (**host = Render**, **email = Resend**, **frontend = Consolidate**); standard items PS-DESIGN-2/3/5/6/8 proceeded on that approval. Promoted **PS-DESIGN-1…8 → RATIFIED**, set [features/platform-staging/DESIGN.md](features/platform-staging/DESIGN.md) → **APPROVED**, and recorded the eight as a single global ADR **[D-12](DECISIONS.md)** (deployment/serving stack + shared frontend shell: Render Blueprint + managed Postgres + disk + free Redis; **gunicorn**/**WhiteNoise**/`dj-database-url`; media on a persistent disk, R2/`django-storages` = growth path; **Resend** over Django SMTP = pure config + an `email-provider-setup.md`; Redis-backed cache from `REDIS_URL` w/ LocMem fallback → the fail-open auth limiter holds across workers; stdout logs + env-gated **Sentry** + a new **DB-only `/health/live`** split from the SMTP-touching `/health`; **one shared responsive base template + stylesheet** in `apps/core` consolidating the 6 `base.html`, widget isolated; 5 new deps; **no schema/migration**; every new behavior env-gated with a dev-safe default). Recorded the ratification in the feature [DECISIONS.md](features/platform-staging/DECISIONS.md), marked **DN-PS-DESIGN RESOLVED** in [OPEN_QUESTIONS.md](features/platform-staging/OPEN_QUESTIONS.md), advanced the [INDEX.md](features/INDEX.md) row → 3-plan. **Hand-off only (one persona/session); no code.** Handed to the **Planner / Tech Lead** to write [TASKS.md](features/platform-staging/TASKS.md). |
| 2026-06-27 | `2-design` (awaiting DN-PS-DESIGN) | **Software Architect** — surveyed the codebase + authored [features/platform-staging/DESIGN.md](features/platform-staging/DESIGN.md) (**PS-DESIGN-1…8 PROPOSED**). This feature ships **no new product capability** — its architecture is a **deployment/serving stack + the frontend shell + defect fixes**. Calls: host = **Render** Blueprint (managed Postgres + persistent disk + free Redis; free now, ~$14–25/mo durable, inside PS-1); **gunicorn** + **WhiteNoise** (serves admin static too) + **`dj-database-url`** close the **C4** static gap; media on a persistent disk (object store = growth path); email = **Resend** over Django SMTP = **pure config** ([apps/core/email.py](apps/core/email.py) already swaps by `EMAIL_BACKEND`) + a setup `.md` (PS-2); shared cache from `REDIS_URL` (LocMem fallback) so the fail-open auth limiter holds across workers; observability = existing stdout logs + env-gated **Sentry** + a **new DB-only `/health/live`** split from the SMTP-touching `/health` (a real footgun found in code). Frontend: **one shared responsive base template + stylesheet** in `apps/core` consolidating the **6 duplicated `base.html`** (zero CSS today), **widget stays isolated** (AC3.3 firewall preserved). **No schema/migration.** Every AC1.1–AC6.2 mapped; global ADR **D-12** pending. Logged **DN-PS-DESIGN** (host / email / consolidation appetite) in [OPEN_QUESTIONS.md](features/platform-staging/OPEN_QUESTIONS.md) + the INDEX row. **Raised DN-PS-DESIGN; stopped at the gate.** No code. |
| 2026-06-27 | `1-define`→`2-design` | **Product Analyst** (DN-PS-BRIEF approved) — user said *proceed* + answered PS-1/2/3 inline → promoted [features/platform-staging/FEATURE_BRIEF.md](features/platform-staging/FEATURE_BRIEF.md) to **APPROVED**. Folded the answers in: **PS-1** free-tier→**$20–100/mo**, **prod-bound** (promote→prod later); **PS-2** **no email provider yet** → Architect picks one in budget + deploy ships an `.md` setup guide (AC3.1/M5 gate on a real inbox); **PS-3** **user** performs/signs off, agent supplies a walkthrough `.md` (Stage 4). Added both `.md` deliverables to In scope, assumptions A1–A3 → Resolved, PS-1/2/3 → Resolved in [OPEN_QUESTIONS.md](features/platform-staging/OPEN_QUESTIONS.md), [INDEX.md](features/INDEX.md) row → 2-design. No architecture/schema/UI (Stage-2 job). One persona/session — handed to the **Software Architect** to author [DESIGN.md](features/platform-staging/DESIGN.md). |
| 2026-06-27 | `1-define` (awaiting DN-PS-BRIEF) | **Product Analyst** — authored [features/platform-staging/FEATURE_BRIEF.md](features/platform-staging/FEATURE_BRIEF.md) from [D-11](DECISIONS.md) + [STRATEGY.md](STRATEGY.md) + vision §5.4 step 3: the problem (wedge code-complete but **never reachable** → unvalidated thesis + unexercised deploy path), 6 user stories, acceptance criteria **AC1.1–AC6.2** (all Given/When/Then, each tagged *agent-verifiable* vs *human-judgment*), metrics **M1–M7**, explicit in/out scope, constraints **C1–C6** (grounded in code: 12-factor config, security gated on `not DEBUG`, email = console stub, **no `STATIC_ROOT`/deploy manifest exists**, PG-only) + assumptions A1–A3, top-5 risks, vision alignment. No architecture/schema/UI (Stage-2). Surfaced 3 user-business scoping calls as gate **DN-PS-BRIEF** (PS-1 budget/posture, PS-2 email provider, PS-3 UX sign-off owner); logged PS-1/2/3 in [OPEN_QUESTIONS.md](features/platform-staging/OPEN_QUESTIONS.md). **Raised DN-PS-BRIEF; stopped at the gate.** |
| 2026-06-27 | `0-coordinator`→`1-define` | **Coordinator** (§4) — scaffolded the ratified [D-11](DECISIONS.md) staging-validation bet into a feature. Created [features/platform-staging/](features/platform-staging/) with all 7 standard artifacts seeded (brief carries the D-11/[STRATEGY.md](STRATEGY.md) upstream pointer + one-line intent; the rest stage-pending), added the [INDEX.md](features/INDEX.md) row (Phase 4 Validation, started 2026-06-27). Set Active feature = `platform-staging`, `Stage: 1-define`, Persona **Product Analyst**. Held bets (network / `weekly-digest` / `editorial-curation-tools` / D-9) stay backlog (density-gated). Did **not** scope stories/AC (Product Analyst's job). No code. Handed to the **Product Analyst**. |
| 2026-06-27 | `6-post-release` (off-pipeline) | **Strategist** (§4.1, user-invoked) — post-wedge next-bet analysis + **ratification** (DN-STRAT-1 → global **[D-11](DECISIONS.md)**). Finding: 13 features built but **all local/dev only** — nothing has touched a real developer/user, so H1/H2/H3 + the §8 one-line test are unvalidated, and **every standing-fork option (D-9 / network / editorial) is density-gated → premature**. **Ratified next bet:** not another feature and not yet a public launch — **stand up staging, deploy the wedge, walk it end-to-end as user/developer/admin on web + mobile** (also exercises the deferred deploy work: hosting, domain, the stubbed D-4 email provider, monitoring); live recruitment (§5.4 step 3) follows once staging validates. **Frontend ratified:** keep server-rendered Django templates ([D-4](DECISIONS.md)), polish + make responsive; dedicated-SPA split **deferred + evidence-gated** (D-11). Recorded **D-11**, created/updated [STRATEGY.md](STRATEGY.md), resolved DN-STRAT-1. **No Stage change, no code.** Handed to the **Coordinator** to scope (suggested slug `platform-staging`). |
| 2026-06-27 | `5-release`→`6-post-release` | **Release Engineer** — **released `widget-conversion-attribution` to local/dev** and wrote [RELEASE_NOTES.md](features/widget-conversion-attribution/RELEASE_NOTES.md). Re-verified the build independently (**962 tests green**, `ruff`/`check`/no drift). **Rehearsed rollback** (hard gate): migration `widget/0002` down→up clean on the live PG cluster; `git revert --no-commit 5df8daa` removed the `source`/`rollup`/`kinds` modules + migration `0002` + the two fail-soft hooks ([`subscriptions.views.follow`](apps/subscriptions/views.py), [`accounts.views.register`](apps/accounts/views.py)) + the dashboard funnel slot in one step, `manage.py check` then **clean with no dangling `subscriptions`/`accounts`/`dashboard → widget` import** (**DU-REL-1** proven); restored to `5df8daa` (git clean). Promotion stops at local/dev; Stage-6 retrospective + M1–M6 outcome review deferred until a prod target exists; ePrivacy consent revisit pre-EU-prod. Updated [INDEX.md](features/INDEX.md) (→ closed-out). **The developer-wedge funnel (impression → click → conversion) is now complete.** Handed to the **Coordinator**. |
| 2026-06-27 | `4-build`→`5-release` | **Senior Engineer** — built **T-01…T-08** from the APPROVED [widget-conversion-attribution/DESIGN.md](features/widget-conversion-attribution/DESIGN.md) + [TASKS.md](features/widget-conversion-attribution/TASKS.md): new `apps/widget` `rollup` (shared `_increment_daily`, extracted from the reach writer) + `source` (the `widget_src` signed source-only cookie codec + window/dedup/credit) + the `widget_conversion_count` table (migration `0002`) + `record_widget_conversion` + the `widget_conversions[_for_apps]` selectors; `set_marker` armed on the click-through 302 + the two fail-soft hooks ([`subscriptions.views.follow`](apps/subscriptions/views.py) on `created`, [`accounts.views.register`](apps/accounts/views.py) on the 202); the dashboard Screen-B slot extended with the conversion funnel stage + derived M2 (reach + conversions degrade together). **Risk front-loaded** (AC5 firewall AST proof + writer at T-03, codec at T-04, before any HTTP). **Full suite 962 green** (+69), `ruff`/`check` clean, no drift, migration up→down→up clean; firewall re-proven structurally (M5=0), no-PII by construction (AC4), DAG preserved. CODEMAP + widget README updated; impl notes WCA-IMPL-1/2/3 logged; [TEST_PLAN.md](features/widget-conversion-attribution/TEST_PLAN.md) maps every AC1–AC6. Updated [INDEX.md](features/INDEX.md) (→ 5-release). Handed to the **Release Engineer**. |
| 2026-06-27 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [widget-conversion-attribution/DESIGN.md](features/widget-conversion-attribution/DESIGN.md) into [TASKS.md](features/widget-conversion-attribution/TASKS.md): **8 ordered tasks T-01…T-08, all S/M** (no L), each with a concrete DoD + files-touched, plus an AC1–AC6 / design-element coverage map. **Risk front-loaded** as named: the AC5 firewall proof (AST) + the concurrency-correct conversion writer at **T-03**, and the novel `widget_src` sign/verify/`credited`-dedup codec at **T-04** — both **before any HTTP wiring** (T-05+). Grounded every task in current code (verified `follow_app` returns `created`, the register 202 path, the reach writer/selector/dashboard-slot patterns being mirrored). Updated [INDEX.md](features/INDEX.md) (→ 4-build). Hand-off only (one persona/session, no re-design). Handed to the **Senior Engineer** to build T-01 first + write [TEST_PLAN.md](features/widget-conversion-attribution/TEST_PLAN.md). |
| 2026-06-27 | `2-design`→`3-plan` | **Software Architect** (DN-WCA-DESIGN approved) — ratified [widget-conversion-attribution/DESIGN.md](features/widget-conversion-attribution/DESIGN.md) (→ APPROVED) and promoted **WCA-DESIGN-1…8 → RATIFIED**: the first-party signed `widget_src` source-only cookie + the separate PII-free `widget_conversion_count` rollup + fail-soft hooks at the follow/register views; AC6 firewall stays structural, no consent banner under the no-PII posture. Marked OQ-WCA-2/3/4 RESOLVED; recorded in the feature [DECISIONS.md](features/widget-conversion-attribution/DECISIONS.md); updated [INDEX.md](features/INDEX.md). Hand-off only (one persona/session). Handed to the **Planner / Tech Lead** to write [TASKS.md](features/widget-conversion-attribution/TASKS.md). |
| 2026-06-27 | `2-design` (awaiting DN-WCA-DESIGN) | **Software Architect** — drafted [widget-conversion-attribution/DESIGN.md](features/widget-conversion-attribution/DESIGN.md) and **resolved all three Stage-2 OQs**. Pivotal finding: the click-through `GET /widget/<id>/view` is a `target="_top"` top-level nav onto the **platform** origin, so a marker on its 302 is **first-party from birth** → dissolves cross-domain identity (OQ-WCA-3) and makes the no-PII posture achievable. Logged **WCA-DESIGN-1…8 PROPOSED**; one surfaced residual = the ePrivacy consent judgment (raised in **DN-WCA-DESIGN** with a one-call consent gate designed in as contingency, not silently relaxed). **Stopped at the gate.** |
| 2026-06-26 | `1-define`→`2-design` | **Product Analyst** (DN-WCA-BRIEF approved) — marked [widget-conversion-attribution/FEATURE_BRIEF.md](features/widget-conversion-attribution/FEATURE_BRIEF.md) **APPROVED**; promoted WCA-1/2/3 → RESOLVED (follow+account distinct; last-touch ~30-day window; aggregate-only no-PII). Left OQ-WCA-2/3/4 (token-carry mechanism) OPEN for Stage 2. Hand-off only. Handed to the **Software Architect**. |
| 2026-06-26 | `1-define` (awaiting DN-WCA-BRIEF) | **Product Analyst** — authored [widget-conversion-attribution/FEATURE_BRIEF.md](features/widget-conversion-attribution/FEATURE_BRIEF.md): the developer-wedge funnel payoff (impression → click → conversion). 5 stories / AC1–AC6 / 6 metrics incl. the M4 firewall + M5 no-PII guardrails; grounded every dependency in code. Did **not** guess the token-carry mechanism (split into OQ-WCA-2/3/4). **Raised DN-WCA-BRIEF; stopped at the gate.** |
| 2026-06-26 | `6-post-release`→`0-coordinator`→`1-define` | **Coordinator** — developer wedge ([D-10](DECISIONS.md)) **complete**; surfaced the next-feature fork; user picked **conversion attribution (M3)** = the deferred OQ-EUW-5. Scaffolded `features/widget-conversion-attribution/` (7 artifacts), added its [INDEX.md](features/INDEX.md) row, set `Stage: 1-define` + Persona **Product Analyst**. Deferred (not dropped) D-9 monetization; kept `weekly-digest` + `editorial-curation-tools` backlog. Handed to the **Product Analyst**. |
| 2026-06-26 | `5-release`→`6-post-release` | **Release Engineer** — **released `embeddable-update-widget` to local/dev** and wrote [RELEASE_NOTES.md](features/embeddable-update-widget/RELEASE_NOTES.md). Re-verified on a throwaway PG cluster (**893 tests green** twice, `ruff`/`check`/no drift). **Rehearsed rollback** (hard gate): migration up→down→up clean; `git revert b7db60f` removed `apps/widget` + the include + the dashboard edit in one step, `check` clean (no dangling `dashboard → widget` import — **DU-REL-1** proven). Promotion stops at local/dev; Stage-6 retrospective + M3 conversion deferred. **The developer wedge (D-10) is now complete.** Handed to the **Coordinator**. |
| 2026-06-26 | `4-build`→`5-release` | **Senior Engineer** — built **T-01…T-07** from the APPROVED [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md): new `apps/widget/` (the `widget_reach_count` daily-rollup table with no user/IP/score column = AC6/AC10 structural; atomic `F()`-increment writer + create-race retry; windowed reader; the two AllowAny views + framable no-JS templates + the `config/urls` `widget/` include = activation switch) + additive `apps/core` GET limiter + additive fail-soft dashboard "Widget reach" slot (AC9). **AC6 firewall proven structurally** (imports no `signals`; render+click writes 0 corpus rows; M5=0). **893 tests green**, `ruff`/`check`/no drift. One impl note **EUW-IMPL-1** (nested-`atomic()` savepoint on the retry). Handed to the **Release Engineer**. |
| 2026-06-26 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md) into [TASKS.md](features/embeddable-update-widget/TASKS.md): **7 ordered tasks T-01…T-07, all S/M**, full AC1–AC9 coverage. **Risk front-loaded at T-02** — the AC6 firewall (structural by absence) + the atomic `F()`-increment/create-race concurrency, both before any HTTP. No re-design. Handed to the **Senior Engineer** (build T-01 first + `TEST_PLAN.md`). |
| 2026-06-26 | `2-design`→`3-plan` | **Software Architect** (DN-EUW-DESIGN approved) — ratified [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md) and promoted **EUW-7…11 → RATIFIED**: embedding = a server-rendered `<iframe>` (AC7); attribution = a dedicated `apps/widget`-owned daily-rollup store that imports nothing from `signals` → the AC6 firewall (M5=0) is **structural by absence**; closed `apps/dashboard` gains a fail-soft off-platform reach slot (AC9); per-IP GET limit + `Cache-Control` (AC8); M3 per-account conversion deferred (OQ-EUW-5). **Also cleaned CONTROL.md** (collapsed closed-feature digests; moved 15 older Activity Log rows to [process/activity-archive.md](process/activity-archive.md)). Handed to the **Planner / Tech Lead**. |
| 2026-06-26 | `2-design` (awaiting DN-EUW-DESIGN) | **Software Architect** — drafted [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md) and resolved all 3 OQs (EUW-7…11 PROPOSED): server-rendered `<iframe>`; a dedicated `apps/widget` store (NOT the D-7 corpus — anonymous reach can't live there cleanly) → firewall structural by absence; per-IP GET limit + `Cache-Control` TTL. M3 conversion deferred (OQ-EUW-5). **Raised DN-EUW-DESIGN; stopped at the gate.** |
| 2026-06-26 | `1-define` | **Product Analyst** — authored [embeddable-update-widget/FEATURE_BRIEF.md](features/embeddable-update-widget/FEATURE_BRIEF.md): the bring-your-own-audience capture widget (D-10 wedge) — a read-only embeddable "what's new" widget surfacing an app's notices + a link back. 6 stories / AC1–AC8 / 6 metrics; firewall fixed as a hard constraint (AC6 / M5=0). Logged EUW-1…5 PROPOSED, left 3 architecture OQs OPEN for Stage 2. **Raised DN-EUW-BRIEF; stopped at the gate.** (User refinement folded in as AC9/EUW-6: anonymous end users, but the view source is always tracked for the dashboard.) |
| 2026-06-26 | `0-coordinator` | **Coordinator** (user-directed **developer-wedge pivot**) — recorded global **[D-10](DECISIONS.md)** (developer-hub-first MVP; network/digest held until density) + **[D-9](DECISIONS.md)** (monetization = labeled time-boxed promotion placements, firewalled from the ranking; tools + curation free). Revised vision §5.4/§5.6. Paused `editorial-curation-tools` → backlog. Scaffolded + activated `embeddable-update-widget`. Handed to the **Product Analyst**. |

> Older rows (the `developer-updates` lifecycle and everything before it — the
> `developer-dashboard`, `open-search-browse`, `interest-profile`, `app-subscriptions`,
> `ratings-reviews`, `app-pages`, `signal-capture`, `submission-intake`,
> `interest-taxonomy`, `identity-accounts` lifecycles) live in
> [process/activity-archive.md](process/activity-archive.md).
