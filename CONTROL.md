# CONTROL.md — Project Control Board

**This is the durable channel between the user and the agent.** It replaces chat
memory. The agent reads it first every session and updates it last. The user checks
this one file to know *where we are* and *what is waiting on a decision*.

Rules:
- The agent **must not** act on a question listed under *Decisions Needed From You*
  until the user answers it here.
- Every work session ends by updating *Current State* and the *Activity Log*.
- Keep it short and current. Detail lives in the feature artifacts, not here.

---

## Current State

| Field              | Value                                                            |
|--------------------|------------------------------------------------------------------|
| **Active feature** | **`widget-conversion-attribution`** — follow-up to the now-closed [embeddable-update-widget](features/embeddable-update-widget/) picking up its deferred **M3 / OQ-EUW-5**: **per-account conversion attribution** — linking a *new account/follow* to the specific widget click-through that led to it, via a widget-source token carried through an **anonymous** click → app page → sign-up (cookie consent + cross-domain identity + no-PII posture). Must **preserve the AC6 firewall** (no widget interaction confers D-8 eligibility, M5=0, structural) and the **no-PII posture**. The widget already ships *reach* (impressions + click-throughs, AC9); this adds the downstream *conversion* payoff. `editorial-curation-tools` + `weekly-digest` stay in **backlog** (D-10: held until per-niche density). |
| **Stage**          | `1-define` — **brief authored, awaiting approval.** [FEATURE_BRIEF.md](features/widget-conversion-attribution/FEATURE_BRIEF.md) written (5 stories / AC1–AC6 / M1–M6; money-buys-position → PASS). The token-carry **mechanism** is deliberately left OPEN for Stage 2 (OQ-WCA-2…4) — not guessed. **Blocked on DN-WCA-BRIEF** (approve brief + scoping calls WCA-1/2/3) below. |
| **Persona**        | **Product Analyst** — [process/personas/phase-1-product-analyst.md](process/personas/phase-1-product-analyst.md). |
| **Folder**         | [features/widget-conversion-attribution/](features/widget-conversion-attribution/) (7 artifacts · **brief PENDING APPROVAL**) |
| **Last updated**   | 2026-06-26 (Product Analyst: authored the brief from OQ-WCA-1, logged WCA-1/2/3 PROPOSED, split the mechanism into OQ-WCA-2…4 for Stage 2, **raised DN-WCA-BRIEF**, stopped at the gate.) |

> **Closed-out features** (Stage 6 skipped per the standing pattern — reopenable; all
> released local/dev, rollback rehearsed; live-metrics/outcome reports deferred until a
> prod target exists). Full record in each feature's `DECISIONS.md` / `RELEASE_NOTES.md`
> and in [features/INDEX.md](features/INDEX.md):
> `identity-accounts` (108) · `interest-taxonomy` (184, D-5) · `submission-intake` (315, D-6) ·
> `signal-capture` (374, D-7) · `app-pages` (417) · `ratings-reviews` (486, D-8) ·
> `app-subscriptions` (552, AS-3 seam) · `interest-profile` (616) · `open-search-browse` (676) ·
> `developer-dashboard` (740) · `developer-updates` (828, AS-3 producer, DU-REL-1) ·
> `embeddable-update-widget` (893, AC6 firewall structural, DU-REL-1 rollback).

> Canonical stage values: `0-coordinator` · `1-define` · `2-design` · `3-plan` ·
> `4-build` · `5-release` · `6-post-release` · `done`. See the routing table in
> [CLAUDE.md](CLAUDE.md) §2.

### Latest session status (CLAUDE.md §6.7 — overwritten each session)

```
Stage: 1-define | Feature: widget-conversion-attribution | Persona: Product Analyst (brief authored, awaiting DN-WCA-BRIEF)
Done: Authored features/widget-conversion-attribution/FEATURE_BRIEF.md from OQ-WCA-1 — the developer-wedge funnel payoff: attribute a downstream CONVERSION (new follow of the app +
      new account) to the specific widget click-through that caused it. 5 stories (S1-S5) / 6 G-W-T ACs (AC1-AC6) / 6 metrics (M1-M6, incl. the M4 firewall + M5 no-PII guardrails);
      all domain terms defined; explicit in/out-of-scope; risks R1-R5; vision §5.4/§5.6 + money-buys-position test -> PASS. Grounded every dependency in code (read the files): widget
      click-through = GET /widget/<id>/view 302 to pages:app-page carrying NO source token today (apps/widget/views.py); conversions = subscriptions.services.follow_app + accounts
      register; the follow's record_subscribe corpus event is the legitimate on-platform action (attribution adds nothing to it); firewall = ratings.gate.CURATED_SURFACES={DIGEST} +
      apps/widget imports nothing from signals. Logged WCA-1/2/3 PROPOSED in the feature DECISIONS.md (conversion set = follow+account; last-touch + ~30d window; aggregate-only
      source-keyed no-PII). DID NOT guess the token-carry mechanism — split it into OQ-WCA-2 (mechanism), OQ-WCA-3 (cross-domain identity), OQ-WCA-4 (consent envelope), all OPEN for
      Stage 2. Updated OPEN_QUESTIONS (OQ-WCA-1 -> SCOPED), feature DECISIONS, features/INDEX.md.
Verified by: n/a (Stage-1 analysis + doc authoring only — no code). Dependency claims verified by reading apps/widget/views.py + attribution.py, subscriptions/services.py, accounts/{urls,views}.py, ratings/gate.py, the widget DESIGN §11.
Blocked/Deferred: BLOCKED on DN-WCA-BRIEF (user approval of the brief + WCA-1/2/3). embeddable-update-widget Stage-6 retrospective still deferred/reopenable. weekly-digest +
      editorial-curation-tools stay backlog (D-10 density hold). D-9 monetization deferred (not dropped).
Decisions needed: DN-WCA-BRIEF (below) — approve the brief + the three bundled scoping calls. No Stage advance until answered.
Next: On approval — mark the brief APPROVED, promote WCA-1/2/3 -> RESOLVED, set Stage: 2-design + Persona Software Architect, hand off (the Architect resolves OQ-WCA-2…4, the token-carry mechanism, under the AC4 no-PII + AC5 firewall envelope).
```

<details><summary>Prior session (Release Engineer: Stage-5->6 hand-off, released local/dev)</summary>

```
Stage: 5-release -> 6-post-release | Feature: embeddable-update-widget | Persona: Release Engineer (released local/dev; Stage-5->6 hand-off, retrospective deferred)
Done: Worked the release checklist and wrote features/embeddable-update-widget/RELEASE_NOTES.md. Independently RE-VERIFIED the build on a throwaway local PostgreSQL cluster: 893 tests
      GREEN (twice), ruff/check clean, no migration drift. REHEARSED ROLLBACK: migration up->down->up clean; `git revert --no-commit b7db60f` removed apps/widget + the widget/ include
      + the dashboard edit in ONE step with `manage.py check` clean (no dangling dashboard->widget import = the DU-REL-1 property proven). The single `git revert b7db60f` is the
      one-action rollback. Promotion stops at local/dev; Stage-6 retrospective (M1-M6) + M3 per-account conversion (OQ-EUW-5) deferred.
Next: Coordinator — developer wedge (D-10) complete; help the user pick the next feature. (done this session)
```

</details>

---

## Decisions Needed From You

The agent is blocked on these. Answer inline (edit the **Answer** cell), then the agent
proceeds.

| ID | Decision needed | Recommendation | **Answer** |
|----|-----------------|----------------|------------|
| **DN-WCA-BRIEF** | Approve [FEATURE_BRIEF.md](features/widget-conversion-attribution/FEATURE_BRIEF.md) for `widget-conversion-attribution`, **and** decide the 3 bundled scoping calls. **(a) WCA-1 — conversion set:** what counts as a conversion? **(b) WCA-2 — attribution model + window.** **(c) WCA-3 — privacy/tracking posture.** (The token-carry *mechanism* is **not** asked here — it is left OPEN for Stage 2, OQ-WCA-2…4.) | **Approve.** (a) **both** a new **follow** of the clicked-through app (primary — the wedge metric) **and** a new **account** (secondary), distinct counts. (b) **last-touch** within a bounded **~30-day** (config) window. (c) **aggregate-only, source-keyed** — no per-person profile, no PII, no consent banner; if Stage 2 finds this infeasible it returns as a decision. | _(awaiting your answer — edit this cell)_ |

> **DN-WCA-BRIEF is open** (above) — the Product Analyst is blocked on it. Earlier:
> **DN-EUW-DESIGN** was approved (2026-06-26): the
> `embeddable-update-widget` [DESIGN.md](features/embeddable-update-widget/DESIGN.md) is
> **APPROVED** and the feature has advanced to **Stage 3 (Planner / Tech Lead)**, which
> decomposes the design into [TASKS.md](features/embeddable-update-widget/TASKS.md).
> Resolved decisions (DN-1 … DN-20, DN-DU-DESIGN, DN-EUW-BRIEF, DN-EUW-DESIGN) are
> summarized under *Decisions Made* below; full rationale lives in the decision logs.

---

## Decisions Made (recently)

A short, human-readable digest. Full rationale lives in [DECISIONS.md](DECISIONS.md)
(global) or `features/<slug>/DECISIONS.md` (local). Only the **active** feature is kept
in full; closed features collapse to a single pointer line.

- **Next feature = `widget-conversion-attribution` (Coordinator, 2026-06-26)** — with the **developer wedge ([D-10](DECISIONS.md)) complete** (`app-pages` + `developer-updates` + `embeddable-update-widget` all shipped local/dev), surfaced the next-feature fork to the user. User picked **conversion attribution (M3)** — the deferred **OQ-EUW-5** from the widget: **per-account conversion attribution** (linking a *new account/follow* to the specific widget click-through that led to it, via a widget-source token carried through an *anonymous* click → app page → sign-up; cookie consent + cross-domain identity + no-PII posture). Scaffolded `features/widget-conversion-attribution/` (7 seeded artifacts; **OQ-WCA-1** carries OQ-EUW-5's statement + the carried-in **AC6-firewall (M5=0, structural)** and **no-PII** constraints), added its [INDEX.md](features/INDEX.md) row (Phase 3, `1-define`), set `Stage: 1-define` + Persona **Product Analyst**. Deferred (not dropped) the other live option, **D-9 monetization**; kept `weekly-digest` + `editorial-curation-tools` **backlog** (D-10 density hold). No code; no new global ADR. Handed to the **Product Analyst** to author [FEATURE_BRIEF.md](features/widget-conversion-attribution/FEATURE_BRIEF.md).
- **`embeddable-update-widget` RELEASED to local/dev → Stage 6 (Release Engineer, 2026-06-26)** — worked the release checklist and wrote [RELEASE_NOTES.md](features/embeddable-update-widget/RELEASE_NOTES.md). **Independently re-verified** the build on a throwaway local PostgreSQL cluster: **893 tests green** (re-run post-rehearsal), `ruff` clean, `manage.py check` clean, `makemigrations --check` no drift. **Rollback rehearsed** (the persona's hard gate): migration up→down→up clean (`widget/0001_initial` applies / `migrate widget zero` drops / `migrate widget` re-applies), and `git revert --no-commit b7db60f` removed `apps/widget` + the `widget/` include + the dashboard edit **in one step** with `manage.py check` then clean (**no dangling `dashboard → widget` import** — the load-bearing **DU-REL-1** property *proven*, not assumed), restored clean. The single `git revert b7db60f` is the one-action operational rollback. **Promotion stops at local/dev** (no prod target — the standing pattern); the **Stage-6 retrospective (M1–M6) is deferred/reopenable** until a prod target/traffic exists; **M3 per-account conversion deferred** (OQ-EUW-5). **EUW-7…11 + EUW-IMPL-1 → BUILT & RELEASED**; no new global ADR (reuses D-4/D-6/D-7/D-8/D-9/D-10). Updated feature [DECISIONS.md](features/embeddable-update-widget/DECISIONS.md) (Stage-5 RELEASED) + [INDEX.md](features/INDEX.md) (→ closed-out). **The developer wedge (D-10) is now complete** (`app-pages` + `developer-updates` + the widget all shipped). Handed to the **Coordinator** to pick the next feature.
- **`embeddable-update-widget` BUILD COMPLETE → Stage 5 (Senior Engineer, 2026-06-26)** — built **all of T-01…T-07** from the APPROVED [DESIGN.md](features/embeddable-update-widget/DESIGN.md) + [TASKS.md](features/embeddable-update-widget/TASKS.md): a new feature-owned **`apps/widget/`** (the `widget_reach_count` daily-rollup table — no `user`/IP/score column = **AC6/AC10 structural**; the single-writer `attribution` atomic `F()`-increment + unique-constraint create-race retry; the single-reader `selectors` windowed `SUM…GROUP BY`, zero-fill, no N+1; the `content` view-model assembler; the two **AllowAny** views — `@xframe_options_exempt` rate-limited render w/ `Cache-Control` + fail-soft impression count, and the `/view` 302 to `reverse(pages:app-page)` with no open redirect; framable no-JS templates; the **`config/urls` `widget/` include = the activation switch**), additive **`apps/core`** (the reusable per-IP **GET** limiter generalising `core.ratelimit`, 3 `widget_*` tunables, 10 `WIDGET_*`/`DASHBOARD_WIDGET_DEGRADED` metrics), and an additive **fail-soft dashboard "Widget reach" slot** (AC9; the only `dashboard → widget` edge). **The AC6 firewall is proven structurally** — `apps/widget` imports nothing from `signals` (AST test), a render+click writes **0** corpus rows, and `has_impression(CURATED_SURFACES)` stays False (**M5 = 0**). **AC1–AC9 covered** ([TEST_PLAN.md](features/embeddable-update-widget/TEST_PLAN.md)); **full suite green (893 tests)**, `ruff` clean, `makemigrations --check` clean, migration up→down→up rehearsed. One implementation addition, **EUW-IMPL-1** (a nested-`atomic()` savepoint on the create-race retry — a bare `except IntegrityError` poisons the Postgres txn; same algorithm, logged in the feature [DECISIONS.md](features/embeddable-update-widget/DECISIONS.md)). **EUW-7…11 → BUILT**; **no new global ADR** (reuses D-4/D-6/D-7/D-8/D-9/D-10). Updated `README`/`CODEMAP`/`DECISIONS`/[INDEX.md](features/INDEX.md) (→ `5-release`). Handed to the **Release Engineer** (rollout per DESIGN §13; rollback = `git revert` of the build commit — DU-REL-1).
- **`embeddable-update-widget` TASKS.md complete → Stage 4 (Planner / Tech Lead, 2026-06-26)** — decomposed the APPROVED [DESIGN.md](features/embeddable-update-widget/DESIGN.md) into [TASKS.md](features/embeddable-update-widget/TASKS.md): **7 ordered, independently-verifiable tasks (T-01…T-07), all S/M (no L)**, full **AC1–AC9** + design-element coverage. **Risk front-loaded at T-02** — the single integrity-critical property, the **AC6 firewall = structural by absence** (`apps/widget` imports nothing from `signals` [AST test] + a widget interaction writes **0** corpus rows + `has_impression(CURATED_SURFACES)` stays False ⇒ M5=0), proven the moment the writer/store exist, alongside the **atomic `F()`-increment + create-race-retry concurrency** on the hot daily-rollup row — both before any HTTP. Spine: T-01 table+app → **T-02 attribution writer + selectors reader + the firewall proof** → T-03 `core` (per-IP GET limiter generalising `core.ratelimit`, 3 `widget_*` tunables, the `WIDGET_*` constants) → T-04 `content.build_widget_view` → T-05 the two views + framable templates + the `config/urls` `widget/` include (activation switch) → T-06 additive fail-soft dashboard "Widget reach" slot (AC9) → T-07 docs/CODEMAP/DECISIONS. **No re-design**; reuses D-4/D-6/D-7/D-8/D-9/D-10 — **no new global ADR**. Did the Stage-3→4 hand-off only (one persona/session); updated [INDEX.md](features/INDEX.md) (→ `4-build`). Handed to the **Senior Engineer** to build T-01 first + produce `TEST_PLAN.md`.
- **`embeddable-update-widget` DESIGN APPROVED (DN-EUW-DESIGN, 2026-06-26)** — Software Architect: user approved the DESIGN, so **ratified [DESIGN.md](features/embeddable-update-widget/DESIGN.md)** (status → APPROVED) and promoted all five **EUW-7…11 → RATIFIED** (binding for Stage 3). Ratified spine: **(a)** embedding = a server-rendered **`<iframe>`** (one-line paste, no JS/build — AC7; "view on platform" = a `target="_top"` link → `/widget/<id>/view` that 302s to the app page); **(b)** attribution = a **dedicated `apps/widget`-owned daily-rollup store** (`widget_reach_count`, no user/PII) — `apps/widget` **imports nothing from `signals`**, so the **AC6 firewall (M5=0) is structural by absence** (a widget interaction creates no corpus row → cannot be curated-surface evidence); the **closed** `apps/dashboard` gains a fail-soft **off-platform widget-reach slot** (AC9), distinct from the on-platform per-`Surface` breakdown; **(c)** per-IP fixed-window GET rate limit + `Cache-Control` TTL (AC8); **M3 per-account conversion deferred** (OQ-EUW-5 — needs a widget-source token carried through an anonymous click→signup; AC9 *reach* ships now). New app **`apps/widget/`** (owns one table) + additive `apps/dashboard`/`apps/core`. **No new global ADR** (reuses D-4/D-6/D-7/D-8/D-9/D-10); rollback = `git revert` of the build commit (DU-REL-1 precedent). Did the Stage-2→3 hand-off only (one persona/session); emptied Decisions Needed; updated [INDEX.md](features/INDEX.md). Handed to the **Planner / Tech Lead** to decompose the DESIGN into [TASKS.md](features/embeddable-update-widget/TASKS.md).
- **`embeddable-update-widget` brief APPROVED + widget-source attribution added (DN-EUW-BRIEF, 2026-06-26)** — Product Analyst: user **approved all 5 bundled scoping calls** (EUW-1…5), with a refinement on EUW-5 — end users are anonymous when not logged in, but the **view source is always tracked** so the developer dashboard shows how many people came from the widget. Marked [FEATURE_BRIEF.md](features/embeddable-update-widget/FEATURE_BRIEF.md) **APPROVED**; folded the refinement into new **AC9** + **EUW-6** (orthogonal to the AC6 firewall — tracking a source confers no D-8 eligibility); reframed **OQ-EUW-2** from *whether* to *how* to emit. **AC6 + AC9 are binding Stage-2 inputs.**
- **Developer-wedge pivot — global D-10 + D-9 (Coordinator, 2026-06-26)** — user-directed strategic pivot ([DECISIONS.md](DECISIONS.md)). **D-10:** the MVP cold-start is re-sequenced to a single-player **developer hub** (app page + changelog feed + the embeddable widget) that drags a developer's existing audience onto the platform; the discovery network/digest is **held back until per-niche density**. Most of the wedge is already built; the missing load-bearing piece is the **embeddable widget** (now active). **D-9:** monetization = paid, labeled, time-boxed **promotion placements** sold for reach, **firewalled from the ranking** (a `Surface` kept outside `CURATED_SURFACES`); tools + curation stay free; ad-inventory cap locked before revenue. **Paused `editorial-curation-tools` → backlog.**

**Closed-feature pointers** (full lifecycle in each folder's `DECISIONS.md`/`RELEASE_NOTES.md`):

- **`developer-updates`** (closed 2026-06-24, 828 tests) — new `apps/updates/` owning `updates_notice`, the single **AS-3 producer**: pull delivery (M5=0 structural, no fan-out), imports no `signals` (AST-enforced), durable table-derived rate limit; first to repoint a *closed* app's seam → rollback = `git revert` of the build commit (**DU-REL-1**). Brief DN-20 → DESIGN DN-DU-DESIGN → built → released. Detail: [developer-updates/DECISIONS.md](features/developer-updates/DECISIONS.md).
- **`developer-dashboard`** (closed 2026-06-24, 740 tests) — model-less `apps/dashboard/`, a read-only view of an app's reception (D-7 reach/engagement + D-8 curated ratings) for its owner; two additive neutral `signals.selectors` reads, no schema. Brief DN-19 → DESIGN DN-DD-DESIGN → built → released. Detail: [developer-dashboard/DECISIONS.md](features/developer-dashboard/DECISIONS.md).
- **`open-search-browse`** (closed 2026-06-23, 676 tests) — `apps/discovery/`, the paginated DB-pushed `catalog.search_catalogue` open surface where money never buys position. Detail: [open-search-browse/DECISIONS.md](features/open-search-browse/DECISIONS.md).
- **`interest-profile`** (closed 2026-06-23, 616 tests) — `apps/interests/`, the user side of the Ring-0 match via the `declared_tag_ids` matcher contract. Detail: [interest-profile/DECISIONS.md](features/interest-profile/DECISIONS.md).
- **`app-subscriptions`** (closed 2026-06-21, 552 tests) — `apps/subscriptions/`, atomic follow + one `subscribe` emit, the empty-until-producer notice seam **AS-3**. Detail: [app-subscriptions/DECISIONS.md](features/app-subscriptions/DECISIONS.md).
- **`ratings-reviews`** (closed 2026-06-21, 486 tests) — `apps/ratings/`; established the global **[D-8](DECISIONS.md)** curated-rating gate ("curated = a `DIGEST` impression"). Detail: [ratings-reviews/DECISIONS.md](features/ratings-reviews/DECISIONS.md).
- **`app-pages`** (closed 2026-06-20, 417 tests) — `apps/pages/`, first live emitter of D-7 `app_page` impressions. Detail: [app-pages/DECISIONS.md](features/app-pages/DECISIONS.md).
- **`signal-capture`** (closed 2026-06-18, 374 tests) — `apps/signals/`; established the global **[D-7](DECISIONS.md)** event schema (append-only, structural no-score, no-PII). Detail: [signal-capture/DECISIONS.md](features/signal-capture/DECISIONS.md).
- **`submission-intake`** (closed 2026-06-18, 315 tests) — `apps/catalog/`, the ACCEPTED-only catalogue substrate = global **[D-6](DECISIONS.md)**. Detail: [submission-intake/DECISIONS.md](features/submission-intake/DECISIONS.md).
- **`interest-taxonomy`** (closed 2026-06-17, 184 tests) — flat tags + clusters, soft-retire + read-time `resolve_tag` = global **[D-5](DECISIONS.md)**. Detail: [interest-taxonomy/DECISIONS.md](features/interest-taxonomy/DECISIONS.md).
- **`identity-accounts`** (closed 2026-06-17, 108 tests) — the foundation (the stack **D-4**, roles **D-3**). Detail: [DECISIONS.md](DECISIONS.md).

> **Foundational decisions** (the stack, niche, build order — D1–D3, A1–A3, R1, global
> D-1/D-2/D-4) live in [DECISIONS.md](DECISIONS.md). Highlights: niche = **"vibecoded
> webapps"** (D-1); stack = **Python / Django + PostgreSQL** (D-4).

---

## Activity Log

Newest first. One line per session. **Keep the most recent ~10 rows here**; older rows
live in [process/activity-archive.md](process/activity-archive.md). The per-feature
folders remain the full record either way.

| Date       | Stage           | Summary                                                                 |
|------------|-----------------|-------------------------------------------------------------------------|
| 2026-06-26 | `1-define` (awaiting DN-WCA-BRIEF) | **Product Analyst** — authored [widget-conversion-attribution/FEATURE_BRIEF.md](features/widget-conversion-attribution/FEATURE_BRIEF.md): the developer-wedge funnel payoff — attribute a downstream **conversion** (new **follow** of the app + new **account**) to the specific widget **click-through** that caused it, completing impression → click → conversion. **5 stories (S1–S5) / 6 G-W-T ACs (AC1–AC6) / 6 metrics (M1–M6)** incl. the **M4 firewall** + **M5 no-PII** guardrails; all domain terms defined; explicit in/out-of-scope; R1–R5; vision §5.4/§5.6 + **money-buys-position → PASS**. **Grounded every dependency in code** (read the files): widget click-through = `GET /widget/<id>/view` 302 → `pages:app-page` carrying **no source token** today ([apps/widget/views.py](apps/widget/views.py)); conversions = [`subscriptions.services.follow_app`](apps/subscriptions/services.py) (returns `created`) + the [`accounts` register](apps/accounts/views.py) view; the follow's `record_subscribe` corpus event is the *legitimate* on-platform action (**attribution adds nothing to it**); firewall = [`ratings.gate`](apps/ratings/gate.py) `CURATED_SURFACES={DIGEST}` + `apps/widget` imports nothing from `signals`. Logged **WCA-1/2/3 PROPOSED** in the feature [DECISIONS.md](features/widget-conversion-attribution/DECISIONS.md) (conversion set = follow+account; last-touch + ~30d window; **aggregate-only source-keyed no-PII**). **Did NOT guess the token-carry mechanism** — split it into **OQ-WCA-2** (mechanism), **OQ-WCA-3** (cross-domain identity), **OQ-WCA-4** (consent envelope), all **OPEN for Stage 2** under the AC4 no-PII + AC5 firewall envelope; marked OQ-WCA-1 **SCOPED**. Reuses D-3/D-4/D-6/D-7/D-8 + the carried-in AC6 firewall — **no new global ADR**. Updated [INDEX.md](features/INDEX.md). **Raised DN-WCA-BRIEF** (approve brief + WCA-1/2/3); **stopped at the gate, no Stage advance until approved.** |
| 2026-06-26 | `6-post-release`→`0-coordinator`→`1-define` | **Coordinator** — developer wedge ([D-10](DECISIONS.md)) **complete** (`app-pages` + `developer-updates` + `embeddable-update-widget` all shipped local/dev; the widget closed-out). Surfaced the next-feature fork to the user (D-9 monetization / **M3 conversion attribution** / turn on the network / something else); user picked **conversion attribution (M3)** = the deferred **OQ-EUW-5**. Scaffolded **`features/widget-conversion-attribution/`** — per-account conversion attribution linking a *new account/follow* to the specific widget click-through that led to it (widget-source token carried through an *anonymous* click → app page → sign-up; cookie consent + cross-domain identity + no-PII). Seeded all 7 standard artifacts (brief/design/tasks/test-plan/release-notes stubbed; **OQ-WCA-1** carries OQ-EUW-5's problem statement + the carried-in **AC6-firewall (M5=0, structural)** + **no-PII** constraints; DECISIONS seeded with the Stage-0 scaffold rationale + rejected alternatives). Added its [INDEX.md](features/INDEX.md) row (Phase 3 Dev value, depends on `embeddable-update-widget`, `1-define`), set the active feature + `Stage: 1-define` + Persona **Product Analyst** in Current State. Deferred (not dropped) **D-9 monetization** (the other live option); kept `weekly-digest` + `editorial-curation-tools` **backlog** (D-10 density hold). No code (Coordinator setup + doc edits only); no new global ADR. Handed to the **Product Analyst** to author [FEATURE_BRIEF.md](features/widget-conversion-attribution/FEATURE_BRIEF.md) (scope the token-carry attribution; preserve the firewall + no-PII; leave the cross-domain-identity/cookie-consent mechanism as Stage-2 OQs). |
| 2026-06-26 | `5-release`→`6-post-release` | **Release Engineer** — **RELEASED `embeddable-update-widget` to local/dev**. Worked the release checklist and wrote [embeddable-update-widget/RELEASE_NOTES.md](features/embeddable-update-widget/RELEASE_NOTES.md) (what changed / who's affected / how to use / operator rollout + promotion table / rehearsed rollback / monitoring metrics→signals→alert / known limitations). **Independently re-verified** the build on a throwaway local PostgreSQL cluster (spun up + torn down this session — no system service touched): **893 tests green** twice (before + after the rollback rehearsal), `ruff` clean, `manage.py check` clean, `makemigrations --check` no drift. **Rehearsed rollback** (the persona's hard gate): the migration up→down→up is clean (`widget/0001_initial` applies; `migrate widget zero` drops `widget_reach_count`; `migrate widget` re-applies), and `git revert --no-commit b7db60f` removed `apps/widget` + the `widget/` include + the closed-`apps/dashboard` edit **atomically in one step** — `manage.py check` then passed with **no dangling `dashboard → widget` import** (the load-bearing **DU-REL-1** property *proven*, not assumed); restored to `b7db60f` clean, suite 893 green again. **The single `git revert b7db60f` is the one-action operational rollback** (any operator with repo/deploy access; the optional `migrate widget zero` needs no data coordination — the table holds only PII-free aggregate reach counts). Promotion **stops at local/dev** (no prod target/traffic — the standing pattern of the prior 11 features); the **Stage-6 retrospective (M1–M6 outcome review) is deferred/reopenable** until a prod target exists; **M3 per-account conversion deferred** (OQ-EUW-5, RELEASE_NOTES §7). Marked **EUW-7…11 + EUW-IMPL-1 BUILT & RELEASED**; no new global ADR (reuses D-4/D-6/D-7/D-8/D-9/D-10). Updated feature [DECISIONS.md](features/embeddable-update-widget/DECISIONS.md) (Stage-5 RELEASED) + [INDEX.md](features/INDEX.md) (→ closed-out / released local/dev) + this CONTROL.md (closed-out pointer list + 893). **The developer wedge (D-10) is now complete** — `app-pages` + `developer-updates` + the embeddable widget all shipped. Stage-5→6 hand-off only (one persona/session); retrospective deferred. Handed to the **Coordinator** to pick the next feature (or reopen the retrospective once a prod target exists). |
| 2026-06-26 | `4-build`→`5-release` | **Senior Engineer** — built **all of T-01…T-07** from the APPROVED [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md) + [TASKS.md](features/embeddable-update-widget/TASKS.md). New feature-owned **`apps/widget/`**: the `widget_reach_count` daily-rollup table (UUID pk, soft D-6 `app_id`, `kind`, `count_date`, `count`; unique `(app_id,kind,count_date)` + `widget_reach_app_kind_date_idx`; **no `user`/IP/referrer/score column** = AC6/AC10 structural) + `0001_initial` (up→down→up clean); the single-writer **`attribution`** (atomic `F()`+1 increment + unique-constraint create-race retry); the single-reader **`selectors`** (windowed `SUM…GROUP BY`, zero-fill, **no N+1**); **`content.build_widget_view`** (capped newest-first notices via `updates.selectors`, server-derived `pages:app-page` link, `notices_degraded` fail-soft split); the two **AllowAny** views — `widget_render` (`@xframe_options_exempt`, `Cache-Control`, per-IP rate-limited, fail-soft impression count, `unavailable` 404 / degraded 200) + `widget_view_redirect` (click-through fail-soft + **302 to `reverse(pages:app-page)`**, no open redirect); framable self-contained templates (no JS, inline CSS, auto-escaped); the **`config/urls` `widget/` include = the activation switch**. Additive **`apps/core`**: the reusable per-IP **GET** limiter `ip_rate_limited_get` (generalises `_exceeds_limit` by a window param; 429-no-call + **fail-open** on cache error; existing auth `rate_limited` unchanged), 3 `widget_*` tunables (in `validate_all`), 10 `WIDGET_*`/`DASHBOARD_WIDGET_DEGRADED` metrics. Additive **fail-soft dashboard "Widget reach" slot** (AC9): `WidgetReachView` + `ReceptionSummary.widget_impressions` (one bulk read, no N+1) + `AppReception.widget_reach`; degrades only the slot (`DASHBOARD_WIDGET_DEGRADED`) — the core signals read stays loud-500; labeled off-platform, distinct from the per-`Surface` breakdown. **AC6 firewall proven structurally** — `apps/widget` imports nothing from `signals` (AST test, the discovery precedent) + a render+click writes **0** `Impression`/`EngagementEvent` rows + `has_impression(CURATED_SURFACES)` stays False (**M5 = 0**), re-asserted end-to-end over the live routes. **AC1–AC9 covered** ([TEST_PLAN.md](features/embeddable-update-widget/TEST_PLAN.md)); **full suite green (893 tests, +65 over the 828 baseline)**, `ruff` clean, `makemigrations --check` clean, `manage.py check` clean. One implementation addition **EUW-IMPL-1** (the create-race retry needed a nested `transaction.atomic()` savepoint — a bare `except IntegrityError` poisons the Postgres transaction; same algorithm, no contract change, logged in the feature [DECISIONS.md](features/embeddable-update-widget/DECISIONS.md)). Marked **EUW-7…11 BUILT**; **no new global ADR** (reuses D-4/D-6/D-7/D-8/D-9/D-10). Wrote `apps/widget/README.md` (one-line `<iframe>`, routes, rollback), updated [CODEMAP.md](CODEMAP.md) (every new shared surface) + [INDEX.md](features/INDEX.md) (→ `5-release`). Stage-4→5 hand-off only (one persona/session). Handed to the **Release Engineer** — rollout per DESIGN §13; rollback = `git revert` of the build commit (DU-REL-1 precedent, since `apps/dashboard` now imports `widget.selectors`). |
| 2026-06-26 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md) into [TASKS.md](features/embeddable-update-widget/TASKS.md): **7 ordered tasks T-01…T-07, all S/M (no L)**, full **AC1–AC9** + a design-element coverage table; every task has a checkable DoD + declared files; each leaves the system green/releasable (the surface goes live only at the T-05 `config/urls` include). **Risk front-loaded at T-02** — the **AC6 firewall = structural by absence** (`apps/widget` imports nothing from `signals` [AST test] + 0 corpus rows + `has_impression(CURATED_SURFACES)` False ⇒ M5=0) **and** the **atomic `F()`-increment + create-race retry** concurrency on the hot daily-rollup row, both proven before any HTTP. Spine: **T-01** scaffold `apps/widget/` + the `widget_reach_count` table (no `user`/IP/score column = AC10 posture structural) + `INSTALLED_APPS` → **T-02** the single-writer `attribution` + single-reader `selectors` + the firewall proof → **T-03** `core` (per-IP GET limiter generalising `core.ratelimit`, 3 `widget_*` tunables, the `WIDGET_*`/`DASHBOARD_WIDGET_DEGRADED` constants) → **T-04** `content.build_widget_view` (capped live notices + link + `notices_degraded` split) → **T-05** `widget_render` (xframe-exempt, `Cache-Control`, rate-limited, fail-soft count) + `widget_view_redirect` (302 to `reverse(pages:app-page)`, no open redirect) + framable templates + the `config/urls` `widget/` include (activation switch) → **T-06** additive fail-soft dashboard **"Widget reach"** slot (AC9) → **T-07** docs/CODEMAP/DECISIONS. Cross-checked every cited contract against the codebase (`updates.selectors`, `catalog.get_catalogued_app`, `pages:app-page`, `core.ratelimit`/`config`/`observability`, the `dashboard.reception` slot pattern, the `discovery` AST precedent); **no re-design**. Reuses **D-4/D-6/D-7/D-8/D-9/D-10 — no new global ADR**. Did the Stage-3→4 hand-off only (one persona/session); updated [INDEX.md](features/INDEX.md) (→ `4-build`). Handed to the **Senior Engineer** to build **T-01** first + produce `TEST_PLAN.md`. |
| 2026-06-26 | `2-design`→`3-plan` | **Software Architect** (hand-off, DN-EUW-DESIGN approved) — user approved the DESIGN, so **ratified [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md)** (status → APPROVED) and promoted all five **EUW-7…11 → RATIFIED** (binding for Stage 3): embedding = a server-rendered **`<iframe>`** (one-line, no JS/build, AC7); attribution = a **dedicated `apps/widget`-owned daily-rollup store** (`widget_reach_count`) that **imports nothing from `signals`** ⇒ the AC6 firewall (M5=0) is **structural by absence**; the closed `apps/dashboard` gains a fail-soft **off-platform widget-reach slot** (AC9); per-IP GET rate limit + `Cache-Control` (AC8); **M3 per-account conversion deferred** (OQ-EUW-5). Closed OQ-EUW-1/2/3; new `apps/widget/` (owns one table) + additive `apps/dashboard`/`apps/core`; **no new global ADR** (reuses D-4/D-6/D-7/D-8/D-9/D-10). Did the Stage-2→3 hand-off only (one persona/session); emptied Decisions Needed; updated [INDEX.md](features/INDEX.md). **Also cleaned CONTROL.md** (collapsed closed-feature digests to pointer lines; moved 15 older Activity Log rows to [process/activity-archive.md](process/activity-archive.md)). Handed to the **Planner / Tech Lead** to decompose [DESIGN.md](features/embeddable-update-widget/DESIGN.md) into [TASKS.md](features/embeddable-update-widget/TASKS.md). |
| 2026-06-26 | `2-design` (awaiting DN-EUW-DESIGN) | **Software Architect** — drafted [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md) via the 14-step protocol against the APPROVED brief (AC1–AC9); surveyed the upstream code (`apps/updates`, `apps/catalog`, `apps/pages`, `apps/ratings/gate`, `apps/signals`, `apps/dashboard`, `core`). **Resolved all 3 OQs** (EUW-7…11 PROPOSED): **OQ-EUW-1 →** a server-rendered `<iframe>` (one-line, no JS/build); **OQ-EUW-2 (the crux) →** a **dedicated `apps/widget` store, NOT the D-7 corpus** (the corpus is keyed user×`App.id`, requires an authenticated actor, forbids PII — anonymous widget reach can't live there cleanly), so `widget_reach_count` counts reach and `apps/widget` imports nothing from `signals` ⇒ the firewall is **structural by absence** (rejected the brief's illustrative `Surface.WIDGET`); **OQ-EUW-3 →** per-IP fixed-window GET limit + `Cache-Control` TTL. **M3 per-account conversion deferred** (OQ-EUW-5); AC9 reach ships now. **Raised DN-EUW-DESIGN**; stopped at the gate (one persona/session). |
| 2026-06-26 | `1-define` | **Product Analyst** — authored [embeddable-update-widget/FEATURE_BRIEF.md](features/embeddable-update-widget/FEATURE_BRIEF.md): the **bring-your-own-audience capture widget** (developer-wedge pivot, [D-10](DECISIONS.md)) — a read-only embeddable "what's new" widget a developer drops in their **own** app, surfacing that app's published notices + a link back to its app page. **6 stories (S1–S6) / 8 G-W-T ACs (AC1–AC8) / 6 metrics (M1–M6)**; all domain terms defined; explicit in/out-of-scope; risks R1–R5; vision §5.4/§5.6 + **money-buys-position test → PASS**. **Grounded every dependency in code** (read the files): notice content + bounded read = [apps/updates/selectors.py](apps/updates/selectors.py) `published_notices_for_apps` (AS-3 `PublishedNotice`); render shape = [apps/subscriptions/notices.py](apps/subscriptions/notices.py); destination = [apps/pages/urls.py](apps/pages/urls.py) (stable `App.id` link); firewall = [apps/ratings/gate.py](apps/ratings/gate.py) `CURATED_SURFACES={DIGEST}` — any widget surface is **already outside** it. **The firewall is fixed as a hard product constraint (AC6 / M5=0):** no widget interaction may confer **D-8** curated-rating eligibility. Logged **5 PROPOSED scoping calls (EUW-1…5)** in the feature [DECISIONS.md](features/embeddable-update-widget/DECISIONS.md): display-only (never authors) / capture = click-through, no in-widget auth / **free** tool, not a paid **[D-9](DECISIONS.md)** promo placement / non-curated surface (binding on Stage 2) / notices become a public anonymous read (deliberate expansion). Left the **3 architecture OQs OPEN for Stage 2** (not guessed): OQ-EUW-1 embedding mechanism, OQ-EUW-2 D-7 emit + new `Surface`?, OQ-EUW-3 abuse limits. Reuses **D-3/D-6/D-7/D-8 + AS-3 — no new global ADR.** Updated [INDEX.md](features/INDEX.md). **Raised DN-EUW-BRIEF** (approve brief + EUW-1…5); **stopped at the gate, no Stage advance until approved.** |
| 2026-06-26 | `0-coordinator` | **Coordinator** (user-directed **developer-wedge pivot**) — recorded two global ADRs: **[D-10](DECISIONS.md)** (MVP cold-start re-sequenced to a single-player **developer hub** — public app page + changelog feed + a new **embeddable widget** — that drags each developer's existing audience in; the **discovery network/digest held back until per-niche density**; niche **D-1** + impression-economy/ranking design **unchanged**; most of the wedge already built, the **embeddable widget** is the missing piece) and **[D-9](DECISIONS.md)** (monetization = paid, **labeled, time-boxed PROMOTION placements** sold for reach, **firewalled from the ranking** — a `Surface` kept **outside `CURATED_SURFACES`**, per-impression-not-per-user; **tools + curation free**; **ad-inventory cap locked before revenue**; **supersedes** the DN-20 tool-subscription sketch). Revised vision **§5.4** (developer-wedge-first) + **§5.6** (promotion model) + header date. **Paused `editorial-curation-tools` `1-define`→`backlog`** (only stubs existed — nothing lost) and left `weekly-digest` backlog, per D-10. **Scaffolded + activated `embeddable-update-widget`** (7 stub artifacts + 3 seeded OQs), added its [INDEX.md](features/INDEX.md) row, set `Stage: 1-define` + Persona **Product Analyst**. Captured both decisions as project memories. No code (Coordinator setup + doc edits only). Handed to the **Product Analyst** to author [FEATURE_BRIEF.md](features/embeddable-update-widget/FEATURE_BRIEF.md) (define the widget scope + the non-curated-surface boundary). |
| 2026-06-24 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the **APPROVED** [developer-updates/DESIGN.md](features/developer-updates/DESIGN.md) into [TASKS.md](features/developer-updates/TASKS.md): **6 ordered tasks (T-01…T-06), all S/M** (no `L`), full design-element coverage + AC1–AC8 roll-up + a sequential file-collision note. **Risk-front-loaded per the CONTROL.md hand-off: T-02 is the load-bearing seam** — the **AS-3 producer read** (`updates.selectors.published_notices_for_apps`) **+ the `subscriptions.notices` repoint adapter** (`PublishedNotice → Notice`) on the **closed** `apps/subscriptions` — landed right after the table (T-01) and proven by an **import-cycle-absence test** (DESIGN §13) + a **feed-integration test on ORM-seeded rows** + **fail-soft preservation** (existing `_notices_fail_soft` → `SUBSCRIPTION_NOTICE_DEGRADED`), **before** any write path/UI; `apps.subscriptions` regression in its DoD. Spine: **T-01** scaffold `apps/updates/` + `updates_notice` + `INSTALLED_APPS` (schema, inert) → **T-02** producer read + seam repoint → **T-03** `updates.services` (post/withdraw, owner-gate, validation, **durable table-derived rate-limit** AC8) + 4 config tunables + the six `UPDATES_*` counters (**no `signals.capture` import**) → **T-04** additive `subscriptions.subscriber_count` + `subscriptions_app_idx` index (`0002`, reversible) → **T-05** the four role+owner-gated views + templates + the `config/urls` `updates/` include (**final activation**) + the AST no-`signals` test → **T-06** README/CODEMAP/DECISIONS (DU-DESIGN-1…6 built). Cross-checked every referenced contract against the codebase (`subscriptions.notices`/`views`, `catalog` owner reads, the `subscriptions_subscription` model+`0001`, `core.config`/`observability`, the `apps/discovery` AST precedent); **no re-design**. Reuses **D-3/D-6/D-7 + AS-3 — no new global ADR**. Set `Stage: 4-build` + Persona **Senior Engineer**; updated [INDEX.md](features/INDEX.md) (→ `4-build`). Advanced to `4-build`; handed to the **Senior Engineer** (build T-01 first, produce `TEST_PLAN.md`). |
| 2026-06-24 | `2-design`→`3-plan` | **Software Architect** (hand-off, DN-DU-DESIGN approved) — user answered **DN-DU-DESIGN = approve**, so **ratified [developer-updates/DESIGN.md](features/developer-updates/DESIGN.md)** (status header → APPROVED) and performed the Stage-2→3 hand-off **only** (no second persona this session, per CLAUDE.md). Promoted all six **DU-DESIGN-1…6 → RATIFIED** (binding for Stage 3) in the feature [DECISIONS.md](features/developer-updates/DECISIONS.md), and **OQ-DU-1/OQ-DU-2 RESOLVED-in-design → RESOLVED** in [OPEN_QUESTIONS.md](features/developer-updates/OPEN_QUESTIONS.md). Ratified spine: a **new feature-owned `apps/updates/`** owning one table **`updates_notice`** (C6 = owns a table) as the single **AS-3 producer**; **pull delivery** (the feed pulls `notices_for_apps` the *followed* app_ids) → **M5 = 0 structural** + no post-time fan-out (R3 dissolved); **transparency line verified** — `apps/updates` imports **no `signals.capture`** (AST-enforced, the discovery/dashboard precedent), posting inert to the corpus, only the follower's **own** return via existing pages `APP_PAGE`/`page_reengagement` kinds counts; **durable table-derived rate limit** (count own recent rows in a config window, AC8); additive `subscriptions.selectors.subscriber_count(app_id)` + the `subscriptions_app_idx` index (audience hint / M2); the AS-3 repoint as a single **`PublishedNotice → Notice` adapter** keeping the dependency graph a **DAG**. Reuses **D-3/D-6/D-7 + AS-3 — no new global ADR**. Set `Stage: 3-plan` + Persona **Planner / Tech Lead** in Current State; **emptied Decisions Needed**; added the Decisions-Made digest; updated [INDEX.md](features/INDEX.md) (→ `3-plan`). Advanced to `3-plan`; handed to the **Planner / Tech Lead** to decompose [DESIGN.md](features/developer-updates/DESIGN.md) into [TASKS.md](features/developer-updates/TASKS.md) (risk-front-load the AS-3 producer seam). |
| 2026-06-24 | `2-design` (awaiting DN-DU-DESIGN) | **Software Architect** — drafted [developer-updates/DESIGN.md](features/developer-updates/DESIGN.md) via the 14-step protocol; **RESOLVED OQ-DU-1 + OQ-DU-2**. Spine: a **NEW feature-owned `apps/updates/`** owning one table **`updates_notice`** (resolves **C6** = owns a table, unlike the model-less pages/discovery/dashboard consumers) as the single **AS-3 producer**. **OQ-DU-1 key finding:** the AS-3 seam is **pull** — the feed already hands `notices_for_apps` the *followed* app_ids, so notice **delivery needs no reverse-audience read**; this makes **M5 (reach beyond followers) = 0 structural** and **dissolves the R3 fan-out**. The reverse read reduces to an additive, bounded `subscriptions.selectors.subscriber_count(app_id)` (one indexed COUNT) + the additive `subscriptions_app_idx` index — for the audience hint + M2 only. **OQ-DU-2 (transparency line) VERIFIED:** `apps/updates` imports **no `signals.capture`** (AST-enforced, the discovery/dashboard precedent), so posting is **inert to the corpus**; only the follower's **own** return via existing pages `APP_PAGE`/`page_reengagement` kinds counts. Durable, table-derived **rate limit** (count own recent rows in a config window, **AC8**). The AS-3 repoint is a single **`PublishedNotice → Notice` adapter** so the dependency graph stays a **DAG** (no `subscriptions↔updates` cycle — verified module-by-module). 5 config tunables + 6 `UPDATES_*` counters (feed-producer health reuses the existing `SUBSCRIPTION_NOTICE_DEGRADED`). Full **AC1–AC8 → design-element map**, per-component failure modes, assumption ledger (**L1–L8 all verified vs code**), 6 rejected alternatives (push/fan-out, provider registry, direct `Notice` import, soft-delete, cache-window limit, model-less). Logged **DU-DESIGN-1…6 (PROPOSED)**; marked **OQ-DU-1/OQ-DU-2 RESOLVED-in-design**. Reuses **D-3/D-6/D-7 + AS-3 — no new global ADR**. Updated Current State + [INDEX.md](features/INDEX.md) (still `2-design`). **Raised DN-DU-DESIGN** (approve DESIGN); **no Stage advance until approved.** Honest rollback note: first feature to repoint a *closed* app's seam → rollback = seam-revert + app-removal (not a single-include removal). |
| 2026-06-24 | `1-define`→`2-design` | **Product Analyst** (DN-20 approved) — user answered **DN-20 = approve the brief + DN-20.a/b/c all as recommended**. Marked [developer-updates/FEATURE_BRIEF.md](features/developer-updates/FEATURE_BRIEF.md) **APPROVED**; promoted **DU-1** (early-access = a notice *kind*), **DU-2** (MVP distribution = in-platform followed-apps feed only), **DU-3** (no score-bearing emit on post + rate-limited) **→ RESOLVED**; performed the Stage-1→2 hand-off **only** (one persona/session). **OQ-DU-1** (additive, bounded reverse-audience read on `apps/subscriptions`) + **OQ-DU-2** (transparency-line verification, vision Open Q#5) carry **OPEN for Stage 2**. Set `Stage: 2-design` + Persona **Software Architect** in Current State; **cleared DN-20** from Decisions Needed; added the Decisions-Made digest; updated [INDEX.md](features/INDEX.md) (→ `2-design`). **Separately (user request, NOT a scope change):** recorded the user's **future monetization plan** — a **per-app subscription** where a developer toggles paid status per app — and **flagged that "pay for curation/visibility" as framed sells position**, which the platform premise forbids (vision §2/§4/§5.6/§8 + the money-buys-position test). Recommended the **position-neutral reframe** (the per-app subscription buys **tools**, never reach; curation stays **earned/free**) + the §5.6 ranking-inputs/ToS safeguard. **No change to the developer-updates scope** (billing stays out of scope per the approved brief); monetization is a **future feature** (Coordinator/D2 call). Captured as a project memory. Advanced to `2-design`; handed to the **Software Architect** to draft [DESIGN.md](features/developer-updates/DESIGN.md) (resolve OQ-DU-1 + OQ-DU-2 first). |
| 2026-06-24 | `1-define` (awaiting DN-20) | **Product Analyst** — drafted [developer-updates/FEATURE_BRIEF.md](features/developer-updates/FEATURE_BRIEF.md): the Phase-3 **developer→follower channel** (post **update** / **early-access** notices to an app's **existing** followers; the producer for the empty-until-producer **AS-3** notice seam). **5 stories / 8 G-W-T ACs (AC1–AC8) / 6 metrics**; all domain terms defined; explicit in/out-of-scope. **Grounded every dependency in code** (read the files): AS-3 producer seam = [`apps/subscriptions/notices.py`](apps/subscriptions/notices.py) (`Notice` DTO {`app_id`, `kind`∈{`update`,`early_access`}, `title`, `summary`, `published_at`} + `notices_for_apps(app_ids)->[]`, the single repoint point — contract already pinned); owner-scoping = `catalog.get_owned_app`/`list_owned_apps` (D-6, AC1); dev-role gate = `accounts.roles.DEVELOPER`+`permissions.require_role` (D-3, AC1); audience store = `subscriptions_subscription(user, app_id)` — **confirmed gap:** only user-scoped reads exist (`is_following`/`followed_apps`), **no reverse "who follows X" selector** → **OQ-DU-1** (additive, bounded read for Stage 2); returns already captured by `signal-capture` via existing kinds (`APP_PAGE`/`page_reengagement`), **no new `EventKind`/`Surface`** needed. **Money-buys-position test → PASS** (reaches only opt-in followers, injects no impression, **M5=0**). Logged **DU-1/DU-2/DU-3 (PROPOSED)** + **OQ-DU-1**/**OQ-DU-2** (transparency-line verification, vision Open Q#5). Out of scope: email/push, update re-boosts (no allocation lever at MVP), early-access entitlement enforcement, two-way messaging, billing, reception display (= `developer-dashboard`). Reuses **D-3/D-6/D-7 + the AS-3 contract — no new global ADR**. Updated [INDEX.md](features/INDEX.md). **Raised DN-20** (approve brief + 3 bundled scoping calls a/b/c); **no Stage advance until approved.** |

> Older rows (the `developer-dashboard`, `open-search-browse`, and `interest-profile`
> lifecycles, the `app-subscriptions` close-out + release/build/plan/define/design, the
> `ratings-reviews`/`app-pages`/`signal-capture`/`submission-intake`/`interest-taxonomy`/
> `identity-accounts` lifecycles) live in [process/activity-archive.md](process/activity-archive.md).
