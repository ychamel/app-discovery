# CONTROL.md — Project Control Board

**This is the durable channel between the user and the agent.** It replaces chat
memory. The agent reads it first every session and updates it last. The user checks
this one file to know *where we are* and *what is waiting on a decision*.

Rules:
- The agent **must not** act on a question listed under *Decisions Needed From You*
  until the user answers it here.
- Every work session ends by updating *Current State* and the *Activity Log*.
- Keep it short and current. Detail lives in the feature artifacts, not here.

---

## Current State

| Field              | Value                                                            |
|--------------------|------------------------------------------------------------------|
| **Active feature** | **`platform-staging`** — execute global [D-11](DECISIONS.md): stand up a reachable **staging** environment, deploy the code-complete developer wedge, and walk it end-to-end as **user / developer / admin on web + mobile**; polish + make the server-rendered Django templates ([D-4](DECISIONS.md)) responsive; exercise the deferred deploy work (hosting, domain, the stubbed D-4 email provider, monitoring). Live recruitment (vision §5.4 step 3) follows once staging validates; SPA split deferred + evidence-gated (D-11). `editorial-curation-tools` + `weekly-digest` + D-9 monetization stay **backlog** (density-gated, D-10). |
| **Stage**          | `1-define` — **brief DRAFTED, at the approval gate.** [FEATURE_BRIEF.md](features/platform-staging/FEATURE_BRIEF.md) authored (6 stories, AC1.1–AC6.2 in Given/When/Then, M1–M7, explicit in/out scope, constraints C1–C6 + assumptions, risks). **Blocked on DN-PS-BRIEF** (PS-1/2/3 below). On approval → `2-design` (Software Architect). |
| **Persona**        | **Product Analyst** (Stage 1, [phase-1-product-analyst.md](process/personas/phase-1-product-analyst.md)) — **stopped at the gate.** |
| **Folder**         | [features/platform-staging/](features/platform-staging/) (7 artifacts seeded; brief pending). Prior feature [widget-conversion-attribution](features/widget-conversion-attribution/) released local/dev — **closed-out**. |
| **Last updated**   | 2026-06-27 (Coordinator — scaffolded `platform-staging` from ratified [D-11](DECISIONS.md), added [INDEX.md](features/INDEX.md) row, set `Stage: 1-define`; no code) |

> **Design in one line:** a **first-party, signed, source-only cookie** `widget_src`
> set on the click-through 302 (the click is a `target="_top"` top-level nav onto the
> platform origin → first-party from birth) carries the source app-id (public id +
> flags, **never a person**) through the anonymous click → app page → conversion;
> conversions are tallied in a **separate, PII-free `widget_conversion_count`** rollup
> via **fail-soft hooks** at the follow + register views; the dashboard widget slot
> gains a conversions funnel stage + M2 rate. AC6 firewall stays structural; no consent
> banner under the no-PII posture (one-call consent gate is a documented contingency,
> revisit with counsel pre-EU-prod). Full rationale in the feature [DECISIONS.md](features/widget-conversion-attribution/DECISIONS.md).

> **Closed-out features** (Stage 6 skipped per the standing pattern — reopenable; all
> released local/dev, rollback rehearsed; live-metrics/outcome reports deferred until a
> prod target exists). Full record in each feature's `DECISIONS.md` / `RELEASE_NOTES.md`
> and in [features/INDEX.md](features/INDEX.md):
> `identity-accounts` (108) · `interest-taxonomy` (184, D-5) · `submission-intake` (315, D-6) ·
> `signal-capture` (374, D-7) · `app-pages` (417) · `ratings-reviews` (486, D-8) ·
> `app-subscriptions` (552, AS-3 seam) · `interest-profile` (616) · `open-search-browse` (676) ·
> `developer-dashboard` (740) · `developer-updates` (828, AS-3 producer, DU-REL-1) ·
> `embeddable-update-widget` (893, AC6 firewall structural, DU-REL-1 rollback).

> Canonical stage values: `0-coordinator` · `1-define` · `2-design` · `3-plan` ·
> `4-build` · `5-release` · `6-post-release` · `done`. See the routing table in
> [CLAUDE.md](CLAUDE.md) §2.

### Latest session status (CLAUDE.md §6.7 — overwritten each session)

```
Stage: 1-define | Feature: platform-staging | Persona: Product Analyst (Stage 1) — stopped at the brief-approval gate
Done: Authored features/platform-staging/FEATURE_BRIEF.md from D-11 + STRATEGY.md + vision §5.4 step 3. Problem (wedge code-complete but never reachable — unvalidated thesis, unexercised deploy path),
      goal, 6 user stories, acceptance criteria AC1.1–AC6.2 (all Given/When/Then; each marked agent-verifiable vs. human-judgment), metrics M1–M7, explicit in/out scope, constraints C1–C6 +
      assumptions A1–A3, top-5 risks, vision alignment. Grounded deploy constraints in code (verified: 12-factor config, security gated on not DEBUG, email = console stub, NO STATIC_ROOT/deploy
      manifest exists, PG-only). Surfaced 3 user-business decisions as gate DN-PS-BRIEF (budget/posture, email provider, UX sign-off owner); logged them in the feature OPEN_QUESTIONS.md (PS-1/2/3).
      No architecture/schema/UI proposed (Stage-2 job).
Verified by: n/a (Stage-1 definition only — no code touched).
Blocked/Deferred: BLOCKED on DN-PS-BRIEF (PS-1/2/3) — user approval of the brief + the 3 scoping calls. Held-back bets unchanged: network/weekly-digest + editorial-curation-tools backlog
      (D-10, density-gated); D-9 monetization ratified-but-unbuilt; SPA split deferred + evidence-gated (D-11, this feature only gathers the evidence). ePrivacy consent revisit pre-EU-prod.
Decisions needed: DN-PS-BRIEF (PS-1 budget & staging posture; PS-2 email provider; PS-3 UX walkthrough sign-off owner) — see Decisions Needed From You.
Next: User answers DN-PS-BRIEF (approve/amend the brief + PS-1/2/3). On approval → set Stage 2-design, persona Software Architect; promote the brief to APPROVED. Stop until answered.
```

---

## Decisions Needed From You

The agent is blocked on these. Answer inline (edit the **Answer** cell), then the agent
proceeds.

### DN-PS-BRIEF — approve the `platform-staging` brief (+ 3 scoping calls) — **OPEN**

Approve (or amend) [features/platform-staging/FEATURE_BRIEF.md](features/platform-staging/FEATURE_BRIEF.md),
and answer the three calls it depends on. The Product Analyst is stopped here.

| # | Question | Why it matters | Answer |
|---|----------|----------------|--------|
| **PS-1** | **Budget & staging posture** — free-tier/low-cost acceptable, or is there a budget? And is staging meant to be **promoted to production later**, or **throwaway** (rebuilt for the real cutover)? | Frames the hosting/Postgres/email tier and whether deploy work targets disposability or durability. *(Specific provider/domain stay a Stage-2 architecture choice per [D-11](DECISIONS.md)/[D-2](DECISIONS.md).)* | start with free tier, when deploying we'll start with a monthly budget of 20-100 dollars. Staging is meant to be promoted to production later|
| **PS-2** | **Email provider** — is a real transactional-email account available (which one)? | Magic-link (passwordless) auth + metric M5 are **blocked** without a real transport; the whole journey starts at sign-in (AC3.1/AC4.1). | currently no email provider exists, I'd love an md file describing how to set one up. |
| **PS-3** | **UX walkthrough sign-off owner** — who performs/signs off the human-judgment walkthrough (AC3.2/AC4.2/AC6) on a real device — you, or a delegate? | The agent can build the env, script the walkthrough, and run all agent-verifiable checks, but UX-credibility judgment is inherently human. | I will perform the walkthrough on my end. Just give a suggested walkthrough in an md file|

**Brief-level approval:** is the brief itself (stories / AC / scope / metrics) approved as
written? Note any change inline.

*(Recently resolved: **DN-STRAT-1** 2026-06-27 → [D-11](DECISIONS.md); **DN-WCA-DESIGN**
approved 2026-06-27; **DN-WCA-BRIEF** approved 2026-06-26 — see *Decisions Made* below.)*

---

## Decisions Made (recently)

A short, human-readable digest. Full rationale lives in [DECISIONS.md](DECISIONS.md)
(global) or `features/<slug>/DECISIONS.md` (local). Only the **active** feature is kept
in full; closed features collapse to a single pointer line.

- **`platform-staging` scaffolded → Stage 1 (Coordinator, 2026-06-27)** — turned the ratified [D-11](DECISIONS.md) staging-validation bet into a feature folder: created [features/platform-staging/](features/platform-staging/) with the 7 standard artifacts seeded (brief carries the D-11/[STRATEGY.md](STRATEGY.md) upstream pointer + one-line intent; rest are stage-pending), added the [INDEX.md](features/INDEX.md) row (Phase 4 Validation), set `Stage: 1-define` + Persona **Product Analyst**. Held bets (network / `weekly-digest` / `editorial-curation-tools` / D-9) stay backlog, density-gated. Handed to the **Product Analyst** to author the brief.
- **Next bet RATIFIED → staging-validation-before-live (Strategist, DN-STRAT-1, 2026-06-27 → global [D-11](DECISIONS.md))** — with the developer wedge code-complete but **all 13 features local/dev only** (nothing has touched a real developer/user; H1/H2/H3 + the §8 one-line test unvalidated) and every standing-fork option (D-9 / network / editorial) **density-gated → premature**, the user ratified: the next bet is **not another feature and not yet a public launch** — **stand up staging, deploy the wedge, and walk it end-to-end as user/developer/admin on web + mobile** (also exercising the deferred deploy work: hosting, domain, the stubbed D-4 email provider, monitoring). Live recruitment (§5.4 step 3) follows once staging validates. **Frontend:** keep server-rendered Django templates ([D-4](DECISIONS.md)), polish + make responsive; a dedicated-SPA split is **deferred + evidence-gated** (only if the walkthrough proves a surface needs it, as an explicit D-4-revisit). Network/D-9/editorial stay density-gated, unchanged. Living picture in [STRATEGY.md](STRATEGY.md). Handed to the **Coordinator** to scope (suggested slug `platform-staging`).
- **`widget-conversion-attribution` RELEASED → closed (Release Engineer, 2026-06-27, 962 tests)** — released to local/dev and wrote [RELEASE_NOTES.md](features/widget-conversion-attribution/RELEASE_NOTES.md); build independently re-verified (962 green, `ruff`/`check`/no drift); migration `widget/0002` down→up clean. **Rollback rehearsed (hard gate):** `git revert 5df8daa` removes the new `source`/`rollup`/`kinds` modules + the migration + the two fail-soft hooks + the dashboard funnel slot atomically, `manage.py check` clean with **no dangling `subscriptions`/`accounts`/`dashboard → widget` import** (**DU-REL-1** proven). The developer-wedge funnel (impression → click → conversion) is now **complete**. Stage-6 retrospective + the live-metrics window (M1–M6) deferred until a prod target exists; ePrivacy consent revisit pre-EU-prod. Handed to the **Coordinator**.
- **`widget-conversion-attribution` DESIGN APPROVED → Stage 3 (DN-WCA-DESIGN, 2026-06-27)** — ratified [DESIGN.md](features/widget-conversion-attribution/DESIGN.md) and promoted **WCA-DESIGN-1…8 → RATIFIED**: a first-party signed source-only cookie `widget_src` (no person field) carries the source through the anonymous click → app page → conversion; conversions tallied in a **separate PII-free `widget_conversion_count`** rollup via **fail-soft hooks** at [`subscriptions.views.follow`](apps/subscriptions/views.py) + [`accounts.views.register`](apps/accounts/views.py) — never breaks a follow/registration/reach count (AC6), adds nothing to `record_subscribe` (AC5). **AC6 firewall stays structural** (`apps/widget` imports no `signals`; AST test extends to the new `source` module); **AC4 no-PII by construction**. ePrivacy resolved **as recommended — no consent banner** under the no-PII WCA-3 posture (one-call consent gate is a documented contingency, revisit with counsel pre-EU-prod). Marked **OQ-WCA-2/3/4 → RESOLVED**. Reuses D-3/D-4/D-6/D-7/D-8/D-9/D-10 — **no new global ADR**. Rollback = single `git revert` of the build commit (DU-REL-1). Handed to the **Planner / Tech Lead**.
- **`widget-conversion-attribution` brief APPROVED → Stage 2 (DN-WCA-BRIEF, 2026-06-26)** — approved the brief + three scoping calls: **WCA-1** conversion set = new **follow** + new **account** (distinct counts); **WCA-2** = **last-touch** within a bounded **~30-day** (config) window; **WCA-3** = **aggregate-only, source-keyed** (no per-person profile, no PII, no consent banner; aggregate-only-infeasible returns as a decision). Token-carry mechanism left **OPEN** as OQ-WCA-2/3/4 for the Architect. Reuses D-3/D-4/D-6/D-7/D-8 — **no new global ADR**.
- **Next feature = `widget-conversion-attribution` (Coordinator, 2026-06-26)** — with the **developer wedge ([D-10](DECISIONS.md)) complete**, user picked **conversion attribution (M3)** (the deferred OQ-EUW-5) over D-9 monetization / turning on the network. Scaffolded the 7-artifact folder; deferred (not dropped) D-9 monetization; kept `weekly-digest` + `editorial-curation-tools` backlog. Handed to the **Product Analyst**.
- **`embeddable-update-widget` RELEASED → closed (Release Engineer, 2026-06-26, 893 tests)** — released to local/dev; rollback rehearsed (single `git revert b7db60f`, **DU-REL-1** proven). Stage-6 retrospective + M3 conversion (OQ-EUW-5 → this feature) deferred. **The developer wedge (D-10) is now complete** (`app-pages` + `developer-updates` + the widget all shipped). Detail: [embeddable-update-widget/DECISIONS.md](features/embeddable-update-widget/DECISIONS.md).
- **Developer-wedge pivot — global D-10 + D-9 (Coordinator, 2026-06-26)** — **D-10:** MVP cold-start re-sequenced to a single-player **developer hub** (app page + changelog + embeddable widget) that drags a developer's existing audience in; the discovery network/digest is **held until per-niche density**. **D-9:** monetization = paid, labeled, time-boxed **promotion placements** sold for reach, **firewalled from the ranking** (a `Surface` outside `CURATED_SURFACES`); tools + curation stay free. **Paused `editorial-curation-tools` → backlog.** Full text in [DECISIONS.md](DECISIONS.md).

**Closed-feature pointers** (full lifecycle in each folder's `DECISIONS.md`/`RELEASE_NOTES.md`):

- **`developer-updates`** (closed 2026-06-24, 828 tests) — new `apps/updates/` owning `updates_notice`, the single **AS-3 producer**: pull delivery (M5=0 structural, no fan-out), imports no `signals` (AST-enforced), durable table-derived rate limit; first to repoint a *closed* app's seam → rollback = `git revert` of the build commit (**DU-REL-1**). Detail: [developer-updates/DECISIONS.md](features/developer-updates/DECISIONS.md).
- **`developer-dashboard`** (closed 2026-06-24, 740 tests) — model-less `apps/dashboard/`, a read-only view of an app's reception (D-7 reach/engagement + D-8 curated ratings) for its owner; two additive neutral `signals.selectors` reads, no schema. Detail: [developer-dashboard/DECISIONS.md](features/developer-dashboard/DECISIONS.md).
- **`open-search-browse`** (closed 2026-06-23, 676 tests) — `apps/discovery/`, the paginated DB-pushed `catalog.search_catalogue` open surface where money never buys position. Detail: [open-search-browse/DECISIONS.md](features/open-search-browse/DECISIONS.md).
- **`interest-profile`** (closed 2026-06-23, 616 tests) — `apps/interests/`, the user side of the Ring-0 match via the `declared_tag_ids` matcher contract. Detail: [interest-profile/DECISIONS.md](features/interest-profile/DECISIONS.md).
- **`app-subscriptions`** (closed 2026-06-21, 552 tests) — `apps/subscriptions/`, atomic follow + one `subscribe` emit, the empty-until-producer notice seam **AS-3**. Detail: [app-subscriptions/DECISIONS.md](features/app-subscriptions/DECISIONS.md).
- **`ratings-reviews`** (closed 2026-06-21, 486 tests) — `apps/ratings/`; established the global **[D-8](DECISIONS.md)** curated-rating gate ("curated = a `DIGEST` impression"). Detail: [ratings-reviews/DECISIONS.md](features/ratings-reviews/DECISIONS.md).
- **`app-pages`** (closed 2026-06-20, 417 tests) — `apps/pages/`, first live emitter of D-7 `app_page` impressions. Detail: [app-pages/DECISIONS.md](features/app-pages/DECISIONS.md).
- **`signal-capture`** (closed 2026-06-18, 374 tests) — `apps/signals/`; established the global **[D-7](DECISIONS.md)** event schema (append-only, structural no-score, no-PII). Detail: [signal-capture/DECISIONS.md](features/signal-capture/DECISIONS.md).
- **`submission-intake`** (closed 2026-06-18, 315 tests) — `apps/catalog/`, the ACCEPTED-only catalogue substrate = global **[D-6](DECISIONS.md)**. Detail: [submission-intake/DECISIONS.md](features/submission-intake/DECISIONS.md).
- **`interest-taxonomy`** (closed 2026-06-17, 184 tests) — flat tags + clusters, soft-retire + read-time `resolve_tag` = global **[D-5](DECISIONS.md)**. Detail: [interest-taxonomy/DECISIONS.md](features/interest-taxonomy/DECISIONS.md).
- **`identity-accounts`** (closed 2026-06-17, 108 tests) — the foundation (the stack **D-4**, roles **D-3**). Detail: [DECISIONS.md](DECISIONS.md).

> **Foundational decisions** (the stack, niche, build order — D1–D3, A1–A3, R1, global
> D-1/D-2/D-4) live in [DECISIONS.md](DECISIONS.md). Highlights: niche = **"vibecoded
> webapps"** (D-1); stack = **Python / Django + PostgreSQL** (D-4).

---

## Activity Log

Newest first. One line per session. **Keep the most recent ~10 rows here**; older rows
live in [process/activity-archive.md](process/activity-archive.md). The per-feature
folders remain the full record either way.

| Date       | Stage           | Summary                                                                 |
|------------|-----------------|-------------------------------------------------------------------------|
| 2026-06-27 | `1-define` (awaiting DN-PS-BRIEF) | **Product Analyst** — authored [features/platform-staging/FEATURE_BRIEF.md](features/platform-staging/FEATURE_BRIEF.md) from [D-11](DECISIONS.md) + [STRATEGY.md](STRATEGY.md) + vision §5.4 step 3: the problem (wedge code-complete but **never reachable** → unvalidated thesis + unexercised deploy path), 6 user stories, acceptance criteria **AC1.1–AC6.2** (all Given/When/Then, each tagged *agent-verifiable* vs *human-judgment*), metrics **M1–M7**, explicit in/out scope, constraints **C1–C6** (grounded in code: 12-factor config, security gated on `not DEBUG`, email = console stub, **no `STATIC_ROOT`/deploy manifest exists**, PG-only) + assumptions A1–A3, top-5 risks, vision alignment. No architecture/schema/UI (Stage-2). Surfaced 3 user-business scoping calls as gate **DN-PS-BRIEF** (PS-1 budget/posture, PS-2 email provider, PS-3 UX sign-off owner); logged PS-1/2/3 in [OPEN_QUESTIONS.md](features/platform-staging/OPEN_QUESTIONS.md). **Raised DN-PS-BRIEF; stopped at the gate.** |
| 2026-06-27 | `0-coordinator`→`1-define` | **Coordinator** (§4) — scaffolded the ratified [D-11](DECISIONS.md) staging-validation bet into a feature. Created [features/platform-staging/](features/platform-staging/) with all 7 standard artifacts seeded (brief carries the D-11/[STRATEGY.md](STRATEGY.md) upstream pointer + one-line intent; the rest stage-pending), added the [INDEX.md](features/INDEX.md) row (Phase 4 Validation, started 2026-06-27). Set Active feature = `platform-staging`, `Stage: 1-define`, Persona **Product Analyst**. Held bets (network / `weekly-digest` / `editorial-curation-tools` / D-9) stay backlog (density-gated). Did **not** scope stories/AC (Product Analyst's job). No code. Handed to the **Product Analyst**. |
| 2026-06-27 | `6-post-release` (off-pipeline) | **Strategist** (§4.1, user-invoked) — post-wedge next-bet analysis + **ratification** (DN-STRAT-1 → global **[D-11](DECISIONS.md)**). Finding: 13 features built but **all local/dev only** — nothing has touched a real developer/user, so H1/H2/H3 + the §8 one-line test are unvalidated, and **every standing-fork option (D-9 / network / editorial) is density-gated → premature**. **Ratified next bet:** not another feature and not yet a public launch — **stand up staging, deploy the wedge, walk it end-to-end as user/developer/admin on web + mobile** (also exercises the deferred deploy work: hosting, domain, the stubbed D-4 email provider, monitoring); live recruitment (§5.4 step 3) follows once staging validates. **Frontend ratified:** keep server-rendered Django templates ([D-4](DECISIONS.md)), polish + make responsive; dedicated-SPA split **deferred + evidence-gated** (D-11). Recorded **D-11**, created/updated [STRATEGY.md](STRATEGY.md), resolved DN-STRAT-1. **No Stage change, no code.** Handed to the **Coordinator** to scope (suggested slug `platform-staging`). |
| 2026-06-27 | `5-release`→`6-post-release` | **Release Engineer** — **released `widget-conversion-attribution` to local/dev** and wrote [RELEASE_NOTES.md](features/widget-conversion-attribution/RELEASE_NOTES.md). Re-verified the build independently (**962 tests green**, `ruff`/`check`/no drift). **Rehearsed rollback** (hard gate): migration `widget/0002` down→up clean on the live PG cluster; `git revert --no-commit 5df8daa` removed the `source`/`rollup`/`kinds` modules + migration `0002` + the two fail-soft hooks ([`subscriptions.views.follow`](apps/subscriptions/views.py), [`accounts.views.register`](apps/accounts/views.py)) + the dashboard funnel slot in one step, `manage.py check` then **clean with no dangling `subscriptions`/`accounts`/`dashboard → widget` import** (**DU-REL-1** proven); restored to `5df8daa` (git clean). Promotion stops at local/dev; Stage-6 retrospective + M1–M6 outcome review deferred until a prod target exists; ePrivacy consent revisit pre-EU-prod. Updated [INDEX.md](features/INDEX.md) (→ closed-out). **The developer-wedge funnel (impression → click → conversion) is now complete.** Handed to the **Coordinator**. |
| 2026-06-27 | `4-build`→`5-release` | **Senior Engineer** — built **T-01…T-08** from the APPROVED [widget-conversion-attribution/DESIGN.md](features/widget-conversion-attribution/DESIGN.md) + [TASKS.md](features/widget-conversion-attribution/TASKS.md): new `apps/widget` `rollup` (shared `_increment_daily`, extracted from the reach writer) + `source` (the `widget_src` signed source-only cookie codec + window/dedup/credit) + the `widget_conversion_count` table (migration `0002`) + `record_widget_conversion` + the `widget_conversions[_for_apps]` selectors; `set_marker` armed on the click-through 302 + the two fail-soft hooks ([`subscriptions.views.follow`](apps/subscriptions/views.py) on `created`, [`accounts.views.register`](apps/accounts/views.py) on the 202); the dashboard Screen-B slot extended with the conversion funnel stage + derived M2 (reach + conversions degrade together). **Risk front-loaded** (AC5 firewall AST proof + writer at T-03, codec at T-04, before any HTTP). **Full suite 962 green** (+69), `ruff`/`check` clean, no drift, migration up→down→up clean; firewall re-proven structurally (M5=0), no-PII by construction (AC4), DAG preserved. CODEMAP + widget README updated; impl notes WCA-IMPL-1/2/3 logged; [TEST_PLAN.md](features/widget-conversion-attribution/TEST_PLAN.md) maps every AC1–AC6. Updated [INDEX.md](features/INDEX.md) (→ 5-release). Handed to the **Release Engineer**. |
| 2026-06-27 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [widget-conversion-attribution/DESIGN.md](features/widget-conversion-attribution/DESIGN.md) into [TASKS.md](features/widget-conversion-attribution/TASKS.md): **8 ordered tasks T-01…T-08, all S/M** (no L), each with a concrete DoD + files-touched, plus an AC1–AC6 / design-element coverage map. **Risk front-loaded** as named: the AC5 firewall proof (AST) + the concurrency-correct conversion writer at **T-03**, and the novel `widget_src` sign/verify/`credited`-dedup codec at **T-04** — both **before any HTTP wiring** (T-05+). Grounded every task in current code (verified `follow_app` returns `created`, the register 202 path, the reach writer/selector/dashboard-slot patterns being mirrored). Updated [INDEX.md](features/INDEX.md) (→ 4-build). Hand-off only (one persona/session, no re-design). Handed to the **Senior Engineer** to build T-01 first + write [TEST_PLAN.md](features/widget-conversion-attribution/TEST_PLAN.md). |
| 2026-06-27 | `2-design`→`3-plan` | **Software Architect** (DN-WCA-DESIGN approved) — ratified [widget-conversion-attribution/DESIGN.md](features/widget-conversion-attribution/DESIGN.md) (→ APPROVED) and promoted **WCA-DESIGN-1…8 → RATIFIED**: the first-party signed `widget_src` source-only cookie + the separate PII-free `widget_conversion_count` rollup + fail-soft hooks at the follow/register views; AC6 firewall stays structural, no consent banner under the no-PII posture. Marked OQ-WCA-2/3/4 RESOLVED; recorded in the feature [DECISIONS.md](features/widget-conversion-attribution/DECISIONS.md); updated [INDEX.md](features/INDEX.md). Hand-off only (one persona/session). Handed to the **Planner / Tech Lead** to write [TASKS.md](features/widget-conversion-attribution/TASKS.md). |
| 2026-06-27 | `2-design` (awaiting DN-WCA-DESIGN) | **Software Architect** — drafted [widget-conversion-attribution/DESIGN.md](features/widget-conversion-attribution/DESIGN.md) and **resolved all three Stage-2 OQs**. Pivotal finding: the click-through `GET /widget/<id>/view` is a `target="_top"` top-level nav onto the **platform** origin, so a marker on its 302 is **first-party from birth** → dissolves cross-domain identity (OQ-WCA-3) and makes the no-PII posture achievable. Logged **WCA-DESIGN-1…8 PROPOSED**; one surfaced residual = the ePrivacy consent judgment (raised in **DN-WCA-DESIGN** with a one-call consent gate designed in as contingency, not silently relaxed). **Stopped at the gate.** |
| 2026-06-26 | `1-define`→`2-design` | **Product Analyst** (DN-WCA-BRIEF approved) — marked [widget-conversion-attribution/FEATURE_BRIEF.md](features/widget-conversion-attribution/FEATURE_BRIEF.md) **APPROVED**; promoted WCA-1/2/3 → RESOLVED (follow+account distinct; last-touch ~30-day window; aggregate-only no-PII). Left OQ-WCA-2/3/4 (token-carry mechanism) OPEN for Stage 2. Hand-off only. Handed to the **Software Architect**. |
| 2026-06-26 | `1-define` (awaiting DN-WCA-BRIEF) | **Product Analyst** — authored [widget-conversion-attribution/FEATURE_BRIEF.md](features/widget-conversion-attribution/FEATURE_BRIEF.md): the developer-wedge funnel payoff (impression → click → conversion). 5 stories / AC1–AC6 / 6 metrics incl. the M4 firewall + M5 no-PII guardrails; grounded every dependency in code. Did **not** guess the token-carry mechanism (split into OQ-WCA-2/3/4). **Raised DN-WCA-BRIEF; stopped at the gate.** |
| 2026-06-26 | `6-post-release`→`0-coordinator`→`1-define` | **Coordinator** — developer wedge ([D-10](DECISIONS.md)) **complete**; surfaced the next-feature fork; user picked **conversion attribution (M3)** = the deferred OQ-EUW-5. Scaffolded `features/widget-conversion-attribution/` (7 artifacts), added its [INDEX.md](features/INDEX.md) row, set `Stage: 1-define` + Persona **Product Analyst**. Deferred (not dropped) D-9 monetization; kept `weekly-digest` + `editorial-curation-tools` backlog. Handed to the **Product Analyst**. |
| 2026-06-26 | `5-release`→`6-post-release` | **Release Engineer** — **released `embeddable-update-widget` to local/dev** and wrote [RELEASE_NOTES.md](features/embeddable-update-widget/RELEASE_NOTES.md). Re-verified on a throwaway PG cluster (**893 tests green** twice, `ruff`/`check`/no drift). **Rehearsed rollback** (hard gate): migration up→down→up clean; `git revert b7db60f` removed `apps/widget` + the include + the dashboard edit in one step, `check` clean (no dangling `dashboard → widget` import — **DU-REL-1** proven). Promotion stops at local/dev; Stage-6 retrospective + M3 conversion deferred. **The developer wedge (D-10) is now complete.** Handed to the **Coordinator**. |
| 2026-06-26 | `4-build`→`5-release` | **Senior Engineer** — built **T-01…T-07** from the APPROVED [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md): new `apps/widget/` (the `widget_reach_count` daily-rollup table with no user/IP/score column = AC6/AC10 structural; atomic `F()`-increment writer + create-race retry; windowed reader; the two AllowAny views + framable no-JS templates + the `config/urls` `widget/` include = activation switch) + additive `apps/core` GET limiter + additive fail-soft dashboard "Widget reach" slot (AC9). **AC6 firewall proven structurally** (imports no `signals`; render+click writes 0 corpus rows; M5=0). **893 tests green**, `ruff`/`check`/no drift. One impl note **EUW-IMPL-1** (nested-`atomic()` savepoint on the retry). Handed to the **Release Engineer**. |
| 2026-06-26 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md) into [TASKS.md](features/embeddable-update-widget/TASKS.md): **7 ordered tasks T-01…T-07, all S/M**, full AC1–AC9 coverage. **Risk front-loaded at T-02** — the AC6 firewall (structural by absence) + the atomic `F()`-increment/create-race concurrency, both before any HTTP. No re-design. Handed to the **Senior Engineer** (build T-01 first + `TEST_PLAN.md`). |
| 2026-06-26 | `2-design`→`3-plan` | **Software Architect** (DN-EUW-DESIGN approved) — ratified [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md) and promoted **EUW-7…11 → RATIFIED**: embedding = a server-rendered `<iframe>` (AC7); attribution = a dedicated `apps/widget`-owned daily-rollup store that imports nothing from `signals` → the AC6 firewall (M5=0) is **structural by absence**; closed `apps/dashboard` gains a fail-soft off-platform reach slot (AC9); per-IP GET limit + `Cache-Control` (AC8); M3 per-account conversion deferred (OQ-EUW-5). **Also cleaned CONTROL.md** (collapsed closed-feature digests; moved 15 older Activity Log rows to [process/activity-archive.md](process/activity-archive.md)). Handed to the **Planner / Tech Lead**. |
| 2026-06-26 | `2-design` (awaiting DN-EUW-DESIGN) | **Software Architect** — drafted [embeddable-update-widget/DESIGN.md](features/embeddable-update-widget/DESIGN.md) and resolved all 3 OQs (EUW-7…11 PROPOSED): server-rendered `<iframe>`; a dedicated `apps/widget` store (NOT the D-7 corpus — anonymous reach can't live there cleanly) → firewall structural by absence; per-IP GET limit + `Cache-Control` TTL. M3 conversion deferred (OQ-EUW-5). **Raised DN-EUW-DESIGN; stopped at the gate.** |
| 2026-06-26 | `1-define` | **Product Analyst** — authored [embeddable-update-widget/FEATURE_BRIEF.md](features/embeddable-update-widget/FEATURE_BRIEF.md): the bring-your-own-audience capture widget (D-10 wedge) — a read-only embeddable "what's new" widget surfacing an app's notices + a link back. 6 stories / AC1–AC8 / 6 metrics; firewall fixed as a hard constraint (AC6 / M5=0). Logged EUW-1…5 PROPOSED, left 3 architecture OQs OPEN for Stage 2. **Raised DN-EUW-BRIEF; stopped at the gate.** (User refinement folded in as AC9/EUW-6: anonymous end users, but the view source is always tracked for the dashboard.) |
| 2026-06-26 | `0-coordinator` | **Coordinator** (user-directed **developer-wedge pivot**) — recorded global **[D-10](DECISIONS.md)** (developer-hub-first MVP; network/digest held until density) + **[D-9](DECISIONS.md)** (monetization = labeled time-boxed promotion placements, firewalled from the ranking; tools + curation free). Revised vision §5.4/§5.6. Paused `editorial-curation-tools` → backlog. Scaffolded + activated `embeddable-update-widget`. Handed to the **Product Analyst**. |

> Older rows (the `developer-updates` lifecycle and everything before it — the
> `developer-dashboard`, `open-search-browse`, `interest-profile`, `app-subscriptions`,
> `ratings-reviews`, `app-pages`, `signal-capture`, `submission-intake`,
> `interest-taxonomy`, `identity-accounts` lifecycles) live in
> [process/activity-archive.md](process/activity-archive.md).
