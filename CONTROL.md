# CONTROL.md — Project Control Board

**This is the durable channel between the user and the agent.** It replaces chat
memory. The agent reads it first every session and updates it last. The user checks
this one file to know *where we are* and *what is waiting on a decision*.

Rules:
- The agent **must not** act on a question listed under *Decisions Needed From You*
  until the user answers it here.
- Every work session ends by updating *Current State* and the *Activity Log*.
- Keep it short and current. Detail lives in the feature artifacts, not here.

---

## Current State

| Field              | Value                                                            |
|--------------------|------------------------------------------------------------------|
| **Active feature** | **ui-modernization** |
| **Stage**          | **`1-define`** |
| **Persona**        | **Product Analyst** (reads the activation request + vision doc; writes `FEATURE_BRIEF.md`. The carried-in activation context + the user's non-binding Stage-2 build-posture lean are seeded in the feature's [OPEN_QUESTIONS.md](features/ui-modernization/OPEN_QUESTIONS.md) + [DECISIONS.md](features/ui-modernization/DECISIONS.md)) |
| **Folder**         | [features/ui-modernization/](features/ui-modernization/) |
| **Last updated**   | 2026-06-30 (**Coordinator handoff.** The user judged `interface-cleanup`'s result unsatisfactory ("the whole UI might be scrapped") and **activated the held `ui-modernization` future bet** — the D2 call — a deep rebrand: distinctive palette + stylized/animated nav + premium motion, the design ceiling D-13 named. `interface-cleanup` **closed-out** at `4b-verify` (build was code-complete, 1104 tests; the blind verify suite + AC-8 sign-off were not authored). Scaffolded `features/ui-modernization/` + 8 seed files; seeded the carried context (D-13 revisit trigger: `app.css`=1113 lines + still bland) and the user's lean — **Tailwind standalone CLI, stay no-Node** (UM-LEAN-1, non-binding). `DN-PS-DEPLOY` re-parked behind this feature. **Stage `1-define`** → Product Analyst). |

> **Closed-out features** (Stage 6 skipped per the standing pattern — reopenable; all
> released local/dev, rollback rehearsed; live-metrics/outcome reports deferred until a
> prod target exists). Full record in each feature's `DECISIONS.md` / `RELEASE_NOTES.md`
> and in [features/INDEX.md](features/INDEX.md):
> `identity-accounts` (108) · `interest-taxonomy` (184, D-5) · `submission-intake` (315, D-6) ·
> `signal-capture` (374, D-7) · `app-pages` (417) · `ratings-reviews` (486, D-8) ·
> `app-subscriptions` (552, AS-3 seam) · `interest-profile` (616) · `open-search-browse` (676) ·
> `developer-dashboard` (740) · `developer-updates` (828, AS-3 producer, DU-REL-1) ·
> `embeddable-update-widget` (893, AC6 firewall structural, DU-REL-1 rollback) ·
> `widget-conversion-attribution` (962, first-party `widget_src` cookie, M5=0 structural) ·
> `platform-staging` (975, D-12 serving stack + shared shell; **live deploy deferred** until staging debuts on the premium UI — artifacts ready, DN-PS-DEPLOY reopenable) ·
> `premium-frontend` (980, D-13 no-build token design system + static `/` landing + vendored HTMX) ·
> `app-page-redesign` (1095, D-14 typed facets firewalled from ranking + uniform 10-slot launch page; closed-out 2026-06-30 at user direction — RELEASE_NOTES + AC-8 sign-off folded into close-out, reopenable) ·
> `interface-cleanup` (1104, closed-out 2026-06-30 at user direction at Stage `4b-verify` — build code-complete but result judged unsatisfactory; **superseded by `ui-modernization`**; blind verify + AC-8 sign-off not authored; reopenable).

> Canonical stage values: `0-coordinator` · `1-define` · `2-design` · `2b-ux` · `3-plan` ·
> `4-build` · `4b-verify` · `5-release` · `6-post-release` · `done`. (`2b-ux` is conditional
> — user-facing features only.) See the routing table in [CLAUDE.md](CLAUDE.md) §2.

### Latest session status (CLAUDE.md §6.7 — overwritten each session)

```
Stage: 1-define | Feature: ui-modernization | Persona: Coordinator → Product Analyst
Done: Coordinator handoff. Per the user's two AskUserQuestion answers:
  (1) close out interface-cleanup ("results aren't satisfactory, the whole UI might be
      scrapped"); (2) build-posture lean = Tailwind standalone CLI, stay no-Node.
  - interface-cleanup closed-out in features/INDEX.md (stage 4b-verify → closed-out,
    superseded-by note) + CONTROL.md closed-features line. Its committed code stays in
    the tree; how much survives is a ui-modernization Stage-2 call.
  - Scaffolded features/ui-modernization/ with the 8 non-conditional seed files
    (EXPERIENCE.md left for the Architect at 2b-ux hand-off, per features/README.md).
  - Seeded carried context in the feature's OPEN_QUESTIONS.md (activation framing, the
    D-13 revisit trigger: app.css=1113 lines + still bland; OQ-UM-1 deploy sequencing;
    OQ-UM-2 fate of the interface-cleanup code) and DECISIONS.md (UM-LEAN-1 = the user's
    non-binding no-Node Tailwind-CLI lean, with the npm-plugin implication spelled out).
  - Added the ui-modernization row to features/INDEX.md; set Current State + INDEX.

Verified by: n/a (coordination only — no code changed, no tests run).

Blocked/Deferred:
  - DN-PS-DEPLOY re-parked behind ui-modernization (was behind interface-cleanup).
  - interface-cleanup's blind ACCEPTANCE_TESTS.md + AC-8 sign-off intentionally not
    authored (feature closed before verify completed; reopenable).

Decisions needed: none right now. The Product Analyst's FEATURE_BRIEF.md will surface
  Stage-1 questions for the user; the build-posture ADR is a Stage-2 decision.

Next: Product Analyst (Stage 1-define) writes features/ui-modernization/FEATURE_BRIEF.md
  from the activation request + the vision doc, keeping tooling and visual-design as
  distinct concerns (bland is a design problem Tailwind alone won't fix).
```

---

## Decisions Needed From You

The agent is blocked on these. Answer inline after the `→`, then the agent proceeds.
Resolved items move down to *Decisions Made (recently)*; full rationale lives in
[DECISIONS.md](DECISIONS.md), [patches/INDEX.md](patches/INDEX.md), or the issue files.

**DN-PS-DEPLOY — REOPENED 2026-06-28 (re-parked 2026-06-30 behind `ui-modernization`).** With `premium-frontend` signed off +
closed out, the sequencing condition is met (staging now debuts on the polished UI), so the live
Render deploy of the developer wedge can proceed. The build is verified and every deploy artifact
([render.yaml](render.yaml), [docs/deploy/](docs/deploy/)) is written + secret-clean + runs
unchanged when staging is reactivated. When you're ready to deploy: run
[docs/deploy/deploy-runbook.md](docs/deploy/deploy-runbook.md) end-to-end (Render account + email
provider with a **verified sending domain** + live secrets), paste the live `https://…` URL back
here, and the agent runs the **AC1.1 / AC1.2 / AC1.3 / AC5.2** checks; the **AC6.1 / AC6.2**
walkthrough ([staging-walkthrough.md](docs/deploy/staging-walkthrough.md)) is yours (PS-3).

---

## Decisions Made (recently)

A short, human-readable digest. Full rationale lives in [DECISIONS.md](DECISIONS.md)
(global) or `features/<slug>/DECISIONS.md` (local). Only the **active** feature is kept
in full; closed features collapse to a single pointer line.

- **`ui-modernization` ACTIVATED + `interface-cleanup` CLOSED-OUT (2026-06-30)** — the user reported `app.css` is *"growing too big"* (1113 lines) and the result *"still bland,"* and via **AskUserQuestion** (a) directed closing out `interface-cleanup` ("its results aren't satisfactory, the whole UI might be scrapped") and (b) leaned **Tailwind via the standalone CLI, stay no-Node** for the rebuild. The Coordinator recognised this as the **D2 activation of the held `ui-modernization` future bet** (the design ceiling D-13 named) **and** the firing of D-13's pre-installed **revisit trigger** (so the Stage-2 build-posture choice reverses/extends global **[D-13](DECISIONS.md)** → a new global ADR). Closed `interface-cleanup` at `4b-verify` (build code-complete, 1104 tests; blind verify + AC-8 sign-off **not** authored — reopenable); scaffolded `features/ui-modernization/` with carried context (OQ-UM-1/2) + the user's non-binding lean (**UM-LEAN-1** — note: no-Node rules out npm-only DaisyUI/Flowbite/Preline; Alpine/Swiper/GSAP can be vendored as plain JS). Key framing recorded for the Analyst: **"bland" is a design problem Tailwind alone won't fix** (D-13). `DN-PS-DEPLOY` re-parked behind it. **→ Stage `1-define`** (Product Analyst). Detail: [features/ui-modernization/DECISIONS.md](features/ui-modernization/DECISIONS.md).
- **`interface-cleanup` TASKS.md written → Stage 4-build (2026-06-30)** — the Planner / Tech Lead decomposed the 8 DESIGN workstreams + the S1–S5/X1–X4 EXPERIENCE feel spec into **12 ordered S/M tasks T-01…T-12** (no `L`) in [TASKS.md](features/interface-cleanup/TASKS.md): W1 tokens/defects first (T-01), the W8 enumeration guard with it (T-02), shared artifacts before consumers, each UI task carrying its feel into its DoD, the final gate (T-12) carrying the AC-8 sign-off checklist to release. OQ-IC-8 (picker view-context) gated at T-10 under C2 — the Engineer's call, recorded in DECISIONS.md. No design gap, no new decision needed → **advanced to `4-build`** (Senior Engineer).
- **DN-IC-BRIEF → approved (2026-06-30)** — the user approved the `interface-cleanup` [FEATURE_BRIEF.md](features/interface-cleanup/FEATURE_BRIEF.md) → advanced to Stage 2. The Software Architect wrote [DESIGN.md](features/interface-cleanup/DESIGN.md): **8 presentation-only workstreams**, two reusable artifacts (a `{% icon %}` inline-SVG tag + a design-system **enumeration guard** that makes the silent-defect class unrepresentable), **no schema/API/view/URL/ADR change** (IC-DESIGN-1…9). One user call via **AskUserQuestion** → a unified **"Developer" hub** with Manage|Analytics tabs ([IC-DESIGN-5](features/interface-cleanup/DECISIONS.md)). The app-page mobile-Try fix uses CSS `order:` only, preserving the `data-slot` source-order fingerprint + M5=0 firewall invariants ([IC-D-3](features/interface-cleanup/DECISIONS.md)). The one envelope boundary (the W7 picker view-context touch) is surfaced as **OQ-IC-8** for Planner confirmation under the C2 gate — not a user block. **User-facing → advanced to `2b-ux`** (Experience Designer). Detail: [features/interface-cleanup/DECISIONS.md](features/interface-cleanup/DECISIONS.md).
- **DN-APR-DESIGN → approved (2026-06-29)** — the user approved the `app-page-redesign` [DESIGN.md](features/app-page-redesign/DESIGN.md). **APR-DESIGN-1/2 promoted to global [D-14](DECISIONS.md)** (a: typed facets are code-fixed structured fields firewalled from ranking; b: re-review policy for the new public-claim fields is config-togglable). The Planner wrote [TASKS.md](features/app-page-redesign/TASKS.md) (**9 ordered S/M tasks T-01…T-09**, risk front-loaded, full AC coverage) → advanced to **Stage 4-build** (Senior Engineer). One additive migration (Feature Track, C4). Detail: [features/app-page-redesign/DECISIONS.md](features/app-page-redesign/DECISIONS.md).
- **DN-APR-BRIEF → approved (2026-06-29)** — the user approved the `app-page-redesign` [FEATURE_BRIEF.md](features/app-page-redesign/FEATURE_BRIEF.md) → advanced to Stage 2; the Architect wrote [DESIGN.md](features/app-page-redesign/DESIGN.md). Two Stage-2 user calls taken: **APR-DESIGN-1** (typed facets = code-fixed structured fields, firewalled from ranking; the existing category tags stay D-5 taxonomy — resolves OQ1) + **APR-DESIGN-2** (re-review for the new public-claim fields is **config-togglable**, default on — the user's refinement). Both → global **D-14** on DESIGN approval. Detail: [features/app-page-redesign/DECISIONS.md](features/app-page-redesign/DECISIONS.md).
- **DN-Q004 → Option C (2026-06-29, answered — queued, not yet built)** — apps show **two distinct review scores**: curated-only on the catalog, curator + community on the detail page. Likely Feature-Track work (computed aggregates); queued behind `app-page-redesign`. Context: [`Q-004`](issues/Q-004.md).
- **Patch-plan approvals resolved → built + closed out by the Maintenance Engineer** (full record in [patches/INDEX.md](patches/INDEX.md) + the issue files): **DN-BSI-PLAN** (`patch-block-self-interaction`; Q-002 + Q-003, 1013 tests) · **DN-MAS-PLAN** (`patch-my-apps-status-display`; UX-004 + UX-006, 1004 tests) · **DN-BUG004-PLAN** (BUG-004, 1000 tests) · **DN-DWL-PLAN** (BUG-003, 999 tests) · **DN-PIPD-PLAN** (BUG-001, 998 tests) · **DN-PFA-PLAN** (BUG-002, 997 tests) · **DN-UX003-PLAN** (UX-003). Scope decisions: **DN-Q002/DN-Q003** → block self-review/self-follow · **DN-Q001-TAXONOMY** → keep duplicate (schema unchanged) · **DN-UX002-SCOPE** → minimal Patch Track. Premium-frontend chain: **DN-PF-SIGNOFF / DN-PF-DESIGN ([D-13](DECISIONS.md)) / DN-PF-BRIEF**; staging chain: **DN-PS-DESIGN ([D-12](DECISIONS.md)) / DN-PS-BRIEF / DN-STRAT-1 ([D-11](DECISIONS.md))**.
- **`premium-frontend` SIGNED OFF + CLOSED-OUT → live deploy reopened (2026-06-28)** — the user gave the **M7 premium visual sign-off** (DN-PF-SIGNOFF): the landing / app page / discover surfaces read as a **polished product, not a prototype**. They noted the palette is still bland with no stylized/animated nav and scoped that **deeper modernization to a future Feature-Track enhancement** (`ui-modernization` — distinctive palette + stylized/animated nav + premium motion; needs a full Stage-1→2 design), explicitly **not** an `issues/` item and **not** a re-style of `premium-frontend`. Closed the feature out in [INDEX.md](features/INDEX.md) (released local/dev; build re-verified at release — 980 tests / no drift / ruff / check; rollback rehearsed, no migration) and **reopened [DN-PS-DEPLOY](#decisions-needed-from-you)** (sequencing condition met). `ui-modernization` queued as a held future bet (activation = a user D2 call). Held density-gated bets (network / `weekly-digest` / `editorial-curation-tools` / D-9) unchanged.
- **`premium-frontend` design + build (2026-06-28 → global [D-13](DECISIONS.md))** — the premium gap was **design depth, not tooling**, so the hand-authored [`core/app.css`](apps/core/static/core/app.css) was deepened into a full token-driven design system (palette/states/elevation, type + spacing scales, motion, a component set, 3 breakpoints) in the same one WhiteNoise-hashable file — **no Tailwind/Node/build step** (Option A; Tailwind-via-CLI kept as an outcome-gated reversible fallback). D-13 affirms + extends [D-12](DECISIONS.md)'s build-free posture. Also: restyled shared shell (additive `{% block body_class %}` + skip-link), a new **static `/` landing** ([`core.views.landing`](apps/core/views.py), no DB → closes PF-CARRY-1/AC-3), an app-page + discover presentation-only restyle (slots/order/SEO unchanged), and **vendored** HTMX + `hx-boost` (light PE, fully degradable). Widget firewall (M5=0), no-JS, SEO, a11y, no-schema preserved by construction.
- **`platform-staging` RELEASED local/dev + closed-out (2026-06-28 → global [D-12](DECISIONS.md))** — the full T-01…T-11 serving stack + shared shell: gunicorn + WhiteNoise + `dj-database-url`, non-`DEBUG` media route, the [render.yaml](render.yaml) Blueprint, Resend SMTP wiring + setup `.md`, the shared responsive shell consolidating the 6 `base.html` (**widget firewall held**), the DB-only `/health/live` split, the `REDIS_URL` cache, env-gated Sentry, the runbook + walkthrough. Build complete + verified local/dev (975 tests / ruff / check / no drift / secret-clean); **live deploy deliberately deferred** to land `premium-frontend` first so staging debuts polished. DN-PS-DEPLOY parked → since reopened.
- **Next bet RATIFIED → staging-validation-before-live (Strategist, 2026-06-27 → global [D-11](DECISIONS.md))** — with the developer wedge code-complete but **all 13 features local/dev only** (H1/H2/H3 + the §8 one-line test unvalidated) and every standing-fork option (D-9 / network / editorial) density-gated → premature, the next bet is **not another feature and not yet a public launch** — stand up staging, deploy the wedge, and walk it end-to-end as user/developer/admin on web + mobile. Frontend: keep server-rendered Django templates ([D-4](DECISIONS.md)), polish + make responsive; a dedicated-SPA split is deferred + evidence-gated. Living picture in [STRATEGY.md](STRATEGY.md).
- **Developer-wedge pivot — global D-10 + D-9 (2026-06-26)** — **D-10:** MVP cold-start re-sequenced to a single-player **developer hub** (app page + changelog + embeddable widget) that drags a developer's existing audience in; the discovery network/digest is **held until per-niche density**. **D-9:** monetization = paid, labeled, time-boxed **promotion placements** sold for reach, **firewalled from the ranking** (a `Surface` outside `CURATED_SURFACES`); tools + curation stay free. The wedge funnel (impression → click → conversion) shipped complete via `embeddable-update-widget` + `widget-conversion-attribution`. Full text in [DECISIONS.md](DECISIONS.md).

**Closed-feature pointers** (full lifecycle in each folder's `DECISIONS.md`/`RELEASE_NOTES.md`):

- **`widget-conversion-attribution`** (closed 2026-06-27, 962 tests) — first-party signed `widget_src` source-only cookie carries the source through the anonymous click → app page → conversion; conversions tallied in a separate PII-free `widget_conversion_count` rollup via fail-soft hooks; AC6 firewall structural (M5=0). Detail: [widget-conversion-attribution/DECISIONS.md](features/widget-conversion-attribution/DECISIONS.md).
- **`embeddable-update-widget`** (closed 2026-06-26, 893 tests) — `apps/widget/` read-only embeddable "what's new" widget + reach rollup; AC6 firewall structural by absence; DU-REL-1 rollback. Detail: [embeddable-update-widget/DECISIONS.md](features/embeddable-update-widget/DECISIONS.md).
- **`developer-updates`** (closed 2026-06-24, 828 tests) — new `apps/updates/` owning `updates_notice`, the single **AS-3 producer**: pull delivery (M5=0 structural, no fan-out), imports no `signals` (AST-enforced), durable table-derived rate limit; rollback = `git revert` of the build commit (**DU-REL-1**). Detail: [developer-updates/DECISIONS.md](features/developer-updates/DECISIONS.md).
- **`developer-dashboard`** (closed 2026-06-24, 740 tests) — model-less `apps/dashboard/`, a read-only view of an app's reception (D-7 reach/engagement + D-8 curated ratings) for its owner; two additive neutral `signals.selectors` reads, no schema. Detail: [developer-dashboard/DECISIONS.md](features/developer-dashboard/DECISIONS.md).
- **`open-search-browse`** (closed 2026-06-23, 676 tests) — `apps/discovery/`, the paginated DB-pushed `catalog.search_catalogue` open surface where money never buys position. Detail: [open-search-browse/DECISIONS.md](features/open-search-browse/DECISIONS.md).
- **`interest-profile`** (closed 2026-06-23, 616 tests) — `apps/interests/`, the user side of the Ring-0 match via the `declared_tag_ids` matcher contract. Detail: [interest-profile/DECISIONS.md](features/interest-profile/DECISIONS.md).
- **`app-subscriptions`** (closed 2026-06-21, 552 tests) — `apps/subscriptions/`, atomic follow + one `subscribe` emit, the empty-until-producer notice seam **AS-3**. Detail: [app-subscriptions/DECISIONS.md](features/app-subscriptions/DECISIONS.md).
- **`ratings-reviews`** (closed 2026-06-21, 486 tests) — `apps/ratings/`; established the global **[D-8](DECISIONS.md)** curated-rating gate ("curated = a `DIGEST` impression"). Detail: [ratings-reviews/DECISIONS.md](features/ratings-reviews/DECISIONS.md).
- **`app-pages`** (closed 2026-06-20, 417 tests) — `apps/pages/`, first live emitter of D-7 `app_page` impressions. Detail: [app-pages/DECISIONS.md](features/app-pages/DECISIONS.md).
- **`signal-capture`** (closed 2026-06-18, 374 tests) — `apps/signals/`; established the global **[D-7](DECISIONS.md)** event schema (append-only, structural no-score, no-PII). Detail: [signal-capture/DECISIONS.md](features/signal-capture/DECISIONS.md).
- **`submission-intake`** (closed 2026-06-18, 315 tests) — `apps/catalog/`, the ACCEPTED-only catalogue substrate = global **[D-6](DECISIONS.md)**. Detail: [submission-intake/DECISIONS.md](features/submission-intake/DECISIONS.md).
- **`interest-taxonomy`** (closed 2026-06-17, 184 tests) — flat tags + clusters, soft-retire + read-time `resolve_tag` = global **[D-5](DECISIONS.md)**. Detail: [interest-taxonomy/DECISIONS.md](features/interest-taxonomy/DECISIONS.md).
- **`identity-accounts`** (closed 2026-06-17, 108 tests) — the foundation (the stack **D-4**, roles **D-3**). Detail: [DECISIONS.md](DECISIONS.md).

> **Foundational decisions** (the stack, niche, build order — D1–D3, A1–A3, R1, global
> D-1/D-2/D-4) live in [DECISIONS.md](DECISIONS.md). Highlights: niche = **"vibecoded
> webapps"** (D-1); stack = **Python / Django + PostgreSQL** (D-4).

---

## Activity Log

Newest first. One line per session. **Keep the most recent ~10 rows here**; older rows
live in [process/activity-archive.md](process/activity-archive.md). The per-feature
folders remain the full record either way.

| Date       | Stage           | Summary                                                                 |
|------------|-----------------|-------------------------------------------------------------------------|
| 2026-06-30 | `4-build` (T-05…T-12 gate) | **Senior Engineer — T-05…T-12 COMPLETE. T-12 gate passed.** Audited the build commit (T-05–T-11 were all present but CONTROL.md was stale). Fixed T-09: Discover caption changed from wrong "Sorted by…" dynamic text to correct static "Ranked by merit, never by spend" with `.text-muted` class + proper `<p>` element (was `<caption>`); 2 tests updated + zero-results absence test added. T-12 gate: **1104 tests OK**, `makemigrations --check` clean, inline-style=388 (≤400 M2), app-page invariants green, design system guard green, ruff clean. AC-9 attestation written. RELEASE_NOTES.md with AC-8 checklist staged. TEST_PLAN.md updated. Advanced to `4b-verify`. |
| 2026-06-30 | `4-build` (T-03+T-04 done) | **Senior Engineer — T-03 + T-04 COMPLETE.** Two sessions combined. **T-03 (bounded CSS layer in `app.css`):** added `.checkbox-label`, `.badge--lg`, `.profile-details` (+`dt`/`dd`), `.stat-value`, `.text-success`, `.btn--danger`, `.full-width` extended, plus `.card-heading`, `.card-heading--divided`, `.metric-value`, `.metric-label`, `.text-accent`, `.text-sm`, `.text-xs`, `.font-semibold`, `.font-bold`, `.font-normal`. Updated `test_design_system.py` `component_exact` set to register all new classes (enumeration guard stays green). **T-04 (template migration, 22 files, 125 removals):** `dashboard/app_reception.html` (26), `catalog/app_detail.html` (19), `catalog/review_detail.html` (11), `dashboard/my_apps.html` (9), `updates/channel.html` (7), `subscriptions/_follow_slot.html` (7), `accounts/check_email.html` (5), `ratings/_reviews_slot.html` (5), `accounts/profile.html` (5), `interests/picker.html` (4), `pages/app_page.html` (4), `subscriptions/feed.html` (3), `discovery/catalogue.html` (3), `catalog/review_queue.html` (3), `core/landing.html` (2), `accounts/register.html` (2), `accounts/signin.html` (2), `updates/my_channels.html` (2), `updates/_devlog_slot.html` (2), `pages/not_available.html` (2), `accounts/verify.html` (1), `catalog/my_apps.html` (1). **Inline style count: 621 → 388** (M2 gate ≤ 400 PASSES). **1103 tests green**, no migration drift. Next: T-05 (`{% icon %}` tag + SVG set). |
| 2026-06-30 | `4-build` (in-progress) | **Senior Engineer — T-03/T-04 partial.** Started the bounded component/utility layer (T-03) and template migration (T-04). **T-03 additions to `app.css`:** `.checkbox-label` (flex inline checkbox+label), `.badge--lg` (larger badge size), `.profile-details` (+`dt`/`dd` rules), `.stat-value` (4xl/bold stat display), `.text-success`, `.btn--danger` (error-coloured secondary), and `.full-width` extended to also clear `max-width: none`. Updated `test_design_system.py` component_exact list to register all new classes (guard stays green). **T-04 migrations:** `catalog/app_detail.html` (badge inline styles → `.badge--lg`, checkbox labels → `.checkbox-label`); `catalog/submit.html` (inputs → `.full-width`, checkbox labels → `.checkbox-label`, submit → `.full-width`); `accounts/profile.html` (dl → `.profile-details`, buttons → `.full-width` / `.btn--danger`); `updates/channel.html` (subscriber stat → `.stat-value .text-success`, withdraw → `.btn--ghost .btn--sm .text-error`, badge → `.badge--lg`, inputs → `.full-width`). **Inline style count: 621 → 513** (goal ≤ 400 — test still failing). 91/92 tests pass; `test_inline_styles_count` FAILS (513 > 400). No migration drift. **Not yet done:** `app_reception.html` (73), residual `app_detail.html` (56), `review_detail.html` (34), `app_page.html` (30), `ratings/_reviews_slot.html` (30), `catalog/my_apps.html` (22) still need migration. |
| 2026-06-30 | `2b-ux`→`3-plan` | **Experience Designer** — ran the 13-pillar experience protocol against [DESIGN.md](features/interface-cleanup/DESIGN.md)'s canvas + the live design system, and wrote [EXPERIENCE.md](features/interface-cleanup/EXPERIENCE.md) (intent-only, zero implementation). **Per-screen feel blocks:** **S1** app-page mobile action panel — answered DESIGN's open "compact bar?" question → **compact action bar, one dominant focal point = Try** (order **Try›Follow[restored from the AC-2 grey demotion]›Share**, differentiated by prominence not by greying Follow; the app identity surfaces immediately after the bar) + facet category as visible text (AC-6b) + Share copy affordance with a "Copied!" live confirmation (AC-6d); **S2** developer hub — **Manage = the "home" tab** (the header "Developer" entry lands there), the active indicator **reuses the existing Discover `.sidebar-link.active` accent "you are here" pattern** + current-page semantics (active signalled by more than colour); **S3** Discover ordering caption = **informational, never a control** (AC-7), suppressed on zero results; **S4** picker dedupe/no-JS; **S5** submit required/optional grouping. **4 cross-surface system layers** (X1 one quiet brand-neutral icon family, X2 component grammar = same meaning→same treatment, X3 wayfinding/active-state, X4 defect-repair "nothing half-finished"). **5 tone adjectives** (Coherent/Polished/Trustworthy/Legible/Calm) each tied to a verifiable choice; **reduced-motion-first** system motion, **no new decorative motion** (holds IC-D-1/IC-EXP-5). Produced the **AC-8-class yes/no sign-off checklist** (the release gate) + a **full AC→spec traceability map** — no unmappable criterion ⇒ **no design gap, not routed back to the Architect**. Logged **IC-EXP-1…5**; surfaced + **accepted** the `order:` focus-vs-visual-order trade-off (**IC-EXP-2** — DOM order stays logical → no WCAG 1.3.2 failure → a documented sign-off item, not an escalation). Every empty/error/boundary state designed (empty hub tabs, zero-results, no-JS/no-clipboard, validation). **No code touched.** OQ-IC-8 untouched (a Planner mechanism confirmation under C2). Updated [INDEX.md](features/INDEX.md) → `3-plan`. Handed to the **Planner / Tech Lead**. |
| 2026-06-30 | `1-define`→`2-design`→`2b-ux` | **Software Architect** (DN-IC-BRIEF approved) — ran the 14-step protocol against live code, **re-verifying every brief finding** (btn--sm in 12 files; `--space-0.5/1.5/2.5`+`--font-size-md` undefined; 621 inline styles; the `.app-page-sidebar form button` specificity demotion; 16 emoji glyphs; the `background-dasharray` B7 bug), and wrote [DESIGN.md](features/interface-cleanup/DESIGN.md). **8 presentation-only workstreams:** W1 token+defect repair (define the missing tokens as valid dotted custom-properties + `.btn--sm`; delete the Follow/Share override) → W2 **bounded** consolidation (a small utility/component layer + the 4 worst-offender files; **M2 = 621→≤400**) → W3 a new reusable `{% icon %}` inline-SVG tag (replaces decorative emoji, `aria-hidden`) → W4 app-page mobile-**Try** via CSS `order:` only (**source DOM order unchanged → the `data-slot` fingerprint + M5=0 firewall invariants stay green**, IC-D-3) + visible facet category + copy-link Share PE (server byte-unchanged, C4) → W5 the user-chosen unified **Developer hub** (`_dev_tabs.html`, Manage|Analytics; no new route) → W6 a Discover ordering-basis label (no control) → W7 picker dedupe + form idiom + submit grouping → W8 a new **enumeration guard** (`test_design_system.py`) making the silent-defect class unrepresentable. **Two reusable artifacts only; no schema/API/view/URL/ADR change** → nothing promoted to global DECISIONS (confirms IC-D-2). One **AskUserQuestion** → the Developer hub IA (IC-DESIGN-5). Recorded **IC-DESIGN-1…9** + **OQ-IC-8** (the W7 view-context touch, surfaced under the C2 gate). Seeded [EXPERIENCE.md](features/interface-cleanup/EXPERIENCE.md) (pending); updated [INDEX.md](features/INDEX.md). **No code touched.** **User-facing → advanced to `2b-ux`**. Handed to the **Experience Designer**. |
| 2026-06-30 | walkthrough → `closed-out` → `0`→`1-define` | **UX/UI walkthrough → new feature `interface-cleanup`.** Ran a full-app Experience-Designer walkthrough (every surface), delivering a prioritized report; the standout concrete findings were **silent design-system defects** (`btn--sm` + `--space-0.5/1.5/2.5` + `--font-size-md` referenced-but-undefined; the `.app-page-sidebar form button` specificity demotion of the primary Follow button) plus ~621 inline-style sprawl and a set of per-surface rough edges. Per two user calls: **(1)** scope = cleanup layer only (rebrand stays the held `ui-modernization` bet); **(2)** close out `app-page-redesign` and activate the cleanup directly. **Coordinator** closed out [app-page-redesign](features/app-page-redesign/) in [INDEX.md](features/INDEX.md) (build verified 1095 tests; RELEASE_NOTES + AC-8 folded into close-out, reopenable) + scaffolded [features/interface-cleanup/](features/interface-cleanup/) (8 artifacts) + added the INDEX row. **Product Analyst** drafted [FEATURE_BRIEF.md](features/interface-cleanup/FEATURE_BRIEF.md) (7 stories · AC-1…AC-9 · M1–M6 · IC-D-1/2/3 · 7 Stage-2 OQs). **No code touched.** Surfaced **DN-IC-BRIEF**; stays at Stage 1 awaiting approval. |
| 2026-06-30 | `4-build`→`5-release` | **Senior Engineer** — built **app-page-redesign T-01…T-09** red-first, every task leaving the system releasable. **T-01** [`catalog/facets.py`](apps/catalog/facets.py) — the code-fixed facet vocabulary (pricing/maturity/modality/platform; `FacetDef`/`FacetValue`/`FacetCardinality`/`resolve_facets`; pure, no DB, registry-ordered resolve drops stale values per D-5). **T-02** the one additive migration [`catalog/0004`](apps/catalog/migrations/0004_app_marketing_columns_and_appfacet.py) — 4 optional `App` columns (`tagline`/`deep_dive`/`demo_clip`/`demo_clip_alt`) + the `AppFacet` table (soft, CASCADE, unique `(app,facet,value)`); applies + reverses cleanly; no tier/payment field. **T-03** [`gate.gate_relevant_fields()`](apps/catalog/gate.py) replacing the constant (core floor ∪ `config.app_page_gated_fields()`, default all-on, relaxable via `APP_PAGE_GATED_FIELDS`, intersected so config can only relax — D-14b) + 5 config tunables. **T-04** extended [`submit_app`/`edit_app`](apps/catalog/services.py): facet validation + cardinality (`InvalidFacetError`), clip magic-byte sniff (MP4/WebM) + size cap + alt-required (`MediaLimitError`), replace-set/remove semantics, gate wiring; required floor unchanged. **T-05** both authoring surfaces ([form](apps/catalog/forms.py) fed facet choices from `FACETS` + [DRF create/patch](apps/catalog/views.py) incl. multipart clip + [serializer](apps/catalog/serializers.py) round-trip). **T-06** [`get_app_page_content`/`AppPageContent`/`CatalogFacet`/`CatalogDeveloper`/`accepted_apps_by_owner`](apps/catalog/selectors.py) — **`CatalogApp` byte-stable** (AC-9), bounded queries (no N+1), accepted-only other-apps (no leak). **T-07** fail-soft [`{% app_devlog %}`](apps/updates/templatetags/updates_tags.py) tag + `APP_PAGE_DEVLOG_DEGRADED` (no `signals` import, M5=0 structural — auto-covered by the updates import test). **T-08** rewrote [`app_page.html`](apps/pages/templates/pages/app_page.html) to the uniform 9-landmark/10-slot contract + view→`get_app_page_content` + `<meta name=description>` + no-JS `<details>` deep-dive + new component CSS in the one [`core/app.css`](apps/core/static/core/app.css). **T-09** [TEST_PLAN.md](features/app-page-redesign/TEST_PLAN.md) (full AC-1…AC-9 + toggle map) + the two hard invariants ([uniformity + ranking/devlog firewall](apps/catalog/tests/test_redesign_invariants.py)) + [CODEMAP](CODEMAP.md) + [APR-BUILD-1 deviations](features/app-page-redesign/DECISIONS.md). Updated the ratings/subscriptions slot-fingerprint tests to the redesigned landmarks. **Verified: 1095 tests green · ruff/check clean · drift = only `catalog/0004` · migration up→down→up rehearsed.** AC-8 (compelling feel) left as the Stage-5 human web+mobile sign-off. Advanced [INDEX.md](features/INDEX.md) → 5-release. Handed to the **Release Engineer**. |
| 2026-06-29 | `2-design`→`3-plan`→`4-build` | **Planner / Tech Lead** (DN-APR-DESIGN approved) — processed the approval gate, then decomposed the design. **Gate:** promoted **APR-DESIGN-1/2 → global [D-14](DECISIONS.md)** (a: typed facets are code-fixed structured fields firewalled from ranking; b: re-review policy for public-claim fields is config-togglable) in [DECISIONS.md](DECISIONS.md); marked [DESIGN.md](features/app-page-redesign/DESIGN.md)/[FEATURE_BRIEF.md](features/app-page-redesign/FEATURE_BRIEF.md)/[feature DECISIONS.md](features/app-page-redesign/DECISIONS.md) approved/ratified. **Plan:** wrote [TASKS.md](features/app-page-redesign/TASKS.md) — **9 ordered S/M tasks T-01…T-09** (no `L`), risk front-loaded per DESIGN §10: **T-01** facet registry [`catalog/facets.py`](apps/catalog/gate.py) → **T-02** additive migration (4 `App` columns + `AppFacet`) → **T-03** re-review/config seam `gate.gate_relevant_fields()` → **T-04** write path (clip validation + atomic facet writes + gate wiring) → **T-05** authoring (server form + DRF round-trip) → **T-06** page-scoped `AppPageContent` read (shared `CatalogApp` byte-stable) → **T-07** fail-soft `{% app_devlog %}` tag (M5=0 structural) → **T-08** uniform 10-slot template + design-system CSS + view wiring (no-JS `<details>` deep-dive) → **T-09** TEST_PLAN + the two hard invariants (uniformity + ranking-firewall) + CODEMAP. **Six standing guardrails G1–G6** (suite/drift · `CatalogApp` byte-stable · firewall M5=0 · no-JS source of truth · no richness-by-identity · one source of truth); full **AC-1…AC-9 + re-review-toggle** coverage map; exit criteria met. Advanced [INDEX.md](features/INDEX.md) → 4-build. **No code touched.** Handed to the **Senior Engineer**. |
| 2026-06-29 | `1-define`→`2-design` | **Software Architect** (DN-APR-BRIEF approved) — ran the 14-step protocol against live code and wrote [app-page-redesign/DESIGN.md](features/app-page-redesign/DESIGN.md). **Architecture:** a **page-scoped `AppPageContent` read** in [`catalog.selectors`](apps/catalog/selectors.py) (composes the existing `_to_catalog_app` + new content) so the shared **`CatalogApp` cross-feature contract stays byte-stable** (AC-9; discovery/dashboard/widget untouched); **4 additive `App` columns** (`tagline`/`deep_dive`/`demo_clip`/`demo_clip_alt`) + a new **`AppFacet`** table; a **code-fixed [`catalog/facets.py`](apps/catalog/gate.py)** vocabulary (pricing/maturity/modality/platform — the `gate.py` precedent, soft-validated like D-5 tags); the **single write path extended** on `submit_app`/`edit_app` (optional params, one source of truth); a new **fail-soft `{% app_devlog %}` inclusion tag** reusing [`updates.published_notices_for_apps`](apps/updates/selectors.py#L54) (**M5=0 structural** — no `signals` emit added, AC-6); a **uniform 10-slot template contract** (AC-7) with the **no-JS deep-dive** via native `<details>` (AC-4). **Two user calls via AskUserQuestion** → **[APR-DESIGN-1](features/app-page-redesign/DECISIONS.md)** (facets = code-fixed structured fields, **firewalled from ranking/discovery**; category tags stay D-5 — resolves OQ1) + **[APR-DESIGN-2](features/app-page-redesign/DECISIONS.md)** (re-review for the new public-claim fields is **config-togglable** via `gate.gate_relevant_fields()`, default all-on — the user's refinement). Every AC mapped; per-component failure modes; alternatives (taxonomy/JSON facets, fat `CatalogApp`, GIF/hosted-video clip) + sacrifices recorded; one **additive migration** (Feature Track, C4). **No code touched.** Surfaced **DN-APR-DESIGN** (approval → promote D-14 + Stage 3). Stays at Stage 2 awaiting approval. |
| 2026-06-29 | `1-define` (housekeeping) | **CONTROL.md cleanup (user-requested)** — trimmed *Decisions Needed From You* to only the genuinely awaiting items (**DN-APR-BRIEF**, **DN-PS-DEPLOY**); removed the 5 struck-through resolved entries + the long *Recently resolved* paragraph + the already-answered **DN-Q004 (→ C)**, digesting all of them into *Decisions Made (recently)* with pointers to [DECISIONS.md](DECISIONS.md)/[patches/INDEX.md](patches/INDEX.md)/issue files. **No stage/decision/code change** — `app-page-redesign` stays at Stage 1 awaiting approval. |
| 2026-06-29 | `1-define` | **Product Analyst** — wrote [features/app-page-redesign/FEATURE_BRIEF.md](features/app-page-redesign/FEATURE_BRIEF.md) from the upstream request + Coordinator brainstorm ([OPEN_QUESTIONS.md](features/app-page-redesign/OPEN_QUESTIONS.md)) + scope decisions [APR-D-1/APR-D-2](features/app-page-redesign/DECISIONS.md) + vision (§1/§4/§5.4/§6) + the live 6-slot [`app_page.html`](apps/pages/templates/pages/app_page.html) over [`CatalogApp`](apps/catalog/selectors.py#L53). Problem/goal; **7 user stories** (pitch · in-action media · typed facets · deep dive · developer-hub identity · on-page devlog · compelling-feel visitor story); **AC-1…AC-9** each Given/When/Then and tagged *agent-verifiable* vs *human-judgment*; **M1–M6** (structural-now vs post-deploy); explicit in/out scope (heavy bets held per [APR-D-1](features/app-page-redesign/DECISIONS.md)); **C1–C6** + A1/A2 (each marked verified/unverified); **R1–R5**; vision alignment. Made the **uniformity guardrail a hard tested invariant (AC-7)** and **"compelling feel" a PS-3-style human sign-off gate (AC-8)**. Deliberately left taxonomy-vs-columns + field shapes + devlog-embed to Stage 2 (OQ1/OQ3/OQ4) — **no architecture/schema/UI proposed**. **No code touched.** Surfaced **DN-APR-BRIEF** (user approval gates Stage 2). Stays at Stage 1 awaiting approval. |
| 2026-06-29 | `0-coordinator`→`1-define` | **Coordinator** — user asked to start a **new feature: a redesign of the app page** into a Steam-like, marketable **developer hub** (pitch line / trailer / typed tags / "show more" deep dive), expandable to any app with a URL. **Brainstormed it grounded in live code:** the current [`app_page.html`](apps/pages/templates/pages/app_page.html) is 6 fixed uniform slots over the [`CatalogApp`](apps/catalog/selectors.py#L53) read-model (`id/name/description/url/tags/media` only) — a directory listing, not a launch page. **Surfaced the load-bearing tension + its reconciliation:** uniformity = fairness (vision §4 / AC3 / the template contract) means richness must be **more slots offered equally to every app, never unlocked by tier/payment** — a Steam-rich page is compatible if it stays uniform. Two user decisions via **AskUserQuestion** → **[APR-D-1](features/app-page-redesign/DECISIONS.md)** *phased v1* (redesign + cheap high-impact slots now; **defer** hosted trailer/video, follow-the-developer, community Q&A to future features) + **[APR-D-2](features/app-page-redesign/DECISIONS.md)** *sequence before the live deploy* (the app page is the dev's marketing face per [D-10](DECISIONS.md); DN-PS-DEPLOY stays parked behind it, mirroring `premium-frontend`). Scaffolded [features/app-page-redesign/](features/app-page-redesign/) (7 artifacts; [OPEN_QUESTIONS.md](features/app-page-redesign/OPEN_QUESTIONS.md) carries the verbatim request + full brainstorm + the uniformity guardrail + 4 open questions for Stage 1/2 + the deferred-bets list; [DECISIONS.md](features/app-page-redesign/DECISIONS.md) carries APR-D-1/APR-D-2), added the [features/INDEX.md](features/INDEX.md) row. **No code touched.** Set **Active=app-page-redesign, Stage=1-define, Persona=Product Analyst**. Handed to the **Product Analyst**. |
| 2026-06-29 | `P-build`→closed-out → `0-coordinator` | **Maintenance Engineer** — built the approved **patch-block-self-interaction** (DN-BSI-PLAN). **T-01 red-first** (9 new tests across 7 files; all failed on unpatched code for correct reasons — 3 `AttributeError` for missing `is_app_owner`, 2 `ImportError` for missing `SelfRatingError`/`SelfFollowError`, 4 `AssertionError` for missing guards/UX). **T-02** `is_app_owner(user, app_id) -> bool` EXISTS selector added to [`catalog/selectors.py`](apps/catalog/selectors.py) (owner reads section; anonymous-safe). **T-03** `SelfRatingError` in [`ratings/errors.py`](apps/ratings/errors.py); `_require_non_owner` guard in [`ratings/services.py`](apps/ratings/services.py) (after `_require_catalogued_app`); `except SelfRatingError` in [`ratings/views.py`](apps/ratings/views.py) (before `RatingValidationError`). **T-04** `SelfFollowError` in [`subscriptions/errors.py`](apps/subscriptions/errors.py); `_require_non_owner` guard in [`subscriptions/services.py`](apps/subscriptions/services.py); `except SelfFollowError` in [`subscriptions/views.py`](apps/subscriptions/views.py) (before broad `except Exception`). **T-05** `is_owner` computed in [`ratings_tags.app_reviews`](apps/ratings/templatetags/ratings_tags.py) + included in both context dicts; [`_reviews_slot.html`](apps/ratings/templates/ratings/_reviews_slot.html) gates submit form on `{% if not is_owner %}` + shows notice + preserves Remove cleanup for pre-existing owner ratings. **T-06** `is_owner` computed in [`subscriptions_tags.app_follow`](apps/subscriptions/templatetags/subscriptions_tags.py); [`_follow_slot.html`](apps/subscriptions/templates/subscriptions/_follow_slot.html) — owner + no follow → empty slot; owner + pre-existing follow → Unfollow only (cleanup). **T-07** verified: **1013 tests green** (+9), `ruff`/`check` clean, **`makemigrations --check` = no drift** (No-Schema Assertion confirmed). **Rollback rehearsed (DU-REL-1):** stashed → 157 targeted tests green on reverted tree → restored intact. Wrote [DECISIONS.md](patches/patch-block-self-interaction/DECISIONS.md) (BSI-D-1) + [TEST_PLAN.md](patches/patch-block-self-interaction/TEST_PLAN.md) + [RELEASE_NOTES.md](patches/patch-block-self-interaction/RELEASE_NOTES.md). **Closed Q-002 + Q-003 RESOLVED** in [issues/README.md](issues/README.md) + issue files + [patches/INDEX.md](patches/INDEX.md). **Active feature → none; Persona → Coordinator.** |
| 2026-06-29 | `0-coordinator`→`P-plan` | **Coordinator** — user picked the **Q-002 + Q-003 bundle** from the queue. **Triaged both against live code:** the public app page's `app` is a [`CatalogApp`](apps/catalog/selectors.py#L54) read-model that **already carries `owner`** ([selectors.py:70](apps/catalog/selectors.py#L70)/[:108](apps/catalog/selectors.py#L108), from the `App.owner` FK) → ownership is determinable with **no schema change**. Self-review path = [`ratings.services.submit_rating`](apps/ratings/services.py) + the form in [`_reviews_slot.html`](apps/ratings/templates/ratings/_reviews_slot.html#L48-L80); self-follow path = [`subscriptions.services.follow_app`](apps/subscriptions/services.py) + the button in [`_follow_slot.html`](apps/subscriptions/templates/subscriptions/_follow_slot.html#L12-L17). **Scope gate → Patch Track** (the owner guard is *added behaviour* on existing endpoints + a template hide; no migration, no new route, no ADR — §5.4 means the template hide alone is insufficient, so the server-side guard is in scope too). **Bundled** because both are the same shape (owner-guard suppressing a public-page control + rejecting its mutation) over the same surface. Scaffolded [patches/patch-block-self-interaction/](patches/patch-block-self-interaction/) (5 artifacts; PATCH.md carries the triage + Planner framing incl. the pre-existing-rating/follow edge case + the stale `app_page.html` "no owner field" comment to reconcile), added the [INDEX.md](patches/INDEX.md) Patch Track row, set **Q-002 + Q-003 IN-PROGRESS** in [issues/](issues/README.md) + both issue files (answers recorded). Marked **DN-Q002 + DN-Q003 RESOLVED**. **No code touched.** Set **Active=patch-block-self-interaction, Stage=P-plan, Persona=Maintenance Planner**. Handed to the **Maintenance Planner**. |
| 2026-06-29 | `0-coordinator` (housekeeping) | **Repo reorg (user-requested)** — made the **Patch Track distinct from the Feature Track** so feature tracking is no longer congested by patches. Moved the 6 registered patch folders out of `features/` into a new top-level **[patches/](patches/)** directory (`git mv`); created **[patches/INDEX.md](patches/INDEX.md)** (the patch registry, moved out of `features/INDEX.md`) + **[patches/README.md](patches/README.md)** (the patch folder convention + creation steps, moved out of `features/README.md`). Trimmed [features/INDEX.md](features/INDEX.md) + [features/README.md](features/README.md) to **Feature Track only** (each now points to `patches/`). Updated [CLAUDE.md](CLAUDE.md) §3 layout + §7 reference index, the [patch personas](process/personas/patch-1-planner.md) (`features/patch-<slug>/` → `patches/patch-<slug>/`, close-out → `patches/INDEX.md`), and every `features/patch-` link in [issues/](issues/) + this file. Removed an empty unregistered orphan folder `features/patch-mobile-touch-targets/`. Historical "Files touched" manifests inside each `PATCH.md` left as-is (accurate record of the build session). **No stage/decision/code change** — DN-PS-DEPLOY remains the open next bet. |
| 2026-06-29 | `P-build`→closed-out → `0-coordinator` | **Maintenance Engineer** — built the approved **patch-my-apps-status-display** (DN-MAS-PLAN). **T-01 red-first** (4 new tests: 3 failed on unpatched code as expected; `assertNotContains` for pending app already passed correctly). **T-02** renamed H1 + nav link `"My submissions"` → `"My Apps"` + updated subtitle ([`my_apps.html`](apps/catalog/templates/catalog/my_apps.html) + [`core/base.html`](apps/core/templates/core/base.html)). **T-03** added `_MY_APPS_STATUS_ORDER` constant + sorted decorated list by status priority in [`my_apps_page`](apps/catalog/views.py). **T-04/T-05** replaced flat `{% for %}` loop with `{% regroup %}`-based section headers (Active / Awaiting Review / Needs Changes / Withdrawn) + conditional `"View live page"` link for accepted apps (opens in new tab; `rel="noopener noreferrer"`). **T-06** verified: **1004 tests green** (+4), `ruff`/`check` clean, **`makemigrations --check` = no drift** (No-Schema Assertion held → Patch Track confirmed). **Rollback rehearsed (DU-REL-1):** stashed → clean `check`/no-drift/170 catalog tests green on reverted tree → restored intact. Wrote [TEST_PLAN.md](patches/patch-my-apps-status-display/TEST_PLAN.md) + [RELEASE_NOTES.md](patches/patch-my-apps-status-display/RELEASE_NOTES.md). **Closed UX-004 + UX-006 RESOLVED** in [INDEX.md](features/INDEX.md) + [issues/README.md](issues/README.md) + issue files. **Active feature → none; Persona → Coordinator.** |
| 2026-06-29 | `0-coordinator`→`P-plan` | **Coordinator** — triaged all 4 NEW issues: **UX-004 + UX-006** bundled → `patch-my-apps-status-display` (both touch [`catalog/my_apps.html`](apps/catalog/templates/catalog/my_apps.html): rename "My Submissions" → "My Apps" + status badges/grouping + "View live page" link for ACCEPTED apps; template-only → Patch Track → IN-PROGRESS); **UX-005** → `patch-developer-app-preview` (view-logic owner bypass, no schema → TRIAGED, queued); **BUG-005** → `patch-share-button-fallback` (JS Web Share fallback → TRIAGED, queued). Scaffolded [patches/patch-my-apps-status-display/](patches/patch-my-apps-status-display/) (5 artifacts), added INDEX.md Patch Track row, updated all 4 issue files + [issues/README.md](issues/README.md). **No code touched.** Set Active=patch-my-apps-status-display, Stage=P-plan, Persona=Maintenance Planner. |
| 2026-06-29 | `P-build`→closed-out → `0-coordinator` | **Maintenance Engineer** — built the approved **BUG-003** patch (DN-DWL-PLAN). **T-01 red-first** ([`test_template_tags_render_no_literal_braces`](apps/dashboard/tests/test_views.py)) — confirmed it failed on the unpatched template with **both** literal tags in the captured response body. **T-02** joined both multiline variable tags onto one line in [`my_apps.html`](apps/dashboard/templates/dashboard/my_apps.html): the selected-window `{{ w.label }}` (L32) **and** the card's `{{ summary.curated_impressions }}` (L57-58). **T-03** verified + wrote [TEST_PLAN.md](patches/patch-dashboard-window-label/TEST_PLAN.md) + [RELEASE_NOTES.md](patches/patch-dashboard-window-label/RELEASE_NOTES.md). **999 tests green** (+1), `ruff`/`check` clean, **`makemigrations --check` = no drift** (No-Schema Assertion held → Patch Track confirmed), and the **repo-wide multiline-tag sweep now returns ZERO matches**. **Rollback rehearsed (DU-REL-1):** stashed the patch → clean `check`/no-drift/60 dashboard tests green on the reverted tree → restored intact. **Closed BUG-003 out** in [INDEX.md](features/INDEX.md) + [issues registry](issues/README.md) + [BUG-003.md](issues/BUG-003.md). **Active feature → none; Persona → Coordinator.** |
| 2026-06-28 | `P-plan` | **Maintenance Planner** — investigated **BUG-003** and wrote the full [PATCH.md](patches/patch-dashboard-window-label/PATCH.md) (Problem Statement / Proposed Fix / Task List). **Root cause confirmed (certain):** Django's `tag_re` is compiled **without** `re.DOTALL`, so a `{{ … }}` tag split across a newline is never tokenized and renders literally. Ran a **repo-wide sweep** of every `*.html` for newline-spanning `{{…}}`/`{%…%}` tags → **exactly two offenders, both in [`my_apps.html`](apps/dashboard/templates/dashboard/my_apps.html)**: [L32-33](apps/dashboard/templates/dashboard/my_apps.html#L32-L33) (`{{ w.label }}`, the reported selected-window symptom) **and** [L58-59](apps/dashboard/templates/dashboard/my_apps.html#L58-L59) (`{{ summary.curated_impressions }}`, an **unreported sibling** that renders literally whenever a dev has curated impressions). Both folded into the fix (the sweep's whole point). **3 ordered tasks, T-01 = red-first regression test** (renders `dashboard:my-apps` with a seeded curated impression; asserts the labels render + `assertNotContains "{{"`). **No-Schema Assertion stated** (template-only → Patch Track holds). **No code touched.** Surfaced **DN-DWL-PLAN** (approval gates `P-build`). |
| 2026-06-28 | `0-coordinator`→`P-plan` | **Coordinator** — new bug report from the user: the dashboard's selected reporting period shows literal `{{ w.label }}`. **Logged [BUG-003](issues/BUG-003.md)** and **triaged it against live code — root cause certain:** [`my_apps.html:32-33`](apps/dashboard/templates/dashboard/my_apps.html#L32-L33) splits the active-window `{{ w.label }}` variable tag across two lines; Django's `tag_re` (`django/template/base.py`) is compiled **without** `re.DOTALL`, so a multiline `{{ … }}` tag isn't tokenized and renders literally — confirmed empirically on Django 5.2.15 (`tag_re.findall` returns nothing for the multiline form). Only the **selected** window uses the multiline form (the non-selected branch at [:36](apps/dashboard/templates/dashboard/my_apps.html#L36) is single-line), matching the reported symptom. **Scope gate → Patch Track** (template-only one-line fix; no schema/API/ADR). Scaffolded [patches/patch-dashboard-window-label/](patches/patch-dashboard-window-label/) (5 artifacts; PATCH.md carries the triage + proposed fix incl. a repo-wide sweep for the same pattern), added the [INDEX.md](features/INDEX.md) Patch Track row, set BUG-003 **IN-PROGRESS** in [issues/README.md](issues/README.md). **No code touched.** Set **Active=patch-dashboard-window-label, Stage=P-plan, Persona=Maintenance Planner**. Handed to the **Maintenance Planner**. |
| 2026-06-28 | `P-build`→closed-out → `0-coordinator` | **Maintenance Engineer** — built the approved **BUG-001** patch. **T-01 red-first** test case passes after template change. **T-02** updated `picker.html` template to namespace checkbox `id` and label `for` attributes with `row.cluster.id` to prevent duplicate IDs. **T-03** added a lightweight client-side vanilla JS synchronizer to `picker.html` to keep identical tags checked/unchecked synchronously. **T-04** verified the full test suite (**998 tests green**), ran code quality tools (`ruff check`), confirmed no database schema drift. Rollback verified successfully (**DU-REL-1**). Wrote [TEST_PLAN.md](patches/patch-interest-picker-duplicates/TEST_PLAN.md) + [RELEASE_NOTES.md](patches/patch-interest-picker-duplicates/RELEASE_NOTES.md). Closed BUG-001 in [INDEX.md](features/INDEX.md) and [issues/README.md](issues/README.md). Active feature → none; Persona → Coordinator. |
| 2026-06-28 | `P-plan` | **Maintenance Planner** — investigated **BUG-001** (duplicate subcategories click target issue) and wrote [PATCH.md](patches/patch-interest-picker-duplicates/PATCH.md). **Root cause pinned (certain):** the checkbox `id` and label `for` attributes in `picker.html` are generated using only `tag_{{ item.tag.id }}`, resulting in duplicate IDs on the page when a tag is assigned to multiple active clusters. Label clicks toggle the first occurrence in the DOM. **Design** = update template to use `id="tag_{{ row.cluster.id }}_{{ item.tag.id }}"` + include lightweight client-side vanilla JavaScript to sync duplicate checkbox states. **No-Schema Assertion** holds. **4 ordered tasks, T-01 = red-first regression test.** **No code touched.** Surfaced **DN-PIPD-PLAN** for user approval. |
| 2026-06-28 | `P-build`→closed-out → `0-coordinator` | **Maintenance Engineer** — built the approved **BUG-002** patch, **T-01 red-first** (9 new `ProfileFormActionTests` failed for the right reason — `NoReverseMatch`, the new routes absent). **T-02** two server-rendered §9 views in [`accounts/views.py`](apps/accounts/views.py): [`update_display_name`](apps/accounts/views.py) (`@login_required @require_POST`; reuses [`DisplayNameForm`](apps/accounts/forms.py#L23); PRG + `messages`) + [`delete_my_account`](apps/accounts/views.py) (named so it doesn't shadow the imported `delete_account` service; `confirm`-guarded; reuses [`services.delete_account`](apps/accounts/services.py#L58) + `auth_logout`; redirects `home`; same `DELETION_FULFILMENT` metric as the §5 path). **T-03** two §9 routes in [`accounts/urls.py`](apps/accounts/urls.py) (`profile/display-name`, `profile/delete`); **T-04** re-pointed both forms in [`profile.html`](apps/accounts/templates/accounts/profile.html) + dropped the dead `data-method`. **`MeView` byte-unchanged → `/me` §5 contract intact.** **997 tests green** (+9), `ruff`/`check` clean, **`makemigrations --check` = no drift** (No-Schema Assertion held → Patch Track confirmed). **Rollback rehearsed (DU-REL-1):** stashed the 4 changed files → clean `check`/no-drift/91 accounts tests green on the reverted tree → restored intact. Wrote [TEST_PLAN.md](patches/patch-profile-form-actions/TEST_PLAN.md) + [RELEASE_NOTES.md](patches/patch-profile-form-actions/RELEASE_NOTES.md); **closed BUG-002 out** in [INDEX.md](features/INDEX.md) + [issues registry](issues/README.md) + [BUG-002.md](issues/BUG-002.md). **Active feature → none; Persona → Coordinator.** |
| 2026-06-28 | `P-plan` | **Maintenance Planner** — investigated **BUG-002** against the live code and wrote [PATCH.md](patches/patch-profile-form-actions/PATCH.md). **Root cause pinned (certain):** the *Edit display name* ([profile.html:37](apps/accounts/templates/accounts/profile.html#L37)) + *Delete account* ([profile.html:81](apps/accounts/templates/accounts/profile.html#L81)) forms plain-`POST` to the JSON `/me` API ([`MeView`](apps/accounts/views.py#L200) has `get`/`patch`/`delete`, **no `post()`**) → **405**; `data-method` is dead markup (no project JS; `hx-boost` submits the real POST + swallows the 405 → silent idle). **Design** = two server-rendered **§9** handlers (Post/Redirect/Get + Django `messages`, the established interests/ratings/updates pattern) on new routes `profile/display-name` + `profile/delete`, re-point the two forms, drop dead `data-method`. **Reuses existing** [`DisplayNameForm`](apps/accounts/forms.py#L23) + [`services.delete_account`](apps/accounts/services.py#L58); **`/me` JSON contract byte-unchanged**. **No-Schema Assertion** stated (no migration/API/ADR → Patch Track holds). **5 ordered tasks, T-01 = red-first regression test.** **No code touched.** Surfaced **DN-PFA-PLAN** (approval gates `P-build`). |
| 2026-06-28 | `0-coordinator`→`P-plan` | **Coordinator** — picked **BUG-002** (High; the only non-Medium open issue) and **triaged it against the live code**: [`profile.html`](apps/accounts/templates/accounts/profile.html) wires **both** the *Edit display name* ([L37](apps/accounts/templates/accounts/profile.html#L37)) and *Delete account* ([L81](apps/accounts/templates/accounts/profile.html#L81)) forms as plain `method="post"` to `accounts:me` — the DRF [`MeView`](apps/accounts/views.py#L200) (get/patch/delete, **no `post()`**) → **405**; `data-method` is **dead markup** (grep: only those 2 forms, and there is **no project JS** — only vendored `htmx.min.js`, and `hx-boost` ignores `data-method` + swallows the 405). **Same root cause for both forms.** **Scope gate → Patch Track** (`display_name` + `delete_account` already exist → no schema; fix = dedicated server-rendered §9 handler(s), `/me` JSON contract untouched). Scaffolded [patches/patch-profile-form-actions/](patches/patch-profile-form-actions/) (5 artifacts; PATCH.md carries the *Source & triage*), added the [INDEX.md](features/INDEX.md) Patch Track row, set BUG-002 **IN-PROGRESS** in [issues/README.md](issues/README.md) + [BUG-002.md](issues/BUG-002.md). Also recorded the user's two answered decisions — **DN-Q001 → Option 1** (Q-001 **ANSWERED**), **DN-UX002 → minimal (a)** (UX-002 **TRIAGED**). **No code touched.** Set **Active=patch-profile-form-actions, Stage=P-plan, Persona=Maintenance Planner**. Handed to the **Maintenance Planner**. |
| 2026-06-28 | `P-build`→closed-out → `0-coordinator` | **Maintenance Engineer** — built the approved UX-003 patch, **T-01 red-first** (confirmed the `account_roles` library absent → `TemplateSyntaxError`, reachability cases failing before the fix). **T-02** new [`is_developer`](apps/accounts/templatetags/account_roles.py) `simple_tag` delegating to the one [`account_has_role`](apps/accounts/permissions.py#L23) gate (CODEMAP indexed); **T-03** developer-gated *"My submissions"* header link in [`core/base.html`](apps/core/templates/core/base.html) (mirrors `@require_role(developer)`); **T-04** dashboard Analytics⇄Submissions sub-nav + empty-state *"View my submissions"* CTA ([`dashboard/my_apps.html`](apps/dashboard/templates/dashboard/my_apps.html)); **T-05** reciprocal *"View analytics"* link on [`catalog/my_apps.html`](apps/catalog/templates/catalog/my_apps.html). **988 tests green** (+8: 3 tag/3 header/1 dashboard/1 catalog), `ruff`/`check` clean, **`makemigrations --check` = no drift** (No-Schema Assertion held → Patch Track confirmed). **Rollback rehearsed (DU-REL-1):** stashed the whole patch → clean `check`/no-drift/391 touched-app tests green on the reverted tree → restored intact. Wrote [TEST_PLAN.md](patches/patch-developer-submissions-nav/TEST_PLAN.md) + [RELEASE_NOTES.md](patches/patch-developer-submissions-nav/RELEASE_NOTES.md); **closed UX-003 out** in [INDEX.md](features/INDEX.md) + [issues registry](issues/README.md). **Active feature → none; Persona → Coordinator.** |
| 2026-06-28 | `P-plan` | **Maintenance Planner** — investigated **UX-003** against the live code and wrote [PATCH.md](patches/patch-developer-submissions-nav/PATCH.md). **Root cause pinned to two presentation-layer gaps:** (1) [`core/base.html:19-32`](apps/core/templates/core/base.html#L19-L33) renders a fixed authenticated link set with no developer link to `catalog:my-apps`, and roles (Django Groups) have **no template-side accessor** today ([`account_has_role`](apps/accounts/permissions.py#L23) is view-only); (2) the [`dashboard/my_apps.html` empty state](apps/dashboard/templates/dashboard/my_apps.html#L60-L67) dead-ends at *Submit* with no path to pending/rejected submissions. **Design** = one reusable `is_developer` `simple_tag` (delegates to the existing gate — one source of truth) + a developer-gated header link + dashboard sub-nav/empty-state CTA + a reciprocal submissions→dashboard link. **No-Schema Assertion** holds (templates + one read-only tag; no migration/API/ADR → Patch Track confirmed). **5 ordered S-tasks, T-01 = red-first regression test.** **No code touched.** Surfaced **DN-UX003-PLAN** (approval gates `P-build`). |
| 2026-06-28 | `0-coordinator`→`P-plan` | **Coordinator** — user switched to **issue-resolution mode** (testing the app + logging issues). **Triaged all 5 open issues** ([issues/README.md](issues/README.md)): **UX-003** (High, no nav path to pending/withdrawn submissions) → routed to a **new Patch Track** folder [patch-developer-submissions-nav](patches/patch-developer-submissions-nav/) (template/nav only — scope gate confirmed; `catalog:my-apps` route verified at [apps/catalog/urls.py:49](apps/catalog/urls.py#L49)), set **IN-PROGRESS**; **UX-001** (responsive CSS) + **BUG-001** (unique per-cluster HTML IDs) → **TRIAGED**, queued behind it; **UX-002** (tag-picker overhaul) + **Q-001** (duplicate-vs-unique taxonomy) → surfaced as **DN-UX002-SCOPE** / **DN-Q001-TAXONOMY** (non-blocking decisions). Scaffolded the 5 patch artifacts, added the **INDEX.md Patch Track** row, updated each routed issue's status/slug. Noted a stale ref: BUG-000 points to a `patch-anonymous-profile-link/` folder that was never created. Set **Active=patch-developer-submissions-nav, Stage=P-plan, Persona=Maintenance Planner**. **No code touched.** Handed to the **Maintenance Planner**. |
| 2026-06-28 | `0-coordinator` | **Coordinator** — **CONTROL.md cleanup** (it had grown past its own "keep it short" rule). Condensed the Current State cells, dropped the stale closed-feature "design in one line" note, collapsed the resolved-decision notes to one-liners, tightened the Decisions Made digest, and moved the older Activity Log rows (the platform-staging design/brief/scaffold rows + the full `widget-conversion-attribution` and `embeddable-update-widget` lifecycles) into [process/activity-archive.md](process/activity-archive.md). **No code touched; no stage/decision change** — DN-PS-DEPLOY remains the open next bet. |
| 2026-06-28 | `5-release`→closed-out → `0-coordinator` | **Release Engineer → Coordinator** — recorded the user's **M7 premium visual sign-off** (DN-PF-SIGNOFF RESOLVED): the `premium-frontend` landing / app page / discover surfaces read as a **polished product, not a prototype**. **Closed `premium-frontend` out** in [INDEX.md](features/INDEX.md) (released local/dev; build independently re-verified at release — **980 tests** / no drift / ruff / check; rollback rehearsed, no migration). The user flagged the **palette is still bland + no stylized/animated navigation** and scoped **deeper modernization to a future Feature-Track enhancement** (`ui-modernization`) — **not** an `issues/` item, **not** a re-style → **queued as a held future bet** (activation = a user **D2** call). **Reopened [DN-PS-DEPLOY](#decisions-needed-from-you)** — the sequencing condition (staging debuts on the polished UI) is now met. Held density-gated bets unchanged. **No code touched.** |
| 2026-06-28 | `5-release` (released local/dev) | **Release Engineer** — **independently re-verified the handed-off `premium-frontend` build** and **released it to local/dev**, writing [RELEASE_NOTES.md](features/premium-frontend/RELEASE_NOTES.md). **980 tests green** (M4: 975+5); `makemigrations --check` no drift (presentation-only, no schema); `manage.py check` clean; **`collectstatic` (DEBUG=false manifest) hashes+gzips `app.css` + `htmx.min.js`** → WhiteNoise serving model intact (**R1 mitigated**); **widget firewall held** (G2/M5=0); `/` → 200/0-queries/405-POST (AC-3/M6); vendored HTMX, no CDN. **Found + `ruff --fix`ed 5 trivial lint defects the handoff missed** (dead import + 3 whitespace + import-sort; no behaviour change). **Rollback rehearsed (hard gate):** no migration → nothing irreversible; `git revert --no-commit 6a01436 b14b238 8be0a51` ran with `manage.py check` clean mid-revert, only CONTROL.md (prose) conflicting; aborted clean, HEAD intact. **Outstanding = DN-PF-SIGNOFF** (the user's, human-judgment, PS-3). The user committed an in-scope `app.css` consistency pass (`8be0a51`) + issue `UX-003` (`c77fd6f`) concurrently — folded into the verified build. Advanced [INDEX.md](features/INDEX.md) → 5-release/released-local-dev. **Awaiting sign-off.** |
| 2026-06-28 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [premium-frontend/DESIGN.md](features/premium-frontend/DESIGN.md) into [TASKS.md](features/premium-frontend/TASKS.md): **8 ordered tasks T-01…T-08, all S/M**. The `app.css` design system split **T-01** (tokens + reset/base + layout primitives) **→ T-02** (components/utilities/3 breakpoints); then **T-03** restyle the shared [`core/base.html`](apps/core/templates/core/base.html) (additive `{% block body_class %}` + skip-link), **T-04** the new **static `/` landing** ([`core.views.landing`](apps/core/views.py), no DB → closes PF-CARRY-1/AC-3/M6), **T-05** restyle [`app_page.html`](apps/pages/templates/pages/app_page.html) (load-bearing wedge; same slots/order/SEO strings), **T-06** restyle [`catalogue.html`](apps/discovery/templates/discovery/catalogue.html), **T-07** **vendored** HTMX (`core/vendor/htmx.min.js`, no CDN, `defer`) + `hx-boost` (no new endpoint), **T-08** [TEST_PLAN.md](features/premium-frontend/TEST_PLAN.md) + the M2 first-paint baseline + the M7 sign-off package. Defined **six standing guardrails** (G1 suite ≥975 + no drift; **G2 widget firewall M5=0**; G3 one-source-of-truth in `:root`; G4 no-JS path is the source of truth; G5 no view/url/schema change on restyle; G6 keep class names). Advanced [INDEX.md](features/INDEX.md) → 4-build. **No code.** Handed to the **Senior Engineer**. |
| 2026-06-28 | `2-design`→`3-plan` | **Software Architect** (DN-PF-DESIGN resolved) — surveyed the codebase + authored [premium-frontend/DESIGN.md](features/premium-frontend/DESIGN.md) (**PF-DESIGN-1…7**), and got it **APPROVED**. **Pivotal PF-2 call → Option A, NO build step**: deepen the hand-authored [`core/app.css`](apps/core/static/core/app.css) into a full token-driven design system in the same one WhiteNoise-hashable file; **reject Tailwind's Node/CLI toolchain** → global ADR **[D-13](DECISIONS.md)** (affirms + extends [D-12](DECISIONS.md)'s build-free posture; eliminates brief risk **R1**). **Option B (Tailwind via the standalone CLI, fully specified §11) kept as an outcome-gated reversible fallback.** Plus a restyled shared shell, a new **static `/` landing** (no DB read → closes PF-CARRY-1/AC-3), an [`app_page.html`](apps/pages/templates/pages/app_page.html) + [`catalogue.html`](apps/discovery/templates/discovery/catalogue.html) presentation-only restyle, and **vendored** HTMX + `hx-boost` (light PE, fully degradable). Firewall (AC-6/M5=0), no-JS, SEO, a11y, no-schema preserved by construction. Advanced [INDEX.md](features/INDEX.md) → 3-plan. **No code.** Handed to the **Planner / Tech Lead**. |
| 2026-06-28 | `1-define`→`2-design` | **Product Analyst** (DN-PF-BRIEF approved) — authored [premium-frontend/FEATURE_BRIEF.md](features/premium-frontend/FEATURE_BRIEF.md) from the upstream 2026-06-28 frontend decision + vision ([D-10](DECISIONS.md): the app page is the developer's marketing landing page). Problem/goal; **6 user stories**; **AC-1…7** each Given/When/Then and tagged *agent-verifiable* vs *human-judgment*; **M1–M7**; in/out scope; **C1–C7** + A1–A3; top-5 risks. Made "premium feel" checkable by anchoring it to a concrete **design system** plus a PS-3-style user sign-off. Grounded in code: **bare `/` 404s** → **PF-CARRY-1** folded in as US-3/AC-3 (real landing); the **7 `base.html` already consolidated** to [`core/base.html`](apps/core/templates/core/base.html) (build on the shell, C7); **widget firewall preserved** (AC-6 / M5=0). **User answered with all 3 recommended defaults** (wedge surfaces first · light PE only · real platform landing). Advanced [INDEX.md](features/INDEX.md) → 2-design. **No code.** Handed to the **Software Architect**. |
| 2026-06-28 | `5-release`→closed-out → `1-define` | **Release Engineer → Coordinator** (user-directed consolidation) — wrote the [platform-staging/RELEASE_NOTES.md](features/platform-staging/RELEASE_NOTES.md) for the verified T-01…T-11 build, **honestly framed: build complete + verified local/dev (975 tests / ruff / check both DEBUG / no drift / secret-clean), live deploy DELIBERATELY DEFERRED** per the user's decision to land `premium-frontend` first so staging debuts polished. **Closed out platform-staging** in [INDEX.md](features/INDEX.md) (**DN-PS-DEPLOY → parked/reopenable**; artifacts ready, run unchanged when reactivated). Then as **Coordinator** scaffolded [features/premium-frontend/](features/premium-frontend/) (7 artifacts; **PS-OQ-1** carried in as **PF-CARRY-1**), added its INDEX row, set **Active = `premium-frontend`, `Stage: 1-define`, Persona Product Analyst**. Held bets unchanged. **No code touched.** Handed to the **Product Analyst**. |
| 2026-06-28 | `5-release` (frontend direction) | **User decision (Strategist-lens), no code** — resolved the [D-11](DECISIONS.md) frontend evidence-gate **within** the [D-4](DECISIONS.md) envelope: build a **premium server-rendered frontend = HTMX + Tailwind + Django templates** (polish, premium feel), **NOT** an SPA. Driver: the local UI is barebone (one **199-line** [`app.css`](apps/core/static/core/app.css), no design system) and the developer's **app page is their marketing landing page** — the gap is *design*, not architecture. **Sequencing (user) = deploy staging first**, then activate. Committed + **queued** as suggested slug **`premium-frontend`**. Tailwind's build step deliberately revises [D-12](DECISIONS.md)'s build-free posture → designed at the new feature's Stage 2 (later resolved to *no build step* in D-13). **No Stage change; `platform-staging` stays active.** |
| 2026-06-28 | `5-release` | **Release Engineer** — **independently re-verified the handed-off `platform-staging` build** against the live local PG and confirmed every agent-runnable deploy artifact. **975 tests green**; `ruff` clean; `manage.py check` clean under **both** DEBUG states; `makemigrations --check` = **no drift**. **[render.yaml](render.yaml)** parses with **zero secret literals** (every secret is `sync:false`/`generateValue`/`fromDatabase|fromService`). **AC2.2 holds** (`.env` gitignored + untracked). `collectstatic` (DEBUG=false manifest) → hashed+gz `core/app.css`; `gunicorn config.wsgi` loads under DEBUG=false. **Surfaced [DN-PS-DEPLOY](#decisions-needed-from-you):** the live Render deploy is outward-facing user action the agent can't perform; user runs [deploy-runbook.md](docs/deploy/deploy-runbook.md) and returns the live URL, then the agent runs AC1/AC5; AC6 walkthrough is the user's (PS-3). **Stays at Stage 5.** No code changed. |
| 2026-06-28 | `4-build`→`5-release` | **Senior Engineer** — built **all T-01…T-11** from the APPROVED [platform-staging/TASKS.md](features/platform-staging/TASKS.md) + [DESIGN.md](features/platform-staging/DESIGN.md) (global [D-12](DECISIONS.md)), T-01 risk first. Serving stack: gunicorn + WhiteNoise (`CompressedManifest` gated on `not DEBUG`) + `dj-database-url` + `STATIC_ROOT` + `CSRF_TRUSTED_ORIGINS`; non-`DEBUG` media via [`serve_media`](apps/core/views.py); the [`render.yaml`](render.yaml) Blueprint; `EMAIL_*` env wiring + [`email-provider-setup.md`](docs/deploy/email-provider-setup.md); the shared shell [`core/base.html`](apps/core/templates/core/base.html) + `app.css` with the **6 app bases consolidated** (messages render once; **widget byte-unchanged → AC3.3 firewall held**); the DB-only [`/health/live`](apps/core/views.py) split; the `REDIS_URL` cache (LocMem fallback); env-gated Sentry; the [`deploy-runbook.md`](docs/deploy/deploy-runbook.md) + AC2.2 no-secret pass; [`TEST_PLAN.md`](features/platform-staging/TEST_PLAN.md) + [`staging-walkthrough.md`](docs/deploy/staging-walkthrough.md). **975 tests green** (+13), `ruff`/`check` (both DEBUG) clean, **no migration drift**. PS-IMPL-1…5 logged; **PS-OQ-1** (bare `/` 404) logged not fixed (out of scope). Advanced [INDEX.md](features/INDEX.md) → 5-release. Handed to the **Release Engineer**. |
| 2026-06-27 | `3-plan`→`4-build` | **Planner / Tech Lead** — decomposed the APPROVED [platform-staging/DESIGN.md](features/platform-staging/DESIGN.md) (global [D-12](DECISIONS.md)) into [TASKS.md](features/platform-staging/TASKS.md): **11 ordered tasks T-01…T-11, all S/M**. **Risk front-loaded at T-01** (production WSGI + WhiteNoise static + `dj-database-url` — the real C4/§2 serving gap). The ~30-template consolidation sequenced **T-05** (shared `core/base.html` + `app.css`) **→ T-06** (re-point the 6 app `base.html`) with a render-every-surface check and the **AC3.3 widget firewall held** as an explicit non-change. Plus T-02 non-`DEBUG` media route, T-03 `render.yaml`, T-04 SMTP wiring, T-07 DB-only `/health/live` split, T-08 `REDIS_URL` cache, T-09 env-gated Sentry, T-10 runbook + AC2.2 no-secret pass, T-11 `TEST_PLAN.md` + walkthrough. Full **AC1.1–AC6.2 + PS-DESIGN-1…8** coverage map. Advanced [INDEX.md](features/INDEX.md) → 4-build. **No code.** Handed to the **Senior Engineer**. |

> Older rows (platform-staging design/brief/scaffold + the `widget-conversion-attribution`,
> `embeddable-update-widget`, `developer-updates`, `developer-dashboard`, `open-search-browse`,
> `interest-profile`, `app-subscriptions`, `ratings-reviews`, `app-pages`, `signal-capture`,
> `submission-intake`, `interest-taxonomy`, `identity-accounts` lifecycles) live in
> [process/activity-archive.md](process/activity-archive.md).
